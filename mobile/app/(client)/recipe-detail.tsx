// Client-level re-export of the nutrition-tab recipe detail screen so that
// it can be pushed onto the client-level stack (from the Today tab) with a
// proper slide-from-right animation instead of a cross-tab jump.
export { default } from './(tabs)/nutrition/recipe-detail'
