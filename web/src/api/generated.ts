//----------------------
// <auto-generated>
//     Generated using the NSwag toolchain v14.6.3.0 (NJsonSchema v11.5.2.0 (Newtonsoft.Json v13.0.0.0)) (http://NSwag.org)
// </auto-generated>
//----------------------

/* eslint-disable */
// @ts-nocheck
// ReSharper disable InconsistentNaming

import axios, { AxiosError } from 'axios';
import type { AxiosInstance, AxiosRequestConfig, AxiosResponse, CancelToken } from 'axios';

export class ApiClient {
    protected instance: AxiosInstance;
    protected baseUrl: string;
    protected jsonParseReviver: ((key: string, value: any) => any) | undefined = undefined;

    constructor(baseUrl?: string, instance?: AxiosInstance) {

        this.instance = instance || axios.create();

        this.baseUrl = baseUrl ?? "https://localhost:5001";

    }

    /**
     * Update workout template
     * @param templateId The template's public identifier (route parameter).
     * @return Success
     */
    updateWorkoutTemplateEndpoint(templateId: string, updateWorkoutTemplateRequest: UpdateWorkoutTemplateRequest, signal?: AbortSignal): Promise<WorkoutTemplateResponse> {
        let url_ = this.baseUrl + "/training/workout-templates/{templateId}";
        if (templateId === undefined || templateId === null)
            throw new globalThis.Error("The parameter 'templateId' must be defined.");
        url_ = url_.replace("{templateId}", encodeURIComponent("" + templateId));
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(updateWorkoutTemplateRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "PUT",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processUpdateWorkoutTemplateEndpoint(_response);
        });
    }

    protected processUpdateWorkoutTemplateEndpoint(response: AxiosResponse): Promise<WorkoutTemplateResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<WorkoutTemplateResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<WorkoutTemplateResponse>(null as any);
    }

    /**
     * Get workout template
     * @param templateId The template's public identifier (route parameter).
     * @return Success
     */
    getWorkoutTemplateEndpoint(templateId: string, signal?: AbortSignal): Promise<WorkoutTemplateResponse> {
        let url_ = this.baseUrl + "/training/workout-templates/{templateId}";
        if (templateId === undefined || templateId === null)
            throw new globalThis.Error("The parameter 'templateId' must be defined.");
        url_ = url_.replace("{templateId}", encodeURIComponent("" + templateId));
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGetWorkoutTemplateEndpoint(_response);
        });
    }

    protected processGetWorkoutTemplateEndpoint(response: AxiosResponse): Promise<WorkoutTemplateResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<WorkoutTemplateResponse>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<WorkoutTemplateResponse>(null as any);
    }

    /**
     * Delete workout template
     * @param templateId The template's public identifier (route parameter).
     * @return No Content
     */
    deleteWorkoutTemplateEndpoint(templateId: string, signal?: AbortSignal): Promise<void> {
        let url_ = this.baseUrl + "/training/workout-templates/{templateId}";
        if (templateId === undefined || templateId === null)
            throw new globalThis.Error("The parameter 'templateId' must be defined.");
        url_ = url_.replace("{templateId}", encodeURIComponent("" + templateId));
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "DELETE",
            url: url_,
            headers: {
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processDeleteWorkoutTemplateEndpoint(_response);
        });
    }

    protected processDeleteWorkoutTemplateEndpoint(response: AxiosResponse): Promise<void> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 204) {
            const _responseText = response.data;
            return Promise.resolve<void>(null as any);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<void>(null as any);
    }

    /**
     * List workout templates
     * @param page Page number (1-based). Defaults to 1.
     * @param pageSize Page size. Defaults to 50.
     * @return Success
     */
    listWorkoutTemplatesEndpoint(page: number, pageSize: number, signal?: AbortSignal): Promise<ListWorkoutTemplatesResponse> {
        let url_ = this.baseUrl + "/training/workout-templates?";
        if (page === undefined || page === null)
            throw new globalThis.Error("The parameter 'page' must be defined and cannot be null.");
        else
            url_ += "page=" + encodeURIComponent("" + page) + "&";
        if (pageSize === undefined || pageSize === null)
            throw new globalThis.Error("The parameter 'pageSize' must be defined and cannot be null.");
        else
            url_ += "pageSize=" + encodeURIComponent("" + pageSize) + "&";
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processListWorkoutTemplatesEndpoint(_response);
        });
    }

    protected processListWorkoutTemplatesEndpoint(response: AxiosResponse): Promise<ListWorkoutTemplatesResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<ListWorkoutTemplatesResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<ListWorkoutTemplatesResponse>(null as any);
    }

    /**
     * Create workout template
     * @return Success
     */
    createWorkoutTemplateEndpoint(createWorkoutTemplateRequest: CreateWorkoutTemplateRequest, signal?: AbortSignal): Promise<WorkoutTemplateResponse> {
        let url_ = this.baseUrl + "/training/workout-templates";
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(createWorkoutTemplateRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processCreateWorkoutTemplateEndpoint(_response);
        });
    }

    protected processCreateWorkoutTemplateEndpoint(response: AxiosResponse): Promise<WorkoutTemplateResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<WorkoutTemplateResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<WorkoutTemplateResponse>(null as any);
    }

    /**
     * Update workout log
     * @param logId The workout log's public identifier.
     * @return Success
     */
    updateWorkoutEndpoint(logId: string, updateWorkoutRequest: UpdateWorkoutRequest, signal?: AbortSignal): Promise<WorkoutLogDetail> {
        let url_ = this.baseUrl + "/client/training/logs/{logId}";
        if (logId === undefined || logId === null)
            throw new globalThis.Error("The parameter 'logId' must be defined.");
        url_ = url_.replace("{logId}", encodeURIComponent("" + logId));
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(updateWorkoutRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "PUT",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processUpdateWorkoutEndpoint(_response);
        });
    }

    protected processUpdateWorkoutEndpoint(response: AxiosResponse): Promise<WorkoutLogDetail> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<WorkoutLogDetail>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<WorkoutLogDetail>(null as any);
    }

    /**
     * Get workout log detail
     * @param logId The workout log's public identifier.
     * @return Success
     */
    getWorkoutLogEndpoint(logId: string, signal?: AbortSignal): Promise<WorkoutLogDetail> {
        let url_ = this.baseUrl + "/client/training/logs/{logId}";
        if (logId === undefined || logId === null)
            throw new globalThis.Error("The parameter 'logId' must be defined.");
        url_ = url_.replace("{logId}", encodeURIComponent("" + logId));
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGetWorkoutLogEndpoint(_response);
        });
    }

    protected processGetWorkoutLogEndpoint(response: AxiosResponse): Promise<WorkoutLogDetail> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<WorkoutLogDetail>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<WorkoutLogDetail>(null as any);
    }

    /**
     * Start a workout (create draft log)
     * @return Success
     */
    startWorkoutEndpoint(startWorkoutRequest: StartWorkoutRequest, signal?: AbortSignal): Promise<StartWorkoutResponse> {
        let url_ = this.baseUrl + "/client/training/logs";
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(startWorkoutRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processStartWorkoutEndpoint(_response);
        });
    }

    protected processStartWorkoutEndpoint(response: AxiosResponse): Promise<StartWorkoutResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<StartWorkoutResponse>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<StartWorkoutResponse>(null as any);
    }

    /**
     * List workout logs
     * @param page Page number (1-based).
     * @param pageSize Number of items per page.
     * @return Success
     */
    getWorkoutLogsEndpoint(page: number, pageSize: number, signal?: AbortSignal): Promise<GetWorkoutLogsResponse> {
        let url_ = this.baseUrl + "/client/training/logs?";
        if (page === undefined || page === null)
            throw new globalThis.Error("The parameter 'page' must be defined and cannot be null.");
        else
            url_ += "page=" + encodeURIComponent("" + page) + "&";
        if (pageSize === undefined || pageSize === null)
            throw new globalThis.Error("The parameter 'pageSize' must be defined and cannot be null.");
        else
            url_ += "pageSize=" + encodeURIComponent("" + pageSize) + "&";
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGetWorkoutLogsEndpoint(_response);
        });
    }

    protected processGetWorkoutLogsEndpoint(response: AxiosResponse): Promise<GetWorkoutLogsResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GetWorkoutLogsResponse>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GetWorkoutLogsResponse>(null as any);
    }

    /**
     * Go live with a workout
     * @param logId The external ID of the workout log to go live with.
    Bound from the route segment {logId}.
     * @return Success
     */
    goLiveEndpoint(logId: string, signal?: AbortSignal): Promise<GoLiveResponse> {
        let url_ = this.baseUrl + "/client/training/logs/{logId}/go-live";
        if (logId === undefined || logId === null)
            throw new globalThis.Error("The parameter 'logId' must be defined.");
        url_ = url_.replace("{logId}", encodeURIComponent("" + logId));
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "POST",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGoLiveEndpoint(_response);
        });
    }

    protected processGoLiveEndpoint(response: AxiosResponse): Promise<GoLiveResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GoLiveResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GoLiveResponse>(null as any);
    }

    /**
     * Get exercise progress
     * @param clientId Client's public user identifier.
     * @param exerciseId Exercise's public identifier.
     * @return Success
     */
    getExerciseProgressEndpoint(clientId: string, exerciseId: string, signal?: AbortSignal): Promise<GetExerciseProgressResponse> {
        let url_ = this.baseUrl + "/training/clients/{clientId}/progress/{exerciseId}";
        if (clientId === undefined || clientId === null)
            throw new globalThis.Error("The parameter 'clientId' must be defined.");
        url_ = url_.replace("{clientId}", encodeURIComponent("" + clientId));
        if (exerciseId === undefined || exerciseId === null)
            throw new globalThis.Error("The parameter 'exerciseId' must be defined.");
        url_ = url_.replace("{exerciseId}", encodeURIComponent("" + exerciseId));
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGetExerciseProgressEndpoint(_response);
        });
    }

    protected processGetExerciseProgressEndpoint(response: AxiosResponse): Promise<GetExerciseProgressResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GetExerciseProgressResponse>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GetExerciseProgressResponse>(null as any);
    }

    /**
     * Complete a workout
     * @param logId The workout log's public identifier.
     * @return Success
     */
    completeWorkoutEndpoint(logId: string, signal?: AbortSignal): Promise<WorkoutLogDetail> {
        let url_ = this.baseUrl + "/client/training/logs/{logId}/complete";
        if (logId === undefined || logId === null)
            throw new globalThis.Error("The parameter 'logId' must be defined.");
        url_ = url_.replace("{logId}", encodeURIComponent("" + logId));
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "POST",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processCompleteWorkoutEndpoint(_response);
        });
    }

    protected processCompleteWorkoutEndpoint(response: AxiosResponse): Promise<WorkoutLogDetail> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<WorkoutLogDetail>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<WorkoutLogDetail>(null as any);
    }

    /**
     * Abandon a workout
     * @param logId The external ID of the workout log to abandon.
    Bound from the route segment {logId}.
     * @return Success
     */
    abandonWorkoutEndpoint(logId: string, signal?: AbortSignal): Promise<AbandonWorkoutResponse> {
        let url_ = this.baseUrl + "/client/training/logs/{logId}/abandon";
        if (logId === undefined || logId === null)
            throw new globalThis.Error("The parameter 'logId' must be defined.");
        url_ = url_.replace("{logId}", encodeURIComponent("" + logId));
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "POST",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processAbandonWorkoutEndpoint(_response);
        });
    }

    protected processAbandonWorkoutEndpoint(response: AxiosResponse): Promise<AbandonWorkoutResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<AbandonWorkoutResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<AbandonWorkoutResponse>(null as any);
    }

    /**
     * Respond to a weekly check-in
     * @param id Route parameter — check-in identifier.
     * @return Success
     */
    respondToCheckInEndpoint(id: string, respondToCheckInRequest: RespondToCheckInRequest, signal?: AbortSignal): Promise<RespondToCheckInResponse> {
        let url_ = this.baseUrl + "/client/weekly-check-ins/{id}/respond";
        if (id === undefined || id === null)
            throw new globalThis.Error("The parameter 'id' must be defined.");
        url_ = url_.replace("{id}", encodeURIComponent("" + id));
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(respondToCheckInRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processRespondToCheckInEndpoint(_response);
        });
    }

    protected processRespondToCheckInEndpoint(response: AxiosResponse): Promise<RespondToCheckInResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<RespondToCheckInResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<RespondToCheckInResponse>(null as any);
    }

    /**
     * Upsert weekly check-in setting
     * @return Success
     */
    putSettingsEndpoint(putSettingsRequest: PutSettingsRequest, signal?: AbortSignal): Promise<PutSettingsResponse> {
        let url_ = this.baseUrl + "/trainer/weekly-check-ins/settings";
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(putSettingsRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "PUT",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processPutSettingsEndpoint(_response);
        });
    }

    protected processPutSettingsEndpoint(response: AxiosResponse): Promise<PutSettingsResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<PutSettingsResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<PutSettingsResponse>(null as any);
    }

    /**
     * Get weekly check-in settings
     * @return Success
     */
    getSettingsEndpoint(signal?: AbortSignal): Promise<GetSettingsResponse> {
        let url_ = this.baseUrl + "/trainer/weekly-check-ins/settings";
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGetSettingsEndpoint(_response);
        });
    }

    protected processGetSettingsEndpoint(response: AxiosResponse): Promise<GetSettingsResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GetSettingsResponse>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GetSettingsResponse>(null as any);
    }

    /**
     * Upsert per-client override
     * @param clientUserId The client's ApplicationUser.Id (route parameter).
     * @param profession Profession ("Training" or "Nutrition") (route parameter).
     * @return Success
     */
    putOverrideEndpoint(clientUserId: string, profession: string, putOverrideRequest: PutOverrideRequest, signal?: AbortSignal): Promise<PutOverrideResponse> {
        let url_ = this.baseUrl + "/trainer/weekly-check-ins/overrides/{clientUserId}/{profession}";
        if (clientUserId === undefined || clientUserId === null)
            throw new globalThis.Error("The parameter 'clientUserId' must be defined.");
        url_ = url_.replace("{clientUserId}", encodeURIComponent("" + clientUserId));
        if (profession === undefined || profession === null)
            throw new globalThis.Error("The parameter 'profession' must be defined.");
        url_ = url_.replace("{profession}", encodeURIComponent("" + profession));
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(putOverrideRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "PUT",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processPutOverrideEndpoint(_response);
        });
    }

    protected processPutOverrideEndpoint(response: AxiosResponse): Promise<PutOverrideResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<PutOverrideResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<PutOverrideResponse>(null as any);
    }

    /**
     * Delete per-client override
     * @param clientUserId The client's ApplicationUser.Id (route parameter).
     * @param profession Profession ("Training" or "Nutrition") (route parameter).
     * @return No Content
     */
    deleteOverrideEndpoint(clientUserId: string, profession: string, signal?: AbortSignal): Promise<void> {
        let url_ = this.baseUrl + "/trainer/weekly-check-ins/overrides/{clientUserId}/{profession}";
        if (clientUserId === undefined || clientUserId === null)
            throw new globalThis.Error("The parameter 'clientUserId' must be defined.");
        url_ = url_.replace("{clientUserId}", encodeURIComponent("" + clientUserId));
        if (profession === undefined || profession === null)
            throw new globalThis.Error("The parameter 'profession' must be defined.");
        url_ = url_.replace("{profession}", encodeURIComponent("" + profession));
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "DELETE",
            url: url_,
            headers: {
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processDeleteOverrideEndpoint(_response);
        });
    }

    protected processDeleteOverrideEndpoint(response: AxiosResponse): Promise<void> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 204) {
            const _responseText = response.data;
            return Promise.resolve<void>(null as any);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<void>(null as any);
    }

    /**
     * Mark a check-in as reviewed (trainer)
     * @param id Check-in identifier (route parameter).
     * @return Success
     */
    markCheckInReviewedEndpoint(id: string, signal?: AbortSignal): Promise<MarkCheckInReviewedResponse> {
        let url_ = this.baseUrl + "/trainer/weekly-check-ins/{id}/mark-reviewed";
        if (id === undefined || id === null)
            throw new globalThis.Error("The parameter 'id' must be defined.");
        url_ = url_.replace("{id}", encodeURIComponent("" + id));
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "POST",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processMarkCheckInReviewedEndpoint(_response);
        });
    }

    protected processMarkCheckInReviewedEndpoint(response: AxiosResponse): Promise<MarkCheckInReviewedResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<MarkCheckInReviewedResponse>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<MarkCheckInReviewedResponse>(null as any);
    }

    /**
     * List check-ins for the trainer
     * @param weekStartDate (optional) ISO-week Monday to filter by (YYYY-MM-DD). Optional — when omitted, the endpoint
    returns the active (not dismissed, not yet reviewed) set across all weeks.
     * @return Success
     */
    getTrainerCheckInsEndpoint(weekStartDate?: string | null | undefined, signal?: AbortSignal): Promise<GetTrainerCheckInsResponse> {
        let url_ = this.baseUrl + "/trainer/weekly-check-ins?";
        if (weekStartDate !== undefined && weekStartDate !== null)
            url_ += "weekStartDate=" + encodeURIComponent("" + weekStartDate) + "&";
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGetTrainerCheckInsEndpoint(_response);
        });
    }

    protected processGetTrainerCheckInsEndpoint(response: AxiosResponse): Promise<GetTrainerCheckInsResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GetTrainerCheckInsResponse>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GetTrainerCheckInsResponse>(null as any);
    }

    /**
     * Get per-client overrides
     * @return Success
     */
    getOverridesEndpoint(signal?: AbortSignal): Promise<GetOverridesResponse> {
        let url_ = this.baseUrl + "/trainer/weekly-check-ins/overrides";
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGetOverridesEndpoint(_response);
        });
    }

    protected processGetOverridesEndpoint(response: AxiosResponse): Promise<GetOverridesResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GetOverridesResponse>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GetOverridesResponse>(null as any);
    }

    /**
     * Get active check-ins (client)
     * @return Success
     */
    getCurrentClientCheckInsEndpoint(signal?: AbortSignal): Promise<GetCurrentClientCheckInsResponse> {
        let url_ = this.baseUrl + "/client/weekly-check-ins/current";
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGetCurrentClientCheckInsEndpoint(_response);
        });
    }

    protected processGetCurrentClientCheckInsEndpoint(response: AxiosResponse): Promise<GetCurrentClientCheckInsResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GetCurrentClientCheckInsResponse>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GetCurrentClientCheckInsResponse>(null as any);
    }

    /**
     * Get the active check-in for a client (trainer)
     * @param clientUserId Client's ApplicationUser.Id (route parameter).
     * @param profession (optional) Profession to filter by ("Training" or "Nutrition"). Optional.
    When omitted, returns check-ins for all professions.
     * @return Success
     */
    getClientCurrentCheckInEndpoint(clientUserId: string, profession?: string | null | undefined, signal?: AbortSignal): Promise<GetClientCurrentCheckInResponse> {
        let url_ = this.baseUrl + "/trainer/clients/{clientUserId}/weekly-check-ins/current?";
        if (clientUserId === undefined || clientUserId === null)
            throw new globalThis.Error("The parameter 'clientUserId' must be defined.");
        url_ = url_.replace("{clientUserId}", encodeURIComponent("" + clientUserId));
        if (profession !== undefined && profession !== null)
            url_ += "profession=" + encodeURIComponent("" + profession) + "&";
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGetClientCurrentCheckInEndpoint(_response);
        });
    }

    protected processGetClientCurrentCheckInEndpoint(response: AxiosResponse): Promise<GetClientCurrentCheckInResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GetClientCurrentCheckInResponse>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GetClientCurrentCheckInResponse>(null as any);
    }

    /**
     * Get check-in detail (trainer)
     * @param id Check-in identifier (route parameter).
     * @return Success
     */
    getCheckInDetailEndpoint(id: string, signal?: AbortSignal): Promise<GetCheckInDetailResponse> {
        let url_ = this.baseUrl + "/trainer/weekly-check-ins/{id}";
        if (id === undefined || id === null)
            throw new globalThis.Error("The parameter 'id' must be defined.");
        url_ = url_.replace("{id}", encodeURIComponent("" + id));
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGetCheckInDetailEndpoint(_response);
        });
    }

    protected processGetCheckInDetailEndpoint(response: AxiosResponse): Promise<GetCheckInDetailResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GetCheckInDetailResponse>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GetCheckInDetailResponse>(null as any);
    }

    /**
     * Dismiss a weekly check-in
     * @param id Route parameter — check-in identifier.
     * @return Success
     */
    dismissCheckInEndpoint(id: string, signal?: AbortSignal): Promise<DismissCheckInResponse> {
        let url_ = this.baseUrl + "/client/weekly-check-ins/{id}/dismiss";
        if (id === undefined || id === null)
            throw new globalThis.Error("The parameter 'id' must be defined.");
        url_ = url_.replace("{id}", encodeURIComponent("" + id));
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "POST",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processDismissCheckInEndpoint(_response);
        });
    }

    protected processDismissCheckInEndpoint(response: AxiosResponse): Promise<DismissCheckInResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<DismissCheckInResponse>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<DismissCheckInResponse>(null as any);
    }

    /**
     * Update current user time zone
     * @return No Content
     */
    updateTimeZoneEndpoint(updateTimeZoneRequest: UpdateTimeZoneRequest, signal?: AbortSignal): Promise<void> {
        let url_ = this.baseUrl + "/users/me/timezone";
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(updateTimeZoneRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "PUT",
            url: url_,
            headers: {
                "Content-Type": "application/json",
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processUpdateTimeZoneEndpoint(_response);
        });
    }

    protected processUpdateTimeZoneEndpoint(response: AxiosResponse): Promise<void> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 204) {
            const _responseText = response.data;
            return Promise.resolve<void>(null as any);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<void>(null as any);
    }

    /**
     * Update current user profile
     * @return No Content
     */
    updateProfileEndpoint(updateProfileRequest: UpdateProfileRequest, signal?: AbortSignal): Promise<void> {
        let url_ = this.baseUrl + "/users/me";
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(updateProfileRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "PUT",
            url: url_,
            headers: {
                "Content-Type": "application/json",
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processUpdateProfileEndpoint(_response);
        });
    }

    protected processUpdateProfileEndpoint(response: AxiosResponse): Promise<void> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 204) {
            const _responseText = response.data;
            return Promise.resolve<void>(null as any);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<void>(null as any);
    }

    /**
     * Get current user profile
     * @return Success
     */
    getProfileEndpoint(signal?: AbortSignal): Promise<GetProfileResponse> {
        let url_ = this.baseUrl + "/users/me";
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGetProfileEndpoint(_response);
        });
    }

    protected processGetProfileEndpoint(response: AxiosResponse): Promise<GetProfileResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GetProfileResponse>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GetProfileResponse>(null as any);
    }

    /**
     * Delete current user account
     * @return No Content
     */
    deleteAccountEndpoint(signal?: AbortSignal): Promise<void> {
        let url_ = this.baseUrl + "/users/me";
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "DELETE",
            url: url_,
            headers: {
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processDeleteAccountEndpoint(_response);
        });
    }

    protected processDeleteAccountEndpoint(response: AxiosResponse): Promise<void> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 204) {
            const _responseText = response.data;
            return Promise.resolve<void>(null as any);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<void>(null as any);
    }

    /**
     * Confirm avatar upload
     * @return No Content
     */
    confirmAvatarEndpoint(confirmAvatarRequest: ConfirmAvatarRequest, signal?: AbortSignal): Promise<void> {
        let url_ = this.baseUrl + "/users/me/avatar";
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(confirmAvatarRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "PUT",
            url: url_,
            headers: {
                "Content-Type": "application/json",
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processConfirmAvatarEndpoint(_response);
        });
    }

    protected processConfirmAvatarEndpoint(response: AxiosResponse): Promise<void> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 204) {
            const _responseText = response.data;
            return Promise.resolve<void>(null as any);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<void>(null as any);
    }

    /**
     * Delete avatar
     * @return No Content
     */
    deleteAvatarEndpoint(signal?: AbortSignal): Promise<void> {
        let url_ = this.baseUrl + "/users/me/avatar";
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "DELETE",
            url: url_,
            headers: {
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processDeleteAvatarEndpoint(_response);
        });
    }

    protected processDeleteAvatarEndpoint(response: AxiosResponse): Promise<void> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 204) {
            const _responseText = response.data;
            return Promise.resolve<void>(null as any);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<void>(null as any);
    }

    /**
     * Generate avatar upload URL
     * @return Success
     */
    generateAvatarUploadUrlEndpoint(generateAvatarUploadUrlRequest: GenerateAvatarUploadUrlRequest, signal?: AbortSignal): Promise<GenerateAvatarUploadUrlResponse> {
        let url_ = this.baseUrl + "/users/me/avatar/upload-url";
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(generateAvatarUploadUrlRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGenerateAvatarUploadUrlEndpoint(_response);
        });
    }

    protected processGenerateAvatarUploadUrlEndpoint(response: AxiosResponse): Promise<GenerateAvatarUploadUrlResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GenerateAvatarUploadUrlResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GenerateAvatarUploadUrlResponse>(null as any);
    }

    /**
     * Add a professional role
     * @return Success
     */
    addRoleEndpoint(addRoleRequest: AddRoleRequest, signal?: AbortSignal): Promise<AddRoleResponse> {
        let url_ = this.baseUrl + "/users/me/roles";
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(addRoleRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processAddRoleEndpoint(_response);
        });
    }

    protected processAddRoleEndpoint(response: AxiosResponse): Promise<AddRoleResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<AddRoleResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<AddRoleResponse>(null as any);
    }

    /**
     * Full-state update of a training plan template
     * @param templateId The template's public identifier (route parameter).
     * @return Success
     */
    updateTrainingPlanTemplateEndpoint(templateId: string, updateTemplateRequest: UpdateTemplateRequest, signal?: AbortSignal): Promise<TrainingPlanTemplateDetailDto> {
        let url_ = this.baseUrl + "/training/plan-templates/{templateId}";
        if (templateId === undefined || templateId === null)
            throw new globalThis.Error("The parameter 'templateId' must be defined.");
        url_ = url_.replace("{templateId}", encodeURIComponent("" + templateId));
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(updateTemplateRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "PUT",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processUpdateTrainingPlanTemplateEndpoint(_response);
        });
    }

    protected processUpdateTrainingPlanTemplateEndpoint(response: AxiosResponse): Promise<TrainingPlanTemplateDetailDto> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<TrainingPlanTemplateDetailDto>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<TrainingPlanTemplateDetailDto>(null as any);
    }

    /**
     * Get training plan template detail
     * @param templateId The template's public identifier (route parameter).
     * @return Success
     */
    getTrainingPlanTemplateEndpoint(templateId: string, signal?: AbortSignal): Promise<TrainingPlanTemplateDetailDto> {
        let url_ = this.baseUrl + "/training/plan-templates/{templateId}";
        if (templateId === undefined || templateId === null)
            throw new globalThis.Error("The parameter 'templateId' must be defined.");
        url_ = url_.replace("{templateId}", encodeURIComponent("" + templateId));
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGetTrainingPlanTemplateEndpoint(_response);
        });
    }

    protected processGetTrainingPlanTemplateEndpoint(response: AxiosResponse): Promise<TrainingPlanTemplateDetailDto> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<TrainingPlanTemplateDetailDto>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<TrainingPlanTemplateDetailDto>(null as any);
    }

    /**
     * Search training plan templates
     * @param page Page number (1-based).
     * @param pageSize Number of items per page.
     * @param search (optional) Optional case-insensitive substring match against the template name.
     * @param goal (optional) Optional filter by primary fitness goal.
     * @param difficulty (optional) Optional filter by difficulty level.
     * @param weekCount (optional) Optional filter by exact week count.
     * @return Success
     */
    searchTrainingPlanTemplatesEndpoint(page: number, pageSize: number, search?: string | null | undefined, goal?: PrimaryGoal | null | undefined, difficulty?: ExerciseDifficulty | null | undefined, weekCount?: number | null | undefined, signal?: AbortSignal): Promise<SearchTemplatesResponse> {
        let url_ = this.baseUrl + "/training/plan-templates?";
        if (page === undefined || page === null)
            throw new globalThis.Error("The parameter 'page' must be defined and cannot be null.");
        else
            url_ += "page=" + encodeURIComponent("" + page) + "&";
        if (pageSize === undefined || pageSize === null)
            throw new globalThis.Error("The parameter 'pageSize' must be defined and cannot be null.");
        else
            url_ += "pageSize=" + encodeURIComponent("" + pageSize) + "&";
        if (search !== undefined && search !== null)
            url_ += "search=" + encodeURIComponent("" + search) + "&";
        if (goal !== undefined && goal !== null)
            url_ += "goal=" + encodeURIComponent("" + goal) + "&";
        if (difficulty !== undefined && difficulty !== null)
            url_ += "difficulty=" + encodeURIComponent("" + difficulty) + "&";
        if (weekCount !== undefined && weekCount !== null)
            url_ += "weekCount=" + encodeURIComponent("" + weekCount) + "&";
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processSearchTrainingPlanTemplatesEndpoint(_response);
        });
    }

    protected processSearchTrainingPlanTemplatesEndpoint(response: AxiosResponse): Promise<SearchTemplatesResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<SearchTemplatesResponse>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<SearchTemplatesResponse>(null as any);
    }

    /**
     * Create a training plan template
     * @return Success
     */
    createTrainingPlanTemplateEndpoint(createTemplateRequest: CreateTemplateRequest, signal?: AbortSignal): Promise<TrainingPlanTemplateSummaryDto> {
        let url_ = this.baseUrl + "/training/plan-templates";
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(createTemplateRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processCreateTrainingPlanTemplateEndpoint(_response);
        });
    }

    protected processCreateTrainingPlanTemplateEndpoint(response: AxiosResponse): Promise<TrainingPlanTemplateSummaryDto> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<TrainingPlanTemplateSummaryDto>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<TrainingPlanTemplateSummaryDto>(null as any);
    }

    /**
     * Instantiate a training plan template
     * @param templateId The template's public identifier (route parameter).
     * @return Success
     */
    instantiateTrainingPlanTemplateEndpoint(templateId: string, instantiateTemplateRequest: InstantiateTemplateRequest, signal?: AbortSignal): Promise<InstantiateTemplateResponse> {
        let url_ = this.baseUrl + "/training/plan-templates/{templateId}/instantiate";
        if (templateId === undefined || templateId === null)
            throw new globalThis.Error("The parameter 'templateId' must be defined.");
        url_ = url_.replace("{templateId}", encodeURIComponent("" + templateId));
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(instantiateTemplateRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processInstantiateTrainingPlanTemplateEndpoint(_response);
        });
    }

    protected processInstantiateTrainingPlanTemplateEndpoint(response: AxiosResponse): Promise<InstantiateTemplateResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<InstantiateTemplateResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<InstantiateTemplateResponse>(null as any);
    }

    /**
     * Delete a training plan template
     * @return No Content
     */
    deleteTrainingPlanTemplateEndpoint(templateId: string, signal?: AbortSignal): Promise<void> {
        let url_ = this.baseUrl + "/training/plan-templates/{TemplateId}";
        if (templateId === undefined || templateId === null)
            throw new globalThis.Error("The parameter 'templateId' must be defined.");
        url_ = url_.replace("{templateId}", encodeURIComponent("" + templateId));
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "DELETE",
            url: url_,
            headers: {
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processDeleteTrainingPlanTemplateEndpoint(_response);
        });
    }

    protected processDeleteTrainingPlanTemplateEndpoint(response: AxiosResponse): Promise<void> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 204) {
            const _responseText = response.data;
            return Promise.resolve<void>(null as any);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<void>(null as any);
    }

    /**
     * Save a training plan as a template
     * @return Success
     */
    createTrainingPlanTemplateFromPlanEndpoint(createTemplateFromPlanRequest: CreateTemplateFromPlanRequest, signal?: AbortSignal): Promise<TrainingPlanTemplateSummaryDto> {
        let url_ = this.baseUrl + "/training/plan-templates/from-plan";
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(createTemplateFromPlanRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processCreateTrainingPlanTemplateFromPlanEndpoint(_response);
        });
    }

    protected processCreateTrainingPlanTemplateFromPlanEndpoint(response: AxiosResponse): Promise<TrainingPlanTemplateSummaryDto> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<TrainingPlanTemplateSummaryDto>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<TrainingPlanTemplateSummaryDto>(null as any);
    }

    /**
     * Copy a training plan template
     * @return No Content
     */
    copyTrainingPlanTemplateEndpoint(templateId: string, signal?: AbortSignal): Promise<void> {
        let url_ = this.baseUrl + "/training/plan-templates/{TemplateId}/copy";
        if (templateId === undefined || templateId === null)
            throw new globalThis.Error("The parameter 'templateId' must be defined.");
        url_ = url_.replace("{templateId}", encodeURIComponent("" + templateId));
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "POST",
            url: url_,
            headers: {
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processCopyTrainingPlanTemplateEndpoint(_response);
        });
    }

    protected processCopyTrainingPlanTemplateEndpoint(response: AxiosResponse): Promise<void> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 204) {
            const _responseText = response.data;
            return Promise.resolve<void>(null as any);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<void>(null as any);
    }

    /**
     * Full-state update of a training plan
     * @param planId The plan's public identifier (route parameter).
     * @return Success
     */
    updateTrainingPlanEndpoint(planId: string, updateTrainingPlanRequest: UpdateTrainingPlanRequest, signal?: AbortSignal): Promise<GetTrainingPlanResponse> {
        let url_ = this.baseUrl + "/training/plans/{planId}";
        if (planId === undefined || planId === null)
            throw new globalThis.Error("The parameter 'planId' must be defined.");
        url_ = url_.replace("{planId}", encodeURIComponent("" + planId));
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(updateTrainingPlanRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "PUT",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processUpdateTrainingPlanEndpoint(_response);
        });
    }

    protected processUpdateTrainingPlanEndpoint(response: AxiosResponse): Promise<GetTrainingPlanResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GetTrainingPlanResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GetTrainingPlanResponse>(null as any);
    }

    /**
     * Get a training plan
     * @param planId The plan's public identifier.
     * @return Success
     */
    getTrainingPlanEndpoint(planId: string, signal?: AbortSignal): Promise<GetTrainingPlanResponse> {
        let url_ = this.baseUrl + "/training/plans/{planId}";
        if (planId === undefined || planId === null)
            throw new globalThis.Error("The parameter 'planId' must be defined.");
        url_ = url_.replace("{planId}", encodeURIComponent("" + planId));
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGetTrainingPlanEndpoint(_response);
        });
    }

    protected processGetTrainingPlanEndpoint(response: AxiosResponse): Promise<GetTrainingPlanResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GetTrainingPlanResponse>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GetTrainingPlanResponse>(null as any);
    }

    /**
     * Delete a training plan
     * @param planId The plan's public identifier.
     * @return No Content
     */
    deleteTrainingPlanEndpoint(planId: string, signal?: AbortSignal): Promise<void> {
        let url_ = this.baseUrl + "/training/plans/{planId}";
        if (planId === undefined || planId === null)
            throw new globalThis.Error("The parameter 'planId' must be defined.");
        url_ = url_.replace("{planId}", encodeURIComponent("" + planId));
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "DELETE",
            url: url_,
            headers: {
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processDeleteTrainingPlanEndpoint(_response);
        });
    }

    protected processDeleteTrainingPlanEndpoint(response: AxiosResponse): Promise<void> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 204) {
            const _responseText = response.data;
            return Promise.resolve<void>(null as any);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<void>(null as any);
    }

    /**
     * Unlock a training session for editing
     * @param planId Training plan identifier.
     * @param sessionId Session identifier to unlock for editing.
     * @return No Content
     */
    unlockTrainingSessionEndpoint(planId: string, sessionId: string, signal?: AbortSignal): Promise<void> {
        let url_ = this.baseUrl + "/training/plans/{planId}/sessions/{sessionId}/unlock";
        if (planId === undefined || planId === null)
            throw new globalThis.Error("The parameter 'planId' must be defined.");
        url_ = url_.replace("{planId}", encodeURIComponent("" + planId));
        if (sessionId === undefined || sessionId === null)
            throw new globalThis.Error("The parameter 'sessionId' must be defined.");
        url_ = url_.replace("{sessionId}", encodeURIComponent("" + sessionId));
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "POST",
            url: url_,
            headers: {
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processUnlockTrainingSessionEndpoint(_response);
        });
    }

    protected processUnlockTrainingSessionEndpoint(response: AxiosResponse): Promise<void> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 204) {
            const _responseText = response.data;
            return Promise.resolve<void>(null as any);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<void>(null as any);
    }

    /**
     * Relock a training session (release the Editing lock)
     * @param planId Training plan identifier.
     * @param sessionId Session identifier to relock.
     * @return No Content
     */
    relockTrainingSessionEndpoint(planId: string, sessionId: string, signal?: AbortSignal): Promise<void> {
        let url_ = this.baseUrl + "/training/plans/{planId}/sessions/{sessionId}/relock";
        if (planId === undefined || planId === null)
            throw new globalThis.Error("The parameter 'planId' must be defined.");
        url_ = url_.replace("{planId}", encodeURIComponent("" + planId));
        if (sessionId === undefined || sessionId === null)
            throw new globalThis.Error("The parameter 'sessionId' must be defined.");
        url_ = url_.replace("{sessionId}", encodeURIComponent("" + sessionId));
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "POST",
            url: url_,
            headers: {
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processRelockTrainingSessionEndpoint(_response);
        });
    }

    protected processRelockTrainingSessionEndpoint(response: AxiosResponse): Promise<void> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 204) {
            const _responseText = response.data;
            return Promise.resolve<void>(null as any);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<void>(null as any);
    }

    /**
     * Publish a week of a training plan
     * @param planId Plan identifier.
     * @param weekNumber Week number to publish.
     * @return Success
     */
    publishTrainingWeekEndpoint(planId: string, weekNumber: number, publishTrainingWeekRequest: PublishTrainingWeekRequest, signal?: AbortSignal): Promise<GetTrainingPlanResponse> {
        let url_ = this.baseUrl + "/training/plans/{planId}/weeks/{weekNumber}/publish";
        if (planId === undefined || planId === null)
            throw new globalThis.Error("The parameter 'planId' must be defined.");
        url_ = url_.replace("{planId}", encodeURIComponent("" + planId));
        if (weekNumber === undefined || weekNumber === null)
            throw new globalThis.Error("The parameter 'weekNumber' must be defined.");
        url_ = url_.replace("{weekNumber}", encodeURIComponent("" + weekNumber));
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(publishTrainingWeekRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processPublishTrainingWeekEndpoint(_response);
        });
    }

    protected processPublishTrainingWeekEndpoint(response: AxiosResponse): Promise<GetTrainingPlanResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GetTrainingPlanResponse>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GetTrainingPlanResponse>(null as any);
    }

    /**
     * Link questionnaire to training plan
     * @param planId Plan identifier.
     * @return Success
     */
    linkTrainingQuestionnaireEndpoint(planId: string, linkQuestionnaireRequest: LinkQuestionnaireRequest, signal?: AbortSignal): Promise<GetTrainingPlanResponse> {
        let url_ = this.baseUrl + "/training/plans/{planId}/link-questionnaire";
        if (planId === undefined || planId === null)
            throw new globalThis.Error("The parameter 'planId' must be defined.");
        url_ = url_.replace("{planId}", encodeURIComponent("" + planId));
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(linkQuestionnaireRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "PUT",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processLinkTrainingQuestionnaireEndpoint(_response);
        });
    }

    protected processLinkTrainingQuestionnaireEndpoint(response: AxiosResponse): Promise<GetTrainingPlanResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GetTrainingPlanResponse>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GetTrainingPlanResponse>(null as any);
    }

    /**
     * List training plans
     * @param page Page number (1-based).
     * @param pageSize Number of items per page.
     * @param clientId (optional) Optional filter by client's public user identifier.
     * @param status (optional) Optional filter by plan status.
     * @return Success
     */
    getTrainingPlansEndpoint(page: number, pageSize: number, clientId?: string | null | undefined, status?: TrainingPlanStatus | null | undefined, signal?: AbortSignal): Promise<GetTrainingPlansResponse> {
        let url_ = this.baseUrl + "/training/plans?";
        if (page === undefined || page === null)
            throw new globalThis.Error("The parameter 'page' must be defined and cannot be null.");
        else
            url_ += "page=" + encodeURIComponent("" + page) + "&";
        if (pageSize === undefined || pageSize === null)
            throw new globalThis.Error("The parameter 'pageSize' must be defined and cannot be null.");
        else
            url_ += "pageSize=" + encodeURIComponent("" + pageSize) + "&";
        if (clientId !== undefined && clientId !== null)
            url_ += "clientId=" + encodeURIComponent("" + clientId) + "&";
        if (status !== undefined && status !== null)
            url_ += "status=" + encodeURIComponent("" + status) + "&";
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGetTrainingPlansEndpoint(_response);
        });
    }

    protected processGetTrainingPlansEndpoint(response: AxiosResponse): Promise<GetTrainingPlansResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GetTrainingPlansResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GetTrainingPlansResponse>(null as any);
    }

    /**
     * Create a training plan
     * @return Success
     */
    createTrainingPlanEndpoint(createTrainingPlanRequest: CreateTrainingPlanRequest, signal?: AbortSignal): Promise<TrainingPlanSummaryDto> {
        let url_ = this.baseUrl + "/training/plans";
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(createTrainingPlanRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processCreateTrainingPlanEndpoint(_response);
        });
    }

    protected processCreateTrainingPlanEndpoint(response: AxiosResponse): Promise<TrainingPlanSummaryDto> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<TrainingPlanSummaryDto>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<TrainingPlanSummaryDto>(null as any);
    }

    /**
     * Finish a session on behalf of a client
     * @param planId The training plan ExternalId (route parameter).
     * @param sessionId The session identifier within the plan's weeks (route parameter).
     * @return Success
     */
    finishSessionEndpoint(planId: string, sessionId: string, finishSessionRequest: FinishSessionRequest, signal?: AbortSignal): Promise<FinishSessionResponse> {
        let url_ = this.baseUrl + "/trainer/training/plans/{planId}/sessions/{sessionId}/finish";
        if (planId === undefined || planId === null)
            throw new globalThis.Error("The parameter 'planId' must be defined.");
        url_ = url_.replace("{planId}", encodeURIComponent("" + planId));
        if (sessionId === undefined || sessionId === null)
            throw new globalThis.Error("The parameter 'sessionId' must be defined.");
        url_ = url_.replace("{sessionId}", encodeURIComponent("" + sessionId));
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(finishSessionRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processFinishSessionEndpoint(_response);
        });
    }

    protected processFinishSessionEndpoint(response: AxiosResponse): Promise<FinishSessionResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<FinishSessionResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<FinishSessionResponse>(null as any);
    }

    /**
     * Complete a training plan
     * @param planId Plan identifier.
     * @return Success
     */
    completeTrainingPlanEndpoint(planId: string, completeTrainingPlanRequest: CompleteTrainingPlanRequest, signal?: AbortSignal): Promise<GetTrainingPlanResponse> {
        let url_ = this.baseUrl + "/training/plans/{planId}/complete";
        if (planId === undefined || planId === null)
            throw new globalThis.Error("The parameter 'planId' must be defined.");
        url_ = url_.replace("{planId}", encodeURIComponent("" + planId));
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(completeTrainingPlanRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processCompleteTrainingPlanEndpoint(_response);
        });
    }

    protected processCompleteTrainingPlanEndpoint(response: AxiosResponse): Promise<GetTrainingPlanResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GetTrainingPlanResponse>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GetTrainingPlanResponse>(null as any);
    }

    /**
     * Update professional profile
     * @return No Content
     */
    updateProfessionalProfileEndpoint(updateProfessionalProfileRequest: UpdateProfessionalProfileRequest, signal?: AbortSignal): Promise<void> {
        let url_ = this.baseUrl + "/trainer/profile";
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(updateProfessionalProfileRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "PUT",
            url: url_,
            headers: {
                "Content-Type": "application/json",
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processUpdateProfessionalProfileEndpoint(_response);
        });
    }

    protected processUpdateProfessionalProfileEndpoint(response: AxiosResponse): Promise<void> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 204) {
            const _responseText = response.data;
            return Promise.resolve<void>(null as any);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<void>(null as any);
    }

    /**
     * Get professional profile
     * @return Success
     */
    getProfessionalProfileEndpoint(signal?: AbortSignal): Promise<GetProfessionalProfileResponse> {
        let url_ = this.baseUrl + "/trainer/profile";
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGetProfessionalProfileEndpoint(_response);
        });
    }

    protected processGetProfessionalProfileEndpoint(response: AxiosResponse): Promise<GetProfessionalProfileResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GetProfessionalProfileResponse>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GetProfessionalProfileResponse>(null as any);
    }

    /**
     * Update client data
     * @param clientId Client profile public ID (route parameter).
     * @return Success
     */
    updateClientDataEndpoint(clientId: string, updateClientDataRequest: UpdateClientDataRequest, signal?: AbortSignal): Promise<UpdateClientDataResponse> {
        let url_ = this.baseUrl + "/trainer/clients/{clientId}";
        if (clientId === undefined || clientId === null)
            throw new globalThis.Error("The parameter 'clientId' must be defined.");
        url_ = url_.replace("{clientId}", encodeURIComponent("" + clientId));
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(updateClientDataRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "PUT",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processUpdateClientDataEndpoint(_response);
        });
    }

    protected processUpdateClientDataEndpoint(response: AxiosResponse): Promise<UpdateClientDataResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<UpdateClientDataResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<UpdateClientDataResponse>(null as any);
    }

    /**
     * Get client dashboard
     * @param clientId The client's public ID, provided as a route parameter.
     * @return Success
     */
    getClientDashboardEndpoint(clientId: string, signal?: AbortSignal): Promise<GetClientDashboardResponse> {
        let url_ = this.baseUrl + "/trainer/clients/{clientId}";
        if (clientId === undefined || clientId === null)
            throw new globalThis.Error("The parameter 'clientId' must be defined.");
        url_ = url_.replace("{clientId}", encodeURIComponent("" + clientId));
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGetClientDashboardEndpoint(_response);
        });
    }

    protected processGetClientDashboardEndpoint(response: AxiosResponse): Promise<GetClientDashboardResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GetClientDashboardResponse>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GetClientDashboardResponse>(null as any);
    }

    /**
     * Replace a pending questionnaire
     * @return No Content
     */
    replaceQuestionnaireEndpoint(clientPublicId: string, replaceQuestionnaireRequest: ReplaceQuestionnaireRequest, signal?: AbortSignal): Promise<void> {
        let url_ = this.baseUrl + "/trainer/clients/{clientPublicId}/replace-questionnaire";
        if (clientPublicId === undefined || clientPublicId === null)
            throw new globalThis.Error("The parameter 'clientPublicId' must be defined.");
        url_ = url_.replace("{clientPublicId}", encodeURIComponent("" + clientPublicId));
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(replaceQuestionnaireRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processReplaceQuestionnaireEndpoint(_response);
        });
    }

    protected processReplaceQuestionnaireEndpoint(response: AxiosResponse): Promise<void> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 204) {
            const _responseText = response.data;
            return Promise.resolve<void>(null as any);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<void>(null as any);
    }

    /**
     * Get pending invitations
     * @return Success
     */
    getPendingInvitesEndpoint(signal?: AbortSignal): Promise<GetPendingInvitesResponse> {
        let url_ = this.baseUrl + "/trainer/pending-invites";
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGetPendingInvitesEndpoint(_response);
        });
    }

    protected processGetPendingInvitesEndpoint(response: AxiosResponse): Promise<GetPendingInvitesResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GetPendingInvitesResponse>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GetPendingInvitesResponse>(null as any);
    }

    /**
     * Create a pending invitation
     * @return Success
     */
    createPendingInviteEndpoint(createPendingInviteRequest: CreatePendingInviteRequest, signal?: AbortSignal): Promise<CreatePendingInviteResponse> {
        let url_ = this.baseUrl + "/trainer/pending-invites";
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(createPendingInviteRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processCreatePendingInviteEndpoint(_response);
        });
    }

    protected processCreatePendingInviteEndpoint(response: AxiosResponse): Promise<CreatePendingInviteResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<CreatePendingInviteResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<CreatePendingInviteResponse>(null as any);
    }

    /**
     * Delete a pending invitation
     * @param id Public identifier of the pending invite to delete (bound from route).
     * @return No Content
     */
    deletePendingInviteEndpoint(id: string, signal?: AbortSignal): Promise<void> {
        let url_ = this.baseUrl + "/trainer/pending-invites/{id}";
        if (id === undefined || id === null)
            throw new globalThis.Error("The parameter 'id' must be defined.");
        url_ = url_.replace("{id}", encodeURIComponent("" + id));
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "DELETE",
            url: url_,
            headers: {
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processDeletePendingInviteEndpoint(_response);
        });
    }

    protected processDeletePendingInviteEndpoint(response: AxiosResponse): Promise<void> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 204) {
            const _responseText = response.data;
            return Promise.resolve<void>(null as any);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<void>(null as any);
    }

    /**
     * List client plans
     * @param clientId The client's PublicId (Guid), as it appears in the route segment.
     * @return Success
     */
    listClientPlansEndpoint(clientId: string, signal?: AbortSignal): Promise<ListClientPlansResponse> {
        let url_ = this.baseUrl + "/trainer/clients/{clientId}/plans";
        if (clientId === undefined || clientId === null)
            throw new globalThis.Error("The parameter 'clientId' must be defined.");
        url_ = url_.replace("{clientId}", encodeURIComponent("" + clientId));
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processListClientPlansEndpoint(_response);
        });
    }

    protected processListClientPlansEndpoint(response: AxiosResponse): Promise<ListClientPlansResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<ListClientPlansResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<ListClientPlansResponse>(null as any);
    }

    /**
     * Invite a client
     * @return Success
     */
    inviteClientEndpoint(inviteClientRequest: InviteClientRequest, signal?: AbortSignal): Promise<InviteClientResponse> {
        let url_ = this.baseUrl + "/trainer/clients/invite";
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(inviteClientRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processInviteClientEndpoint(_response);
        });
    }

    protected processInviteClientEndpoint(response: AxiosResponse): Promise<InviteClientResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<InviteClientResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<InviteClientResponse>(null as any);
    }

    /**
     * Get trainer dashboard summary
     * @return Success
     */
    getDashboardSummaryEndpoint(signal?: AbortSignal): Promise<GetDashboardSummaryResponse> {
        let url_ = this.baseUrl + "/trainer/dashboard-summary";
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGetDashboardSummaryEndpoint(_response);
        });
    }

    protected processGetDashboardSummaryEndpoint(response: AxiosResponse): Promise<GetDashboardSummaryResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GetDashboardSummaryResponse>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GetDashboardSummaryResponse>(null as any);
    }

    /**
     * Get client on-track verdict
     * @return Success
     */
    getClientVerdictEndpoint(clientId: string, signal?: AbortSignal): Promise<GetClientVerdictResponse> {
        let url_ = this.baseUrl + "/trainer/clients/{clientId}/verdict";
        if (clientId === undefined || clientId === null)
            throw new globalThis.Error("The parameter 'clientId' must be defined.");
        url_ = url_.replace("{clientId}", encodeURIComponent("" + clientId));
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGetClientVerdictEndpoint(_response);
        });
    }

    protected processGetClientVerdictEndpoint(response: AxiosResponse): Promise<GetClientVerdictResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GetClientVerdictResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GetClientVerdictResponse>(null as any);
    }

    /**
     * Get client activity timeline
     * @param clientId The client's ApplicationUser.Id, provided as a route parameter.
     * @param limit Maximum number of items to return. Defaults to 30, capped at 100.
     * @return Success
     */
    getClientTimelineEndpoint(clientId: string, limit: number, signal?: AbortSignal): Promise<GetClientTimelineResponse> {
        let url_ = this.baseUrl + "/trainer/clients/{clientId}/timeline?";
        if (clientId === undefined || clientId === null)
            throw new globalThis.Error("The parameter 'clientId' must be defined.");
        url_ = url_.replace("{clientId}", encodeURIComponent("" + clientId));
        if (limit === undefined || limit === null)
            throw new globalThis.Error("The parameter 'limit' must be defined and cannot be null.");
        else
            url_ += "limit=" + encodeURIComponent("" + limit) + "&";
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGetClientTimelineEndpoint(_response);
        });
    }

    protected processGetClientTimelineEndpoint(response: AxiosResponse): Promise<GetClientTimelineResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GetClientTimelineResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GetClientTimelineResponse>(null as any);
    }

    /**
     * Get trainer's clients
     * @param page Page number (1-based). Defaults to 1.
     * @param pageSize Number of items per page. Defaults to 20.
     * @param search (optional) Optional search filter by client name or email.
     * @return Success
     */
    getClientsEndpoint(page: number, pageSize: number, search?: string | null | undefined, signal?: AbortSignal): Promise<GetClientsResponse> {
        let url_ = this.baseUrl + "/trainer/clients?";
        if (page === undefined || page === null)
            throw new globalThis.Error("The parameter 'page' must be defined and cannot be null.");
        else
            url_ += "page=" + encodeURIComponent("" + page) + "&";
        if (pageSize === undefined || pageSize === null)
            throw new globalThis.Error("The parameter 'pageSize' must be defined and cannot be null.");
        else
            url_ += "pageSize=" + encodeURIComponent("" + pageSize) + "&";
        if (search !== undefined && search !== null)
            url_ += "search=" + encodeURIComponent("" + search) + "&";
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGetClientsEndpoint(_response);
        });
    }

    protected processGetClientsEndpoint(response: AxiosResponse): Promise<GetClientsResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GetClientsResponse>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GetClientsResponse>(null as any);
    }

    /**
     * Get client progress
     * @param clientId The client's ApplicationUser.Id, provided as a route parameter.
     * @param from (optional) Start date for the progress calculation. Defaults to 7 days ago if not provided.
     * @param to (optional) End date for the progress calculation. Defaults to today if not provided.
     * @return Success
     */
    getClientProgressEndpoint(clientId: string, from?: string | null | undefined, to?: string | null | undefined, signal?: AbortSignal): Promise<GetClientProgressResponse> {
        let url_ = this.baseUrl + "/trainer/clients/{clientId}/progress?";
        if (clientId === undefined || clientId === null)
            throw new globalThis.Error("The parameter 'clientId' must be defined.");
        url_ = url_.replace("{clientId}", encodeURIComponent("" + clientId));
        if (from !== undefined && from !== null)
            url_ += "from=" + encodeURIComponent("" + from) + "&";
        if (to !== undefined && to !== null)
            url_ += "to=" + encodeURIComponent("" + to) + "&";
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGetClientProgressEndpoint(_response);
        });
    }

    protected processGetClientProgressEndpoint(response: AxiosResponse): Promise<GetClientProgressResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GetClientProgressResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GetClientProgressResponse>(null as any);
    }

    /**
     * Create collaboration
     * @return Success
     */
    createCollaborationEndpoint(createCollaborationRequest: CreateCollaborationRequest, signal?: AbortSignal): Promise<CreateCollaborationResponse> {
        let url_ = this.baseUrl + "/trainer/collaborations";
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(createCollaborationRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processCreateCollaborationEndpoint(_response);
        });
    }

    protected processCreateCollaborationEndpoint(response: AxiosResponse): Promise<CreateCollaborationResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<CreateCollaborationResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<CreateCollaborationResponse>(null as any);
    }

    /**
     * Reject a client request
     * @param publicId Public identifier of the client request (from route).
     * @return No Content
     */
    rejectClientRequestEndpoint(publicId: string, rejectClientRequestRequest: RejectClientRequestRequest, signal?: AbortSignal): Promise<void> {
        let url_ = this.baseUrl + "/trainer/client-requests/{publicId}/reject";
        if (publicId === undefined || publicId === null)
            throw new globalThis.Error("The parameter 'publicId' must be defined.");
        url_ = url_.replace("{publicId}", encodeURIComponent("" + publicId));
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(rejectClientRequestRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processRejectClientRequestEndpoint(_response);
        });
    }

    protected processRejectClientRequestEndpoint(response: AxiosResponse): Promise<void> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 204) {
            const _responseText = response.data;
            return Promise.resolve<void>(null as any);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<void>(null as any);
    }

    /**
     * Get incoming client requests
     * @return Success
     */
    getIncomingRequestsEndpoint(signal?: AbortSignal): Promise<GetIncomingRequestsResponse> {
        let url_ = this.baseUrl + "/trainer/client-requests";
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGetIncomingRequestsEndpoint(_response);
        });
    }

    protected processGetIncomingRequestsEndpoint(response: AxiosResponse): Promise<GetIncomingRequestsResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GetIncomingRequestsResponse>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GetIncomingRequestsResponse>(null as any);
    }

    /**
     * Accept a client request
     * @param publicId Public identifier of the client request (from route).
     * @return No Content
     */
    acceptClientRequestEndpoint(publicId: string, acceptClientRequestRequest: AcceptClientRequestRequest, signal?: AbortSignal): Promise<void> {
        let url_ = this.baseUrl + "/trainer/client-requests/{publicId}/accept";
        if (publicId === undefined || publicId === null)
            throw new globalThis.Error("The parameter 'publicId' must be defined.");
        url_ = url_.replace("{publicId}", encodeURIComponent("" + publicId));
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(acceptClientRequestRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processAcceptClientRequestEndpoint(_response);
        });
    }

    protected processAcceptClientRequestEndpoint(response: AxiosResponse): Promise<void> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 204) {
            const _responseText = response.data;
            return Promise.resolve<void>(null as any);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<void>(null as any);
    }

    /**
     * List trainer notes for a client
     * @return Success
     */
    listNotesEndpoint(clientId: string, page: number, pageSize: number, signal?: AbortSignal): Promise<ListNotesResponse> {
        let url_ = this.baseUrl + "/trainer/clients/{clientId}/notes?";
        if (clientId === undefined || clientId === null)
            throw new globalThis.Error("The parameter 'clientId' must be defined.");
        url_ = url_.replace("{clientId}", encodeURIComponent("" + clientId));
        if (page === undefined || page === null)
            throw new globalThis.Error("The parameter 'page' must be defined and cannot be null.");
        else
            url_ += "page=" + encodeURIComponent("" + page) + "&";
        if (pageSize === undefined || pageSize === null)
            throw new globalThis.Error("The parameter 'pageSize' must be defined and cannot be null.");
        else
            url_ += "pageSize=" + encodeURIComponent("" + pageSize) + "&";
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processListNotesEndpoint(_response);
        });
    }

    protected processListNotesEndpoint(response: AxiosResponse): Promise<ListNotesResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<ListNotesResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<ListNotesResponse>(null as any);
    }

    /**
     * Create a trainer note for a client
     * @return Success
     */
    createNoteEndpoint(clientId: string, createNoteRequest: CreateNoteRequest, signal?: AbortSignal): Promise<CreateNoteResponse> {
        let url_ = this.baseUrl + "/trainer/clients/{clientId}/notes";
        if (clientId === undefined || clientId === null)
            throw new globalThis.Error("The parameter 'clientId' must be defined.");
        url_ = url_.replace("{clientId}", encodeURIComponent("" + clientId));
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(createNoteRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processCreateNoteEndpoint(_response);
        });
    }

    protected processCreateNoteEndpoint(response: AxiosResponse): Promise<CreateNoteResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<CreateNoteResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<CreateNoteResponse>(null as any);
    }

    /**
     * Edit a trainer note
     * @return Success
     */
    editNoteEndpoint(clientId: string, noteId: string, editNoteRequest: EditNoteRequest, signal?: AbortSignal): Promise<EditNoteResponse> {
        let url_ = this.baseUrl + "/trainer/clients/{clientId}/notes/{noteId}";
        if (clientId === undefined || clientId === null)
            throw new globalThis.Error("The parameter 'clientId' must be defined.");
        url_ = url_.replace("{clientId}", encodeURIComponent("" + clientId));
        if (noteId === undefined || noteId === null)
            throw new globalThis.Error("The parameter 'noteId' must be defined.");
        url_ = url_.replace("{noteId}", encodeURIComponent("" + noteId));
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(editNoteRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "PATCH",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processEditNoteEndpoint(_response);
        });
    }

    protected processEditNoteEndpoint(response: AxiosResponse): Promise<EditNoteResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<EditNoteResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<EditNoteResponse>(null as any);
    }

    /**
     * Delete a trainer note
     * @return No Content
     */
    deleteNoteEndpoint(clientId: string, noteId: string, signal?: AbortSignal): Promise<void> {
        let url_ = this.baseUrl + "/trainer/clients/{clientId}/notes/{noteId}";
        if (clientId === undefined || clientId === null)
            throw new globalThis.Error("The parameter 'clientId' must be defined.");
        url_ = url_.replace("{clientId}", encodeURIComponent("" + clientId));
        if (noteId === undefined || noteId === null)
            throw new globalThis.Error("The parameter 'noteId' must be defined.");
        url_ = url_.replace("{noteId}", encodeURIComponent("" + noteId));
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "DELETE",
            url: url_,
            headers: {
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processDeleteNoteEndpoint(_response);
        });
    }

    protected processDeleteNoteEndpoint(response: AxiosResponse): Promise<void> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 204) {
            const _responseText = response.data;
            return Promise.resolve<void>(null as any);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<void>(null as any);
    }

    /**
     * Cancel a pending questionnaire
     * @return No Content
     */
    cancelQuestionnaireEndpoint(clientPublicId: string, signal?: AbortSignal): Promise<void> {
        let url_ = this.baseUrl + "/trainer/clients/{clientPublicId}/cancel-questionnaire";
        if (clientPublicId === undefined || clientPublicId === null)
            throw new globalThis.Error("The parameter 'clientPublicId' must be defined.");
        url_ = url_.replace("{clientPublicId}", encodeURIComponent("" + clientPublicId));
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "POST",
            url: url_,
            headers: {
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processCancelQuestionnaireEndpoint(_response);
        });
    }

    protected processCancelQuestionnaireEndpoint(response: AxiosResponse): Promise<void> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 204) {
            const _responseText = response.data;
            return Promise.resolve<void>(null as any);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<void>(null as any);
    }

    /**
     * Assign a questionnaire to a client
     * @param clientPublicId Public identifier of the client (from route).
     * @return No Content
     */
    assignQuestionnaireEndpoint(clientPublicId: string, assignQuestionnaireRequest: AssignQuestionnaireRequest, signal?: AbortSignal): Promise<void> {
        let url_ = this.baseUrl + "/trainer/clients/{clientPublicId}/assign-questionnaire";
        if (clientPublicId === undefined || clientPublicId === null)
            throw new globalThis.Error("The parameter 'clientPublicId' must be defined.");
        url_ = url_.replace("{clientPublicId}", encodeURIComponent("" + clientPublicId));
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(assignQuestionnaireRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processAssignQuestionnaireEndpoint(_response);
        });
    }

    protected processAssignQuestionnaireEndpoint(response: AxiosResponse): Promise<void> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 204) {
            const _responseText = response.data;
            return Promise.resolve<void>(null as any);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<void>(null as any);
    }

    /**
     * Update session template
     * @param templateId Public identifier of the session template to update.
     * @return Session template updated
     */
    updateSessionTemplateEndpoint(templateId: string, updateSessionTemplateRequest: UpdateSessionTemplateRequest, signal?: AbortSignal): Promise<SessionTemplateDetailResponse> {
        let url_ = this.baseUrl + "/training/session-templates/{templateId}";
        if (templateId === undefined || templateId === null)
            throw new globalThis.Error("The parameter 'templateId' must be defined.");
        url_ = url_.replace("{templateId}", encodeURIComponent("" + templateId));
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(updateSessionTemplateRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "PUT",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processUpdateSessionTemplateEndpoint(_response);
        });
    }

    protected processUpdateSessionTemplateEndpoint(response: AxiosResponse): Promise<SessionTemplateDetailResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<SessionTemplateDetailResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Invalid request body", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Missing or invalid credentials", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Readable but owned by another trainer", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<SessionTemplateDetailResponse>(null as any);
    }

    /**
     * Get session template
     * @param templateId Public identifier of the session template.
     * @return Session template detail
     */
    getSessionTemplateEndpoint(templateId: string, signal?: AbortSignal): Promise<SessionTemplateDetailResponse> {
        let url_ = this.baseUrl + "/training/session-templates/{templateId}";
        if (templateId === undefined || templateId === null)
            throw new globalThis.Error("The parameter 'templateId' must be defined.");
        url_ = url_.replace("{templateId}", encodeURIComponent("" + templateId));
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGetSessionTemplateEndpoint(_response);
        });
    }

    protected processGetSessionTemplateEndpoint(response: AxiosResponse): Promise<SessionTemplateDetailResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<SessionTemplateDetailResponse>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Missing or invalid credentials", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<SessionTemplateDetailResponse>(null as any);
    }

    /**
     * Delete session template
     * @param templateId Public identifier of the session template to delete.
     * @return Session template deleted
     */
    deleteSessionTemplateEndpoint(templateId: string, signal?: AbortSignal): Promise<void> {
        let url_ = this.baseUrl + "/training/session-templates/{templateId}";
        if (templateId === undefined || templateId === null)
            throw new globalThis.Error("The parameter 'templateId' must be defined.");
        url_ = url_.replace("{templateId}", encodeURIComponent("" + templateId));
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "DELETE",
            url: url_,
            headers: {
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processDeleteSessionTemplateEndpoint(_response);
        });
    }

    protected processDeleteSessionTemplateEndpoint(response: AxiosResponse): Promise<void> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 204) {
            const _responseText = response.data;
            return Promise.resolve<void>(null as any);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Missing or invalid credentials", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Readable but owned by another trainer", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<void>(null as any);
    }

    /**
     * Search session templates
     * @param page Page number (1-based). Defaults to 1.
     * @param pageSize Number of items per page. Defaults to 20.
     * @param search (optional) Optional search term to filter templates by name.
     * @param difficulty (optional) Optional difficulty filter.
     * @param maxEstimatedDurationMinutes (optional) Optional maximum estimated duration in minutes (inclusive).
     * @return Paged session template results
     */
    searchSessionTemplatesEndpoint(page: number, pageSize: number, search?: string | null | undefined, difficulty?: ExerciseDifficulty | null | undefined, maxEstimatedDurationMinutes?: number | null | undefined, signal?: AbortSignal): Promise<SearchSessionTemplatesResponse> {
        let url_ = this.baseUrl + "/training/session-templates?";
        if (page === undefined || page === null)
            throw new globalThis.Error("The parameter 'page' must be defined and cannot be null.");
        else
            url_ += "page=" + encodeURIComponent("" + page) + "&";
        if (pageSize === undefined || pageSize === null)
            throw new globalThis.Error("The parameter 'pageSize' must be defined and cannot be null.");
        else
            url_ += "pageSize=" + encodeURIComponent("" + pageSize) + "&";
        if (search !== undefined && search !== null)
            url_ += "search=" + encodeURIComponent("" + search) + "&";
        if (difficulty !== undefined && difficulty !== null)
            url_ += "difficulty=" + encodeURIComponent("" + difficulty) + "&";
        if (maxEstimatedDurationMinutes !== undefined && maxEstimatedDurationMinutes !== null)
            url_ += "maxEstimatedDurationMinutes=" + encodeURIComponent("" + maxEstimatedDurationMinutes) + "&";
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processSearchSessionTemplatesEndpoint(_response);
        });
    }

    protected processSearchSessionTemplatesEndpoint(response: AxiosResponse): Promise<SearchSessionTemplatesResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<SearchSessionTemplatesResponse>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Missing or invalid credentials", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<SearchSessionTemplatesResponse>(null as any);
    }

    /**
     * Create session template
     * @return Success
     */
    createSessionTemplateEndpoint(createSessionTemplateRequest: CreateSessionTemplateRequest, signal?: AbortSignal): Promise<SessionTemplateDetailResponse> {
        let url_ = this.baseUrl + "/training/session-templates";
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(createSessionTemplateRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processCreateSessionTemplateEndpoint(_response);
        });
    }

    protected processCreateSessionTemplateEndpoint(response: AxiosResponse): Promise<SessionTemplateDetailResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<SessionTemplateDetailResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Invalid request body", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Missing or invalid credentials", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<SessionTemplateDetailResponse>(null as any);
    }

    /**
     * Save session template from plan
     * @return Success
     */
    saveSessionTemplateFromPlanEndpoint(saveSessionTemplateFromPlanRequest: SaveSessionTemplateFromPlanRequest, signal?: AbortSignal): Promise<SessionTemplateDetailResponse> {
        let url_ = this.baseUrl + "/training/session-templates/from-plan";
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(saveSessionTemplateFromPlanRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processSaveSessionTemplateFromPlanEndpoint(_response);
        });
    }

    protected processSaveSessionTemplateFromPlanEndpoint(response: AxiosResponse): Promise<SessionTemplateDetailResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<SessionTemplateDetailResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Invalid request body", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Missing or invalid credentials", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<SessionTemplateDetailResponse>(null as any);
    }

    /**
     * Copy session template
     * @param templateId Public identifier of the source session template to copy.
     * @return Success
     */
    copySessionTemplateEndpoint(templateId: string, signal?: AbortSignal): Promise<SessionTemplateDetailResponse> {
        let url_ = this.baseUrl + "/training/session-templates/{templateId}/copy";
        if (templateId === undefined || templateId === null)
            throw new globalThis.Error("The parameter 'templateId' must be defined.");
        url_ = url_.replace("{templateId}", encodeURIComponent("" + templateId));
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "POST",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processCopySessionTemplateEndpoint(_response);
        });
    }

    protected processCopySessionTemplateEndpoint(response: AxiosResponse): Promise<SessionTemplateDetailResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<SessionTemplateDetailResponse>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Missing or invalid credentials", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<SessionTemplateDetailResponse>(null as any);
    }

    /**
     * Generate recipe image upload URL
     * @param recipeId The recipe's public identifier (from route).
     * @param slot Image slot: main (overwrites) or gallery (appends, max 6).
    Provided as a query parameter: ?slot=main or ?slot=gallery.
     * @return Success
     */
    uploadRecipeImageUrlEndpoint(recipeId: string, slot: string, uploadRecipeImageUrlRequest: UploadRecipeImageUrlRequest, signal?: AbortSignal): Promise<UploadRecipeImageUrlResponse> {
        let url_ = this.baseUrl + "/recipes/{recipeId}/image/upload-url?";
        if (recipeId === undefined || recipeId === null)
            throw new globalThis.Error("The parameter 'recipeId' must be defined.");
        url_ = url_.replace("{recipeId}", encodeURIComponent("" + recipeId));
        if (slot === undefined || slot === null)
            throw new globalThis.Error("The parameter 'slot' must be defined and cannot be null.");
        else
            url_ += "slot=" + encodeURIComponent("" + slot) + "&";
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(uploadRecipeImageUrlRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processUploadRecipeImageUrlEndpoint(_response);
        });
    }

    protected processUploadRecipeImageUrlEndpoint(response: AxiosResponse): Promise<UploadRecipeImageUrlResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<UploadRecipeImageUrlResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<UploadRecipeImageUrlResponse>(null as any);
    }

    /**
     * Update recipe
     * @param recipeId Public identifier of the recipe to update.
     * @return Success
     */
    updateRecipeEndpoint(recipeId: string, updateRecipeRequest: UpdateRecipeRequest, signal?: AbortSignal): Promise<GetRecipeResponse> {
        let url_ = this.baseUrl + "/recipes/{recipeId}";
        if (recipeId === undefined || recipeId === null)
            throw new globalThis.Error("The parameter 'recipeId' must be defined.");
        url_ = url_.replace("{recipeId}", encodeURIComponent("" + recipeId));
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(updateRecipeRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "PUT",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processUpdateRecipeEndpoint(_response);
        });
    }

    protected processUpdateRecipeEndpoint(response: AxiosResponse): Promise<GetRecipeResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GetRecipeResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GetRecipeResponse>(null as any);
    }

    /**
     * Get recipe
     * @param recipeId Public identifier of the recipe.
     * @return Success
     */
    getRecipeEndpoint(recipeId: string, signal?: AbortSignal): Promise<GetRecipeResponse> {
        let url_ = this.baseUrl + "/recipes/{recipeId}";
        if (recipeId === undefined || recipeId === null)
            throw new globalThis.Error("The parameter 'recipeId' must be defined.");
        url_ = url_.replace("{recipeId}", encodeURIComponent("" + recipeId));
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGetRecipeEndpoint(_response);
        });
    }

    protected processGetRecipeEndpoint(response: AxiosResponse): Promise<GetRecipeResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GetRecipeResponse>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GetRecipeResponse>(null as any);
    }

    /**
     * Delete recipe
     * @param recipeId Public identifier of the recipe to delete.
     * @return No Content
     */
    deleteRecipeEndpoint(recipeId: string, signal?: AbortSignal): Promise<void> {
        let url_ = this.baseUrl + "/recipes/{recipeId}";
        if (recipeId === undefined || recipeId === null)
            throw new globalThis.Error("The parameter 'recipeId' must be defined.");
        url_ = url_.replace("{recipeId}", encodeURIComponent("" + recipeId));
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "DELETE",
            url: url_,
            headers: {
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processDeleteRecipeEndpoint(_response);
        });
    }

    protected processDeleteRecipeEndpoint(response: AxiosResponse): Promise<void> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 204) {
            const _responseText = response.data;
            return Promise.resolve<void>(null as any);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<void>(null as any);
    }

    /**
     * Search recipes
     * @param page Page number (1-based). Defaults to 1.
     * @param pageSize Number of items per page. Defaults to 20.
     * @param search (optional) Optional search term to filter recipes by name.
     * @return Success
     */
    searchRecipesEndpoint(page: number, pageSize: number, search?: string | null | undefined, signal?: AbortSignal): Promise<SearchRecipesResponse> {
        let url_ = this.baseUrl + "/recipes?";
        if (page === undefined || page === null)
            throw new globalThis.Error("The parameter 'page' must be defined and cannot be null.");
        else
            url_ += "page=" + encodeURIComponent("" + page) + "&";
        if (pageSize === undefined || pageSize === null)
            throw new globalThis.Error("The parameter 'pageSize' must be defined and cannot be null.");
        else
            url_ += "pageSize=" + encodeURIComponent("" + pageSize) + "&";
        if (search !== undefined && search !== null)
            url_ += "search=" + encodeURIComponent("" + search) + "&";
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processSearchRecipesEndpoint(_response);
        });
    }

    protected processSearchRecipesEndpoint(response: AxiosResponse): Promise<SearchRecipesResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<SearchRecipesResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<SearchRecipesResponse>(null as any);
    }

    /**
     * Create recipe
     * @return Success
     */
    createRecipeEndpoint(createRecipeRequest: CreateRecipeRequest, signal?: AbortSignal): Promise<GetRecipeResponse> {
        let url_ = this.baseUrl + "/recipes";
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(createRecipeRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processCreateRecipeEndpoint(_response);
        });
    }

    protected processCreateRecipeEndpoint(response: AxiosResponse): Promise<GetRecipeResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GetRecipeResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GetRecipeResponse>(null as any);
    }

    /**
     * Confirm recipe image upload
     * @param recipeId The recipe's public identifier (from route).
     * @param slot Image slot: main (overwrites ImageUrl) or gallery (appends to GalleryImageUrls).
    Provided as a query parameter: ?slot=main or ?slot=gallery.
     * @return No Content
     */
    confirmRecipeImageEndpoint(recipeId: string, slot: string, confirmRecipeImageRequest: ConfirmRecipeImageRequest, signal?: AbortSignal): Promise<void> {
        let url_ = this.baseUrl + "/recipes/{recipeId}/image?";
        if (recipeId === undefined || recipeId === null)
            throw new globalThis.Error("The parameter 'recipeId' must be defined.");
        url_ = url_.replace("{recipeId}", encodeURIComponent("" + recipeId));
        if (slot === undefined || slot === null)
            throw new globalThis.Error("The parameter 'slot' must be defined and cannot be null.");
        else
            url_ += "slot=" + encodeURIComponent("" + slot) + "&";
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(confirmRecipeImageRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "PUT",
            url: url_,
            headers: {
                "Content-Type": "application/json",
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processConfirmRecipeImageEndpoint(_response);
        });
    }

    protected processConfirmRecipeImageEndpoint(response: AxiosResponse): Promise<void> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 204) {
            const _responseText = response.data;
            return Promise.resolve<void>(null as any);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<void>(null as any);
    }

    /**
     * Update questionnaire response
     * @return No Content
     */
    updateResponseEndpoint(responsePublicId: string, updateResponseRequest: UpdateResponseRequest, signal?: AbortSignal): Promise<void> {
        let url_ = this.baseUrl + "/client/questionnaire/response/{responsePublicId}";
        if (responsePublicId === undefined || responsePublicId === null)
            throw new globalThis.Error("The parameter 'responsePublicId' must be defined.");
        url_ = url_.replace("{responsePublicId}", encodeURIComponent("" + responsePublicId));
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(updateResponseRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "PUT",
            url: url_,
            headers: {
                "Content-Type": "application/json",
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processUpdateResponseEndpoint(_response);
        });
    }

    protected processUpdateResponseEndpoint(response: AxiosResponse): Promise<void> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 204) {
            const _responseText = response.data;
            return Promise.resolve<void>(null as any);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<void>(null as any);
    }

    /**
     * Update questionnaire
     * @return Success
     */
    updateQuestionnaireEndpoint(publicId: string, updateQuestionnaireRequest: UpdateQuestionnaireRequest, signal?: AbortSignal): Promise<GetTrainerQuestionnaireResponse> {
        let url_ = this.baseUrl + "/trainer/questionnaires/{publicId}";
        if (publicId === undefined || publicId === null)
            throw new globalThis.Error("The parameter 'publicId' must be defined.");
        url_ = url_.replace("{publicId}", encodeURIComponent("" + publicId));
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(updateQuestionnaireRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "PUT",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processUpdateQuestionnaireEndpoint(_response);
        });
    }

    protected processUpdateQuestionnaireEndpoint(response: AxiosResponse): Promise<GetTrainerQuestionnaireResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GetTrainerQuestionnaireResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GetTrainerQuestionnaireResponse>(null as any);
    }

    /**
     * Get questionnaire detail
     * @return Success
     */
    getQuestionnaireDetailEndpoint(publicId: string, signal?: AbortSignal): Promise<GetTrainerQuestionnaireResponse> {
        let url_ = this.baseUrl + "/trainer/questionnaires/{publicId}";
        if (publicId === undefined || publicId === null)
            throw new globalThis.Error("The parameter 'publicId' must be defined.");
        url_ = url_.replace("{publicId}", encodeURIComponent("" + publicId));
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGetQuestionnaireDetailEndpoint(_response);
        });
    }

    protected processGetQuestionnaireDetailEndpoint(response: AxiosResponse): Promise<GetTrainerQuestionnaireResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GetTrainerQuestionnaireResponse>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GetTrainerQuestionnaireResponse>(null as any);
    }

    /**
     * Delete questionnaire
     * @return No Content
     */
    deleteQuestionnaireEndpoint(publicId: string, signal?: AbortSignal): Promise<void> {
        let url_ = this.baseUrl + "/trainer/questionnaires/{publicId}";
        if (publicId === undefined || publicId === null)
            throw new globalThis.Error("The parameter 'publicId' must be defined.");
        url_ = url_.replace("{publicId}", encodeURIComponent("" + publicId));
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "DELETE",
            url: url_,
            headers: {
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processDeleteQuestionnaireEndpoint(_response);
        });
    }

    protected processDeleteQuestionnaireEndpoint(response: AxiosResponse): Promise<void> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 204) {
            const _responseText = response.data;
            return Promise.resolve<void>(null as any);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<void>(null as any);
    }

    /**
     * Submit questionnaire response
     * @return No Content
     */
    submitResponseEndpoint(responsePublicId: string, signal?: AbortSignal): Promise<void> {
        let url_ = this.baseUrl + "/client/questionnaire/response/{responsePublicId}/submit";
        if (responsePublicId === undefined || responsePublicId === null)
            throw new globalThis.Error("The parameter 'responsePublicId' must be defined.");
        url_ = url_.replace("{responsePublicId}", encodeURIComponent("" + responsePublicId));
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "POST",
            url: url_,
            headers: {
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processSubmitResponseEndpoint(_response);
        });
    }

    protected processSubmitResponseEndpoint(response: AxiosResponse): Promise<void> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 204) {
            const _responseText = response.data;
            return Promise.resolve<void>(null as any);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<void>(null as any);
    }

    /**
     * Preview questionnaire
     * @return Success
     */
    previewQuestionnaireEndpoint(publicId: string, signal?: AbortSignal): Promise<GetTrainerQuestionnaireResponse> {
        let url_ = this.baseUrl + "/trainer/questionnaires/{publicId}/preview";
        if (publicId === undefined || publicId === null)
            throw new globalThis.Error("The parameter 'publicId' must be defined.");
        url_ = url_.replace("{publicId}", encodeURIComponent("" + publicId));
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processPreviewQuestionnaireEndpoint(_response);
        });
    }

    protected processPreviewQuestionnaireEndpoint(response: AxiosResponse): Promise<GetTrainerQuestionnaireResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GetTrainerQuestionnaireResponse>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GetTrainerQuestionnaireResponse>(null as any);
    }

    /**
     * List trainer questionnaires
     * @return Success
     */
    getTrainerQuestionnairesEndpoint(signal?: AbortSignal): Promise<GetTrainerQuestionnairesResponse> {
        let url_ = this.baseUrl + "/trainer/questionnaires";
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGetTrainerQuestionnairesEndpoint(_response);
        });
    }

    protected processGetTrainerQuestionnairesEndpoint(response: AxiosResponse): Promise<GetTrainerQuestionnairesResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GetTrainerQuestionnairesResponse>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GetTrainerQuestionnairesResponse>(null as any);
    }

    /**
     * Create questionnaire
     * @return Success
     */
    createQuestionnaireEndpoint(createQuestionnaireRequest: CreateQuestionnaireRequest, signal?: AbortSignal): Promise<GetTrainerQuestionnaireResponse> {
        let url_ = this.baseUrl + "/trainer/questionnaires";
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(createQuestionnaireRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processCreateQuestionnaireEndpoint(_response);
        });
    }

    protected processCreateQuestionnaireEndpoint(response: AxiosResponse): Promise<GetTrainerQuestionnaireResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GetTrainerQuestionnaireResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GetTrainerQuestionnaireResponse>(null as any);
    }

    /**
     * Get all submitted questionnaires grouped by professional
     * @return Success
     */
    getClientSubmittedResponsesEndpoint(signal?: AbortSignal): Promise<GetClientSubmittedResponsesResponse> {
        let url_ = this.baseUrl + "/client/questionnaires/submitted";
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGetClientSubmittedResponsesEndpoint(_response);
        });
    }

    protected processGetClientSubmittedResponsesEndpoint(response: AxiosResponse): Promise<GetClientSubmittedResponsesResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GetClientSubmittedResponsesResponse>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GetClientSubmittedResponsesResponse>(null as any);
    }

    /**
     * Get client's submitted questionnaire
     * @return No Content
     */
    getClientSubmittedResponseEndpoint(signal?: AbortSignal): Promise<void> {
        let url_ = this.baseUrl + "/client/questionnaire/submitted";
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGetClientSubmittedResponseEndpoint(_response);
        });
    }

    protected processGetClientSubmittedResponseEndpoint(response: AxiosResponse): Promise<void> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 204) {
            const _responseText = response.data;
            return Promise.resolve<void>(null as any);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<void>(null as any);
    }

    /**
     * Get all questionnaire responses for a client
     * @return Success
     */
    getClientResponsesEndpoint(clientPublicId: string, signal?: AbortSignal): Promise<GetClientResponsesResponse> {
        let url_ = this.baseUrl + "/trainer/clients/{clientPublicId}/questionnaire-responses";
        if (clientPublicId === undefined || clientPublicId === null)
            throw new globalThis.Error("The parameter 'clientPublicId' must be defined.");
        url_ = url_.replace("{clientPublicId}", encodeURIComponent("" + clientPublicId));
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGetClientResponsesEndpoint(_response);
        });
    }

    protected processGetClientResponsesEndpoint(response: AxiosResponse): Promise<GetClientResponsesResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GetClientResponsesResponse>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GetClientResponsesResponse>(null as any);
    }

    /**
     * Get a specific submitted questionnaire response
     * @return No Content
     */
    getClientResponseByIdEndpoint(responseId: string, signal?: AbortSignal): Promise<void> {
        let url_ = this.baseUrl + "/client/questionnaire/response/{responseId}";
        if (responseId === undefined || responseId === null)
            throw new globalThis.Error("The parameter 'responseId' must be defined.");
        url_ = url_.replace("{responseId}", encodeURIComponent("" + responseId));
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGetClientResponseByIdEndpoint(_response);
        });
    }

    protected processGetClientResponseByIdEndpoint(response: AxiosResponse): Promise<void> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 204) {
            const _responseText = response.data;
            return Promise.resolve<void>(null as any);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<void>(null as any);
    }

    /**
     * Get client questionnaire response
     * @return Success
     */
    getClientResponseEndpoint(clientPublicId: string, signal?: AbortSignal): Promise<GetClientResponseResponse> {
        let url_ = this.baseUrl + "/trainer/clients/{clientPublicId}/questionnaire-response";
        if (clientPublicId === undefined || clientPublicId === null)
            throw new globalThis.Error("The parameter 'clientPublicId' must be defined.");
        url_ = url_.replace("{clientPublicId}", encodeURIComponent("" + clientPublicId));
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGetClientResponseEndpoint(_response);
        });
    }

    protected processGetClientResponseEndpoint(response: AxiosResponse): Promise<GetClientResponseResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GetClientResponseResponse>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GetClientResponseResponse>(null as any);
    }

    /**
     * Get client questionnaire
     * @return Success
     */
    getClientQuestionnaireEndpoint(signal?: AbortSignal): Promise<GetClientQuestionnaireResponse> {
        let url_ = this.baseUrl + "/client/questionnaire";
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGetClientQuestionnaireEndpoint(_response);
        });
    }

    protected processGetClientQuestionnaireEndpoint(response: AxiosResponse): Promise<GetClientQuestionnaireResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GetClientQuestionnaireResponse>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GetClientQuestionnaireResponse>(null as any);
    }

    /**
     * Get pending questionnaires and diary requests for the client
     * @return Success
     */
    getClientPendingQuestionnairesEndpoint(signal?: AbortSignal): Promise<GetClientPendingQuestionnairesResponse> {
        let url_ = this.baseUrl + "/client/questionnaires/pending";
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGetClientPendingQuestionnairesEndpoint(_response);
        });
    }

    protected processGetClientPendingQuestionnairesEndpoint(response: AxiosResponse): Promise<GetClientPendingQuestionnairesResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GetClientPendingQuestionnairesResponse>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GetClientPendingQuestionnairesResponse>(null as any);
    }

    /**
     * Create questionnaire response
     * @return No Content
     */
    createResponseEndpoint(createResponseRequest: CreateResponseRequest, signal?: AbortSignal): Promise<void> {
        let url_ = this.baseUrl + "/client/questionnaire/response";
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(createResponseRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processCreateResponseEndpoint(_response);
        });
    }

    protected processCreateResponseEndpoint(response: AxiosResponse): Promise<void> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 204) {
            const _responseText = response.data;
            return Promise.resolve<void>(null as any);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<void>(null as any);
    }

    /**
     * Search professionals
     * @param city (optional) 
     * @param specialization (optional) 
     * @param role (optional) 
     * @param search (optional) 
     * @return Success
     */
    searchProfessionalsEndpoint(page: number, pageSize: number, city?: string | null | undefined, specialization?: string | null | undefined, role?: string | null | undefined, search?: string | null | undefined, signal?: AbortSignal): Promise<SearchProfessionalsResponse> {
        let url_ = this.baseUrl + "/professionals/search?";
        if (page === undefined || page === null)
            throw new globalThis.Error("The parameter 'page' must be defined and cannot be null.");
        else
            url_ += "page=" + encodeURIComponent("" + page) + "&";
        if (pageSize === undefined || pageSize === null)
            throw new globalThis.Error("The parameter 'pageSize' must be defined and cannot be null.");
        else
            url_ += "pageSize=" + encodeURIComponent("" + pageSize) + "&";
        if (city !== undefined && city !== null)
            url_ += "city=" + encodeURIComponent("" + city) + "&";
        if (specialization !== undefined && specialization !== null)
            url_ += "specialization=" + encodeURIComponent("" + specialization) + "&";
        if (role !== undefined && role !== null)
            url_ += "role=" + encodeURIComponent("" + role) + "&";
        if (search !== undefined && search !== null)
            url_ += "search=" + encodeURIComponent("" + search) + "&";
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processSearchProfessionalsEndpoint(_response);
        });
    }

    protected processSearchProfessionalsEndpoint(response: AxiosResponse): Promise<SearchProfessionalsResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<SearchProfessionalsResponse>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<SearchProfessionalsResponse>(null as any);
    }

    /**
     * Get professional public profile
     * @return Success
     */
    getPublicProfileEndpoint(publicId: string, signal?: AbortSignal): Promise<GetPublicProfileResponse> {
        let url_ = this.baseUrl + "/professionals/{publicId}";
        if (publicId === undefined || publicId === null)
            throw new globalThis.Error("The parameter 'publicId' must be defined.");
        url_ = url_.replace("{publicId}", encodeURIComponent("" + publicId));
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGetPublicProfileEndpoint(_response);
        });
    }

    protected processGetPublicProfileEndpoint(response: AxiosResponse): Promise<GetPublicProfileResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GetPublicProfileResponse>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GetPublicProfileResponse>(null as any);
    }

    /**
     * Confirm professional avatar upload
     * @return No Content
     */
    confirmProfessionalAvatarEndpoint(confirmProfessionalAvatarRequest: ConfirmProfessionalAvatarRequest, signal?: AbortSignal): Promise<void> {
        let url_ = this.baseUrl + "/professionals/me/avatar";
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(confirmProfessionalAvatarRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "PUT",
            url: url_,
            headers: {
                "Content-Type": "application/json",
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processConfirmProfessionalAvatarEndpoint(_response);
        });
    }

    protected processConfirmProfessionalAvatarEndpoint(response: AxiosResponse): Promise<void> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 204) {
            const _responseText = response.data;
            return Promise.resolve<void>(null as any);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<void>(null as any);
    }

    /**
     * Delete professional avatar
     * @return No Content
     */
    deleteProfessionalAvatarEndpoint(signal?: AbortSignal): Promise<void> {
        let url_ = this.baseUrl + "/professionals/me/avatar";
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "DELETE",
            url: url_,
            headers: {
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processDeleteProfessionalAvatarEndpoint(_response);
        });
    }

    protected processDeleteProfessionalAvatarEndpoint(response: AxiosResponse): Promise<void> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 204) {
            const _responseText = response.data;
            return Promise.resolve<void>(null as any);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<void>(null as any);
    }

    /**
     * Generate professional avatar upload URL
     * @return Success
     */
    generateProfessionalAvatarUploadUrlEndpoint(generateProfessionalAvatarUploadUrlRequest: GenerateProfessionalAvatarUploadUrlRequest, signal?: AbortSignal): Promise<GenerateProfessionalAvatarUploadUrlResponse> {
        let url_ = this.baseUrl + "/professionals/me/avatar/upload-url";
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(generateProfessionalAvatarUploadUrlRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGenerateProfessionalAvatarUploadUrlEndpoint(_response);
        });
    }

    protected processGenerateProfessionalAvatarUploadUrlEndpoint(response: AxiosResponse): Promise<GenerateProfessionalAvatarUploadUrlResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GenerateProfessionalAvatarUploadUrlResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GenerateProfessionalAvatarUploadUrlResponse>(null as any);
    }

    /**
     * Submit / finalize a photo diary
     * @param id The photo diary request ID (from route).
     * @return Success
     */
    submitRequestEndpoint(id: string, signal?: AbortSignal): Promise<SubmitRequestResponse> {
        let url_ = this.baseUrl + "/client/photo-diary-requests/{id}/submit";
        if (id === undefined || id === null)
            throw new globalThis.Error("The parameter 'id' must be defined.");
        url_ = url_.replace("{id}", encodeURIComponent("" + id));
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "POST",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processSubmitRequestEndpoint(_response);
        });
    }

    protected processSubmitRequestEndpoint(response: AxiosResponse): Promise<SubmitRequestResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<SubmitRequestResponse>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<SubmitRequestResponse>(null as any);
    }

    /**
     * List photo diary requests (trainer view)
     * @param page Page number (1-based). Defaults to 1.
     * @param pageSize Number of items per page. Defaults to 20.
     * @param status (optional) Optional filter by status.
     * @param linkId (optional) Optional filter by client-professional link ID.
     * @param pendingInviteId (optional) Optional filter by pending invite ID.
     * @param planId (optional) Optional filter by plan ID.
     * @return Success
     */
    listTrainerRequestsEndpoint(page: number, pageSize: number, status?: PhotoDiaryStatus | null | undefined, linkId?: number | null | undefined, pendingInviteId?: number | null | undefined, planId?: string | null | undefined, signal?: AbortSignal): Promise<ListTrainerRequestsResponse> {
        let url_ = this.baseUrl + "/trainer/photo-diary-requests?";
        if (page === undefined || page === null)
            throw new globalThis.Error("The parameter 'page' must be defined and cannot be null.");
        else
            url_ += "page=" + encodeURIComponent("" + page) + "&";
        if (pageSize === undefined || pageSize === null)
            throw new globalThis.Error("The parameter 'pageSize' must be defined and cannot be null.");
        else
            url_ += "pageSize=" + encodeURIComponent("" + pageSize) + "&";
        if (status !== undefined && status !== null)
            url_ += "status=" + encodeURIComponent("" + status) + "&";
        if (linkId !== undefined && linkId !== null)
            url_ += "linkId=" + encodeURIComponent("" + linkId) + "&";
        if (pendingInviteId !== undefined && pendingInviteId !== null)
            url_ += "pendingInviteId=" + encodeURIComponent("" + pendingInviteId) + "&";
        if (planId !== undefined && planId !== null)
            url_ += "planId=" + encodeURIComponent("" + planId) + "&";
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processListTrainerRequestsEndpoint(_response);
        });
    }

    protected processListTrainerRequestsEndpoint(response: AxiosResponse): Promise<ListTrainerRequestsResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<ListTrainerRequestsResponse>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<ListTrainerRequestsResponse>(null as any);
    }

    /**
     * Create a photo diary request
     * @return Success
     */
    createRequestEndpoint(createRequestRequest: CreateRequestRequest, signal?: AbortSignal): Promise<CreateRequestResponse> {
        let url_ = this.baseUrl + "/trainer/photo-diary-requests";
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(createRequestRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processCreateRequestEndpoint(_response);
        });
    }

    protected processCreateRequestEndpoint(response: AxiosResponse): Promise<CreateRequestResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<CreateRequestResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<CreateRequestResponse>(null as any);
    }

    /**
     * List photo diary requests (client view)
     * @param page Page number (1-based). Defaults to 1.
     * @param pageSize Number of items per page. Defaults to 20.
     * @param status (optional) Optional filter by status.
     * @param planId (optional) Optional filter by plan ID.
     * @return Success
     */
    listClientRequestsEndpoint(page: number, pageSize: number, status?: PhotoDiaryStatus | null | undefined, planId?: string | null | undefined, signal?: AbortSignal): Promise<ListClientRequestsResponse> {
        let url_ = this.baseUrl + "/client/photo-diary-requests?";
        if (page === undefined || page === null)
            throw new globalThis.Error("The parameter 'page' must be defined and cannot be null.");
        else
            url_ += "page=" + encodeURIComponent("" + page) + "&";
        if (pageSize === undefined || pageSize === null)
            throw new globalThis.Error("The parameter 'pageSize' must be defined and cannot be null.");
        else
            url_ += "pageSize=" + encodeURIComponent("" + pageSize) + "&";
        if (status !== undefined && status !== null)
            url_ += "status=" + encodeURIComponent("" + status) + "&";
        if (planId !== undefined && planId !== null)
            url_ += "planId=" + encodeURIComponent("" + planId) + "&";
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processListClientRequestsEndpoint(_response);
        });
    }

    protected processListClientRequestsEndpoint(response: AxiosResponse): Promise<ListClientRequestsResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<ListClientRequestsResponse>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<ListClientRequestsResponse>(null as any);
    }

    /**
     * Retroactively link a photo diary request to a plan
     * @param requestId The photo diary request ID (from route).
     * @return Success
     */
    linkPlanEndpoint(requestId: string, linkPlanRequest: LinkPlanRequest, signal?: AbortSignal): Promise<LinkPlanResponse> {
        let url_ = this.baseUrl + "/trainer/photo-diary-requests/{requestId}/link";
        if (requestId === undefined || requestId === null)
            throw new globalThis.Error("The parameter 'requestId' must be defined.");
        url_ = url_.replace("{requestId}", encodeURIComponent("" + requestId));
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(linkPlanRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processLinkPlanEndpoint(_response);
        });
    }

    protected processLinkPlanEndpoint(response: AxiosResponse): Promise<LinkPlanResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<LinkPlanResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<LinkPlanResponse>(null as any);
    }

    /**
     * Dismiss a photo diary request
     * @param id The photo diary request ID (from route).
     * @return Success
     */
    dismissRequestEndpoint(id: string, dismissRequestRequest: DismissRequestRequest, signal?: AbortSignal): Promise<DismissRequestResponse> {
        let url_ = this.baseUrl + "/client/photo-diary-requests/{id}/dismiss";
        if (id === undefined || id === null)
            throw new globalThis.Error("The parameter 'id' must be defined.");
        url_ = url_.replace("{id}", encodeURIComponent("" + id));
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(dismissRequestRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processDismissRequestEndpoint(_response);
        });
    }

    protected processDismissRequestEndpoint(response: AxiosResponse): Promise<DismissRequestResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<DismissRequestResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<DismissRequestResponse>(null as any);
    }

    /**
     * Accept a photo diary request
     * @param id The photo diary request ID (from route).
     * @return Success
     */
    acceptRequestEndpoint(id: string, acceptRequestRequest: AcceptRequestRequest, signal?: AbortSignal): Promise<AcceptRequestResponse> {
        let url_ = this.baseUrl + "/client/photo-diary-requests/{id}/accept";
        if (id === undefined || id === null)
            throw new globalThis.Error("The parameter 'id' must be defined.");
        url_ = url_.replace("{id}", encodeURIComponent("" + id));
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(acceptRequestRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processAcceptRequestEndpoint(_response);
        });
    }

    protected processAcceptRequestEndpoint(response: AxiosResponse): Promise<AcceptRequestResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<AcceptRequestResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<AcceptRequestResponse>(null as any);
    }

    /**
     * Full-state update of a nutrition plan template
     * @param templateId The template's public identifier (route parameter).
     * @return Success
     */
    updateTemplateEndpoint(templateId: string, updateTemplateRequest: UpdateTemplateRequest2, signal?: AbortSignal): Promise<NutritionPlanTemplateDetailDto> {
        let url_ = this.baseUrl + "/nutrition/plan-templates/{templateId}";
        if (templateId === undefined || templateId === null)
            throw new globalThis.Error("The parameter 'templateId' must be defined.");
        url_ = url_.replace("{templateId}", encodeURIComponent("" + templateId));
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(updateTemplateRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "PUT",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processUpdateTemplateEndpoint(_response);
        });
    }

    protected processUpdateTemplateEndpoint(response: AxiosResponse): Promise<NutritionPlanTemplateDetailDto> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<NutritionPlanTemplateDetailDto>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<NutritionPlanTemplateDetailDto>(null as any);
    }

    /**
     * Get nutrition plan template detail
     * @param templateId The template's public identifier (route parameter).
     * @return Success
     */
    getTemplateEndpoint(templateId: string, signal?: AbortSignal): Promise<NutritionPlanTemplateDetailDto> {
        let url_ = this.baseUrl + "/nutrition/plan-templates/{templateId}";
        if (templateId === undefined || templateId === null)
            throw new globalThis.Error("The parameter 'templateId' must be defined.");
        url_ = url_.replace("{templateId}", encodeURIComponent("" + templateId));
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGetTemplateEndpoint(_response);
        });
    }

    protected processGetTemplateEndpoint(response: AxiosResponse): Promise<NutritionPlanTemplateDetailDto> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<NutritionPlanTemplateDetailDto>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<NutritionPlanTemplateDetailDto>(null as any);
    }

    /**
     * Search nutrition plan templates
     * @param page Page number (1-based).
     * @param pageSize Number of items per page.
     * @param search (optional) Optional case-insensitive substring match against the template name.
     * @param goal (optional) Optional filter by primary fitness goal.
     * @param dietaryStyle (optional) Optional filter by dietary style.
     * @param weekCount (optional) Optional filter by exact week count.
     * @return Success
     */
    searchTemplatesEndpoint(page: number, pageSize: number, search?: string | null | undefined, goal?: PrimaryGoal | null | undefined, dietaryStyle?: DietaryStyle | null | undefined, weekCount?: number | null | undefined, signal?: AbortSignal): Promise<SearchTemplatesResponse2> {
        let url_ = this.baseUrl + "/nutrition/plan-templates?";
        if (page === undefined || page === null)
            throw new globalThis.Error("The parameter 'page' must be defined and cannot be null.");
        else
            url_ += "page=" + encodeURIComponent("" + page) + "&";
        if (pageSize === undefined || pageSize === null)
            throw new globalThis.Error("The parameter 'pageSize' must be defined and cannot be null.");
        else
            url_ += "pageSize=" + encodeURIComponent("" + pageSize) + "&";
        if (search !== undefined && search !== null)
            url_ += "search=" + encodeURIComponent("" + search) + "&";
        if (goal !== undefined && goal !== null)
            url_ += "goal=" + encodeURIComponent("" + goal) + "&";
        if (dietaryStyle !== undefined && dietaryStyle !== null)
            url_ += "dietaryStyle=" + encodeURIComponent("" + dietaryStyle) + "&";
        if (weekCount !== undefined && weekCount !== null)
            url_ += "weekCount=" + encodeURIComponent("" + weekCount) + "&";
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processSearchTemplatesEndpoint(_response);
        });
    }

    protected processSearchTemplatesEndpoint(response: AxiosResponse): Promise<SearchTemplatesResponse2> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<SearchTemplatesResponse2>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<SearchTemplatesResponse2>(null as any);
    }

    /**
     * Create a nutrition plan template
     * @return Success
     */
    createTemplateEndpoint(createTemplateRequest: CreateTemplateRequest2, signal?: AbortSignal): Promise<NutritionPlanTemplateSummaryDto> {
        let url_ = this.baseUrl + "/nutrition/plan-templates";
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(createTemplateRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processCreateTemplateEndpoint(_response);
        });
    }

    protected processCreateTemplateEndpoint(response: AxiosResponse): Promise<NutritionPlanTemplateSummaryDto> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<NutritionPlanTemplateSummaryDto>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<NutritionPlanTemplateSummaryDto>(null as any);
    }

    /**
     * Instantiate a nutrition plan template
     * @param templateId The template's public identifier (route parameter).
     * @return Success
     */
    instantiateTemplateEndpoint(templateId: string, instantiateTemplateRequest: InstantiateTemplateRequest2, signal?: AbortSignal): Promise<InstantiateTemplateResponse2> {
        let url_ = this.baseUrl + "/nutrition/plan-templates/{templateId}/instantiate";
        if (templateId === undefined || templateId === null)
            throw new globalThis.Error("The parameter 'templateId' must be defined.");
        url_ = url_.replace("{templateId}", encodeURIComponent("" + templateId));
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(instantiateTemplateRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processInstantiateTemplateEndpoint(_response);
        });
    }

    protected processInstantiateTemplateEndpoint(response: AxiosResponse): Promise<InstantiateTemplateResponse2> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<InstantiateTemplateResponse2>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<InstantiateTemplateResponse2>(null as any);
    }

    /**
     * Delete a nutrition plan template
     * @return No Content
     */
    deleteTemplateEndpoint(templateId: string, signal?: AbortSignal): Promise<void> {
        let url_ = this.baseUrl + "/nutrition/plan-templates/{TemplateId}";
        if (templateId === undefined || templateId === null)
            throw new globalThis.Error("The parameter 'templateId' must be defined.");
        url_ = url_.replace("{templateId}", encodeURIComponent("" + templateId));
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "DELETE",
            url: url_,
            headers: {
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processDeleteTemplateEndpoint(_response);
        });
    }

    protected processDeleteTemplateEndpoint(response: AxiosResponse): Promise<void> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 204) {
            const _responseText = response.data;
            return Promise.resolve<void>(null as any);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<void>(null as any);
    }

    /**
     * Save a nutrition plan as a template
     * @return Success
     */
    createTemplateFromPlanEndpoint(createTemplateFromPlanRequest: CreateTemplateFromPlanRequest2, signal?: AbortSignal): Promise<NutritionPlanTemplateSummaryDto> {
        let url_ = this.baseUrl + "/nutrition/plan-templates/from-plan";
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(createTemplateFromPlanRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processCreateTemplateFromPlanEndpoint(_response);
        });
    }

    protected processCreateTemplateFromPlanEndpoint(response: AxiosResponse): Promise<NutritionPlanTemplateSummaryDto> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<NutritionPlanTemplateSummaryDto>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<NutritionPlanTemplateSummaryDto>(null as any);
    }

    /**
     * Copy a nutrition plan template
     * @return No Content
     */
    copyTemplateEndpoint(templateId: string, signal?: AbortSignal): Promise<void> {
        let url_ = this.baseUrl + "/nutrition/plan-templates/{TemplateId}/copy";
        if (templateId === undefined || templateId === null)
            throw new globalThis.Error("The parameter 'templateId' must be defined.");
        url_ = url_.replace("{templateId}", encodeURIComponent("" + templateId));
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "POST",
            url: url_,
            headers: {
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processCopyTemplateEndpoint(_response);
        });
    }

    protected processCopyTemplateEndpoint(response: AxiosResponse): Promise<void> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 204) {
            const _responseText = response.data;
            return Promise.resolve<void>(null as any);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<void>(null as any);
    }

    /**
     * Full-state update of a nutrition plan
     * @param planId The plan's public identifier (route parameter).
     * @return Success
     */
    updatePlanEndpoint(planId: string, updatePlanRequest: UpdatePlanRequest, signal?: AbortSignal): Promise<GetPlanResponse> {
        let url_ = this.baseUrl + "/nutrition/plans/{planId}";
        if (planId === undefined || planId === null)
            throw new globalThis.Error("The parameter 'planId' must be defined.");
        url_ = url_.replace("{planId}", encodeURIComponent("" + planId));
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(updatePlanRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "PUT",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processUpdatePlanEndpoint(_response);
        });
    }

    protected processUpdatePlanEndpoint(response: AxiosResponse): Promise<GetPlanResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GetPlanResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GetPlanResponse>(null as any);
    }

    /**
     * Get a nutrition plan
     * @param planId The plan's public identifier (route parameter).
     * @return Success
     */
    getPlanEndpoint(planId: string, signal?: AbortSignal): Promise<GetPlanResponse> {
        let url_ = this.baseUrl + "/nutrition/plans/{planId}";
        if (planId === undefined || planId === null)
            throw new globalThis.Error("The parameter 'planId' must be defined.");
        url_ = url_.replace("{planId}", encodeURIComponent("" + planId));
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGetPlanEndpoint(_response);
        });
    }

    protected processGetPlanEndpoint(response: AxiosResponse): Promise<GetPlanResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GetPlanResponse>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GetPlanResponse>(null as any);
    }

    /**
     * Delete a nutrition plan
     * @param planId The plan's public identifier (route parameter).
     * @return No Content
     */
    deletePlanEndpoint(planId: string, signal?: AbortSignal): Promise<void> {
        let url_ = this.baseUrl + "/nutrition/plans/{planId}";
        if (planId === undefined || planId === null)
            throw new globalThis.Error("The parameter 'planId' must be defined.");
        url_ = url_.replace("{planId}", encodeURIComponent("" + planId));
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "DELETE",
            url: url_,
            headers: {
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processDeletePlanEndpoint(_response);
        });
    }

    protected processDeletePlanEndpoint(response: AxiosResponse): Promise<void> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 204) {
            const _responseText = response.data;
            return Promise.resolve<void>(null as any);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<void>(null as any);
    }

    /**
     * Publish a week of a nutrition plan
     * @param planId Plan identifier.
     * @param weekNumber Week number to publish.
     * @return Success
     */
    publishWeekEndpoint(planId: string, weekNumber: number, publishWeekRequest: PublishWeekRequest, signal?: AbortSignal): Promise<GetPlanResponse> {
        let url_ = this.baseUrl + "/nutrition/plans/{planId}/weeks/{weekNumber}/publish";
        if (planId === undefined || planId === null)
            throw new globalThis.Error("The parameter 'planId' must be defined.");
        url_ = url_.replace("{planId}", encodeURIComponent("" + planId));
        if (weekNumber === undefined || weekNumber === null)
            throw new globalThis.Error("The parameter 'weekNumber' must be defined.");
        url_ = url_.replace("{weekNumber}", encodeURIComponent("" + weekNumber));
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(publishWeekRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processPublishWeekEndpoint(_response);
        });
    }

    protected processPublishWeekEndpoint(response: AxiosResponse): Promise<GetPlanResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GetPlanResponse>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GetPlanResponse>(null as any);
    }

    /**
     * Link questionnaire to nutrition plan
     * @param planId Plan identifier.
     * @return Success
     */
    linkQuestionnaireEndpoint(planId: string, linkQuestionnaireRequest: LinkQuestionnaireRequest2, signal?: AbortSignal): Promise<GetPlanResponse> {
        let url_ = this.baseUrl + "/nutrition/plans/{planId}/link-questionnaire";
        if (planId === undefined || planId === null)
            throw new globalThis.Error("The parameter 'planId' must be defined.");
        url_ = url_.replace("{planId}", encodeURIComponent("" + planId));
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(linkQuestionnaireRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "PUT",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processLinkQuestionnaireEndpoint(_response);
        });
    }

    protected processLinkQuestionnaireEndpoint(response: AxiosResponse): Promise<GetPlanResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GetPlanResponse>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GetPlanResponse>(null as any);
    }

    /**
     * List nutrition plans
     * @param page Page number (1-based).
     * @param pageSize Number of items per page.
     * @param clientId (optional) Optional filter by client's public user identifier.
     * @param status (optional) Optional filter by plan status.
     * @return Success
     */
    getPlansEndpoint(page: number, pageSize: number, clientId?: string | null | undefined, status?: NutritionPlanStatus | null | undefined, signal?: AbortSignal): Promise<GetPlansResponse> {
        let url_ = this.baseUrl + "/nutrition/plans?";
        if (page === undefined || page === null)
            throw new globalThis.Error("The parameter 'page' must be defined and cannot be null.");
        else
            url_ += "page=" + encodeURIComponent("" + page) + "&";
        if (pageSize === undefined || pageSize === null)
            throw new globalThis.Error("The parameter 'pageSize' must be defined and cannot be null.");
        else
            url_ += "pageSize=" + encodeURIComponent("" + pageSize) + "&";
        if (clientId !== undefined && clientId !== null)
            url_ += "clientId=" + encodeURIComponent("" + clientId) + "&";
        if (status !== undefined && status !== null)
            url_ += "status=" + encodeURIComponent("" + status) + "&";
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGetPlansEndpoint(_response);
        });
    }

    protected processGetPlansEndpoint(response: AxiosResponse): Promise<GetPlansResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GetPlansResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GetPlansResponse>(null as any);
    }

    /**
     * Create a nutrition plan
     * @return Success
     */
    createPlanEndpoint(createPlanRequest: CreatePlanRequest, signal?: AbortSignal): Promise<PlanSummaryDto> {
        let url_ = this.baseUrl + "/nutrition/plans";
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(createPlanRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processCreatePlanEndpoint(_response);
        });
    }

    protected processCreatePlanEndpoint(response: AxiosResponse): Promise<PlanSummaryDto> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<PlanSummaryDto>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<PlanSummaryDto>(null as any);
    }

    /**
     * Complete a nutrition plan
     * @param planId Plan identifier.
     * @return Success
     */
    completePlanEndpoint(planId: string, completePlanRequest: CompletePlanRequest, signal?: AbortSignal): Promise<GetPlanResponse> {
        let url_ = this.baseUrl + "/nutrition/plans/{planId}/complete";
        if (planId === undefined || planId === null)
            throw new globalThis.Error("The parameter 'planId' must be defined.");
        url_ = url_.replace("{planId}", encodeURIComponent("" + planId));
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(completePlanRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processCompletePlanEndpoint(_response);
        });
    }

    protected processCompletePlanEndpoint(response: AxiosResponse): Promise<GetPlanResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GetPlanResponse>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GetPlanResponse>(null as any);
    }

    /**
     * Calculate nutrition goals
     * @param clientId The client's ApplicationUser.Id (route parameter).
     * @return Success
     */
    calculateGoalsEndpoint(clientId: string, calculateGoalsRequest: CalculateGoalsRequest, signal?: AbortSignal): Promise<CalculateGoalsResponse> {
        let url_ = this.baseUrl + "/nutrition/clients/{clientId}/calculate-goals";
        if (clientId === undefined || clientId === null)
            throw new globalThis.Error("The parameter 'clientId' must be defined.");
        url_ = url_.replace("{clientId}", encodeURIComponent("" + clientId));
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(calculateGoalsRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processCalculateGoalsEndpoint(_response);
        });
    }

    protected processCalculateGoalsEndpoint(response: AxiosResponse): Promise<CalculateGoalsResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<CalculateGoalsResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<CalculateGoalsResponse>(null as any);
    }

    /**
     * Unarchive conversation
     * @return No Content
     */
    unarchiveConversationEndpoint(conversationId: string, signal?: AbortSignal): Promise<void> {
        let url_ = this.baseUrl + "/conversations/{conversationId}/unarchive";
        if (conversationId === undefined || conversationId === null)
            throw new globalThis.Error("The parameter 'conversationId' must be defined.");
        url_ = url_.replace("{conversationId}", encodeURIComponent("" + conversationId));
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "PATCH",
            url: url_,
            headers: {
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processUnarchiveConversationEndpoint(_response);
        });
    }

    protected processUnarchiveConversationEndpoint(response: AxiosResponse): Promise<void> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 204) {
            const _responseText = response.data;
            return Promise.resolve<void>(null as any);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<void>(null as any);
    }

    /**
     * Start or get a conversation
     * @return Success
     */
    startConversationEndpoint(startConversationRequest: StartConversationRequest, signal?: AbortSignal): Promise<ConversationDto> {
        let url_ = this.baseUrl + "/conversations";
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(startConversationRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processStartConversationEndpoint(_response);
        });
    }

    protected processStartConversationEndpoint(response: AxiosResponse): Promise<ConversationDto> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<ConversationDto>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<ConversationDto>(null as any);
    }

    /**
     * Get conversations
     * @return Success
     */
    getConversationsEndpoint(archived: boolean, signal?: AbortSignal): Promise<ConversationDto[]> {
        let url_ = this.baseUrl + "/conversations?";
        if (archived === undefined || archived === null)
            throw new globalThis.Error("The parameter 'archived' must be defined and cannot be null.");
        else
            url_ += "archived=" + encodeURIComponent("" + archived) + "&";
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGetConversationsEndpoint(_response);
        });
    }

    protected processGetConversationsEndpoint(response: AxiosResponse): Promise<ConversationDto[]> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<ConversationDto[]>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<ConversationDto[]>(null as any);
    }

    /**
     * Send a message
     * @return Success
     */
    sendMessageEndpoint(conversationId: string, sendMessageRequest: SendMessageRequest, signal?: AbortSignal): Promise<SendMessageResponse> {
        let url_ = this.baseUrl + "/conversations/{conversationId}/messages";
        if (conversationId === undefined || conversationId === null)
            throw new globalThis.Error("The parameter 'conversationId' must be defined.");
        url_ = url_.replace("{conversationId}", encodeURIComponent("" + conversationId));
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(sendMessageRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processSendMessageEndpoint(_response);
        });
    }

    protected processSendMessageEndpoint(response: AxiosResponse): Promise<SendMessageResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<SendMessageResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<SendMessageResponse>(null as any);
    }

    /**
     * Get conversation messages
     * @param limit (optional) 
     * @param cursor (optional) 
     * @return Success
     */
    getMessagesEndpoint(conversationId: string, limit?: number | null | undefined, cursor?: string | null | undefined, signal?: AbortSignal): Promise<GetMessagesResponse> {
        let url_ = this.baseUrl + "/conversations/{conversationId}/messages?";
        if (conversationId === undefined || conversationId === null)
            throw new globalThis.Error("The parameter 'conversationId' must be defined.");
        url_ = url_.replace("{conversationId}", encodeURIComponent("" + conversationId));
        if (limit !== undefined && limit !== null)
            url_ += "limit=" + encodeURIComponent("" + limit) + "&";
        if (cursor !== undefined && cursor !== null)
            url_ += "cursor=" + encodeURIComponent("" + cursor) + "&";
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGetMessagesEndpoint(_response);
        });
    }

    protected processGetMessagesEndpoint(response: AxiosResponse): Promise<GetMessagesResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GetMessagesResponse>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GetMessagesResponse>(null as any);
    }

    /**
     * Mark conversation as read
     * @return No Content
     */
    markConversationReadEndpoint(conversationId: string, signal?: AbortSignal): Promise<void> {
        let url_ = this.baseUrl + "/conversations/{conversationId}/read";
        if (conversationId === undefined || conversationId === null)
            throw new globalThis.Error("The parameter 'conversationId' must be defined.");
        url_ = url_.replace("{conversationId}", encodeURIComponent("" + conversationId));
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "POST",
            url: url_,
            headers: {
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processMarkConversationReadEndpoint(_response);
        });
    }

    protected processMarkConversationReadEndpoint(response: AxiosResponse): Promise<void> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 204) {
            const _responseText = response.data;
            return Promise.resolve<void>(null as any);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<void>(null as any);
    }

    /**
     * Get conversation context
     * @return Success
     */
    getConversationContextEndpoint(conversationId: string, signal?: AbortSignal): Promise<ConversationContextResponse> {
        let url_ = this.baseUrl + "/conversations/{conversationId}/context";
        if (conversationId === undefined || conversationId === null)
            throw new globalThis.Error("The parameter 'conversationId' must be defined.");
        url_ = url_.replace("{conversationId}", encodeURIComponent("" + conversationId));
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGetConversationContextEndpoint(_response);
        });
    }

    protected processGetConversationContextEndpoint(response: AxiosResponse): Promise<ConversationContextResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<ConversationContextResponse>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<ConversationContextResponse>(null as any);
    }

    /**
     * Archive conversation
     * @return No Content
     */
    archiveConversationEndpoint(conversationId: string, signal?: AbortSignal): Promise<void> {
        let url_ = this.baseUrl + "/conversations/{conversationId}/archive";
        if (conversationId === undefined || conversationId === null)
            throw new globalThis.Error("The parameter 'conversationId' must be defined.");
        url_ = url_.replace("{conversationId}", encodeURIComponent("" + conversationId));
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "PATCH",
            url: url_,
            headers: {
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processArchiveConversationEndpoint(_response);
        });
    }

    protected processArchiveConversationEndpoint(response: AxiosResponse): Promise<void> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 204) {
            const _responseText = response.data;
            return Promise.resolve<void>(null as any);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<void>(null as any);
    }

    /**
     * Update meal template
     * @param templateId Public identifier of the meal template to update.
     * @return Meal template updated
     */
    updateMealTemplateEndpoint(templateId: string, updateMealTemplateRequest: UpdateMealTemplateRequest, signal?: AbortSignal): Promise<MealTemplateDetailResponse> {
        let url_ = this.baseUrl + "/nutrition/meal-templates/{templateId}";
        if (templateId === undefined || templateId === null)
            throw new globalThis.Error("The parameter 'templateId' must be defined.");
        url_ = url_.replace("{templateId}", encodeURIComponent("" + templateId));
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(updateMealTemplateRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "PUT",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processUpdateMealTemplateEndpoint(_response);
        });
    }

    protected processUpdateMealTemplateEndpoint(response: AxiosResponse): Promise<MealTemplateDetailResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<MealTemplateDetailResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Invalid request body", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Missing or invalid credentials", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Readable but owned by another nutritionist", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<MealTemplateDetailResponse>(null as any);
    }

    /**
     * Get meal template
     * @param templateId Public identifier of the meal template.
     * @return Meal template detail
     */
    getMealTemplateEndpoint(templateId: string, signal?: AbortSignal): Promise<MealTemplateDetailResponse> {
        let url_ = this.baseUrl + "/nutrition/meal-templates/{templateId}";
        if (templateId === undefined || templateId === null)
            throw new globalThis.Error("The parameter 'templateId' must be defined.");
        url_ = url_.replace("{templateId}", encodeURIComponent("" + templateId));
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGetMealTemplateEndpoint(_response);
        });
    }

    protected processGetMealTemplateEndpoint(response: AxiosResponse): Promise<MealTemplateDetailResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<MealTemplateDetailResponse>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Missing or invalid credentials", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<MealTemplateDetailResponse>(null as any);
    }

    /**
     * Delete meal template
     * @param templateId Public identifier of the meal template to delete.
     * @return Meal template deleted
     */
    deleteMealTemplateEndpoint(templateId: string, signal?: AbortSignal): Promise<void> {
        let url_ = this.baseUrl + "/nutrition/meal-templates/{templateId}";
        if (templateId === undefined || templateId === null)
            throw new globalThis.Error("The parameter 'templateId' must be defined.");
        url_ = url_.replace("{templateId}", encodeURIComponent("" + templateId));
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "DELETE",
            url: url_,
            headers: {
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processDeleteMealTemplateEndpoint(_response);
        });
    }

    protected processDeleteMealTemplateEndpoint(response: AxiosResponse): Promise<void> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 204) {
            const _responseText = response.data;
            return Promise.resolve<void>(null as any);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Missing or invalid credentials", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Readable but owned by another nutritionist", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<void>(null as any);
    }

    /**
     * Search meal templates
     * @param page Page number (1-based). Defaults to 1.
     * @param pageSize Number of items per page. Defaults to 20.
     * @param search (optional) Optional search term to filter templates by name.
     * @return Paged meal template results
     */
    searchMealTemplatesEndpoint(page: number, pageSize: number, search?: string | null | undefined, signal?: AbortSignal): Promise<SearchMealTemplatesResponse> {
        let url_ = this.baseUrl + "/nutrition/meal-templates?";
        if (page === undefined || page === null)
            throw new globalThis.Error("The parameter 'page' must be defined and cannot be null.");
        else
            url_ += "page=" + encodeURIComponent("" + page) + "&";
        if (pageSize === undefined || pageSize === null)
            throw new globalThis.Error("The parameter 'pageSize' must be defined and cannot be null.");
        else
            url_ += "pageSize=" + encodeURIComponent("" + pageSize) + "&";
        if (search !== undefined && search !== null)
            url_ += "search=" + encodeURIComponent("" + search) + "&";
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processSearchMealTemplatesEndpoint(_response);
        });
    }

    protected processSearchMealTemplatesEndpoint(response: AxiosResponse): Promise<SearchMealTemplatesResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<SearchMealTemplatesResponse>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Missing or invalid credentials", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<SearchMealTemplatesResponse>(null as any);
    }

    /**
     * Create meal template
     * @return Success
     */
    createMealTemplateEndpoint(createMealTemplateRequest: CreateMealTemplateRequest, signal?: AbortSignal): Promise<MealTemplateDetailResponse> {
        let url_ = this.baseUrl + "/nutrition/meal-templates";
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(createMealTemplateRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processCreateMealTemplateEndpoint(_response);
        });
    }

    protected processCreateMealTemplateEndpoint(response: AxiosResponse): Promise<MealTemplateDetailResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<MealTemplateDetailResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Invalid request body", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Missing or invalid credentials", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<MealTemplateDetailResponse>(null as any);
    }

    /**
     * Save meal template from plan
     * @return Success
     */
    saveMealTemplateFromPlanEndpoint(saveMealTemplateFromPlanRequest: SaveMealTemplateFromPlanRequest, signal?: AbortSignal): Promise<MealTemplateDetailResponse> {
        let url_ = this.baseUrl + "/nutrition/meal-templates/from-plan";
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(saveMealTemplateFromPlanRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processSaveMealTemplateFromPlanEndpoint(_response);
        });
    }

    protected processSaveMealTemplateFromPlanEndpoint(response: AxiosResponse): Promise<MealTemplateDetailResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<MealTemplateDetailResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Invalid request body", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Missing or invalid credentials", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<MealTemplateDetailResponse>(null as any);
    }

    /**
     * Copy meal template
     * @param templateId Public identifier of the source meal template to copy.
     * @return Success
     */
    copyMealTemplateEndpoint(templateId: string, signal?: AbortSignal): Promise<MealTemplateDetailResponse> {
        let url_ = this.baseUrl + "/nutrition/meal-templates/{templateId}/copy";
        if (templateId === undefined || templateId === null)
            throw new globalThis.Error("The parameter 'templateId' must be defined.");
        url_ = url_.replace("{templateId}", encodeURIComponent("" + templateId));
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "POST",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processCopyMealTemplateEndpoint(_response);
        });
    }

    protected processCopyMealTemplateEndpoint(response: AxiosResponse): Promise<MealTemplateDetailResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<MealTemplateDetailResponse>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Missing or invalid credentials", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<MealTemplateDetailResponse>(null as any);
    }

    /**
     * Generate food image upload URL
     * @param foodId The food's public identifier (from route).
     * @param slot Image slot: main (overwrites) or gallery (appends, max 6).
    Provided as a query parameter: ?slot=main or ?slot=gallery.
     * @return Success
     */
    uploadFoodImageUrlEndpoint(foodId: string, slot: string, uploadFoodImageUrlRequest: UploadFoodImageUrlRequest, signal?: AbortSignal): Promise<UploadFoodImageUrlResponse> {
        let url_ = this.baseUrl + "/foods/{foodId}/image/upload-url?";
        if (foodId === undefined || foodId === null)
            throw new globalThis.Error("The parameter 'foodId' must be defined.");
        url_ = url_.replace("{foodId}", encodeURIComponent("" + foodId));
        if (slot === undefined || slot === null)
            throw new globalThis.Error("The parameter 'slot' must be defined and cannot be null.");
        else
            url_ += "slot=" + encodeURIComponent("" + slot) + "&";
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(uploadFoodImageUrlRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processUploadFoodImageUrlEndpoint(_response);
        });
    }

    protected processUploadFoodImageUrlEndpoint(response: AxiosResponse): Promise<UploadFoodImageUrlResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<UploadFoodImageUrlResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<UploadFoodImageUrlResponse>(null as any);
    }

    /**
     * Update custom food
     * @param foodId The food's public identifier (from route).
     * @return Success
     */
    updateFoodEndpoint(foodId: string, updateFoodRequest: UpdateFoodRequest, signal?: AbortSignal): Promise<FoodSummary> {
        let url_ = this.baseUrl + "/foods/{foodId}";
        if (foodId === undefined || foodId === null)
            throw new globalThis.Error("The parameter 'foodId' must be defined.");
        url_ = url_.replace("{foodId}", encodeURIComponent("" + foodId));
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(updateFoodRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "PUT",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processUpdateFoodEndpoint(_response);
        });
    }

    protected processUpdateFoodEndpoint(response: AxiosResponse): Promise<FoodSummary> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<FoodSummary>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<FoodSummary>(null as any);
    }

    /**
     * Get food by ID
     * @param foodId The food's public identifier.
     * @return Success
     */
    getFoodEndpoint(foodId: string, signal?: AbortSignal): Promise<FoodSummary> {
        let url_ = this.baseUrl + "/foods/{foodId}";
        if (foodId === undefined || foodId === null)
            throw new globalThis.Error("The parameter 'foodId' must be defined.");
        url_ = url_.replace("{foodId}", encodeURIComponent("" + foodId));
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGetFoodEndpoint(_response);
        });
    }

    protected processGetFoodEndpoint(response: AxiosResponse): Promise<FoodSummary> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<FoodSummary>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<FoodSummary>(null as any);
    }

    /**
     * Delete custom food
     * @param foodId The food's public identifier.
     * @return No Content
     */
    deleteFoodEndpoint(foodId: string, signal?: AbortSignal): Promise<void> {
        let url_ = this.baseUrl + "/foods/{foodId}";
        if (foodId === undefined || foodId === null)
            throw new globalThis.Error("The parameter 'foodId' must be defined.");
        url_ = url_.replace("{foodId}", encodeURIComponent("" + foodId));
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "DELETE",
            url: url_,
            headers: {
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processDeleteFoodEndpoint(_response);
        });
    }

    protected processDeleteFoodEndpoint(response: AxiosResponse): Promise<void> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 204) {
            const _responseText = response.data;
            return Promise.resolve<void>(null as any);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<void>(null as any);
    }

    /**
     * Search foods
     * @param page Page number (1-based). Defaults to 1.
     * @param pageSize Number of items per page. Defaults to 20.
     * @param q (optional) Free-text search query.
     * @param category (optional) Optional category filter.
     * @return Success
     */
    searchFoodsEndpoint(page: number, pageSize: number, q?: string | null | undefined, category?: FoodCategory | null | undefined, signal?: AbortSignal): Promise<SearchFoodsResponse> {
        let url_ = this.baseUrl + "/foods/search?";
        if (page === undefined || page === null)
            throw new globalThis.Error("The parameter 'page' must be defined and cannot be null.");
        else
            url_ += "page=" + encodeURIComponent("" + page) + "&";
        if (pageSize === undefined || pageSize === null)
            throw new globalThis.Error("The parameter 'pageSize' must be defined and cannot be null.");
        else
            url_ += "pageSize=" + encodeURIComponent("" + pageSize) + "&";
        if (q !== undefined && q !== null)
            url_ += "q=" + encodeURIComponent("" + q) + "&";
        if (category !== undefined && category !== null)
            url_ += "category=" + encodeURIComponent("" + category) + "&";
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processSearchFoodsEndpoint(_response);
        });
    }

    protected processSearchFoodsEndpoint(response: AxiosResponse): Promise<SearchFoodsResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<SearchFoodsResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<SearchFoodsResponse>(null as any);
    }

    /**
     * Get custom foods
     * @param page Page number (1-based). Defaults to 1.
     * @param pageSize Number of items per page. Defaults to 20.
     * @return Success
     */
    getCustomFoodsEndpoint(page: number, pageSize: number, signal?: AbortSignal): Promise<GetCustomFoodsResponse> {
        let url_ = this.baseUrl + "/foods/custom?";
        if (page === undefined || page === null)
            throw new globalThis.Error("The parameter 'page' must be defined and cannot be null.");
        else
            url_ += "page=" + encodeURIComponent("" + page) + "&";
        if (pageSize === undefined || pageSize === null)
            throw new globalThis.Error("The parameter 'pageSize' must be defined and cannot be null.");
        else
            url_ += "pageSize=" + encodeURIComponent("" + pageSize) + "&";
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGetCustomFoodsEndpoint(_response);
        });
    }

    protected processGetCustomFoodsEndpoint(response: AxiosResponse): Promise<GetCustomFoodsResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GetCustomFoodsResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GetCustomFoodsResponse>(null as any);
    }

    /**
     * Create custom food
     * @return Success
     */
    createFoodEndpoint(createFoodRequest: CreateFoodRequest, signal?: AbortSignal): Promise<FoodSummary> {
        let url_ = this.baseUrl + "/foods";
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(createFoodRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processCreateFoodEndpoint(_response);
        });
    }

    protected processCreateFoodEndpoint(response: AxiosResponse): Promise<FoodSummary> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<FoodSummary>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<FoodSummary>(null as any);
    }

    /**
     * Confirm food image upload
     * @param foodId The food's public identifier (from route).
     * @param slot Image slot: main (overwrites ImageUrl) or gallery (appends to GalleryImageUrls).
    Provided as a query parameter: ?slot=main or ?slot=gallery.
     * @return No Content
     */
    confirmFoodImageEndpoint(foodId: string, slot: string, confirmFoodImageRequest: ConfirmFoodImageRequest, signal?: AbortSignal): Promise<void> {
        let url_ = this.baseUrl + "/foods/{foodId}/image?";
        if (foodId === undefined || foodId === null)
            throw new globalThis.Error("The parameter 'foodId' must be defined.");
        url_ = url_.replace("{foodId}", encodeURIComponent("" + foodId));
        if (slot === undefined || slot === null)
            throw new globalThis.Error("The parameter 'slot' must be defined and cannot be null.");
        else
            url_ += "slot=" + encodeURIComponent("" + slot) + "&";
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(confirmFoodImageRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "PUT",
            url: url_,
            headers: {
                "Content-Type": "application/json",
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processConfirmFoodImageEndpoint(_response);
        });
    }

    protected processConfirmFoodImageEndpoint(response: AxiosResponse): Promise<void> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 204) {
            const _responseText = response.data;
            return Promise.resolve<void>(null as any);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<void>(null as any);
    }

    /**
     * Update custom exercise
     * @param exerciseId The public identifier of the exercise to update.
     * @return Success
     */
    updateExerciseEndpoint(exerciseId: string, updateExerciseRequest: UpdateExerciseRequest, signal?: AbortSignal): Promise<ExerciseSummary> {
        let url_ = this.baseUrl + "/exercises/{exerciseId}";
        if (exerciseId === undefined || exerciseId === null)
            throw new globalThis.Error("The parameter 'exerciseId' must be defined.");
        url_ = url_.replace("{exerciseId}", encodeURIComponent("" + exerciseId));
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(updateExerciseRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "PUT",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processUpdateExerciseEndpoint(_response);
        });
    }

    protected processUpdateExerciseEndpoint(response: AxiosResponse): Promise<ExerciseSummary> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<ExerciseSummary>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<ExerciseSummary>(null as any);
    }

    /**
     * Get exercise by ID
     * @param exerciseId The public identifier of the exercise.
     * @return Success
     */
    getExerciseEndpoint(exerciseId: string, signal?: AbortSignal): Promise<ExerciseDetail> {
        let url_ = this.baseUrl + "/exercises/{exerciseId}";
        if (exerciseId === undefined || exerciseId === null)
            throw new globalThis.Error("The parameter 'exerciseId' must be defined.");
        url_ = url_.replace("{exerciseId}", encodeURIComponent("" + exerciseId));
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGetExerciseEndpoint(_response);
        });
    }

    protected processGetExerciseEndpoint(response: AxiosResponse): Promise<ExerciseDetail> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<ExerciseDetail>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<ExerciseDetail>(null as any);
    }

    /**
     * Delete custom exercise
     * @param exerciseId The public identifier of the exercise to delete.
     * @return No Content
     */
    deleteExerciseEndpoint(exerciseId: string, deleteExerciseRequest: DeleteExerciseRequest, signal?: AbortSignal): Promise<void> {
        let url_ = this.baseUrl + "/exercises/{exerciseId}";
        if (exerciseId === undefined || exerciseId === null)
            throw new globalThis.Error("The parameter 'exerciseId' must be defined.");
        url_ = url_.replace("{exerciseId}", encodeURIComponent("" + exerciseId));
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(deleteExerciseRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "DELETE",
            url: url_,
            headers: {
                "Content-Type": "*/*",
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processDeleteExerciseEndpoint(_response);
        });
    }

    protected processDeleteExerciseEndpoint(response: AxiosResponse): Promise<void> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 204) {
            const _responseText = response.data;
            return Promise.resolve<void>(null as any);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<void>(null as any);
    }

    /**
     * Search exercises
     * @param page Page number (1-based). Defaults to 1.
     * @param pageSize Number of items per page. Defaults to 20.
     * @param q (optional) Free-text search query.
     * @param muscleGroup (optional) Filter by muscle group.
     * @param equipment (optional) Filter by equipment type.
     * @param category (optional) Filter by exercise category.
     * @param difficulty (optional) Filter by difficulty level.
     * @return Success
     */
    searchExercisesEndpoint(page: number, pageSize: number, q?: string | null | undefined, muscleGroup?: MuscleGroup | null | undefined, equipment?: ExerciseEquipment | null | undefined, category?: ExerciseCategory | null | undefined, difficulty?: ExerciseDifficulty | null | undefined, signal?: AbortSignal): Promise<SearchExercisesResponse> {
        let url_ = this.baseUrl + "/exercises/search?";
        if (page === undefined || page === null)
            throw new globalThis.Error("The parameter 'page' must be defined and cannot be null.");
        else
            url_ += "page=" + encodeURIComponent("" + page) + "&";
        if (pageSize === undefined || pageSize === null)
            throw new globalThis.Error("The parameter 'pageSize' must be defined and cannot be null.");
        else
            url_ += "pageSize=" + encodeURIComponent("" + pageSize) + "&";
        if (q !== undefined && q !== null)
            url_ += "q=" + encodeURIComponent("" + q) + "&";
        if (muscleGroup !== undefined && muscleGroup !== null)
            url_ += "muscleGroup=" + encodeURIComponent("" + muscleGroup) + "&";
        if (equipment !== undefined && equipment !== null)
            url_ += "equipment=" + encodeURIComponent("" + equipment) + "&";
        if (category !== undefined && category !== null)
            url_ += "category=" + encodeURIComponent("" + category) + "&";
        if (difficulty !== undefined && difficulty !== null)
            url_ += "difficulty=" + encodeURIComponent("" + difficulty) + "&";
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processSearchExercisesEndpoint(_response);
        });
    }

    protected processSearchExercisesEndpoint(response: AxiosResponse): Promise<SearchExercisesResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<SearchExercisesResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<SearchExercisesResponse>(null as any);
    }

    /**
     * Get custom exercises
     * @param page Page number (1-based). Defaults to 1.
     * @param pageSize Number of items per page. Defaults to 20.
     * @return Success
     */
    getCustomExercisesEndpoint(page: number, pageSize: number, signal?: AbortSignal): Promise<GetCustomExercisesResponse> {
        let url_ = this.baseUrl + "/exercises/custom?";
        if (page === undefined || page === null)
            throw new globalThis.Error("The parameter 'page' must be defined and cannot be null.");
        else
            url_ += "page=" + encodeURIComponent("" + page) + "&";
        if (pageSize === undefined || pageSize === null)
            throw new globalThis.Error("The parameter 'pageSize' must be defined and cannot be null.");
        else
            url_ += "pageSize=" + encodeURIComponent("" + pageSize) + "&";
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGetCustomExercisesEndpoint(_response);
        });
    }

    protected processGetCustomExercisesEndpoint(response: AxiosResponse): Promise<GetCustomExercisesResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GetCustomExercisesResponse>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GetCustomExercisesResponse>(null as any);
    }

    /**
     * Generate video upload URL
     * @param exerciseId The public identifier of the exercise.
     * @return Success
     */
    generateUploadUrlEndpoint(exerciseId: string, generateUploadUrlRequest: GenerateUploadUrlRequest, signal?: AbortSignal): Promise<GenerateUploadUrlResponse> {
        let url_ = this.baseUrl + "/exercises/{exerciseId}/upload-url";
        if (exerciseId === undefined || exerciseId === null)
            throw new globalThis.Error("The parameter 'exerciseId' must be defined.");
        url_ = url_.replace("{exerciseId}", encodeURIComponent("" + exerciseId));
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(generateUploadUrlRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGenerateUploadUrlEndpoint(_response);
        });
    }

    protected processGenerateUploadUrlEndpoint(response: AxiosResponse): Promise<GenerateUploadUrlResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GenerateUploadUrlResponse>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GenerateUploadUrlResponse>(null as any);
    }

    /**
     * Create custom exercise
     * @return Success
     */
    createExerciseEndpoint(createExerciseRequest: CreateExerciseRequest, signal?: AbortSignal): Promise<ExerciseSummary> {
        let url_ = this.baseUrl + "/exercises";
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(createExerciseRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processCreateExerciseEndpoint(_response);
        });
    }

    protected processCreateExerciseEndpoint(response: AxiosResponse): Promise<ExerciseSummary> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<ExerciseSummary>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<ExerciseSummary>(null as any);
    }

    /**
     * Save photos / note for a training session diary entry
     * @param sessionId The unique identifier of the training session whose photos/note are being saved.
    Sourced from the route segment {SessionId}.
     * @return No Content
     */
    saveSessionPhotosEndpoint(sessionId: string, saveSessionPhotosRequest: SaveSessionPhotosRequest, signal?: AbortSignal): Promise<void> {
        let url_ = this.baseUrl + "/client/training/log/sessions/{sessionId}/photos";
        if (sessionId === undefined || sessionId === null)
            throw new globalThis.Error("The parameter 'sessionId' must be defined.");
        url_ = url_.replace("{sessionId}", encodeURIComponent("" + sessionId));
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(saveSessionPhotosRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processSaveSessionPhotosEndpoint(_response);
        });
    }

    protected processSaveSessionPhotosEndpoint(response: AxiosResponse): Promise<void> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 204) {
            const _responseText = response.data;
            return Promise.resolve<void>(null as any);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<void>(null as any);
    }

    /**
     * Un-mark a workout as complete
     * @param sessionId The session ID (from the training plan). Bound from the route.
     * @param workoutId The workout ID within the session. Bound from the route.
     * @return Success
     */
    markWorkoutIncompleteEndpoint(sessionId: string, workoutId: string, markWorkoutIncompleteRequest: MarkWorkoutIncompleteRequest, signal?: AbortSignal): Promise<MarkWorkoutIncompleteResponse> {
        let url_ = this.baseUrl + "/client/training/sessions/{sessionId}/workouts/{workoutId}/complete";
        if (sessionId === undefined || sessionId === null)
            throw new globalThis.Error("The parameter 'sessionId' must be defined.");
        url_ = url_.replace("{sessionId}", encodeURIComponent("" + sessionId));
        if (workoutId === undefined || workoutId === null)
            throw new globalThis.Error("The parameter 'workoutId' must be defined.");
        url_ = url_.replace("{workoutId}", encodeURIComponent("" + workoutId));
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(markWorkoutIncompleteRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "DELETE",
            url: url_,
            headers: {
                "Content-Type": "*/*",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processMarkWorkoutIncompleteEndpoint(_response);
        });
    }

    protected processMarkWorkoutIncompleteEndpoint(response: AxiosResponse): Promise<MarkWorkoutIncompleteResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<MarkWorkoutIncompleteResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<MarkWorkoutIncompleteResponse>(null as any);
    }

    /**
     * Mark a workout complete
     * @param sessionId The session ID (from the training plan). Bound from the route.
     * @param workoutId The workout ID within the session. Bound from the route.
     * @return Success
     */
    markWorkoutCompleteEndpoint(sessionId: string, workoutId: string, markWorkoutCompleteRequest: MarkWorkoutCompleteRequest, signal?: AbortSignal): Promise<MarkWorkoutCompleteResponse> {
        let url_ = this.baseUrl + "/client/training/sessions/{sessionId}/workouts/{workoutId}/complete";
        if (sessionId === undefined || sessionId === null)
            throw new globalThis.Error("The parameter 'sessionId' must be defined.");
        url_ = url_.replace("{sessionId}", encodeURIComponent("" + sessionId));
        if (workoutId === undefined || workoutId === null)
            throw new globalThis.Error("The parameter 'workoutId' must be defined.");
        url_ = url_.replace("{workoutId}", encodeURIComponent("" + workoutId));
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(markWorkoutCompleteRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processMarkWorkoutCompleteEndpoint(_response);
        });
    }

    protected processMarkWorkoutCompleteEndpoint(response: AxiosResponse): Promise<MarkWorkoutCompleteResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<MarkWorkoutCompleteResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<MarkWorkoutCompleteResponse>(null as any);
    }

    /**
     * Mark all training sessions for a day complete
     * @return Success
     */
    markWholeDayCompleteEndpoint(markWholeDayCompleteRequest: MarkWholeDayCompleteRequest, signal?: AbortSignal): Promise<MarkWholeDayCompleteResponse> {
        let url_ = this.baseUrl + "/client/training/day/complete";
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(markWholeDayCompleteRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processMarkWholeDayCompleteEndpoint(_response);
        });
    }

    protected processMarkWholeDayCompleteEndpoint(response: AxiosResponse): Promise<MarkWholeDayCompleteResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<MarkWholeDayCompleteResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<MarkWholeDayCompleteResponse>(null as any);
    }

    /**
     * Un-mark a training session as complete
     * @param sessionId The session ID (from the training plan). Bound from the route.
     * @return Success
     */
    markSessionIncompleteEndpoint(sessionId: string, markSessionIncompleteRequest: MarkSessionIncompleteRequest, signal?: AbortSignal): Promise<MarkSessionIncompleteResponse> {
        let url_ = this.baseUrl + "/client/training/sessions/{sessionId}/complete";
        if (sessionId === undefined || sessionId === null)
            throw new globalThis.Error("The parameter 'sessionId' must be defined.");
        url_ = url_.replace("{sessionId}", encodeURIComponent("" + sessionId));
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(markSessionIncompleteRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "DELETE",
            url: url_,
            headers: {
                "Content-Type": "*/*",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processMarkSessionIncompleteEndpoint(_response);
        });
    }

    protected processMarkSessionIncompleteEndpoint(response: AxiosResponse): Promise<MarkSessionIncompleteResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<MarkSessionIncompleteResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<MarkSessionIncompleteResponse>(null as any);
    }

    /**
     * Mark a training session complete
     * @param sessionId The session ID (from the training plan). Bound from the route.
     * @return Success
     */
    markSessionCompleteEndpoint(sessionId: string, markSessionCompleteRequest: MarkSessionCompleteRequest, signal?: AbortSignal): Promise<MarkSessionCompleteResponse> {
        let url_ = this.baseUrl + "/client/training/sessions/{sessionId}/complete";
        if (sessionId === undefined || sessionId === null)
            throw new globalThis.Error("The parameter 'sessionId' must be defined.");
        url_ = url_.replace("{sessionId}", encodeURIComponent("" + sessionId));
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(markSessionCompleteRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processMarkSessionCompleteEndpoint(_response);
        });
    }

    protected processMarkSessionCompleteEndpoint(response: AxiosResponse): Promise<MarkSessionCompleteResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<MarkSessionCompleteResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<MarkSessionCompleteResponse>(null as any);
    }

    /**
     * Un-mark an exercise as complete
     * @param sessionId The session ID (from the training plan). Bound from the route.
     * @param exerciseId The exercise instance ID (ExerciseId) within
    the session. Bound from the route.
     * @return Success
     */
    markExerciseIncompleteEndpoint(sessionId: string, exerciseId: string, markExerciseIncompleteRequest: MarkExerciseIncompleteRequest, signal?: AbortSignal): Promise<MarkExerciseIncompleteResponse> {
        let url_ = this.baseUrl + "/client/training/sessions/{sessionId}/exercises/{exerciseId}/complete";
        if (sessionId === undefined || sessionId === null)
            throw new globalThis.Error("The parameter 'sessionId' must be defined.");
        url_ = url_.replace("{sessionId}", encodeURIComponent("" + sessionId));
        if (exerciseId === undefined || exerciseId === null)
            throw new globalThis.Error("The parameter 'exerciseId' must be defined.");
        url_ = url_.replace("{exerciseId}", encodeURIComponent("" + exerciseId));
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(markExerciseIncompleteRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "DELETE",
            url: url_,
            headers: {
                "Content-Type": "*/*",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processMarkExerciseIncompleteEndpoint(_response);
        });
    }

    protected processMarkExerciseIncompleteEndpoint(response: AxiosResponse): Promise<MarkExerciseIncompleteResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<MarkExerciseIncompleteResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<MarkExerciseIncompleteResponse>(null as any);
    }

    /**
     * Mark an exercise complete
     * @param sessionId The session ID (from the training plan). Bound from the route.
     * @param exerciseId The exercise instance ID (ExerciseId) within
    the session. Bound from the route.
     * @return Success
     */
    markExerciseCompleteEndpoint(sessionId: string, exerciseId: string, markExerciseCompleteRequest: MarkExerciseCompleteRequest, signal?: AbortSignal): Promise<MarkExerciseCompleteResponse> {
        let url_ = this.baseUrl + "/client/training/sessions/{sessionId}/exercises/{exerciseId}/complete";
        if (sessionId === undefined || sessionId === null)
            throw new globalThis.Error("The parameter 'sessionId' must be defined.");
        url_ = url_.replace("{sessionId}", encodeURIComponent("" + sessionId));
        if (exerciseId === undefined || exerciseId === null)
            throw new globalThis.Error("The parameter 'exerciseId' must be defined.");
        url_ = url_.replace("{exerciseId}", encodeURIComponent("" + exerciseId));
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(markExerciseCompleteRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processMarkExerciseCompleteEndpoint(_response);
        });
    }

    protected processMarkExerciseCompleteEndpoint(response: AxiosResponse): Promise<MarkExerciseCompleteResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<MarkExerciseCompleteResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<MarkExerciseCompleteResponse>(null as any);
    }

    /**
     * Get today's training session
     * @return Success
     */
    getTodaySessionEndpoint(signal?: AbortSignal): Promise<GetTodaySessionResponse> {
        let url_ = this.baseUrl + "/client/training/plan/today";
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGetTodaySessionEndpoint(_response);
        });
    }

    protected processGetTodaySessionEndpoint(response: AxiosResponse): Promise<GetTodaySessionResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GetTodaySessionResponse>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GetTodaySessionResponse>(null as any);
    }

    /**
     * Get full training plan
     * @return Success
     */
    getFullTrainingPlanEndpoint(planId: string, signal?: AbortSignal): Promise<GetFullTrainingPlanResponse> {
        let url_ = this.baseUrl + "/client/training/plans/{planId}";
        if (planId === undefined || planId === null)
            throw new globalThis.Error("The parameter 'planId' must be defined.");
        url_ = url_.replace("{planId}", encodeURIComponent("" + planId));
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGetFullTrainingPlanEndpoint(_response);
        });
    }

    protected processGetFullTrainingPlanEndpoint(response: AxiosResponse): Promise<GetFullTrainingPlanResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GetFullTrainingPlanResponse>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GetFullTrainingPlanResponse>(null as any);
    }

    /**
     * Generate training session photo upload URL
     * @param sessionId The unique identifier of the session the photo will be attached to.
    Sourced from the route segment {SessionId}.
     * @return Success
     */
    generateSessionPhotoUploadUrlEndpoint(sessionId: string, generateSessionPhotoUploadUrlRequest: GenerateSessionPhotoUploadUrlRequest, signal?: AbortSignal): Promise<GenerateSessionPhotoUploadUrlResponse> {
        let url_ = this.baseUrl + "/client/training/log/sessions/{sessionId}/photo-upload-url";
        if (sessionId === undefined || sessionId === null)
            throw new globalThis.Error("The parameter 'sessionId' must be defined.");
        url_ = url_.replace("{sessionId}", encodeURIComponent("" + sessionId));
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(generateSessionPhotoUploadUrlRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGenerateSessionPhotoUploadUrlEndpoint(_response);
        });
    }

    protected processGenerateSessionPhotoUploadUrlEndpoint(response: AxiosResponse): Promise<GenerateSessionPhotoUploadUrlResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GenerateSessionPhotoUploadUrlResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GenerateSessionPhotoUploadUrlResponse>(null as any);
    }

    /**
     * Send a client request
     * @return Success
     */
    sendClientRequestEndpoint(sendClientRequestRequest: SendClientRequestRequest, signal?: AbortSignal): Promise<SendClientRequestResponse> {
        let url_ = this.baseUrl + "/client/requests";
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(sendClientRequestRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processSendClientRequestEndpoint(_response);
        });
    }

    protected processSendClientRequestEndpoint(response: AxiosResponse): Promise<SendClientRequestResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<SendClientRequestResponse>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<SendClientRequestResponse>(null as any);
    }

    /**
     * Get my requests
     * @return Success
     */
    getMyRequestsEndpoint(signal?: AbortSignal): Promise<GetMyRequestsResponse> {
        let url_ = this.baseUrl + "/client/requests";
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGetMyRequestsEndpoint(_response);
        });
    }

    protected processGetMyRequestsEndpoint(response: AxiosResponse): Promise<GetMyRequestsResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GetMyRequestsResponse>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GetMyRequestsResponse>(null as any);
    }

    /**
     * Cancel a client request
     * @return No Content
     */
    cancelClientRequestEndpoint(publicId: string, signal?: AbortSignal): Promise<void> {
        let url_ = this.baseUrl + "/client/requests/{PublicId}";
        if (publicId === undefined || publicId === null)
            throw new globalThis.Error("The parameter 'publicId' must be defined.");
        url_ = url_.replace("{publicId}", encodeURIComponent("" + publicId));
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "DELETE",
            url: url_,
            headers: {
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processCancelClientRequestEndpoint(_response);
        });
    }

    protected processCancelClientRequestEndpoint(response: AxiosResponse): Promise<void> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 204) {
            const _responseText = response.data;
            return Promise.resolve<void>(null as any);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<void>(null as any);
    }

    /**
     * List plan photos
     * @param planId Route: the plan's public identifier.
     * @param page 1-based page number (default 1).
     * @param pageSize Number of items per page (default 20, max 100).
     * @param category (optional) Optional category filter (Food / Body / FreeForm).
    When null, all categories are returned.
     * @return Success
     */
    getPlanPhotosEndpoint(planId: string, page: number, pageSize: number, category?: PlanPhotoCategory | null | undefined, signal?: AbortSignal): Promise<PlanPhotoResponse[]> {
        let url_ = this.baseUrl + "/client/plans/{planId}/photos?";
        if (planId === undefined || planId === null)
            throw new globalThis.Error("The parameter 'planId' must be defined.");
        url_ = url_.replace("{planId}", encodeURIComponent("" + planId));
        if (page === undefined || page === null)
            throw new globalThis.Error("The parameter 'page' must be defined and cannot be null.");
        else
            url_ += "page=" + encodeURIComponent("" + page) + "&";
        if (pageSize === undefined || pageSize === null)
            throw new globalThis.Error("The parameter 'pageSize' must be defined and cannot be null.");
        else
            url_ += "pageSize=" + encodeURIComponent("" + pageSize) + "&";
        if (category !== undefined && category !== null)
            url_ += "category=" + encodeURIComponent("" + category) + "&";
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGetPlanPhotosEndpoint(_response);
        });
    }

    protected processGetPlanPhotosEndpoint(response: AxiosResponse): Promise<PlanPhotoResponse[]> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<PlanPhotoResponse[]>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<PlanPhotoResponse[]>(null as any);
    }

    /**
     * Finalize plan photo upload
     * @param planId Route: the plan's public identifier (NutritionPlan.ExternalId or TrainingPlan.ExternalId).
     * @return Success
     */
    finalizePlanPhotoEndpoint(planId: string, finalizePlanPhotoRequest: FinalizePlanPhotoRequest, signal?: AbortSignal): Promise<PlanPhotoResponse> {
        let url_ = this.baseUrl + "/client/plans/{planId}/photos";
        if (planId === undefined || planId === null)
            throw new globalThis.Error("The parameter 'planId' must be defined.");
        url_ = url_.replace("{planId}", encodeURIComponent("" + planId));
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(finalizePlanPhotoRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processFinalizePlanPhotoEndpoint(_response);
        });
    }

    protected processFinalizePlanPhotoEndpoint(response: AxiosResponse): Promise<PlanPhotoResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<PlanPhotoResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<PlanPhotoResponse>(null as any);
    }

    /**
     * Generate plan photo upload URL
     * @param planId Route: the plan's public identifier (NutritionPlan.ExternalId or TrainingPlan.ExternalId).
     * @return Success
     */
    generatePlanPhotoUploadUrlEndpoint(planId: string, generatePlanPhotoUploadUrlRequest: GeneratePlanPhotoUploadUrlRequest, signal?: AbortSignal): Promise<GeneratePlanPhotoUploadUrlResponse> {
        let url_ = this.baseUrl + "/client/plans/{planId}/photos/upload-url";
        if (planId === undefined || planId === null)
            throw new globalThis.Error("The parameter 'planId' must be defined.");
        url_ = url_.replace("{planId}", encodeURIComponent("" + planId));
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(generatePlanPhotoUploadUrlRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGeneratePlanPhotoUploadUrlEndpoint(_response);
        });
    }

    protected processGeneratePlanPhotoUploadUrlEndpoint(response: AxiosResponse): Promise<GeneratePlanPhotoUploadUrlResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GeneratePlanPhotoUploadUrlResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GeneratePlanPhotoUploadUrlResponse>(null as any);
    }

    /**
     * Delete plan photo
     * @param photoId Route: the photo's public identifier (PublicId).
     * @return No Content
     */
    deletePlanPhotoEndpoint(photoId: string, signal?: AbortSignal): Promise<void> {
        let url_ = this.baseUrl + "/client/photos/{photoId}";
        if (photoId === undefined || photoId === null)
            throw new globalThis.Error("The parameter 'photoId' must be defined.");
        url_ = url_.replace("{photoId}", encodeURIComponent("" + photoId));
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "DELETE",
            url: url_,
            headers: {
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processDeletePlanPhotoEndpoint(_response);
        });
    }

    protected processDeletePlanPhotoEndpoint(response: AxiosResponse): Promise<void> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 204) {
            const _responseText = response.data;
            return Promise.resolve<void>(null as any);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<void>(null as any);
    }

    /**
     * List a client's photos (trainer view)
     * @param clientId The client profile's public identifier (route parameter).
     * @param page Page number (1-based). Defaults to 1.
     * @param pageSize Number of items per page. Defaults to 20.
     * @param groupByMonth When true the response items are MonthGroupResponse objects
    grouped by YYYY-MM. When false (default) items are flat
    ClientPhotoResponse objects.
    Pagination applies to groups when true, to individual photos when false.
     * @param category (optional) Optional category filter (Food / Body / FreeForm).
     * @param from (optional) Optional inclusive lower bound on TakenAt (UTC).
     * @param to (optional) Optional inclusive upper bound on TakenAt (UTC).
     * @param planId (optional) Optional plan filter. When provided, only photos belonging to this plan
    (matching PlanPhoto.PlanId) are returned. Applies regardless of
    PlanType; the caller is expected to know whether the plan is a
    nutrition or training plan.
     * @return Success
     */
    getTrainerClientPhotosEndpoint(clientId: string, page: number, pageSize: number, groupByMonth: boolean, category?: PlanPhotoCategory | null | undefined, from?: string | null | undefined, to?: string | null | undefined, planId?: string | null | undefined, signal?: AbortSignal): Promise<GetTrainerClientPhotosResponse> {
        let url_ = this.baseUrl + "/trainer/clients/{clientId}/photos?";
        if (clientId === undefined || clientId === null)
            throw new globalThis.Error("The parameter 'clientId' must be defined.");
        url_ = url_.replace("{clientId}", encodeURIComponent("" + clientId));
        if (page === undefined || page === null)
            throw new globalThis.Error("The parameter 'page' must be defined and cannot be null.");
        else
            url_ += "page=" + encodeURIComponent("" + page) + "&";
        if (pageSize === undefined || pageSize === null)
            throw new globalThis.Error("The parameter 'pageSize' must be defined and cannot be null.");
        else
            url_ += "pageSize=" + encodeURIComponent("" + pageSize) + "&";
        if (groupByMonth === undefined || groupByMonth === null)
            throw new globalThis.Error("The parameter 'groupByMonth' must be defined and cannot be null.");
        else
            url_ += "groupByMonth=" + encodeURIComponent("" + groupByMonth) + "&";
        if (category !== undefined && category !== null)
            url_ += "category=" + encodeURIComponent("" + category) + "&";
        if (from !== undefined && from !== null)
            url_ += "from=" + encodeURIComponent("" + from) + "&";
        if (to !== undefined && to !== null)
            url_ += "to=" + encodeURIComponent("" + to) + "&";
        if (planId !== undefined && planId !== null)
            url_ += "planId=" + encodeURIComponent("" + planId) + "&";
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGetTrainerClientPhotosEndpoint(_response);
        });
    }

    protected processGetTrainerClientPhotosEndpoint(response: AxiosResponse): Promise<GetTrainerClientPhotosResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GetTrainerClientPhotosResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GetTrainerClientPhotosResponse>(null as any);
    }

    /**
     * List my photos (client view)
     * @param page Page number (1-based). Defaults to 1.
     * @param pageSize Number of items per page. Defaults to 20.
     * @param groupByMonth When true the response items are MonthGroupResponse objects
    grouped by YYYY-MM. When false (default) items are flat
    ClientPhotoResponse objects.
    Pagination applies to groups when true, to individual photos when false.
     * @param category (optional) Optional category filter (Food / Body / FreeForm).
     * @param from (optional) Optional inclusive lower bound on TakenAt (UTC).
     * @param to (optional) Optional inclusive upper bound on TakenAt (UTC).
     * @return Success
     */
    getMyPhotosEndpoint(page: number, pageSize: number, groupByMonth: boolean, category?: PlanPhotoCategory | null | undefined, from?: string | null | undefined, to?: string | null | undefined, signal?: AbortSignal): Promise<GetMyPhotosResponse> {
        let url_ = this.baseUrl + "/client/me/photos?";
        if (page === undefined || page === null)
            throw new globalThis.Error("The parameter 'page' must be defined and cannot be null.");
        else
            url_ += "page=" + encodeURIComponent("" + page) + "&";
        if (pageSize === undefined || pageSize === null)
            throw new globalThis.Error("The parameter 'pageSize' must be defined and cannot be null.");
        else
            url_ += "pageSize=" + encodeURIComponent("" + pageSize) + "&";
        if (groupByMonth === undefined || groupByMonth === null)
            throw new globalThis.Error("The parameter 'groupByMonth' must be defined and cannot be null.");
        else
            url_ += "groupByMonth=" + encodeURIComponent("" + groupByMonth) + "&";
        if (category !== undefined && category !== null)
            url_ += "category=" + encodeURIComponent("" + category) + "&";
        if (from !== undefined && from !== null)
            url_ += "from=" + encodeURIComponent("" + from) + "&";
        if (to !== undefined && to !== null)
            url_ += "to=" + encodeURIComponent("" + to) + "&";
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGetMyPhotosEndpoint(_response);
        });
    }

    protected processGetMyPhotosEndpoint(response: AxiosResponse): Promise<GetMyPhotosResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GetMyPhotosResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GetMyPhotosResponse>(null as any);
    }

    /**
     * Unmark a meal as eaten
     * @param mealId The unique identifier of the meal to unmark.
     * @return No Content
     */
    unlogMealEatenEndpoint(mealId: string, signal?: AbortSignal): Promise<void> {
        let url_ = this.baseUrl + "/client/nutrition/log/meals/{mealId}/eaten";
        if (mealId === undefined || mealId === null)
            throw new globalThis.Error("The parameter 'mealId' must be defined.");
        url_ = url_.replace("{mealId}", encodeURIComponent("" + mealId));
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "DELETE",
            url: url_,
            headers: {
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processUnlogMealEatenEndpoint(_response);
        });
    }

    protected processUnlogMealEatenEndpoint(response: AxiosResponse): Promise<void> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 204) {
            const _responseText = response.data;
            return Promise.resolve<void>(null as any);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<void>(null as any);
    }

    /**
     * Log a meal as eaten
     * @param mealId The unique identifier of the meal to log as eaten.
     * @return No Content
     */
    logMealEatenEndpoint(mealId: string, logMealEatenRequest: LogMealEatenRequest, signal?: AbortSignal): Promise<void> {
        let url_ = this.baseUrl + "/client/nutrition/log/meals/{mealId}/eaten";
        if (mealId === undefined || mealId === null)
            throw new globalThis.Error("The parameter 'mealId' must be defined.");
        url_ = url_.replace("{mealId}", encodeURIComponent("" + mealId));
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(logMealEatenRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processLogMealEatenEndpoint(_response);
        });
    }

    protected processLogMealEatenEndpoint(response: AxiosResponse): Promise<void> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 204) {
            const _responseText = response.data;
            return Promise.resolve<void>(null as any);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<void>(null as any);
    }

    /**
     * Save photos / note for a meal diary entry
     * @param mealId The unique identifier of the meal whose photos/note are being saved.
    Sourced from the route segment {MealId}.
     * @return No Content
     */
    saveMealPhotosEndpoint(mealId: string, saveMealPhotosRequest: SaveMealPhotosRequest, signal?: AbortSignal): Promise<void> {
        let url_ = this.baseUrl + "/client/nutrition/log/meals/{mealId}/photos";
        if (mealId === undefined || mealId === null)
            throw new globalThis.Error("The parameter 'mealId' must be defined.");
        url_ = url_.replace("{mealId}", encodeURIComponent("" + mealId));
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(saveMealPhotosRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processSaveMealPhotosEndpoint(_response);
        });
    }

    protected processSaveMealPhotosEndpoint(response: AxiosResponse): Promise<void> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 204) {
            const _responseText = response.data;
            return Promise.resolve<void>(null as any);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<void>(null as any);
    }

    /**
     * Save plan-level day photos / note for today
     * @return No Content
     */
    saveDayPhotosEndpoint(saveDayPhotosRequest: SaveDayPhotosRequest, signal?: AbortSignal): Promise<void> {
        let url_ = this.baseUrl + "/client/nutrition/log/day/photos";
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(saveDayPhotosRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processSaveDayPhotosEndpoint(_response);
        });
    }

    protected processSaveDayPhotosEndpoint(response: AxiosResponse): Promise<void> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 204) {
            const _responseText = response.data;
            return Promise.resolve<void>(null as any);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<void>(null as any);
    }

    /**
     * Get current week's nutrition plan
     * @return Success
     */
    getWeekPlanEndpoint(signal?: AbortSignal): Promise<GetWeekPlanResponse> {
        let url_ = this.baseUrl + "/client/nutrition/plan/week";
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGetWeekPlanEndpoint(_response);
        });
    }

    protected processGetWeekPlanEndpoint(response: AxiosResponse): Promise<GetWeekPlanResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GetWeekPlanResponse>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GetWeekPlanResponse>(null as any);
    }

    /**
     * Get today's nutrition plan
     * @return Success
     */
    getTodayPlanEndpoint(signal?: AbortSignal): Promise<GetTodayPlanResponse> {
        let url_ = this.baseUrl + "/client/nutrition/plan/today";
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGetTodayPlanEndpoint(_response);
        });
    }

    protected processGetTodayPlanEndpoint(response: AxiosResponse): Promise<GetTodayPlanResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GetTodayPlanResponse>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GetTodayPlanResponse>(null as any);
    }

    /**
     * Get today's meal log
     * @return Success
     */
    getTodayLogEndpoint(signal?: AbortSignal): Promise<GetTodayLogResponse> {
        let url_ = this.baseUrl + "/client/nutrition/log/today";
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGetTodayLogEndpoint(_response);
        });
    }

    protected processGetTodayLogEndpoint(response: AxiosResponse): Promise<GetTodayLogResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GetTodayLogResponse>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GetTodayLogResponse>(null as any);
    }

    /**
     * Get today's plan-level day log
     * @return Success
     */
    getTodayDayLogEndpoint(signal?: AbortSignal): Promise<GetTodayDayLogResponse> {
        let url_ = this.baseUrl + "/client/nutrition/log/day/today";
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGetTodayDayLogEndpoint(_response);
        });
    }

    protected processGetTodayDayLogEndpoint(response: AxiosResponse): Promise<GetTodayDayLogResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GetTodayDayLogResponse>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GetTodayDayLogResponse>(null as any);
    }

    /**
     * Get shopping list from active plan
     * @param weekFrom Starting week number (1-based, inclusive). Defaults to 1.
     * @param weekTo (optional) Ending week number (1-based, inclusive). Defaults to all weeks.
     * @return Success
     */
    getShoppingListEndpoint(weekFrom: number, weekTo?: number | null | undefined, signal?: AbortSignal): Promise<GetShoppingListResponse> {
        let url_ = this.baseUrl + "/client/nutrition/plan/shopping-list?";
        if (weekFrom === undefined || weekFrom === null)
            throw new globalThis.Error("The parameter 'weekFrom' must be defined and cannot be null.");
        else
            url_ += "weekFrom=" + encodeURIComponent("" + weekFrom) + "&";
        if (weekTo !== undefined && weekTo !== null)
            url_ += "weekTo=" + encodeURIComponent("" + weekTo) + "&";
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGetShoppingListEndpoint(_response);
        });
    }

    protected processGetShoppingListEndpoint(response: AxiosResponse): Promise<GetShoppingListResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GetShoppingListResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GetShoppingListResponse>(null as any);
    }

    /**
     * Get recipe detail (client)
     * @param recipeId Public identifier of the recipe.
     * @return Success
     */
    getRecipeDetailEndpoint(recipeId: string, signal?: AbortSignal): Promise<GetRecipeResponse> {
        let url_ = this.baseUrl + "/client/recipes/{recipeId}";
        if (recipeId === undefined || recipeId === null)
            throw new globalThis.Error("The parameter 'recipeId' must be defined.");
        url_ = url_.replace("{recipeId}", encodeURIComponent("" + recipeId));
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGetRecipeDetailEndpoint(_response);
        });
    }

    protected processGetRecipeDetailEndpoint(response: AxiosResponse): Promise<GetRecipeResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GetRecipeResponse>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GetRecipeResponse>(null as any);
    }

    /**
     * Get full nutrition plan
     * @return Success
     */
    getFullPlanEndpoint(signal?: AbortSignal): Promise<GetFullPlanResponse> {
        let url_ = this.baseUrl + "/client/nutrition/plan/full";
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGetFullPlanEndpoint(_response);
        });
    }

    protected processGetFullPlanEndpoint(response: AxiosResponse): Promise<GetFullPlanResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GetFullPlanResponse>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GetFullPlanResponse>(null as any);
    }

    /**
     * Generate meal diary photo upload URL
     * @param mealId The unique identifier of the meal the photo will be attached to.
    Sourced from the route segment {mealId}.
     * @return Success
     */
    generateMealPhotoUploadUrlEndpoint(mealId: string, generateMealPhotoUploadUrlRequest: GenerateMealPhotoUploadUrlRequest, signal?: AbortSignal): Promise<GenerateMealPhotoUploadUrlResponse> {
        let url_ = this.baseUrl + "/client/nutrition/log/meals/{mealId}/photo-upload-url";
        if (mealId === undefined || mealId === null)
            throw new globalThis.Error("The parameter 'mealId' must be defined.");
        url_ = url_.replace("{mealId}", encodeURIComponent("" + mealId));
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(generateMealPhotoUploadUrlRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGenerateMealPhotoUploadUrlEndpoint(_response);
        });
    }

    protected processGenerateMealPhotoUploadUrlEndpoint(response: AxiosResponse): Promise<GenerateMealPhotoUploadUrlResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GenerateMealPhotoUploadUrlResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GenerateMealPhotoUploadUrlResponse>(null as any);
    }

    /**
     * Generate plan-level day photo upload URL
     * @return Success
     */
    generateDayPhotoUploadUrlEndpoint(generateDayPhotoUploadUrlRequest: GenerateDayPhotoUploadUrlRequest, signal?: AbortSignal): Promise<GenerateDayPhotoUploadUrlResponse> {
        let url_ = this.baseUrl + "/client/nutrition/log/day/photo-upload-url";
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(generateDayPhotoUploadUrlRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGenerateDayPhotoUploadUrlEndpoint(_response);
        });
    }

    protected processGenerateDayPhotoUploadUrlEndpoint(response: AxiosResponse): Promise<GenerateDayPhotoUploadUrlResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GenerateDayPhotoUploadUrlResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GenerateDayPhotoUploadUrlResponse>(null as any);
    }

    /**
     * Get measurement statistics
     * @return Success
     */
    getMeasurementStatsEndpoint(signal?: AbortSignal): Promise<MeasurementStatsResponse> {
        let url_ = this.baseUrl + "/client/measurements/stats";
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGetMeasurementStatsEndpoint(_response);
        });
    }

    protected processGetMeasurementStatsEndpoint(response: AxiosResponse): Promise<MeasurementStatsResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<MeasurementStatsResponse>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<MeasurementStatsResponse>(null as any);
    }

    /**
     * List body measurements
     * @param page Page number (1-based).
     * @param pageSize Number of items per page.
     * @param from (optional) Optional start date filter (inclusive).
     * @param to (optional) Optional end date filter (inclusive).
     * @return Success
     */
    getMeasurementsEndpoint(page: number, pageSize: number, from?: string | null | undefined, to?: string | null | undefined, signal?: AbortSignal): Promise<GetMeasurementsResponse> {
        let url_ = this.baseUrl + "/client/measurements?";
        if (page === undefined || page === null)
            throw new globalThis.Error("The parameter 'page' must be defined and cannot be null.");
        else
            url_ += "page=" + encodeURIComponent("" + page) + "&";
        if (pageSize === undefined || pageSize === null)
            throw new globalThis.Error("The parameter 'pageSize' must be defined and cannot be null.");
        else
            url_ += "pageSize=" + encodeURIComponent("" + pageSize) + "&";
        if (from !== undefined && from !== null)
            url_ += "from=" + encodeURIComponent("" + from) + "&";
        if (to !== undefined && to !== null)
            url_ += "to=" + encodeURIComponent("" + to) + "&";
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGetMeasurementsEndpoint(_response);
        });
    }

    protected processGetMeasurementsEndpoint(response: AxiosResponse): Promise<GetMeasurementsResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GetMeasurementsResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GetMeasurementsResponse>(null as any);
    }

    /**
     * Add a body measurement
     * @return Success
     */
    addMeasurementEndpoint(addMeasurementRequest: AddMeasurementRequest, signal?: AbortSignal): Promise<MeasurementDto> {
        let url_ = this.baseUrl + "/client/measurements";
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(addMeasurementRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processAddMeasurementEndpoint(_response);
        });
    }

    protected processAddMeasurementEndpoint(response: AxiosResponse): Promise<MeasurementDto> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<MeasurementDto>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<MeasurementDto>(null as any);
    }

    /**
     * Get latest body measurement
     * @return Success
     */
    getLatestMeasurementEndpoint(signal?: AbortSignal): Promise<MeasurementDto> {
        let url_ = this.baseUrl + "/client/measurements/latest";
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGetLatestMeasurementEndpoint(_response);
        });
    }

    protected processGetLatestMeasurementEndpoint(response: AxiosResponse): Promise<MeasurementDto> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<MeasurementDto>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<MeasurementDto>(null as any);
    }

    /**
     * Get a client's body measurements
     * @param clientId The client profile's public identifier (route parameter).
     * @param page Page number (1-based).
     * @param pageSize Number of items per page.
     * @param from (optional) Optional start date filter (inclusive).
     * @param to (optional) Optional end date filter (inclusive).
     * @return Success
     */
    getClientMeasurementsEndpoint(clientId: string, page: number, pageSize: number, from?: string | null | undefined, to?: string | null | undefined, signal?: AbortSignal): Promise<GetMeasurementsResponse> {
        let url_ = this.baseUrl + "/trainer/clients/{clientId}/measurements?";
        if (clientId === undefined || clientId === null)
            throw new globalThis.Error("The parameter 'clientId' must be defined.");
        url_ = url_.replace("{clientId}", encodeURIComponent("" + clientId));
        if (page === undefined || page === null)
            throw new globalThis.Error("The parameter 'page' must be defined and cannot be null.");
        else
            url_ += "page=" + encodeURIComponent("" + page) + "&";
        if (pageSize === undefined || pageSize === null)
            throw new globalThis.Error("The parameter 'pageSize' must be defined and cannot be null.");
        else
            url_ += "pageSize=" + encodeURIComponent("" + pageSize) + "&";
        if (from !== undefined && from !== null)
            url_ += "from=" + encodeURIComponent("" + from) + "&";
        if (to !== undefined && to !== null)
            url_ += "to=" + encodeURIComponent("" + to) + "&";
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGetClientMeasurementsEndpoint(_response);
        });
    }

    protected processGetClientMeasurementsEndpoint(response: AxiosResponse): Promise<GetMeasurementsResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GetMeasurementsResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GetMeasurementsResponse>(null as any);
    }

    /**
     * Record a client's body measurement
     * @param clientId The client profile's public identifier (route parameter).
     * @return Success
     */
    addClientMeasurementEndpoint(clientId: string, addClientMeasurementRequest: AddClientMeasurementRequest, signal?: AbortSignal): Promise<MeasurementDto> {
        let url_ = this.baseUrl + "/trainer/clients/{clientId}/measurements";
        if (clientId === undefined || clientId === null)
            throw new globalThis.Error("The parameter 'clientId' must be defined.");
        url_ = url_.replace("{clientId}", encodeURIComponent("" + clientId));
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(addClientMeasurementRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processAddClientMeasurementEndpoint(_response);
        });
    }

    protected processAddClientMeasurementEndpoint(response: AxiosResponse): Promise<MeasurementDto> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<MeasurementDto>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<MeasurementDto>(null as any);
    }

    /**
     * Submit client onboarding questionnaire
     * @return Onboarding complete
     */
    submitOnboardingEndpoint(submitOnboardingRequest: SubmitOnboardingRequest, signal?: AbortSignal): Promise<SubmitOnboardingResponse> {
        let url_ = this.baseUrl + "/client/onboarding";
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(submitOnboardingRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processSubmitOnboardingEndpoint(_response);
        });
    }

    protected processSubmitOnboardingEndpoint(response: AxiosResponse): Promise<SubmitOnboardingResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<SubmitOnboardingResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<SubmitOnboardingResponse>(null as any);
    }

    /**
     * Register push token
     * @return No Content
     */
    registerPushTokenEndpoint(registerPushTokenRequest: RegisterPushTokenRequest, signal?: AbortSignal): Promise<void> {
        let url_ = this.baseUrl + "/client/push-token";
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(registerPushTokenRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processRegisterPushTokenEndpoint(_response);
        });
    }

    protected processRegisterPushTokenEndpoint(response: AxiosResponse): Promise<void> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 204) {
            const _responseText = response.data;
            return Promise.resolve<void>(null as any);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<void>(null as any);
    }

    /**
     * Get weekly overview
     * @return Success
     */
    getWeeklyOverviewEndpoint(signal?: AbortSignal): Promise<GetWeeklyOverviewResponse> {
        let url_ = this.baseUrl + "/client/progress/weekly";
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGetWeeklyOverviewEndpoint(_response);
        });
    }

    protected processGetWeeklyOverviewEndpoint(response: AxiosResponse): Promise<GetWeeklyOverviewResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GetWeeklyOverviewResponse>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GetWeeklyOverviewResponse>(null as any);
    }

    /**
     * Get compliance score
     * @param from (optional) Start date for the compliance calculation. Defaults to 7 days ago if not provided.
     * @param to (optional) End date for the compliance calculation. Defaults to today if not provided.
     * @return Success
     */
    getComplianceScoreEndpoint(from?: string | null | undefined, to?: string | null | undefined, signal?: AbortSignal): Promise<GetComplianceScoreResponse> {
        let url_ = this.baseUrl + "/client/progress/compliance?";
        if (from !== undefined && from !== null)
            url_ += "from=" + encodeURIComponent("" + from) + "&";
        if (to !== undefined && to !== null)
            url_ += "to=" + encodeURIComponent("" + to) + "&";
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGetComplianceScoreEndpoint(_response);
        });
    }

    protected processGetComplianceScoreEndpoint(response: AxiosResponse): Promise<GetComplianceScoreResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GetComplianceScoreResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GetComplianceScoreResponse>(null as any);
    }

    /**
     * List client plans
     * @param status (optional) Filter by plan status (e.g. "Completed", "Active").
    When null, returns all non-draft plans.
     * @return Success
     */
    getClientPlansEndpoint(status?: string | null | undefined, signal?: AbortSignal): Promise<GetClientPlansResponse> {
        let url_ = this.baseUrl + "/client/plans?";
        if (status !== undefined && status !== null)
            url_ += "status=" + encodeURIComponent("" + status) + "&";
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGetClientPlansEndpoint(_response);
        });
    }

    protected processGetClientPlansEndpoint(response: AxiosResponse): Promise<GetClientPlansResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GetClientPlansResponse>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GetClientPlansResponse>(null as any);
    }

    /**
     * List personal records
     * @param page Page number (1-based). Defaults to 1.
     * @param pageSize Number of items per page. Defaults to 20; maximum 100.
     * @param exerciseExternalId (optional) Optional filter to return records for a specific exercise only.
    When null, all exercises are returned.
     * @return Success
     */
    getClientRecordsEndpoint(page: number, pageSize: number, exerciseExternalId?: string | null | undefined, signal?: AbortSignal): Promise<GetClientRecordsResponse> {
        let url_ = this.baseUrl + "/client/records?";
        if (page === undefined || page === null)
            throw new globalThis.Error("The parameter 'page' must be defined and cannot be null.");
        else
            url_ += "page=" + encodeURIComponent("" + page) + "&";
        if (pageSize === undefined || pageSize === null)
            throw new globalThis.Error("The parameter 'pageSize' must be defined and cannot be null.");
        else
            url_ += "pageSize=" + encodeURIComponent("" + pageSize) + "&";
        if (exerciseExternalId !== undefined && exerciseExternalId !== null)
            url_ += "exerciseExternalId=" + encodeURIComponent("" + exerciseExternalId) + "&";
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGetClientRecordsEndpoint(_response);
        });
    }

    protected processGetClientRecordsEndpoint(response: AxiosResponse): Promise<GetClientRecordsResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GetClientRecordsResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GetClientRecordsResponse>(null as any);
    }

    /**
     * Mark notification as read
     * @return No Content
     */
    markReadEndpoint(id: string, signal?: AbortSignal): Promise<void> {
        let url_ = this.baseUrl + "/client/notifications/{id}/read";
        if (id === undefined || id === null)
            throw new globalThis.Error("The parameter 'id' must be defined.");
        url_ = url_.replace("{id}", encodeURIComponent("" + id));
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "POST",
            url: url_,
            headers: {
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processMarkReadEndpoint(_response);
        });
    }

    protected processMarkReadEndpoint(response: AxiosResponse): Promise<void> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 204) {
            const _responseText = response.data;
            return Promise.resolve<void>(null as any);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<void>(null as any);
    }

    /**
     * Mark all notifications as read
     * @return No Content
     */
    markAllReadEndpoint(signal?: AbortSignal): Promise<void> {
        let url_ = this.baseUrl + "/client/notifications/read-all";
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "POST",
            url: url_,
            headers: {
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processMarkAllReadEndpoint(_response);
        });
    }

    protected processMarkAllReadEndpoint(response: AxiosResponse): Promise<void> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 204) {
            const _responseText = response.data;
            return Promise.resolve<void>(null as any);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<void>(null as any);
    }

    /**
     * Get client notifications
     * @param limit (optional) 
     * @param cursor (optional) 
     * @return Success
     */
    getNotificationsEndpoint(limit?: number | null | undefined, cursor?: string | null | undefined, signal?: AbortSignal): Promise<GetNotificationsResponse> {
        let url_ = this.baseUrl + "/client/notifications?";
        if (limit !== undefined && limit !== null)
            url_ += "limit=" + encodeURIComponent("" + limit) + "&";
        if (cursor !== undefined && cursor !== null)
            url_ += "cursor=" + encodeURIComponent("" + cursor) + "&";
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGetNotificationsEndpoint(_response);
        });
    }

    protected processGetNotificationsEndpoint(response: AxiosResponse): Promise<GetNotificationsResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GetNotificationsResponse>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GetNotificationsResponse>(null as any);
    }

    /**
     * Get pending invite
     * @return Success
     */
    getPendingInviteEndpoint(signal?: AbortSignal): Promise<PendingInviteResponse> {
        let url_ = this.baseUrl + "/client/invites/pending";
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGetPendingInviteEndpoint(_response);
        });
    }

    protected processGetPendingInviteEndpoint(response: AxiosResponse): Promise<PendingInviteResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<PendingInviteResponse>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<PendingInviteResponse>(null as any);
    }

    /**
     * Decline a pending invite
     * @return No Content
     */
    declineClientInviteEndpoint(id: string, signal?: AbortSignal): Promise<void> {
        let url_ = this.baseUrl + "/client/invites/{id}/decline";
        if (id === undefined || id === null)
            throw new globalThis.Error("The parameter 'id' must be defined.");
        url_ = url_.replace("{id}", encodeURIComponent("" + id));
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "POST",
            url: url_,
            headers: {
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processDeclineClientInviteEndpoint(_response);
        });
    }

    protected processDeclineClientInviteEndpoint(response: AxiosResponse): Promise<void> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 204) {
            const _responseText = response.data;
            return Promise.resolve<void>(null as any);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<void>(null as any);
    }

    /**
     * Accept a pending invite
     * @return No Content
     */
    acceptClientInviteEndpoint(id: string, signal?: AbortSignal): Promise<void> {
        let url_ = this.baseUrl + "/client/invites/{id}/accept";
        if (id === undefined || id === null)
            throw new globalThis.Error("The parameter 'id' must be defined.");
        url_ = url_.replace("{id}", encodeURIComponent("" + id));
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "POST",
            url: url_,
            headers: {
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processAcceptClientInviteEndpoint(_response);
        });
    }

    protected processAcceptClientInviteEndpoint(response: AxiosResponse): Promise<void> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 204) {
            const _responseText = response.data;
            return Promise.resolve<void>(null as any);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<void>(null as any);
    }

    /**
     * Get active collaborations
     * @return Success
     */
    getCollaborationsEndpoint(signal?: AbortSignal): Promise<GetCollaborationsResponse> {
        let url_ = this.baseUrl + "/client/collaborations";
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "GET",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGetCollaborationsEndpoint(_response);
        });
    }

    protected processGetCollaborationsEndpoint(response: AxiosResponse): Promise<GetCollaborationsResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GetCollaborationsResponse>(result200);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GetCollaborationsResponse>(null as any);
    }

    /**
     * End a collaboration
     * @return No Content
     */
    endCollaborationEndpoint(publicId: string, signal?: AbortSignal): Promise<void> {
        let url_ = this.baseUrl + "/client/collaborations/{PublicId}";
        if (publicId === undefined || publicId === null)
            throw new globalThis.Error("The parameter 'publicId' must be defined.");
        url_ = url_.replace("{publicId}", encodeURIComponent("" + publicId));
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "DELETE",
            url: url_,
            headers: {
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processEndCollaborationEndpoint(_response);
        });
    }

    protected processEndCollaborationEndpoint(response: AxiosResponse): Promise<void> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 204) {
            const _responseText = response.data;
            return Promise.resolve<void>(null as any);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<void>(null as any);
    }

    /**
     * Verify email address
     * @return No Content
     */
    verifyEmailEndpoint(verifyEmailRequest: VerifyEmailRequest, signal?: AbortSignal): Promise<void> {
        let url_ = this.baseUrl + "/auth/verify-email";
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(verifyEmailRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processVerifyEmailEndpoint(_response);
        });
    }

    protected processVerifyEmailEndpoint(response: AxiosResponse): Promise<void> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 204) {
            const _responseText = response.data;
            return Promise.resolve<void>(null as any);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<void>(null as any);
    }

    /**
     * Request a social sign-in nonce
     * @return Nonce issued successfully
     */
    requestNonceEndpoint(signal?: AbortSignal): Promise<RequestNonceResponse> {
        let url_ = this.baseUrl + "/auth/social/nonce";
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "POST",
            url: url_,
            headers: {
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processRequestNonceEndpoint(_response);
        });
    }

    protected processRequestNonceEndpoint(response: AxiosResponse): Promise<RequestNonceResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<RequestNonceResponse>(result200);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<RequestNonceResponse>(null as any);
    }

    /**
     * Google social login
     * @return Login successful
     */
    googleSocialLoginEndpoint(googleSocialLoginRequest: GoogleSocialLoginRequest, signal?: AbortSignal): Promise<GoogleSocialLoginResponse> {
        let url_ = this.baseUrl + "/auth/social/google";
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(googleSocialLoginRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processGoogleSocialLoginEndpoint(_response);
        });
    }

    protected processGoogleSocialLoginEndpoint(response: AxiosResponse): Promise<GoogleSocialLoginResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<GoogleSocialLoginResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("idToken or nonce is missing or empty", status, _responseText, _headers, result400);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<GoogleSocialLoginResponse>(null as any);
    }

    /**
     * Apple Sign-In
     * @return Login successful
     */
    appleSocialLoginEndpoint(appleSocialLoginRequest: AppleSocialLoginRequest, signal?: AbortSignal): Promise<AppleSocialLoginResponse> {
        let url_ = this.baseUrl + "/auth/social/apple";
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(appleSocialLoginRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processAppleSocialLoginEndpoint(_response);
        });
    }

    protected processAppleSocialLoginEndpoint(response: AxiosResponse): Promise<AppleSocialLoginResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<AppleSocialLoginResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("identityToken or nonce is missing or empty", status, _responseText, _headers, result400);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<AppleSocialLoginResponse>(null as any);
    }

    /**
     * Reset password
     * @return No Content
     */
    resetPasswordEndpoint(resetPasswordRequest: ResetPasswordRequest, signal?: AbortSignal): Promise<void> {
        let url_ = this.baseUrl + "/auth/password/reset";
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(resetPasswordRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "PUT",
            url: url_,
            headers: {
                "Content-Type": "application/json",
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processResetPasswordEndpoint(_response);
        });
    }

    protected processResetPasswordEndpoint(response: AxiosResponse): Promise<void> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 204) {
            const _responseText = response.data;
            return Promise.resolve<void>(null as any);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<void>(null as any);
    }

    /**
     * Request password reset
     * @return No Content
     */
    requestPasswordResetEndpoint(requestPasswordResetRequest: RequestPasswordResetRequest, signal?: AbortSignal): Promise<void> {
        let url_ = this.baseUrl + "/auth/password/reset";
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(requestPasswordResetRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processRequestPasswordResetEndpoint(_response);
        });
    }

    protected processRequestPasswordResetEndpoint(response: AxiosResponse): Promise<void> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 204) {
            const _responseText = response.data;
            return Promise.resolve<void>(null as any);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<void>(null as any);
    }

    /**
     * Resend verification email
     * @return No Content
     */
    resendVerificationEndpoint(signal?: AbortSignal): Promise<void> {
        let url_ = this.baseUrl + "/auth/resend-verification";
        url_ = url_.replace(/[?&]$/, "");

        let options_: AxiosRequestConfig = {
            method: "POST",
            url: url_,
            headers: {
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processResendVerificationEndpoint(_response);
        });
    }

    protected processResendVerificationEndpoint(response: AxiosResponse): Promise<void> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 204) {
            const _responseText = response.data;
            return Promise.resolve<void>(null as any);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<void>(null as any);
    }

    /**
     * Register a new user
     * @return Registration successful
     */
    registerEndpoint(registerRequest: RegisterRequest, signal?: AbortSignal): Promise<RegisterResponse> {
        let url_ = this.baseUrl + "/auth/register";
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(registerRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processRegisterEndpoint(_response);
        });
    }

    protected processRegisterEndpoint(response: AxiosResponse): Promise<RegisterResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 201) {
            const _responseText = response.data;
            let result201: any = null;
            let resultData201  = _responseText;
            result201 = JSON.parse(resultData201);
            return Promise.resolve<RegisterResponse>(result201);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Validation error", status, _responseText, _headers, result400);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<RegisterResponse>(null as any);
    }

    /**
     * Refresh access token
     * @return Success
     */
    refreshTokenEndpoint(refreshTokenRequest: RefreshTokenRequest, signal?: AbortSignal): Promise<RefreshTokenResponse> {
        let url_ = this.baseUrl + "/auth/refresh";
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(refreshTokenRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processRefreshTokenEndpoint(_response);
        });
    }

    protected processRefreshTokenEndpoint(response: AxiosResponse): Promise<RefreshTokenResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<RefreshTokenResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<RefreshTokenResponse>(null as any);
    }

    /**
     * Logout user
     * @return No Content
     */
    logoutEndpoint(logoutRequest: LogoutRequest, signal?: AbortSignal): Promise<void> {
        let url_ = this.baseUrl + "/auth/logout";
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(logoutRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processLogoutEndpoint(_response);
        });
    }

    protected processLogoutEndpoint(response: AxiosResponse): Promise<void> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 204) {
            const _responseText = response.data;
            return Promise.resolve<void>(null as any);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<void>(null as any);
    }

    /**
     * Authenticate user
     * @return Success
     */
    loginEndpoint(loginRequest: LoginRequest, signal?: AbortSignal): Promise<LoginResponse> {
        let url_ = this.baseUrl + "/auth/login";
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(loginRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processLoginEndpoint(_response);
        });
    }

    protected processLoginEndpoint(response: AxiosResponse): Promise<LoginResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<LoginResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<LoginResponse>(null as any);
    }

    /**
     * Resend verification email (anonymous)
     * @return Success
     */
    anonymousResendVerificationEndpoint(anonymousResendVerificationRequest: AnonymousResendVerificationRequest, signal?: AbortSignal): Promise<AnonymousResendVerificationResponse> {
        let url_ = this.baseUrl + "/auth/resend-verification/anonymous";
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(anonymousResendVerificationRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processAnonymousResendVerificationEndpoint(_response);
        });
    }

    protected processAnonymousResendVerificationEndpoint(response: AxiosResponse): Promise<AnonymousResendVerificationResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<AnonymousResendVerificationResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<AnonymousResendVerificationResponse>(null as any);
    }

    /**
     * Accept a trainer invitation
     * @return Success
     */
    acceptInvitationEndpoint(acceptInvitationRequest: AcceptInvitationRequest, signal?: AbortSignal): Promise<AcceptInvitationResponse> {
        let url_ = this.baseUrl + "/auth/invite/accept";
        url_ = url_.replace(/[?&]$/, "");

        const content_ = JSON.stringify(acceptInvitationRequest);

        let options_: AxiosRequestConfig = {
            data: content_,
            method: "POST",
            url: url_,
            headers: {
                "Content-Type": "application/json",
                "Accept": "application/json"
            },
            signal
        };

        return this.instance.request(options_).catch((_error: any) => {
            if (isAxiosError(_error) && _error.response) {
                return _error.response;
            } else {
                throw _error;
            }
        }).then((_response: AxiosResponse) => {
            return this.processAcceptInvitationEndpoint(_response);
        });
    }

    protected processAcceptInvitationEndpoint(response: AxiosResponse): Promise<AcceptInvitationResponse> {
        const status = response.status;
        let _headers: any = {};
        if (response.headers && typeof response.headers === "object") {
            for (const k in response.headers) {
                if (response.headers.hasOwnProperty(k)) {
                    _headers[k] = response.headers[k];
                }
            }
        }
        if (status === 200) {
            const _responseText = response.data;
            let result200: any = null;
            let resultData200  = _responseText;
            result200 = JSON.parse(resultData200);
            return Promise.resolve<AcceptInvitationResponse>(result200);

        } else if (status === 400) {
            const _responseText = response.data;
            let result400: any = null;
            let resultData400  = _responseText;
            result400 = JSON.parse(resultData400);
            return throwException("Bad Request", status, _responseText, _headers, result400);

        } else if (status === 401) {
            const _responseText = response.data;
            return throwException("Unauthorized", status, _responseText, _headers);

        } else if (status === 403) {
            const _responseText = response.data;
            return throwException("Forbidden", status, _responseText, _headers);

        } else if (status !== 200 && status !== 204) {
            const _responseText = response.data;
            return throwException("An unexpected server error occurred.", status, _responseText, _headers);
        }
        return Promise.resolve<AcceptInvitationResponse>(null as any);
    }
}

/** Response DTO for a single workout template. */
export interface WorkoutTemplateResponse {
    /** Template's public identifier. */
    templateId?: string;
    /** Display name of the template. */
    name?: string;
    /** Optional coach notes describing the workout as a whole. */
    notes?: string | undefined;
    /** Default workout format. Null means no format override. */
    defaultFormat?: string | undefined;
    /** Default format configuration. */
    defaultFormatConfig?: WodConfig | undefined;
    /** Default exercises pre-populated when applying this template. */
    defaultExercises?: SessionExercise[];
    /** Optimistic concurrency version. */
    version?: number;
    /** When this template was created. */
    createdAt?: string;
    /** When this template was last updated. */
    updatedAt?: string;
}

/** Configuration parameters for a WOD (Workout Of the Day) format. Which fields are meaningful depends on the WorkoutFormat. All fields are nullable — only those relevant to the chosen format are expected to be set. */
export interface WodConfig {
    /** Maximum allowed time in seconds (used by ForTime and AMRAP). */
    timeCapSeconds?: number | undefined;
    /** Duration of each interval in seconds (used by EMOM). */
    intervalSeconds?: number | undefined;
    /** Total number of rounds (used by EMOM and Tabata). */
    totalRounds?: number | undefined;
    /** Work interval duration in seconds (used by Tabata). */
    workSeconds?: number | undefined;
    /** Rest interval duration in seconds (used by Tabata). */
    restSeconds?: number | undefined;
}

/** An exercise within a training session — denormalized snapshot of exercise data. */
export interface SessionExercise {
    /** Instance identifier for this specific exercise entry within its session — distinguishes
two occurrences of the same catalog exercise (ExerciseExternalId)
programmed twice in one workout, or once standalone and once nested in a workout of the
same session. Mirrors WorkoutId and
MealId. Assigned a fresh, distinct value per exercise when the
plan is created or updated (#857 phase 3a) — there is no production data to backfill, so
no boot migration is required. */
    exerciseId?: string;
    /** Reference to the original exercise document's ExternalId. */
    exerciseExternalId?: string;
    /** Snapshot of the exercise name at time of addition. */
    exerciseName?: string;
    /** Display order within the session (1-based). */
    order?: number;
    /** Optional coach notes for this exercise. */
    notes?: string | undefined;
    /** Rest time between sets in seconds. */
    restSeconds?: number | undefined;
    /** How performance for this exercise is measured. Defaults to Reps. */
    movementType?: MovementType;
    /** Per-exercise format override. Null means the exercise inherits the session's format. */
    format?: WorkoutFormat | undefined;
    /** Per-exercise format configuration. Null when Format is null or Standard. */
    formatConfig?: WodConfig | undefined;
    /** Planned sets for this exercise. */
    sets?: ExerciseSet[];
}

/** Defines how performance in an exercise is measured. */
export enum MovementType {
    Reps = "Reps",
    Time = "Time",
    Distance = "Distance",
    RepsForTime = "RepsForTime",
}

/** Defines the workout format / scoring methodology for a training session or exercise. */
export enum WorkoutFormat {
    Standard = "Standard",
    ForTime = "ForTime",
    AMRAP = "AMRAP",
    EMOM = "EMOM",
    Tabata = "Tabata",
}

/** A single set within an exercise — represents planned (prescription) values. */
export interface ExerciseSet {
    /** Set number within the exercise (1-based). */
    setNumber?: number;
    /** Type of set (Normal, Warmup, Dropset, Superset). */
    type?: SetType;
    /** Target number of repetitions. */
    reps?: number | undefined;
    /** Target weight in kilograms. */
    weightKg?: number | undefined;
    /** Target duration in seconds (for timed exercises). */
    durationSeconds?: number | undefined;
    /** Target Rate of Perceived Exertion (1-10 scale). */
    rpe?: number | undefined;
    /** Target distance in meters (for distance-based exercises). */
    distanceMeters?: number | undefined;
    /** Rest time after this set in seconds. */
    restSeconds?: number | undefined;
}

/** Type of exercise set within a training session. */
export enum SetType {
    Normal = "Normal",
    Warmup = "Warmup",
    Dropset = "Dropset",
    Superset = "Superset",
}

/** Request for updating an existing workout template. */
export interface UpdateWorkoutTemplateRequest {
    /** Updated display name of the template. */
    name: string;
    /** Updated optional coach notes describing the workout as a whole. */
    notes?: string | undefined;
    /** Updated default workout format. Null means no format override. */
    defaultFormat?: WorkoutFormat | undefined;
    /** Updated default format configuration. */
    defaultFormatConfig?: WodConfig | undefined;
    /** Updated default exercises. */
    defaultExercises?: CreateWorkoutTemplateExerciseRequest[];
    /** Expected version for optimistic concurrency control. */
    version?: number;
}

/** An exercise entry in a workout template. */
export interface CreateWorkoutTemplateExerciseRequest {
    /** External (public) identifier of the exercise. */
    exerciseExternalId?: string;
    /** Display name of the exercise (snapshot). */
    exerciseName?: string;
    /** Display order within the workout (1-based). */
    order?: number;
    /** Optional coach notes for this exercise. */
    notes?: string | undefined;
    /** Rest time between sets in seconds. */
    restSeconds?: number | undefined;
    /** How performance for this exercise is measured. Defaults to Reps. */
    movementType?: MovementType;
    /** Per-exercise format override. Null means inherits the workout's format. */
    format?: WorkoutFormat | undefined;
    /** Per-exercise format configuration. Null when Format is null or Standard. */
    formatConfig?: WodConfig | undefined;
    /** Planned sets for this exercise. */
    sets?: CreateWorkoutTemplateSetRequest[];
}

/** A planned set in a workout template exercise. */
export interface CreateWorkoutTemplateSetRequest {
    /** Set number within the exercise (1-based). */
    setNumber?: number;
    /** Type of set. */
    type?: SetType;
    /** Target number of repetitions. */
    reps?: number | undefined;
    /** Target weight in kilograms. */
    weightKg?: number | undefined;
    /** Target duration in seconds. */
    durationSeconds?: number | undefined;
    /** Target RPE (1-10). */
    rpe?: number | undefined;
    /** Target distance in meters. */
    distanceMeters?: number | undefined;
    /** Rest time after this set in seconds. */
    restSeconds?: number | undefined;
}

/** RFC7807 compatible problem details/ error response class. this can be used by configuring startup like so: app.UseFastEndpoints(c => c.Errors.UseProblemDetails()) */
export interface ProblemDetails {
    type?: string;
    title?: string;
    status?: number;
    instance?: string;
    traceId?: string;
    /** the details of the error */
    detail?: string | undefined;
    errors?: ProblemDetails_Error[];
}

/** the error details object */
export interface ProblemDetails_Error {
    /** the name of the error or property of the dto that caused the error */
    name?: string;
    /** the reason for the error */
    reason?: string;
    /** the code of the error */
    code?: string | undefined;
    /** the severity of the error */
    severity?: string | undefined;
}

/** Response wrapper for listing workout templates: the calling trainer's own templates, paginated. Session templates now have their own paginated search endpoint under /training/session-templates — see SearchSessionTemplatesEndpoint. */
export interface ListWorkoutTemplatesResponse {
    /** The calling trainer's own workout templates, paginated (unchanged shape/semantics). */
    ownTemplates?: WorkoutTemplateResponse[];
}

/** Request for listing the calling trainer's workout templates. */
export interface ListWorkoutTemplatesRequest {
}

/** Request for retrieving a single workout template by its public identifier. */
export interface GetWorkoutTemplateRequest {
}

/** Request for deleting a workout template. */
export interface DeleteWorkoutTemplateRequest {
}

/** Request for creating a new workout template. */
export interface CreateWorkoutTemplateRequest {
    /** Display name of the template. */
    name: string;
    /** Optional coach notes describing the workout as a whole. */
    notes?: string | undefined;
    /** Default workout format. Null means no format override (Standard / inherits from session). */
    defaultFormat?: WorkoutFormat | undefined;
    /** Default format configuration. Null when DefaultFormat is null or Standard. */
    defaultFormatConfig?: WodConfig | undefined;
    /** Default exercises to pre-populate when applying this template. */
    defaultExercises?: CreateWorkoutTemplateExerciseRequest[];
}

/** Full workout log detail DTO. Byte-stable wire shape — sourced from SessionExecution (#841) instead of the retired standalone WorkoutLog. */
export interface WorkoutLogDetail {
    /** Workout log public identifier. */
    logId?: string;
    /** Client's user ID. */
    clientId?: string;
    /** Training plan reference. */
    planId?: string | undefined;
    /** Training session reference. */
    sessionId?: string | undefined;
    /** When the workout started. */
    startedAt?: string;
    /** When the workout was completed. */
    completedAt?: string | undefined;
    /** Duration in seconds (null if not completed). */
    durationSeconds?: number | undefined;
    /** Client mood (1-5). */
    mood?: number | undefined;
    /** Client notes. */
    notes?: string | undefined;
    /** Whether the workout is completed. */
    isCompleted?: boolean;
    /** Exercises performed. */
    exercises?: WorkoutExercise[];
    /** Whether any set in this workout is a PR. */
    hasPR?: boolean;
}

/** An exercise performed during a workout — denormalized snapshot with actual results. */
export interface WorkoutExercise {
    /** Reference to the exercise document's ExternalId. */
    exerciseExternalId?: string;
    /** Snapshot of the exercise name. */
    exerciseName?: string;
    /** WOD format result for this individual exercise.
Null for Standard exercises or when not yet recorded. */
    wodResult?: WodResult | undefined;
    /** Actual sets performed. */
    sets?: WorkoutSet[];
}

/** Records the outcome of a WOD (Workout Of the Day) format session or exercise. Which fields are meaningful depends on the WorkoutFormat. All fields are nullable — only those relevant to the actual result need to be set. */
export interface WodResult {
    /** Number of complete rounds completed (AMRAP, Tabata). */
    roundsCompleted?: number | undefined;
    /** Extra reps accumulated after the last complete round (AMRAP). */
    extraReps?: number | undefined;
    /** Total time taken to complete the workout in seconds (ForTime). */
    totalTimeSeconds?: number | undefined;
    /** List of round numbers that were not completed (Tabata, EMOM). */
    failedRounds?: number[] | undefined;
    /** Reps completed per round, indexed 1-based (Tabata, EMOM, AMRAP). */
    repsByRound?: number[] | undefined;
}

/** A single set actually performed during a workout with recorded values. Snapshot-planned fields capture the prescribed values at the time the set was first logged; they are immutable after initial persistence so later plan edits do not affect them. */
export interface WorkoutSet {
    /** Set number within the exercise (1-based). */
    setNumber?: number;
    /** Actual repetitions completed. */
    reps?: number | undefined;
    /** Actual weight used in kilograms. */
    weightKg?: number | undefined;
    /** Rate of Perceived Exertion (1-10 scale). */
    rpe?: number | undefined;
    /** Actual duration in seconds (for timed exercises). */
    durationSeconds?: number | undefined;
    /** Actual distance in meters (for distance-based exercises). */
    distanceMeters?: number | undefined;
    /** When this set was completed. */
    completedAt?: string | undefined;
    /** Whether this set is a personal record for this exercise. */
    isPR?: boolean;
    /** Prescribed repetitions at the time this set was first logged. */
    plannedReps?: number | undefined;
    /** Prescribed weight (kg) at the time this set was first logged. */
    plannedWeightKg?: number | undefined;
    /** Prescribed RPE at the time this set was first logged. */
    plannedRpe?: number | undefined;
    /** Prescribed duration (seconds) at the time this set was first logged. */
    plannedDurationSeconds?: number | undefined;
    /** Prescribed distance (meters) at the time this set was first logged. */
    plannedDistanceMeters?: number | undefined;
    /** Backend-computed flag: true when any actual field differs from its snapshot-planned counterpart.
Always false for legacy sets whose planned fields are all null (backward-compatible default).
Never stored — derived on read. */
    isModified?: boolean;
}

/** Request to progressively update a workout log with exercise data. Designed for offline-first: client sends all exercises/sets accumulated so far. */
export interface UpdateWorkoutRequest {
    /** Client's subjective mood rating (1-5). */
    mood?: number | undefined;
    /** Optional client notes. */
    notes?: string | undefined;
    /** WOD format result for the whole session (ForTime, AMRAP, etc.).
Null for Standard workouts or when not yet recorded. */
    wodResult?: WodResult | undefined;
    /** Current state of exercises performed. */
    exercises?: UpdateWorkoutExerciseRequest[];
}

/** Exercise data in a workout update. */
export interface UpdateWorkoutExerciseRequest {
    /** The workout this exercise belongs to — must match WorkoutId
(and by design the source WorkoutId).
Null for requests from legacy clients that do not yet send workout context;
in that case the exercise is stored in the first workout of the log (single-workout fallback). */
    workoutId?: string | undefined;
    /** Reference to the exercise document's ExternalId. */
    exerciseExternalId?: string;
    /** Snapshot of the exercise name. */
    exerciseName?: string;
    /** WOD format result for this individual exercise.
Null for Standard exercises or when not yet recorded. */
    wodResult?: WodResult | undefined;
    /** Sets performed for this exercise. */
    sets?: UpdateWorkoutSetRequest[];
}

/** Set data in a workout update. */
export interface UpdateWorkoutSetRequest {
    /** Set number (1-based). */
    setNumber?: number;
    /** Actual reps completed. */
    reps?: number | undefined;
    /** Actual weight in kg. */
    weightKg?: number | undefined;
    /** Rate of Perceived Exertion (1-10). */
    rpe?: number | undefined;
    /** Duration in seconds. */
    durationSeconds?: number | undefined;
    /** Distance in meters. */
    distanceMeters?: number | undefined;
    /** When this set was completed. */
    completedAt?: string | undefined;
    /** Prescribed repetitions from the plan prescription. */
    plannedReps?: number | undefined;
    /** Prescribed weight (kg) from the plan prescription. */
    plannedWeightKg?: number | undefined;
    /** Prescribed RPE from the plan prescription. */
    plannedRpe?: number | undefined;
    /** Prescribed duration (seconds) from the plan prescription. */
    plannedDurationSeconds?: number | undefined;
    /** Prescribed distance (meters) from the plan prescription. */
    plannedDistanceMeters?: number | undefined;
}

/** Response after starting a workout. */
export interface StartWorkoutResponse {
    /** The new workout log's public identifier. */
    logId?: string;
    /** When the workout was started. */
    startedAt?: string;
}

/** Request to start a new workout session. */
export interface StartWorkoutRequest {
    /** Optional reference to the training plan. */
    planId?: string | undefined;
    /** Optional reference to the training session within the plan. */
    sessionId?: string | undefined;
}

/** Response returned when a workout log transitions to Live state. */
export interface GoLiveResponse {
    /** The external ID of the workout log now in Live state. */
    logId?: string;
    /** UTC timestamp when the Live lock was acquired. */
    liveAt?: string;
}

/** Request to transition an existing draft workout log to Live state. */
export interface GoLiveRequest {
}

/** Paginated response with workout log summaries. */
export interface GetWorkoutLogsResponse {
    /** List of workout log summaries. */
    logs?: WorkoutLogSummary[];
    /** Total count of matching logs. */
    totalCount?: number;
    /** Current page. */
    page?: number;
    /** Page size. */
    pageSize?: number;
}

/** Lightweight workout log summary for list views. Byte-stable wire shape — sourced from SessionExecution (#841) instead of the retired standalone WorkoutLog. */
export interface WorkoutLogSummary {
    /** Workout log public identifier. */
    logId?: string;
    /** When the workout started. */
    startedAt?: string;
    /** When the workout was completed. */
    completedAt?: string | undefined;
    /** Duration in seconds. */
    durationSeconds?: number | undefined;
    /** Client mood (1-5). */
    mood?: number | undefined;
    /** Whether the workout is completed. */
    isCompleted?: boolean;
    /** Number of exercises performed. */
    exerciseCount?: number;
    /** Total number of sets performed. */
    setCount?: number;
    /** Whether any set is a PR. */
    hasPR?: boolean;
}

/** Request to list client's workout logs. */
export interface GetWorkoutLogsRequest {
}

/** Request to get a single workout log detail. */
export interface GetWorkoutLogRequest {
}

/** Time series of exercise performance data points. */
export interface GetExerciseProgressResponse {
    /** Exercise name. */
    exerciseName?: string;
    /** Performance data points ordered by date. */
    dataPoints?: ExerciseProgressPoint[];
}

/** A single data point of exercise performance. */
export interface ExerciseProgressPoint {
    /** Date of the workout. */
    date?: string;
    /** Best weight used in this workout (kg). */
    bestWeightKg?: number | undefined;
    /** Best reps at best weight. */
    bestReps?: number | undefined;
    /** Total volume (sum of weight × reps across all sets). */
    totalVolume?: number;
    /** Whether any set was a PR. */
    hasPR?: boolean;
}

/** Request to get a client's exercise progress over time. */
export interface GetExerciseProgressRequest {
}

/** Request to complete a workout session. */
export interface CompleteWorkoutRequest {
}

/** Response returned when an abandon request is processed (including idempotent no-op case). */
export interface AbandonWorkoutResponse {
    /** Whether a Live lock was actually released. False when the lock was already gone (idempotent). */
    released?: boolean;
}

/** Request to abandon (discard) a draft workout session and release the Live lock. */
export interface AbandonWorkoutRequest {
}

/** Response for POST /client/weekly-check-ins/{id}/respond. */
export interface RespondToCheckInResponse {
    /** Check-in identifier. */
    id?: string;
    /** Flags persisted. */
    flags?: CheckInFlag[];
    /** Note persisted. */
    note?: string | undefined;
    /** When the response was recorded (UTC). */
    respondedAt?: string;
}

/** Flags a client can select when responding to a weekly check-in reminder, indicating events or circumstances that may affect the upcoming week's plan. */
export enum CheckInFlag {
    Traveling = "Traveling",
    EventOrCelebration = "EventOrCelebration",
    SickOrLowEnergy = "SickOrLowEnergy",
    InjuryOrPain = "InjuryOrPain",
    MoreTimeAvailable = "MoreTimeAvailable",
    LessTimeAvailable = "LessTimeAvailable",
}

/** Request body for POST /client/weekly-check-ins/{id}/respond. */
export interface RespondToCheckInRequest {
    /** Zero or more flags selected by the client. */
    flags?: CheckInFlag[];
    /** Optional free-text note. ≤ 500 characters. */
    note?: string | undefined;
}

/** Response for PUT /trainer/weekly-check-ins/settings. */
export interface PutSettingsResponse {
    /** The identifier of the created or updated setting. */
    id?: string;
    /** Profession this setting applies to. */
    profession?: string;
    /** Day of the week (0 = Sunday … 6 = Saturday). */
    dayOfWeek?: number;
    /** Time of day for the reminder. Between 00:00:00 and 23:59:59. */
    timeOfDay?: string;
    /** Whether the reminder is enabled. */
    enabled?: boolean;
    /** Optional addendum. */
    defaultAddendum?: string | undefined;
    /** Hours after dispatch before the check-in expires. */
    deadlineOffsetHours?: number;
}

/** Request body for PUT /trainer/weekly-check-ins/settings. */
export interface PutSettingsRequest {
    /** Profession this setting applies to. Must be one of the trainer's specializations.
Accepted values: "Training", "Nutrition". */
    profession: string;
    /** Day of the week on which the reminder fires (0 = Sunday … 6 = Saturday). */
    dayOfWeek?: number;
    /** Local time of day for the reminder. Must be between 00:00:00 and 23:59:59. */
    timeOfDay?: string;
    /** Whether the reminder is enabled. */
    enabled?: boolean;
    /** Optional addendum appended to the default reminder message. ≤ 200 characters. */
    defaultAddendum?: string | undefined;
    /** Number of hours after the check-in is sent before it expires.
Must be one of: 24, 48, 72, 120, 168.
Defaults to 72 (3 days) when not specified. */
    deadlineOffsetHours?: number;
}

/** Response for PUT /trainer/weekly-check-ins/overrides/{clientUserId}/{profession}. */
export interface PutOverrideResponse {
    /** Override identifier. */
    id?: string;
    /** The client's ApplicationUser.Id. */
    clientUserId?: string;
    /** Profession this override applies to. */
    profession?: string;
    /** Override day of week. Null = inherit. */
    dayOfWeek?: number | undefined;
    /** Override time of day. Null = inherit. */
    timeOfDay?: string | undefined;
    /** Override enabled flag. Null = inherit. */
    enabled?: boolean | undefined;
    /** Override addendum. Null = inherit. */
    addendum?: string | undefined;
    /** Override deadline offset in hours. Null = inherit. */
    deadlineOffsetHours?: number | undefined;
}

/** Request model for PUT /trainer/weekly-check-ins/overrides/{clientUserId}/{profession}. Route params identify the override; body provides the values (null = inherit from setting). */
export interface PutOverrideRequest {
    /** Override day of week (0 = Sunday … 6 = Saturday). Null = inherit. */
    dayOfWeek?: number | undefined;
    /** Override time of day. Null = inherit. Must be between 00:00:00 and 23:59:59 if set. */
    timeOfDay?: string | undefined;
    /** Override enabled flag. Null = inherit. */
    enabled?: boolean | undefined;
    /** Override addendum (≤ 200 chars). Null = inherit. */
    addendum?: string | undefined;
    /** Override deadline offset in hours. Null = inherit from the professional's setting.
When set, must be one of 24, 48, 72, 120, or 168. */
    deadlineOffsetHours?: number | undefined;
}

/** Response for POST /trainer/weekly-check-ins/{id}/mark-reviewed. */
export interface MarkCheckInReviewedResponse {
    /** Check-in identifier. */
    id?: string;
    /** When the trainer marked the check-in reviewed (UTC). */
    reviewedAt?: string;
}

/** Request for POST /trainer/weekly-check-ins/{id}/mark-reviewed. */
export interface MarkCheckInReviewedRequest {
}

/** Response for GET /trainer/weekly-check-ins. */
export interface GetTrainerCheckInsResponse {
    /** Check-ins for the requested week, filtered to the caller's clients. */
    checkIns?: TrainerCheckInDto[];
}

/** One check-in row as seen by the trainer. */
export interface TrainerCheckInDto {
    /** Check-in identifier. */
    id?: string;
    /** Client's ApplicationUser.Id. */
    clientUserId?: string;
    /** Client's ClientProfile.PublicId — the id every client-detail link in the app uses
(e.g. /clients/{clientPublicId}). Defaults to Guid.Empty if the client has no
ClientProfile row (should not happen in practice, but the join is null-safe). */
    clientPublicId?: string;
    /** Client's display name. */
    clientName?: string;
    /** Profession context. */
    profession?: string;
    /** ISO-week Monday. */
    weekStartDate?: string;
    /** Flags selected by the client. Empty until responded. */
    flags?: CheckInFlag[];
    /** Client's note. Null until responded. */
    note?: string | undefined;
    /** When the scheduler sent this check-in. */
    sentAt?: string;
    /** When the client responded. Null if not yet responded. */
    respondedAt?: string | undefined;
    /** When the client dismissed. Null if not dismissed. */
    dismissedByClientAt?: string | undefined;
    /** When the trainer marked this reviewed. Null if not reviewed. */
    reviewedByTrainerAt?: string | undefined;
    /** Lifecycle status of the check-in. */
    status?: string;
    /** UTC deadline by which the client must respond. Null for rows created before v1 deadline feature. */
    dueAt?: string | undefined;
}

/** Query parameters for GET /trainer/weekly-check-ins. */
export interface GetTrainerCheckInsRequest {
}

/** Response model for GET /trainer/weekly-check-ins/settings. */
export interface GetSettingsResponse {
    /** The trainer's current weekly check-in settings (0–2 items). */
    settings?: CheckInSettingDto[];
}

/** DTO for a single weekly check-in setting. */
export interface CheckInSettingDto {
    /** Setting identifier. */
    id?: string;
    /** Profession this setting applies to ("Training" or "Nutrition"). */
    profession?: string;
    /** Day of the week (0 = Sunday, 1 = Monday, …, 6 = Saturday). */
    dayOfWeek?: number;
    /** Time of day for the reminder in "HH:mm:ss" format. Between 00:00:00 and 23:59:59. */
    timeOfDay?: string;
    /** Whether the reminder is enabled. */
    enabled?: boolean;
    /** Optional addendum appended to the default reminder message. */
    defaultAddendum?: string | undefined;
    /** Hours after dispatch before the check-in expires. */
    deadlineOffsetHours?: number;
}

/** Response model for GET /trainer/weekly-check-ins/overrides. */
export interface GetOverridesResponse {
    /** All per-client overrides for the authenticated trainer. */
    overrides?: CheckInOverrideDto[];
}

/** DTO for a single per-client weekly check-in override. Null values mean "inherit from the professional's default setting". */
export interface CheckInOverrideDto {
    /** Override identifier. */
    id?: string;
    /** The client's ApplicationUser.Id. */
    clientUserId?: string;
    /** Client's first name. */
    clientFirstName?: string;
    /** Client's last name. */
    clientLastName?: string;
    /** Profession this override applies to ("Training" or "Nutrition"). */
    profession?: string;
    /** Override day of week. Null = inherit. */
    dayOfWeek?: number | undefined;
    /** Override time of day. Null = inherit. */
    timeOfDay?: string | undefined;
    /** Override enabled flag. Null = inherit. */
    enabled?: boolean | undefined;
    /** Override addendum. Null = inherit. */
    addendum?: string | undefined;
    /** Override deadline offset in hours. Null = inherit. */
    deadlineOffsetHours?: number | undefined;
}

/** Response for GET /client/weekly-check-ins/current. Returns 0–2 active (not responded, not dismissed) check-ins for the current ISO week. */
export interface GetCurrentClientCheckInsResponse {
    /** Active check-ins for the current ISO week. */
    checkIns?: CheckInSummary[];
}

/** Summary of a single active check-in. */
export interface CheckInSummary {
    /** Check-in identifier. */
    id?: string;
    /** Professional who sent the check-in. */
    professionalUserId?: string;
    /** Professional's display name. */
    professionalName?: string;
    /** Profession type (Training or Nutrition). */
    profession?: string;
    /** ISO-week Monday being planned. */
    weekStartDate?: string;
    /** When the scheduler sent this check-in. */
    sentAt?: string;
}

/** Response for GET /trainer/clients/{clientUserId}/weekly-check-ins/current. */
export interface GetClientCurrentCheckInResponse {
    /** Latest check-in(s) for the current ISO week and the given client.
Typically 0–2 items (one per profession). */
    checkIns?: ClientCheckInDto[];
}

/** A single check-in as seen from the plan-editor banner. */
export interface ClientCheckInDto {
    /** Check-in identifier. */
    id?: string;
    /** Profession context. */
    profession?: string;
    /** ISO-week Monday. */
    weekStartDate?: string;
    /** Flags selected by the client. */
    flags?: CheckInFlag[];
    /** Client's note. */
    note?: string | undefined;
    /** When the scheduler sent this. */
    sentAt?: string;
    /** When the client responded. Null if pending. */
    respondedAt?: string | undefined;
    /** When the client dismissed. Null if not dismissed. */
    dismissedByClientAt?: string | undefined;
    /** When the trainer marked this reviewed. */
    reviewedByTrainerAt?: string | undefined;
}

/** Request for GET /trainer/clients/{clientUserId}/weekly-check-ins/current. */
export interface GetClientCurrentCheckInRequest {
}

/** Response for GET /trainer/weekly-check-ins/{id}. */
export interface GetCheckInDetailResponse {
    /** Check-in identifier. */
    id?: string;
    /** Client's ApplicationUser.Id. */
    clientUserId?: string;
    /** Client's display name. */
    clientName?: string;
    /** Professional's ApplicationUser.Id. */
    professionalUserId?: string;
    /** Profession context. */
    profession?: string;
    /** ISO-week Monday being planned. */
    weekStartDate?: string;
    /** Flags selected by the client. */
    flags?: CheckInFlag[];
    /** Client's optional note. */
    note?: string | undefined;
    /** When the scheduler sent this check-in. */
    sentAt?: string;
    /** When the client responded. Null if not yet responded. */
    respondedAt?: string | undefined;
    /** When the client dismissed. Null if not dismissed. */
    dismissedByClientAt?: string | undefined;
    /** When the trainer marked this reviewed. Null if not reviewed. */
    reviewedByTrainerAt?: string | undefined;
    /** Lifecycle status of the check-in. */
    status?: string;
    /** UTC deadline by which the client must respond. Null for rows created before v1 deadline feature. */
    dueAt?: string | undefined;
}

/** Request for GET /trainer/weekly-check-ins/{id}. */
export interface GetCheckInDetailRequest {
}

/** Response for POST /client/weekly-check-ins/{id}/dismiss. */
export interface DismissCheckInResponse {
    /** Check-in identifier. */
    id?: string;
    /** When the check-in was dismissed (UTC). */
    dismissedAt?: string;
}

/** Request for POST /client/weekly-check-ins/{id}/dismiss. */
export interface DismissCheckInRequest {
}

/** Request model for DELETE /trainer/weekly-check-ins/overrides/{clientUserId}/{profession}. Route parameters identify the override to remove. */
export interface DeleteOverrideRequest {
}

/** Request model for updating the authenticated user's time zone. */
export interface UpdateTimeZoneRequest {
    /** IANA time zone identifier (e.g. "Europe/Prague", "America/New_York"). */
    timeZone: string;
}

/** Request model for updating the authenticated user's profile. */
export interface UpdateProfileRequest {
    /** Updated first name. */
    firstName: string;
    /** Updated last name. */
    lastName: string;
    /** Updated phone number (optional). */
    phoneNumber?: string | undefined;
}

/** Response model for the authenticated user's profile. */
export interface GetProfileResponse {
    /** User's public ID. */
    userId?: string;
    /** User's email address. */
    email?: string;
    /** User's first name. */
    firstName?: string;
    /** User's last name. */
    lastName?: string;
    /** User's phone number. */
    phoneNumber?: string | undefined;
    /** User's assigned roles. */
    roles?: string[];
    /** Date and time when the account was created. */
    dateCreated?: string;
    /** Whether the client has completed onboarding. Null for non-client users. */
    isOnboardingComplete?: boolean | undefined;
    /** Whether the user's email address has been verified. */
    emailConfirmed?: boolean;
    /** Whether the client has an active link to a professional. Null/false for non-client users. */
    hasActiveLink?: boolean;
    /** Whether the client has a pending (not yet submitted) questionnaire. Null/false for non-client users. */
    hasPendingQuestionnaire?: boolean;
    /** Roles of professionals the client is actively linked with (e.g. ["Trainer"], ["Nutritionist"], or both).
Empty for non-client users. */
    linkedRoles?: string[];
    /** User's IANA time zone identifier (e.g. "Europe/Prague"). */
    timeZone?: string;
    /** Permanent blob URL of the user's avatar, or null if no avatar has been uploaded. */
    avatarBlobUrl?: string | undefined;
}

/** Request model for confirming the uploaded avatar blob URL. */
export interface ConfirmAvatarRequest {
    /** The permanent blob URL returned by POST /users/me/avatar/upload-url. */
    blobUrl: string;
}

/** Response model containing the pre-signed upload URL and the permanent blob URL. */
export interface GenerateAvatarUploadUrlResponse {
    /** Time-limited pre-signed URL the client should PUT the image file to. */
    uploadUrl?: string;
    /** Permanent blob URL at which the avatar will be accessible after a successful upload.
Always starts with avatars/. */
    blobUrl?: string;
}

/** Request model for generating a pre-signed avatar upload URL. */
export interface GenerateAvatarUploadUrlRequest {
    /** MIME type of the image file (e.g. "image/jpeg", "image/png", "image/webp", "image/heic", "image/heif") */
    contentType: string;
    /** Declared file size in bytes. Must not exceed 5 MiB. */
    sizeBytes?: number;
}

/** Response returned after successfully adding a role, containing fresh tokens with updated claims. */
export interface AddRoleResponse {
    /** Fresh JWT access token containing the updated roles. */
    accessToken?: string;
    /** New refresh token. */
    refreshToken?: string;
    /** Expiration time of the new access token. */
    expiresAt?: string;
    /** The role that was added. */
    addedRole?: string;
}

/** Request to add a professional role to the current user. */
export interface AddRoleRequest {
    /** The role to add (Trainer or Nutritionist). */
    role: string;
}

/** Full training-plan-template detail including all weeks, days, sessions, workouts, and exercises. Used by the detail GET and by PUT's response. */
export interface TrainingPlanTemplateDetailDto {
    /** Template's public identifier. */
    templateId?: string;
    /** Display name. */
    name?: string;
    /** Optional free-text description. */
    description?: string | undefined;
    /** Primary fitness goal this template targets. */
    goal?: PrimaryGoal | undefined;
    /** Difficulty level this template targets. */
    difficulty?: ExerciseDifficulty | undefined;
    /** All weeks in the template with their days, sessions, workouts, and exercises. */
    weeks?: TrainingTemplateWeek[];
    /** Number of weeks, server-computed from Weeks. */
    weekCount?: number;
    /** Who can read this entry besides its owner. */
    visibility?: LibraryVisibility;
    /** True when the authenticated caller is the trainer who owns this template. */
    isOwnedByCurrentUser?: boolean;
    /** Optimistic concurrency version. */
    version?: number;
    /** When the template was created. */
    dateCreated?: string;
    /** When the template was last updated. */
    dateUpdated?: string | undefined;
}

/** Primary fitness or health goal selected during onboarding. */
export enum PrimaryGoal {
    LoseFat = "LoseFat",
    GainMuscle = "GainMuscle",
    Recomposition = "Recomposition",
    Fitness = "Fitness",
    Health = "Health",
}

/** Difficulty level of an exercise. */
export enum ExerciseDifficulty {
    Beginner = "Beginner",
    Intermediate = "Intermediate",
    Advanced = "Advanced",
}

/** A single week within a TrainingPlanTemplate. Slim compared to TrainingWeek — no Status or DatePublished, both meaningless outside a client plan. Everything below the week — TrainingDay, TrainingSession, TrainingWorkout, SessionExercise, ExerciseSet — is reused unchanged from the client-plan shape, so copying either direction (template ↔ plan) is a straight clone of the same types. */
export interface TrainingTemplateWeek {
    /** Week number within the template (1-based). */
    weekNumber?: number;
    /** Days in this week. Always 7 entries (Monday through Sunday) — see
TrainingDay. */
    days?: TrainingDay[];
}

/** A single day within a training week (1 = Monday … 7 = Sunday). Every TrainingWeek materialises all 7 days, always — a rest day is a day with no sessions, mirroring PlanDay on the nutrition side. */
export interface TrainingDay {
    /** Day of the week (1 = Monday, 7 = Sunday). */
    dayOfWeek?: number;
    /** Training sessions scheduled for this day, ordered by Order. */
    sessions?: TrainingSession[];
    /** Optional coach note for this day. */
    note?: string | undefined;
}

/** A single training session within a TrainingDay (e.g. "Push Day", "Leg Day"). The parent day owns the day-of-week; a session no longer carries its own — see DayOfWeek. */
export interface TrainingSession {
    /** Unique identifier for this session within the plan. */
    sessionId?: string;
    /** Display name (e.g. "Push Day", "Upper Body"). */
    name?: string;
    /** Display order within the day (1-based). */
    order?: number;
    /** Optional coach notes for this session. */
    notes?: string | undefined;
    /** Session-level workout format. Kept nullable for one release as an inheritable default —
workouts inherit when their own Format is null. Null means Standard. */
    format?: WorkoutFormat | undefined;
    /** Session-level format configuration. Null when Format is null or Standard. */
    formatConfig?: WodConfig | undefined;
    /** Workouts in this session. Each workout contains its own exercises. Every document is
created directly in this shape — there is no production data predating the workouts
model, so no migration or read-time backfill from a legacy flat exercises field
exists or is needed. */
    workouts?: TrainingWorkout[];
    /** Standalone exercises directly on this session — not grouped under any
TrainingWorkout (e.g. a single finisher movement that doesn't warrant its
own workout block). Sits alongside Workouts, mirroring how
Foods and Recipes sit side by side (#857
phase 3a). Shares one ordering sequence with Workouts — a duplicate
Order/Order across the two
lists is rejected by UpdateTrainingPlanValidator. */
    standaloneExercises?: SessionExercise[];
    /** Flat view of every exercise in this session — the standalone StandaloneExercises
plus every workout's nested exercises. Computed, never persisted (BsonIgnoreAttribute).
Read-only on the wire as allExercises — a client MUST NOT round-trip this field back on
write; UpdateSessionRequest has no member for it, so it is structurally ignored, not
rejected, if present in a PUT body (#874). */
    allExercises?: SessionExercise[];
}

/** An ordered workout within a training session (e.g. "Warm-up", "Hlavní", "Cool-down") — a block of exercises. Embedded sub-document inside Workouts. */
export interface TrainingWorkout {
    /** Client-side stable identifier for this workout. */
    workoutId?: string;
    /** Display order within the session (0-based). Shares one ordering sequence with the
session's standalone StandaloneExercises. */
    order?: number;
    /** Display name of the workout (e.g. "Hlavní", "Warm-up"). */
    name?: string;
    /** Workout format for this workout. Null means it inherits the session-level format. */
    format?: WorkoutFormat | undefined;
    /** Format configuration for this workout. Null when Format is null or Standard. */
    formatConfig?: WodConfig | undefined;
    /** Optional coach note for this workout. */
    notes?: string | undefined;
    /** Exercises in this workout. */
    exercises?: SessionExercise[];
}

/** Controls who can read a sharing-library entry (meal, session, nutrition-plan, or training-plan template) besides its owner. Every consuming document stores this as a string via [BsonRepresentation(BsonType.String)] on the property, so a seeded or legacy value keeps deserializing correctly regardless of this enum's numeric order. */
export enum LibraryVisibility {
    Private = "Private",
    Public = "Public",
}

/** Request for a full-state update of a training plan template: replaces name, description, goal/difficulty, and the week tree. */
export interface UpdateTemplateRequest {
    /** Updated display name. */
    name: string;
    /** Updated free-text description. */
    description?: string | undefined;
    /** Updated primary fitness goal. */
    goal?: PrimaryGoal | undefined;
    /** Updated difficulty level. */
    difficulty?: ExerciseDifficulty | undefined;
    /** Full week structure to persist. Replaces all existing weeks, days, sessions, workouts,
and exercises. */
    weeks: TemplateWeekRequest[];
    /** Expected version for optimistic concurrency control. */
    version?: number;
}

/** Represents a single week submitted when creating or fully replacing a training plan template. */
export interface TemplateWeekRequest {
    /** Week number within the template (1-based). */
    weekNumber?: number;
    /** Days in this week. */
    days?: TemplateDayRequest[];
}

/** Represents a single day within a template week. */
export interface TemplateDayRequest {
    /** Day of week (1 = Monday … 7 = Sunday). */
    dayOfWeek?: number;
    /** Optional coach note for this day. */
    note?: string | undefined;
    /** Training sessions scheduled for this day. */
    sessions?: TemplateSessionRequest[];
}

/** Represents a training session submitted in a template create/update request. */
export interface TemplateSessionRequest {
    /** Optional existing session identifier. New GUID generated if null or empty. */
    sessionId?: string | undefined;
    /** Display name (e.g. "Push Day"). */
    name?: string;
    /** Display order within the day (1-based). */
    order?: number;
    /** Optional coach notes. */
    notes?: string | undefined;
    /** Session-level workout format. Null means no format override at session level.
Workouts inherit this when their own Format is null. */
    format?: WorkoutFormat | undefined;
    /** Session-level format configuration. Null when Format is null or Standard. */
    formatConfig?: WodConfig | undefined;
    /** Ordered workouts in this session. Each workout contains its own exercises. */
    workouts?: TemplateWorkoutRequest[];
    /** Standalone exercises directly on this session — not grouped under any workout.
Shares one ordering sequence with Workouts. */
    standaloneExercises?: TemplateSessionExerciseRequest[];
}

/** Represents a training workout submitted in a template session request. */
export interface TemplateWorkoutRequest {
    /** Optional existing workout identifier. New GUID generated if null or empty. */
    workoutId?: string | undefined;
    /** Display order within the session (0-based). */
    order?: number;
    /** Display name of the workout (e.g. "Hlavní", "Warm-up"). */
    name?: string;
    /** Workout format for this workout. Null means workout inherits the session-level format. */
    format?: WorkoutFormat | undefined;
    /** Format configuration. Required for non-Standard, non-null workout formats; must be null
for Standard. */
    formatConfig?: WodConfig | undefined;
    /** Optional coach note for this workout. */
    notes?: string | undefined;
    /** Exercises in this workout. */
    exercises?: TemplateSessionExerciseRequest[];
}

/** Represents an exercise entry in a template workout or a session's standalone exercise list. */
export interface TemplateSessionExerciseRequest {
    /** Optional existing instance identifier for this exercise entry. New GUID generated if
null or empty. */
    exerciseId?: string | undefined;
    /** External (public) identifier of the exercise. */
    exerciseExternalId?: string;
    /** Display name of the exercise (snapshot at time of planning). */
    exerciseName?: string;
    /** Display order within the workout (1-based). */
    order?: number;
    /** Optional coach notes for this exercise. */
    notes?: string | undefined;
    /** Rest time between sets in seconds. */
    restSeconds?: number | undefined;
    /** How performance for this exercise is measured. Defaults to Reps. */
    movementType?: MovementType;
    /** Per-exercise format override. Null means the exercise inherits the workout's format. */
    format?: WorkoutFormat | undefined;
    /** Per-exercise format configuration. Null when Format is null or Standard. */
    formatConfig?: WodConfig | undefined;
    /** Planned sets for this exercise. */
    sets?: TemplateExerciseSetRequest[];
}

/** Represents a single set in a template exercise request. */
export interface TemplateExerciseSetRequest {
    /** Set number within the exercise (1-based). */
    setNumber?: number;
    /** Type of set. */
    type?: SetType;
    /** Target number of repetitions. */
    reps?: number | undefined;
    /** Target weight in kilograms. */
    weightKg?: number | undefined;
    /** Target duration in seconds. */
    durationSeconds?: number | undefined;
    /** Target RPE (1-10). */
    rpe?: number | undefined;
    /** Target distance in meters. */
    distanceMeters?: number | undefined;
    /** Rest time after this set in seconds. */
    restSeconds?: number | undefined;
}

/** Paginated response containing a list of training plan template summaries. */
export interface SearchTemplatesResponse {
    /** List of template summaries for the current page. */
    templates?: TrainingPlanTemplateSummaryDto[];
    /** Total number of templates matching the filter. */
    totalCount?: number;
    /** Current page number. */
    page?: number;
    /** Number of items per page. */
    pageSize?: number;
}

/** Lightweight training-plan-template summary — used for search results and as the response of endpoints that create or clone a template without needing the full week tree back. */
export interface TrainingPlanTemplateSummaryDto {
    /** Template's public identifier. */
    templateId?: string;
    /** Display name. */
    name?: string;
    /** Optional free-text description. */
    description?: string | undefined;
    /** Primary fitness goal this template targets. */
    goal?: PrimaryGoal | undefined;
    /** Difficulty level this template targets. */
    difficulty?: ExerciseDifficulty | undefined;
    /** Number of weeks, server-computed from the template's week tree. */
    weekCount?: number;
    /** Who can read this entry besides its owner. */
    visibility?: LibraryVisibility;
    /** True when the authenticated caller is the trainer who owns this template. */
    isOwnedByCurrentUser?: boolean;
    /** Optimistic concurrency version. */
    version?: number;
    /** When the template was created. */
    dateCreated?: string;
    /** When the template was last updated. */
    dateUpdated?: string | undefined;
}

/** Request to search training plan templates with optional filters and pagination. */
export interface SearchTemplatesRequest {
}

/** Response describing the new client plan created by instantiating a template. */
export interface InstantiateTemplateResponse {
    /** The newly created plan's public identifier. */
    planId?: string;
    /** The client's ClientProfile.PublicId — echoes the caller-supplied value. */
    clientId?: string;
    /** Display name of the new plan. */
    name?: string;
    /** Status of the new plan — always Draft immediately after instantiation. */
    status?: string;
    /** When the new plan was created. */
    dateCreated?: string;
}

/** Request to instantiate a training plan template into a new Draft client plan. */
export interface InstantiateTemplateRequest {
    /** The target client's ClientProfile.PublicId — the trainer-facing identifier, not
the internal ApplicationUser.Id storage key. */
    clientId: string;
    /** Display name for the new plan. */
    name: string;
    /** Optional start date. Must be a Monday and not in the past. */
    startDate?: string | undefined;
}

/** Request to fetch a single training plan template's full detail. */
export interface GetTemplateRequest {
}

/** Request to save an existing training plan as a new template. */
export interface CreateTemplateFromPlanRequest {
    /** The source plan's public identifier (TrainingPlan.ExternalId — NOT the client's
ClientProfile.PublicId). */
    planId: string;
    /** Display name for the new template. */
    name: string;
    /** Optional free-text description for the new template. */
    description?: string | undefined;
    /** Who can read the new template besides the caller. Defaults to Private. */
    visibility?: LibraryVisibility;
}

/** Request to create a new training plan template — either empty (materialized from WeekCount) or with a full week tree supplied directly. */
export interface CreateTemplateRequest {
    /** Display name of the template. */
    name: string;
    /** Optional free-text description. */
    description?: string | undefined;
    /** Optional primary fitness goal this template targets. */
    goal?: PrimaryGoal | undefined;
    /** Optional difficulty level this template targets. */
    difficulty?: ExerciseDifficulty | undefined;
    /** A materialisation instruction for the empty-weeks path: creates this many weeks, each
with all 7 days and no sessions. Mutually exclusive with Weeks. Never
persisted as supplied — WeekCount is
always server-computed from the resulting week tree. */
    weekCount?: number | undefined;
    /** A full week tree to persist directly. Mutually exclusive with WeekCount. */
    weeks?: TemplateWeekRequest[] | undefined;
    /** Who can read this entry besides the caller. Defaults to Private. */
    visibility?: LibraryVisibility;
}

/** Detailed training plan response including all weeks, sessions, exercises, and sets. */
export interface GetTrainingPlanResponse {
    /** Plan's public identifier. */
    planId?: string;
    /** The client's ClientProfile.PublicId — the client-facing identifier consumed by
web/mobile to build routes like /trainer/clients/{{clientId}}/.... NOT the
internal Mongo storage key (ApplicationUser.Id since #840) — see
FromDocument. */
    clientId?: string;
    /** Trainer's public user identifier. */
    trainerId?: string;
    /** Display name of the plan. */
    name?: string;
    /** Optional plan description. */
    description?: string | undefined;
    /** Current plan status as string (Draft, Active, Archived). */
    status?: string;
    /** All weeks in the plan with their sessions, exercises, and sets. */
    weeks?: TrainingWeek[];
    /** Optimistic concurrency version. */
    version?: number;
    /** When the plan was created. */
    dateCreated?: string;
    /** When the plan was last updated. */
    dateUpdated?: string | undefined;
    /** The Monday when Week 1 begins, if set. */
    startDate?: string | undefined;
    /** When this plan was marked as completed, if applicable. */
    dateCompleted?: string | undefined;
    /** Linked questionnaire response (cross-DB reference to PostgreSQL QuestionnaireResponse.PublicId).
Null if no questionnaire is linked to this plan. */
    questionnaireResponseId?: string | undefined;
    /** All completion records for the plan's client, sorted by (Date asc, SessionId asc).
Populated by the endpoint after loading the plan; the client side should filter
to dates that fall within the plan's active weeks. */
    completions?: TrainingPlanCompletionDto[];
    /** Per-session workout-log execution data for the plan's client.
One entry per session that has at least one WorkoutLog record.
Sessions with no log entry are absent (equivalent to all sets being not-yet-reached).
The web layer uses this together with Completions to render per-set,
per-exercise, and per-session completed/skipped/unreached state indicators. */
    sessionExecutions?: SessionExecutionDto[];
    /** Per-session edit-lock state for all sessions in the plan.
Only sessions with an active (non-expired) lock appear here; a session absent from
this list is implicitly Stable.
The web editor uses this on initial load to show the Live in-progress badge and gate
the unlock affordance — SignalR events update this state while the page is open. */
    sessionLockStates?: SessionLockStateDto[];
}

/** A single week within a training plan. */
export interface TrainingWeek {
    /** Week number within the plan (1-based). */
    weekNumber?: number;
    /** Publish status of this week (Draft or Published). */
    status?: WeekStatus;
    /** When this week was published (status changed to Published). */
    datePublished?: string | undefined;
    /** Days in this week. Always 7 entries (Monday through Sunday) — see
TrainingDay. */
    days?: TrainingDay[];
}

/** Publish status of a single week within a nutrition plan. */
export enum WeekStatus {
    Draft = "Draft",
    Published = "Published",
}

/** Per-(date, sessionId) completion record for the plan's client. One entry per (clientId, date, sessionId) tuple. Surfaces which exercises have already been marked complete so the trainer editor can lock the corresponding fields. */
export interface TrainingPlanCompletionDto {
    date?: string;
    sessionId?: string;
    /** Flat list of completed exercise external IDs for this session on this date.
Deprecated. Use CompletedExerciseIdsByWorkout for workout-aware tracking.
Retained for backward compatibility. */
    completedExerciseIds?: string[];
    /** Workout-aware completed exercise IDs. Key = WorkoutId, value = list of completed
ExerciseExternalIds within that workout. Populated via read-time backfill so legacy
completion documents are transparently migrated. */
    completedExerciseIdsByWorkout?: { [key: string]: string[]; };
    completedWorkoutIds?: string[];
    version?: number;
}

/** Per-set execution data returned by the trainer endpoint. Lets the web layer derive completed / skipped / not-yet-reached states without storing those as flags on the document. */
export interface SessionExecutionDto {
    /** The SessionId this execution belongs to. */
    sessionId?: string;
    /** Whether the workout was finalised by the client (IsCompleted was true). */
    isSessionFinished?: boolean;
    /** Per-exercise map of which set numbers were completed (i.e. had a non-null
CompletedAt). Key = ExerciseExternalId; value = sorted list
of 1-based set numbers that were stamped as complete in the WorkoutLog.
An absent key means no sets for that exercise were logged.
An empty list should not occur but is treated identically to an absent key.
Deprecated in favour of CompletedSetsByWorkoutAndExercise.
Retained for backward compatibility. When a multi-workout log has the same exercise
in two workouts, only the last-encountered workout's data appears here — use the
workout-aware map for reliable results. */
    completedSetsByExercise?: { [key: string]: number[]; };
    /** Workout-aware completed sets map. Key = (WorkoutId, ExerciseExternalId) encoded as
the string "{workoutId}:{exerciseId}"; value = sorted list of completed set numbers.
Use this in preference to CompletedSetsByExercise when workout context
is available (i.e. when the plan has multi-workout sessions). */
    completedSetsByWorkoutAndExercise?: { [key: string]: number[]; };
    /** Per-exercise map of per-set actual values, snapshot-planned values, and isModified flags.
Key = ExerciseExternalId; value = list of LoggedSetDto (one per logged set).
An absent key means no sets for that exercise were logged.
The web layer uses this together with CompletedSetsByExercise to render
the actual-vs-planned comparison and the upraveno (modified) indicator per set.
Deprecated in favour of LoggedSetsByWorkoutAndExercise.
Retained for backward compatibility. When a multi-workout log has the same exercise
in two workouts, only the last-encountered workout's data appears here. */
    loggedSetsByExercise?: { [key: string]: LoggedSetDto[]; };
    /** Workout-aware logged sets map. Key = (WorkoutId, ExerciseExternalId) encoded as
the string "{workoutId}:{exerciseId}"; value = list of LoggedSetDto.
Use this in preference to LoggedSetsByExercise for multi-workout sessions. */
    loggedSetsByWorkoutAndExercise?: { [key: string]: LoggedSetDto[]; };
    /** True when at least one set in any exercise under this session has IsModified == true.
The web layer uses this to show the upraveno badge at the session-header level.
Always false when the session has no WorkoutLog (or all logs are legacy without snapshots). */
    hasModifications?: boolean;
    /** Per-workout finished state for all workouts in this session.
Populated by the endpoint from both WorkoutLog and TrainingCompletion signals.
A workout is finished when IsSessionFinished is true (session-level completion
implies every workout is done), OR when the TrainingCompletion document records that specific
workout as complete.
Empty for sessions with no completion data.
The web layer uses this to render the finished label and disable editing on completed workouts
independently of the session-level finished state. */
    finishedWorkouts?: WorkoutFinishedStateDto[];
}

/** Per-set actual values + snapshot-planned values + backend-computed isModified flag, sourced from a WorkoutSet. Used by training-read endpoints that need to surface actual-vs-planned comparison (GetTodaySession, GetFullTrainingPlan, and the trainer GetTrainingPlan). */
export interface LoggedSetDto {
    /** 1-based set number within the exercise. */
    setNumber?: number;
    /** Actual repetitions logged. Null when the set has not been performed. */
    actualReps?: number | undefined;
    /** Actual weight (kg) logged. Null when not performed. */
    actualWeightKg?: number | undefined;
    /** Actual RPE logged. Null when not performed. */
    actualRpe?: number | undefined;
    /** Actual duration (seconds) logged. Null when not performed. */
    actualDurationSeconds?: number | undefined;
    /** Actual distance (meters) logged. Null when not performed. */
    actualDistanceMeters?: number | undefined;
    /** Snapshot-planned repetitions at log time. Null for legacy logs. */
    plannedReps?: number | undefined;
    /** Snapshot-planned weight (kg) at log time. Null for legacy logs. */
    plannedWeightKg?: number | undefined;
    /** Snapshot-planned RPE at log time. Null for legacy logs. */
    plannedRpe?: number | undefined;
    /** Snapshot-planned duration (seconds) at log time. Null for legacy logs. */
    plannedDurationSeconds?: number | undefined;
    /** Snapshot-planned distance (meters) at log time. Null for legacy logs. */
    plannedDistanceMeters?: number | undefined;
    /** Backend-computed flag: true when any actual field differs from its snapshot-planned counterpart.
Always false for legacy sets (no snapshot → treated as planned == actual). */
    isModified?: boolean;
}

/** Per-workout finished state for a training session. A workout is "finished" when the client has completed it via either the WorkoutLog path (session-level completion) or the TrainingCompletion path (home-checkbox / workout-complete). */
export interface WorkoutFinishedStateDto {
    /** The WorkoutId this finished state belongs to. */
    workoutId?: string;
    /** Whether this workout is finished.
True when a completed WorkoutLog exists for the session (all workouts done),
or when the TrainingCompletion document shows this workout as complete. */
    isFinished?: boolean;
}

/** Per-session edit-lock state projected into the trainer read model. A session with no active lock document reports Stable with a null holder. */
export interface SessionLockStateDto {
    /** The SessionId this lock state belongs to. */
    sessionId?: string;
    /** Current lock state of this session.
Possible values: "Stable" (no active lock), "Editing" (trainer holds an editing lock),
"Live" (client has an in-progress workout lock).
Populated via a batch GetStateAsync call on the session lock service. */
    lockState?: string;
    /** Who currently holds the lock, if any.
Possible values: "Coach", "Client", or null when the session is Stable. */
    lockHolder?: string | undefined;
}

/** Request for a full-state update of a training plan. */
export interface UpdateTrainingPlanRequest {
    /** Updated display name. */
    name: string;
    /** Updated plan description. */
    description?: string | undefined;
    /** Expected version for optimistic concurrency control. */
    version?: number;
    /** Full week structure to persist. */
    weeks: UpdateTrainingWeekRequest[];
    /** Updated start date. Must be a Monday and not in the past. */
    startDate?: string | undefined;
    /** Optional primary fitness goal for this plan period.
When set, read sites prefer this value over the client's onboarding baseline. */
    goal?: PrimaryGoal | undefined;
    /** Optional target body weight in kilograms for this plan period.
Must be greater than zero when provided. */
    targetWeightKg?: number | undefined;
}

/** Represents a single week submitted in a full-state training plan update. */
export interface UpdateTrainingWeekRequest {
    /** Week number within the plan (1-based). */
    weekNumber?: number;
    /** Sessions in this week. */
    sessions?: UpdateSessionRequest[];
    /** Optional day-level notes keyed by day of week (1 = Monday … 7 = Sunday). */
    dayNotes?: { [key: string]: string; } | undefined;
}

/** Represents a training session submitted in a full-state plan update. */
export interface UpdateSessionRequest {
    /** Optional existing session identifier. New GUID generated if null. */
    sessionId?: string | undefined;
    /** Day of week (1 = Monday … 7 = Sunday). */
    dayOfWeek?: number;
    /** Display name (e.g. "Push Day"). */
    name?: string;
    /** Display order within the day (1-based). */
    order?: number;
    /** Optional coach notes. */
    notes?: string | undefined;
    /** Session-level workout format. Null means no format override at session level.
Workouts inherit this when their own Format is null. */
    format?: WorkoutFormat | undefined;
    /** Session-level format configuration. Null when Format is null or Standard. */
    formatConfig?: WodConfig | undefined;
    /** Ordered workouts in this session. Each workout contains its own exercises. */
    workouts?: UpdateTrainingWorkoutRequest[];
    /** Standalone exercises directly on this session — not grouped under any workout.
Shares one ordering sequence with Workouts: a duplicate Order/
Order across the two lists is rejected (#857 phase 3a). */
    standaloneExercises?: UpdateSessionExerciseRequest[];
}

/** Represents a training workout submitted in a full-state session update. */
export interface UpdateTrainingWorkoutRequest {
    /** Optional existing workout identifier. New GUID generated if null. */
    workoutId?: string | undefined;
    /** Display order within the session (0-based). */
    order?: number;
    /** Display name of the workout (e.g. "Hlavní", "Warm-up"). */
    name?: string;
    /** Workout format for this workout. Null means workout inherits the session-level format. */
    format?: WorkoutFormat | undefined;
    /** Format configuration. Required for non-Standard, non-null workout formats; must be null for Standard. */
    formatConfig?: WodConfig | undefined;
    /** Optional coach note for this workout. */
    notes?: string | undefined;
    /** Exercises in this workout. */
    exercises?: UpdateSessionExerciseRequest[];
}

/** Represents an exercise entry in a training workout update. */
export interface UpdateSessionExerciseRequest {
    /** Optional existing instance identifier for this exercise entry. New GUID generated if null.
Distinguishes two occurrences of the same catalog exercise within one session (#857 phase 3a). */
    exerciseId?: string | undefined;
    /** External (public) identifier of the exercise. */
    exerciseExternalId?: string;
    /** Display name of the exercise (snapshot at time of planning). */
    exerciseName?: string;
    /** Display order within the workout (1-based). */
    order?: number;
    /** Optional coach notes for this exercise. */
    notes?: string | undefined;
    /** Rest time between sets in seconds. */
    restSeconds?: number | undefined;
    /** How performance for this exercise is measured. Defaults to Reps. */
    movementType?: MovementType;
    /** Per-exercise format override. Null means the exercise inherits the workout's format. */
    format?: WorkoutFormat | undefined;
    /** Per-exercise format configuration. Null when Format is null or Standard. */
    formatConfig?: WodConfig | undefined;
    /** Planned sets for this exercise. */
    sets?: UpdateExerciseSetRequest[];
}

/** Represents a single set in an exercise update. */
export interface UpdateExerciseSetRequest {
    /** Set number within the exercise (1-based). */
    setNumber?: number;
    /** Type of set. */
    type?: SetType;
    /** Target number of repetitions. */
    reps?: number | undefined;
    /** Target weight in kilograms. */
    weightKg?: number | undefined;
    /** Target duration in seconds. */
    durationSeconds?: number | undefined;
    /** Target RPE (1-10). */
    rpe?: number | undefined;
    /** Target distance in meters. */
    distanceMeters?: number | undefined;
    /** Rest time after this set in seconds. */
    restSeconds?: number | undefined;
}

/** Request to acquire an Editing lock on a published training session. */
export interface UnlockTrainingSessionRequest {
}

/** Request to release the Editing lock on a training session (relock it to Stable). */
export interface RelockTrainingSessionRequest {
}

/** Request to publish a specific week of a training plan. */
export interface PublishTrainingWeekRequest {
    /** Optimistic concurrency version. */
    version?: number;
}

/** Request to link or unlink a questionnaire response to/from a training plan. */
export interface LinkQuestionnaireRequest {
    /** Questionnaire response PublicId to link. Set to null to unlink. */
    questionnaireResponseId?: string | undefined;
    /** Optimistic concurrency version. */
    version?: number;
}

/** Paginated response containing a list of training plan summaries. */
export interface GetTrainingPlansResponse {
    /** List of plan summaries for the current page. */
    plans?: TrainingPlanSummaryDto[];
    /** Total number of plans matching the filter. */
    totalCount?: number;
    /** Current page number. */
    page?: number;
    /** Number of items per page. */
    pageSize?: number;
}

/** Lightweight training plan summary for list views. */
export interface TrainingPlanSummaryDto {
    /** Plan's public identifier. */
    planId?: string;
    /** Display name. */
    name?: string;
    /** Optional plan description. */
    description?: string | undefined;
    /** The client's ClientProfile.PublicId — the client-facing identifier consumed by
web/mobile to build routes like /trainer/clients/{clientId}/.... NOT the
internal Mongo storage key (ApplicationUser.Id since #840) — see
FromDocument. */
    clientId?: string;
    /** Current status as string (Draft, Active, Archived). */
    status?: string;
    /** Number of weeks. */
    weekCount?: number;
    /** Optimistic concurrency version. */
    version?: number;
    /** When the plan was created. */
    dateCreated?: string;
    /** When the plan was last updated. */
    dateUpdated?: string | undefined;
    /** The Monday when Week 1 begins, if set. */
    startDate?: string | undefined;
    /** When this plan was marked as completed, if applicable. */
    dateCompleted?: string | undefined;
    /** Linked questionnaire response ID, if any. */
    questionnaireResponseId?: string | undefined;
}

/** Request to list training plans with optional filters and pagination. */
export interface GetTrainingPlansRequest {
}

/** Status of a training plan. */
export enum TrainingPlanStatus {
    Draft = "Draft",
    Active = "Active",
    Completed = "Completed",
    Archived = "Archived",
}

/** Request to retrieve a single training plan. */
export interface GetTrainingPlanRequest {
}

/** Response after a trainer successfully finishes a session. */
export interface FinishSessionResponse {
    /** The workout log ExternalId that was created or completed. */
    workoutLogId?: string;
    /** The plan ExternalId. */
    planId?: string;
    /** The session identifier within the plan. */
    sessionId?: string;
    /** The instant the session was marked as completed (the effective completedAt value used). */
    completedAt?: string;
}

/** Request for the trainer to retroactively finish a skipped or untouched session. */
export interface FinishSessionRequest {
    /** Optional backdated completion instant.
When omitted the server uses UtcNow.
Must be in the past (or present) and not before the plan's start date. */
    completedAt?: string | undefined;
}

/** Request to delete a training plan. */
export interface DeleteTrainingPlanRequest {
}

/** Request to create a new training plan for a client. */
export interface CreateTrainingPlanRequest {
    /** The client's public user identifier. */
    clientId: string;
    /** Display name of the plan. */
    name: string;
    /** Optional plan description. */
    description?: string | undefined;
    /** Number of weeks to initialize (default 1). */
    weekCount?: number;
    /** Optional start date for the plan. Must be a Monday and not in the past. */
    startDate?: string | undefined;
    /** Optional questionnaire response to link to this plan (cross-DB reference).
Must be a submitted response owned by this professional for the same client. */
    questionnaireResponseId?: string | undefined;
    /** Optional primary fitness goal for this plan period.
When set, read sites prefer this value over the client's onboarding baseline. */
    goal?: PrimaryGoal | undefined;
    /** Optional target body weight in kilograms for this plan period.
Must be greater than zero when provided. */
    targetWeightKg?: number | undefined;
}

/** Request to mark a training plan as completed. */
export interface CompleteTrainingPlanRequest {
    /** Optimistic concurrency version. */
    version?: number;
}

/** Request model for updating the professional's profile. */
export interface UpdateProfessionalProfileRequest {
    /** Short biography of the professional (max 1000 characters). */
    bio?: string | undefined;
    /** Legacy single specialization field (max 100 characters). */
    specialization?: string | undefined;
    /** City where the professional is based. */
    city?: string | undefined;
    /** Estimated price per month (free-text). */
    estimatedPrice?: string | undefined;
    /** Specializations as a JSON array string. */
    specializations?: string | undefined;
    /** Certificates and education as a JSON array string. */
    certificates?: string | undefined;
    /** Spoken languages as a JSON array string. */
    languages?: string | undefined;
    /** Collaboration type: "both", "online", or "inperson". */
    collaborationType?: string | undefined;
    /** Maximum number of clients. */
    maxClients?: number | undefined;
    /** LinkedIn profile URL or handle. */
    linkedIn?: string | undefined;
    /** Instagram handle. */
    instagram?: string | undefined;
    /** Website URL. */
    website?: string | undefined;
    /** Whether the profile is visible in marketplace search. */
    showInSearch?: boolean;
    /** Whether the professional is currently accepting new clients. */
    acceptNewClients?: boolean;
}

/** Response after updating client data. */
export interface UpdateClientDataResponse {
    /** Confirmation message. */
    message?: string;
}

/** Request to update a client's profile and nutrition target data. Only non-null fields are updated. */
export interface UpdateClientDataRequest {
    /** Client's first name. */
    firstName?: string | undefined;
    /** Client's last name. */
    lastName?: string | undefined;
    /** Client's email address (also the login identifier). */
    email?: string | undefined;
    /** Weight in kg. */
    weightKg?: number | undefined;
    /** Height in cm. */
    heightCm?: number | undefined;
    /** Age in years (converted to DateOfBirth). */
    age?: number | undefined;
    /** Biological sex. */
    sex?: string | undefined;
    /** Activity level used for calculation. */
    derivedActivityLevel?: string | undefined;
    /** Nutrition goal used for calculation. */
    derivedNutritionGoal?: string | undefined;
    /** BMR in kcal/day. */
    bmr?: number | undefined;
    /** TDEE in kcal/day. */
    tdee?: number | undefined;
    /** Adjusted daily calories. */
    adjustedKcal?: number | undefined;
    /** Daily protein grams. */
    proteinGrams?: number | undefined;
    /** Daily carbs grams. */
    carbsGrams?: number | undefined;
    /** Daily fat grams. */
    fatGrams?: number | undefined;
    /** Meal distribution percentages as JSON. */
    mealDistribution?: string | undefined;
}

export interface ReplaceQuestionnaireRequest {
    questionnairePublicId?: string;
}

/** Response model containing a list of pending invitations. */
export interface GetPendingInvitesResponse {
    /** List of pending invitations. */
    invites?: PendingInviteDto[];
}

/** DTO representing a single pending invitation. */
export interface PendingInviteDto {
    /** Internal integer primary key of the PendingInvite row.
Used to populate pendingInviteId on the photo-diary-request create form. */
    id?: number;
    /** Public identifier of the pending invite. */
    publicId?: string;
    /** First name of the invited client. */
    firstName?: string;
    /** Last name of the invited client. */
    lastName?: string;
    /** Email address of the invited client. */
    email?: string;
    /** Optional introduction message. */
    message?: string | undefined;
    /** Timestamp when the invitation was sent. */
    sentAt?: string;
    /** Whether the invitation has been accepted. */
    isAccepted?: boolean;
    /** Public ID of the assigned questionnaire, if any. */
    questionnairePublicId?: string | undefined;
    /** Title of the assigned questionnaire, if any. */
    questionnaireTitle?: string | undefined;
}

/** Request model for deleting a pending invitation. */
export interface DeletePendingInviteRequest {
}

/** Response model returned after creating a pending invitation. */
export interface CreatePendingInviteResponse {
    /** Internal integer primary key of the PendingInvite row.
Used to populate pendingInviteId on the photo-diary-request create form. */
    id?: number;
    /** Public identifier of the created pending invite. */
    publicId?: string;
    /** First name of the invited client. */
    firstName?: string;
    /** Last name of the invited client. */
    lastName?: string;
    /** Email address of the invited client. */
    email?: string;
    /** Timestamp when the invitation was sent. */
    sentAt?: string;
    /** Public ID of the assigned questionnaire, if any. */
    questionnairePublicId?: string | undefined;
}

/** Request model for creating a pending client invitation. */
export interface CreatePendingInviteRequest {
    /** First name of the invited client. */
    firstName: string;
    /** Last name of the invited client. */
    lastName: string;
    /** Email address of the invited client. */
    email: string;
    /** Optional introduction message from the professional. */
    message?: string | undefined;
    /** Optional public ID of a questionnaire to assign to the client. */
    questionnairePublicId?: string | undefined;
}

/** Combined list of a client's nutrition and training plans returned by GET /trainer/clients/{clientId}/plans. */
export interface ListClientPlansResponse {
    /** All plans (nutrition + training) across all statuses, newest first. */
    plans?: ClientPlanItem[];
    /** Whether the caller's link permits viewing the client's nutrition plans.
Mirrors ClientProfessionalLink.CanViewNutritionPlans. Lets the portal
distinguish "no nutrition plans exist" from "you may not see them". */
    canViewNutritionPlans?: boolean;
    /** Whether the caller's link permits viewing the client's training plans.
Mirrors ClientProfessionalLink.CanViewTrainingPlans. Lets the portal
distinguish "no training plans exist" from "you may not see them". */
    canViewTrainingPlans?: boolean;
}

/** A single plan entry in the combined list. */
export interface ClientPlanItem {
    /** The plan's public ExternalId (Guid) as stored in MongoDB. */
    planId?: string;
    /** Discriminates the plan type: "Nutrition" or "Training". */
    planType?: string;
    /** Display name of the plan. */
    name?: string;
    /** The Monday when Week 1 begins (UTC). Null for plans where StartDate has not been set. */
    periodStart?: string | undefined;
    /** When the plan was marked completed (UTC). Null for active/draft plans and open-ended plans. */
    periodEnd?: string | undefined;
    /** Current plan status as a string: "Draft", "Active", "Completed", "Archived". */
    status?: string;
    /** Per-plan result summary. Null fields indicate the metric is not applicable
(e.g. compliance is null for training plans; totalTrainings is null for nutrition plans). */
    resultSummary?: ClientPlanResultSummary;
}

/** Per-plan result summary. Fields not applicable to the plan type are null. */
export interface ClientPlanResultSummary {
    /** Count of WorkoutLogs where PlanId == plan.ExternalId and IsCompleted == true.
Null for nutrition plans. */
    totalTrainings?: number | undefined;
    /** Count of PersonalRecords with AchievedAt in [plan.StartDate .. plan.DateCompleted ?? now].
Null for nutrition plans or plans without a StartDate. */
    prCount?: number | undefined;
    /** Nutrition compliance % (0-100) over the plan period via IComplianceService.
Null for training plans, or when the plan has no StartDate. */
    compliancePercent?: number | undefined;
    /** Body weight delta (kg) from the first BodyMeasurement on/after plan start
to the last BodyMeasurement on/before plan end (DateCompleted ?? now).
Positive = weight gained; negative = weight lost.
Null for training plans, or when fewer than two measurements exist in the window. */
    weightDeltaKg?: number | undefined;
}

export interface ListClientPlansRequest {
}

/** Response model returned after sending a client invitation. */
export interface InviteClientResponse {
    /** Confirmation message. */
    message?: string;
    /** Invitation token (exposed for dev/testing; would be emailed in production). */
    invitationToken?: string;
}

/** Request model for inviting a client via email. */
export interface InviteClientRequest {
    /** Email address of the client to invite. */
    email: string;
}

/** Response model for the professional's own profile data. */
export interface GetProfessionalProfileResponse {
    /** Short biography of the professional. */
    bio?: string | undefined;
    /** Legacy single specialization field. */
    specialization?: string | undefined;
    /** City where the professional is based. */
    city?: string | undefined;
    /** Estimated price per month. */
    estimatedPrice?: string | undefined;
    /** Specializations as a JSON array string. */
    specializations?: string | undefined;
    /** Certificates and education as a JSON array string. */
    certificates?: string | undefined;
    /** Spoken languages as a JSON array string. */
    languages?: string | undefined;
    /** Collaboration type. */
    collaborationType?: string | undefined;
    /** Maximum number of clients. */
    maxClients?: number | undefined;
    /** LinkedIn profile URL or handle. */
    linkedIn?: string | undefined;
    /** Instagram handle. */
    instagram?: string | undefined;
    /** Website URL. */
    website?: string | undefined;
    /** Whether the profile is visible in marketplace search. */
    showInSearch?: boolean;
    /** Whether the professional is currently accepting new clients. */
    acceptNewClients?: boolean;
}

/** Aggregated dashboard data for the trainer's client list. */
export interface GetDashboardSummaryResponse {
    /** Per-client stats. */
    clients?: ClientDashboardItem[];
}

/** Dashboard-level stats for a single client. */
export interface ClientDashboardItem {
    /** Client's profile public ID. */
    publicId?: string;
    /** Client's first name. */
    firstName?: string;
    /** Client's last name. */
    lastName?: string;
    /** Client's email address. */
    email?: string;
    /** Optional MinIO blob URL for the client's avatar (from the user record). Null if no avatar uploaded. */
    avatarBlobUrl?: string | undefined;
    /** Whether the trainer-client link is active. */
    isActive?: boolean;
    /** Client's fitness goal text (from ClientProfile.Goals). */
    goal?: string | undefined;
    /** 7-day compliance percentage (0–100). */
    compliancePercent?: number;
    /** Current streak of consecutive compliant days. */
    currentStreak?: number;
    /** Average daily kcal consumed over the last 7 days. */
    avgDailyKcal?: number;
    /** Kcal consumed today (from meal logs). */
    todayKcal?: number;
    /** Daily kcal target from the active nutrition plan (null if none). */
    kcalGoal?: number | undefined;
    /** Completed workouts this week (Mon–today). */
    workoutsCompleted?: number;
    /** Planned training sessions this week (from active plan's current week). */
    workoutsPlanned?: number;
    /** UTC timestamp of the client's most recent activity (meal log, workout, measurement). */
    lastActivityAt?: string | undefined;
    /** Number of nutrition plans that have started, are not completed, and have at least one published week. */
    activeNutritionPlansCount?: number;
    /** Whether the client has an active training plan. */
    hasActiveTrainingPlan?: boolean;
}

export interface GetClientVerdictResponse {
    /** Overall on-track verdict for the client. */
    verdict?: ClientVerdict;
    /** Nutrition plan compliance percent (0-100), or null when no active nutrition plan. */
    compliancePercent?: number | undefined;
    /** Delta between current weight and target weight in kg, or null when no measurements/target. */
    weightDeltaToGoal?: number | undefined;
    /** Direction the client's weight is moving relative to their target. */
    weightDirection?: WeightDirection;
    /** Number of workout sessions completed in the current ISO week, or null when no active training plan. */
    trainingFrequencyActual?: number | undefined;
    /** Number of sessions prescribed per week in the active training plan, or null when no active training plan. */
    trainingFrequencyPrescribed?: number | undefined;
    /** UTC timestamp of the most recent activity (workout log, meal log, or measurement), or null. */
    lastActiveAt?: string | undefined;
    /** Number of personal records achieved in the current calendar month. */
    prCountThisMonth?: number;
}

/** Represents the on-track verdict for a client as assessed by the trainer dashboard. */
export enum ClientVerdict {
    OnTrack = "OnTrack",
    NeedsAttention = "NeedsAttention",
    OffTrack = "OffTrack",
}

/** Indicates the direction a client's weight is moving relative to their target. */
export enum WeightDirection {
    Towards = "Towards",
    Away = "Away",
    Stable = "Stable",
}

export interface GetClientVerdictRequest {
}

/** Response returning a client's activity timeline, ordered newest first. */
export interface GetClientTimelineResponse {
    /** Timeline items, ordered by OccurredAt descending. */
    items?: ClientTimelineItem[];
    /** Whether the caller's link permits viewing the client's nutrition-domain timeline
entries. Mirrors ClientProfessionalLink.CanViewNutritionPlans. */
    canViewNutritionPlans?: boolean;
    /** Whether the caller's link permits viewing the client's training-domain timeline
entries. Mirrors ClientProfessionalLink.CanViewTrainingPlans. */
    canViewTrainingPlans?: boolean;
}

/** A single event in a client's activity timeline. */
export interface ClientTimelineItem {
    /** Stable unique identifier for this timeline entry. */
    id?: string;
    /** Event kind — used on the client to pick icon/colour.
One of: meal_day, workout, measurement, questionnaire,
nutrition_plan_published, training_plan_published, linked,
personal_record. */
    type?: string;
    /** When this event occurred (UTC). */
    occurredAt?: string;
    /** Short, human-readable title (e.g. "Splnil jídelníček"). */
    title?: string;
    /** Optional secondary text (e.g. "5 z 5 jídel zaznamenáno"). */
    description?: string | undefined;
    /** Optional emoji/icon hint for the client. */
    icon?: string | undefined;
    /** Structured payload for personal_record items.
Null for all other event types.
Exposed as typed fields so the web/mobile i18n layer can compose
locale-specific copy without depending on server-side strings. */
    personalRecord?: PersonalRecordPayload | undefined;
}

/** Structured payload embedded in a personal_record timeline item. */
export interface PersonalRecordPayload {
    /** Public-facing identifier of the personal record document. */
    externalId?: string;
    /** ExternalId of the exercise for which the PR was achieved. */
    exerciseExternalId?: string;
    /** Snapshot of the exercise name at the time the PR was set. */
    exerciseName?: string;
    /** Weight lifted in kilograms. */
    weightKg?: number;
    /** Repetitions completed in the PR set. */
    reps?: number;
    /** ExternalId of the workout log that contains this PR set. */
    workoutLogId?: string;
}

/** Request model for retrieving a client's activity timeline. */
export interface GetClientTimelineRequest {
}

/** Response model for the trainer's client list. */
export interface GetClientsResponse {
    /** List of client summaries. */
    clients?: ClientSummary[];
    /** Total number of clients matching the filter. */
    totalCount?: number;
    /** Current page number. */
    page?: number;
    /** Number of items per page. */
    pageSize?: number;
}

/** Summary of a client in the trainer's client list. */
export interface ClientSummary {
    /** Internal integer primary key of the ClientProfessionalLink row.
Used to populate linkId on the photo-diary-request create form. */
    linkId?: number;
    /** Client profile's public ID. */
    publicId?: string;
    /** Client's email address. */
    email?: string;
    /** Client's first name. */
    firstName?: string;
    /** Client's last name. */
    lastName?: string;
    /** Whether this trainer-client relationship is active. */
    isActive?: boolean;
    /** Date when the trainer-client relationship was established. */
    linkedAt?: string;
}

/** Request model for retrieving a trainer's client list with pagination. */
export interface GetClientsRequest {
}

/** Response model containing a client's progress data for the trainer view. */
export interface GetClientProgressResponse {
    /** Compliance percentage (0-100), representing how many planned meals were logged. */
    compliancePercent?: number;
    /** Total number of meals planned in the date range. */
    mealsPlanned?: number;
    /** Total number of meals logged in the date range. */
    mealsLogged?: number;
    /** Current streak of consecutive compliant days. */
    currentStreak?: number;
    /** Average daily macronutrient totals for the date range. */
    averageDailyMacros?: NutrientTotals;
    /** Start date of the progress calculation range. */
    from?: string;
    /** End date of the progress calculation range. */
    to?: string;
}

/** Computed macro totals for a meal or day. */
export interface NutrientTotals {
    /** Total kilocalories. */
    kcal?: number;
    /** Total protein in grams. */
    protein?: number;
    /** Total carbohydrates in grams. */
    carbs?: number;
    /** Total fat in grams. */
    fat?: number;
    /** Total dietary fiber in grams. */
    fiber?: number;
}

/** Request model for retrieving a client's progress data from the trainer's perspective. */
export interface GetClientProgressRequest {
}

/** Response model containing a client's dashboard summary for a trainer. */
export interface GetClientDashboardResponse {
    /** Internal integer primary key of the ClientProfessionalLink row.
Used to populate linkId on the photo-diary-request create form. */
    linkId?: number;
    /** The client profile's public ID. */
    clientPublicId?: string;
    /** The client's ApplicationUser.Id (= ClientProfile.UserId).
This is the identifier used by the weekly-check-in and schedule-override
endpoints — it differs from ClientPublicId. */
    clientUserId?: string;
    /** The client's email address. */
    email?: string;
    /** The client's first name. */
    firstName?: string;
    /** The client's last name. */
    lastName?: string;
    /** The client's date of birth. */
    dateOfBirth?: string | undefined;
    /** The client's height in centimeters. */
    heightCm?: number | undefined;
    /** The client's current weight in kilograms. */
    weightKg?: number | undefined;
    /** The client's fitness or health goals. */
    goals?: string | undefined;
    /** Date when the trainer-client relationship was established. */
    linkedAt?: string;
    /** Whether the trainer-client relationship is currently active. */
    isActive?: boolean;
    /** Whether this professional is permitted to view the client's nutrition plans.
Mirrors ClientProfessionalLink.CanViewNutritionPlans. */
    canViewNutritionPlans?: boolean;
    /** Whether this professional is permitted to view the client's training plans.
Mirrors ClientProfessionalLink.CanViewTrainingPlans. */
    canViewTrainingPlans?: boolean;
    /** Whether the client has registered an account (email confirmed). */
    hasRegistered?: boolean;
    /** Status of the client's questionnaire: "none", "pending", or "submitted". */
    questionnaireStatus?: string;
    /** Title of the pending or submitted questionnaire, if any. */
    questionnaireTitle?: string | undefined;
    /** PublicId of the questionnaire response, if any. */
    questionnaireResponsePublicId?: string | undefined;
    /** When the questionnaire was submitted, if submitted. */
    questionnaireSubmittedAt?: string | undefined;
    /** Total number of body measurements recorded for the client. */
    totalMeasurements?: number;
    /** Total number of progress photos uploaded for the client. */
    totalProgressPhotos?: number;
    /** The most recent body measurement, or null if none exist. */
    latestMeasurement?: LatestMeasurementDto | undefined;
    /** Compliance percentage for the last 7 days (0-100), or null if no active nutrition plan exists. */
    compliancePercent?: number | undefined;
    /** Current streak of consecutive compliant days. */
    currentStreak?: number;
    /** Client's onboarding questionnaire data, or null if not completed. */
    onboarding?: OnboardingDataDto | undefined;
}

/** Summary of the most recent body measurement for a client. */
export interface LatestMeasurementDto {
    /** Date and time when the measurement was taken. */
    measuredAt?: string;
    /** Weight in kilograms. */
    weightKg?: number | undefined;
    /** Body fat percentage. */
    bodyFatPercentage?: number | undefined;
}

/** Client onboarding questionnaire data summary. */
export interface OnboardingDataDto {
    /** Biological sex. */
    sex?: string | undefined;
    /** Target weight in kg. */
    targetWeightKg?: number | undefined;
    /** Body type / somatotype. */
    bodyType?: string | undefined;
    /** Primary fitness goal. */
    primaryGoal?: string | undefined;
    /** Desired time horizon. */
    timeHorizon?: string | undefined;
    /** Job/activity type. */
    jobType?: string | undefined;
    /** Sleep hours per night. */
    sleepHours?: number | undefined;
    /** Stress level (1-5). */
    stressLevel?: number | undefined;
    /** Current training frequency. */
    currentTrainingFrequency?: string | undefined;
    /** Desired training frequency. */
    desiredTrainingFrequency?: string | undefined;
    /** Self-rated fitness (1-10). */
    fitnessRating?: number | undefined;
    /** Preferred activities (comma-separated). */
    preferredActivities?: string | undefined;
    /** Injuries/limitations (comma-separated). */
    injuries?: string | undefined;
    /** Meals per day. */
    mealsPerDay?: string | undefined;
    /** Dietary style. */
    dietaryStyle?: string | undefined;
    /** Allergies (comma-separated). */
    allergies?: string | undefined;
    /** Plan experience. */
    planExperience?: string | undefined;
    /** Past blockers (comma-separated). */
    pastBlockers?: string | undefined;
    /** Primary motivation. */
    primaryMotivation?: string | undefined;
    /** Derived activity level. */
    derivedActivityLevel?: string | undefined;
    /** Derived nutrition goal. */
    derivedNutritionGoal?: string | undefined;
    /** BMR in kcal/day. */
    bmr?: number | undefined;
    /** TDEE in kcal/day. */
    tdee?: number | undefined;
    /** Adjusted daily calories. */
    adjustedKcal?: number | undefined;
    /** Daily protein target in grams. */
    proteinGrams?: number | undefined;
    /** Daily carbs target in grams. */
    carbsGrams?: number | undefined;
    /** Daily fat target in grams. */
    fatGrams?: number | undefined;
    /** Meal distribution percentages as JSON. */
    mealDistribution?: string | undefined;
}

/** Request model for retrieving a client's dashboard summary. */
export interface GetClientDashboardRequest {
}

/** Response model for a successfully created collaboration. */
export interface CreateCollaborationResponse {
    /** A message describing the result of the operation. */
    message?: string;
}

/** Request model for creating a collaboration between a trainer and another professional for a shared client. */
export interface CreateCollaborationRequest {
    /** The public ID of the client to share with the collaborator. */
    clientPublicId: string;
    /** The public ID of the collaborator's ProfessionalProfile. */
    collaboratorPublicId: string;
}

export interface RejectClientRequestRequest {
    /** Optional statement from the professional (saved for future display in chat). */
    statement?: string | undefined;
}

/** Response model containing incoming client requests for a professional. */
export interface GetIncomingRequestsResponse {
    requests?: IncomingClientRequestDto[];
}

export interface IncomingClientRequestDto {
    publicId?: string;
    clientFirstName?: string;
    clientLastName?: string;
    clientEmail?: string;
    message?: string | undefined;
    sentAt?: string;
}

/** Request model for accepting a client request. */
export interface AcceptClientRequestRequest {
    /** Optional public ID of a questionnaire to assign to the client upon acceptance. */
    questionnairePublicId?: string | undefined;
    /** Optional statement from the professional (saved for future display in chat). */
    statement?: string | undefined;
}

export interface ListNotesResponse {
    notes?: NoteDto[];
}

export interface NoteDto {
    noteId?: string;
    text?: string;
    createdAt?: string;
    updatedAt?: string;
}

export interface ListNotesRequest {
}

export interface EditNoteResponse {
    noteId?: string;
    text?: string;
    updatedAt?: string;
}

export interface EditNoteRequest {
    text: string;
}

export interface DeleteNoteRequest {
}

export interface CreateNoteResponse {
    noteId?: string;
    createdAt?: string;
}

export interface CreateNoteRequest {
    text: string;
}

export interface CancelQuestionnaireRequest {
}

/** Request model for assigning a questionnaire to an existing client. */
export interface AssignQuestionnaireRequest {
    /** Public identifier of the questionnaire to assign. */
    questionnairePublicId?: string;
}

/** Full session template detail returned by get, create, update, copy, and from-plan endpoints. */
export interface SessionTemplateDetailResponse {
    /** Public identifier of the session template. */
    templateId?: string;
    /** Display name of the template. */
    name?: string;
    /** Localized template names (en, cs, de), when available. */
    localizedNames?: LocalizedNames | undefined;
    /** Optional description of the template. */
    description?: string | undefined;
    /** Difficulty level of the template. */
    difficulty?: ExerciseDifficulty;
    /** Estimated total duration of the session in minutes. */
    estimatedDurationMinutes?: number | undefined;
    /** Session-level workout format / scoring methodology. */
    format?: WorkoutFormat;
    /** Format configuration for the session. Null when Format is Standard. */
    formatConfig?: WodConfig | undefined;
    /** Ordered workouts making up the template — the same TrainingWorkout shape
used inside a training plan session, so this response maps write-side to
UpdateSessionRequest.Workouts verbatim. */
    workouts?: TrainingWorkout[];
    /** Standalone exercises directly on this template (not grouped under any workout) — the
same SessionExercise shape used inside a training plan session, so this
response maps write-side to UpdateSessionRequest.StandaloneExercises verbatim. */
    standaloneExercises?: SessionExercise[];
    /** Flat, read-only view of every exercise in this template — StandaloneExercises
plus every workout's nested exercises. Never send this back on a write: it has no
corresponding member on the write-side session request, and sending it as standalone would
persist every nested workout exercise a second time, compounding on each save. */
    allExercises?: SessionExercise[];
    /** Who can read this template besides its owner. */
    visibility?: LibraryVisibility;
    /** True when the authenticated caller is the trainer who owns this template. */
    isOwnedByCurrentUser?: boolean;
    /** When the template was created. */
    dateCreated?: string;
    /** When the template was last updated. */
    dateUpdated?: string | undefined;
    /** Optimistic concurrency version, required on PUT requests. */
    version?: number;
}

/** Stores food names in supported languages for localization. */
export interface LocalizedNames {
    /** English name. */
    en?: string | undefined;
    /** Czech name. */
    cs?: string | undefined;
    /** German name. */
    de?: string | undefined;
}

/** Request model for updating an existing session template. */
export interface UpdateSessionTemplateRequest {
    /** Updated display name. */
    name: string;
    /** Updated localized template names (en, cs, de). */
    localizedNames?: LocalizedNames | undefined;
    /** Updated description. */
    description?: string | undefined;
    /** Updated difficulty level. */
    difficulty?: ExerciseDifficulty;
    /** Updated estimated total duration in minutes. */
    estimatedDurationMinutes?: number | undefined;
    /** Updated session-level workout format. */
    format?: WorkoutFormat;
    /** Updated format configuration. Must be null for Standard format. */
    formatConfig?: WodConfig | undefined;
    /** Updated workouts — the existing TrainingWorkout snapshot shape, verbatim. */
    workouts?: TrainingWorkout[];
    /** Updated standalone exercises — the existing SessionExercise snapshot shape,
verbatim. */
    standaloneExercises?: SessionExercise[];
    /** Updated visibility. */
    visibility?: LibraryVisibility;
    /** The version the caller last read. Used for optimistic-concurrency CAS; a stale value
returns 409. */
    version?: number;
}

/** Paginated response for session template search results. */
export interface SearchSessionTemplatesResponse {
    /** List of matching session templates. */
    templates?: SessionTemplateSummaryDto[];
    /** Total number of matching templates. */
    totalCount?: number;
    /** Current page number. */
    page?: number;
    /** Number of items per page. */
    pageSize?: number;
}

/** Lightweight session template summary for search/list views. */
export interface SessionTemplateSummaryDto {
    /** Public identifier of the session template. */
    templateId?: string;
    /** Display name of the template. */
    name?: string;
    /** Localized template names (en, cs, de), when available. */
    localizedNames?: LocalizedNames | undefined;
    /** Optional description of the template. */
    description?: string | undefined;
    /** Difficulty level of the template. */
    difficulty?: ExerciseDifficulty;
    /** Estimated total duration of the session in minutes. */
    estimatedDurationMinutes?: number | undefined;
    /** Session-level workout format / scoring methodology. */
    format?: WorkoutFormat;
    /** Number of workouts in this template. */
    workoutCount?: number;
    /** Number of standalone exercises (not grouped under any workout) in this template. */
    standaloneExerciseCount?: number;
    /** Who can read this template besides its owner. */
    visibility?: LibraryVisibility;
    /** True when the authenticated caller is the trainer who owns this template. */
    isOwnedByCurrentUser?: boolean;
    /** When the template was created. */
    dateCreated?: string;
}

/** Request model for searching session templates. */
export interface SearchSessionTemplatesRequest {
}

/** Request model for saving a session template from an existing training plan session. */
export interface SaveSessionTemplateFromPlanRequest {
    /** The source training plan's public identifier (TrainingPlan.ExternalId) — not the
client id. */
    planId: string;
    /** The week number (1-based) within the plan that contains the source session. */
    weekNumber?: number;
    /** The day of week (1 = Monday … 7 = Sunday) within the week that contains the source
session. */
    dayOfWeek?: number;
    /** Identifier of the source TrainingSession within the addressed day. */
    sessionId: string;
    /** Display name for the new template. */
    name: string;
    /** Optional description for the new template. */
    description?: string | undefined;
    /** Who can read the new template besides the caller. */
    visibility?: LibraryVisibility;
}

/** Request model for retrieving a single session template. */
export interface GetSessionTemplateRequest {
}

/** Request model for deleting a session template. */
export interface DeleteSessionTemplateRequest {
}

/** Request model for creating a new session template. */
export interface CreateSessionTemplateRequest {
    /** Display name of the template. */
    name: string;
    /** Localized template names (en, cs, de), when available. */
    localizedNames?: LocalizedNames | undefined;
    /** Optional description of the template. */
    description?: string | undefined;
    /** Difficulty level of the template. */
    difficulty?: ExerciseDifficulty;
    /** Estimated total duration of the session in minutes. */
    estimatedDurationMinutes?: number | undefined;
    /** Session-level workout format / scoring methodology. */
    format?: WorkoutFormat;
    /** Format configuration for the session. Must be null for Standard format. */
    formatConfig?: WodConfig | undefined;
    /** Ordered workouts to include — the existing TrainingWorkout snapshot shape,
verbatim, so a template copies directly into a plan session. */
    workouts?: TrainingWorkout[];
    /** Standalone exercises to include — the existing SessionExercise snapshot
shape, verbatim. Shares one ordering sequence with Workouts: a duplicate
Order/Order across the two
lists is rejected. */
    standaloneExercises?: SessionExercise[];
    /** Who can read this template besides the caller. Defaults to
Private when omitted. */
    visibility?: LibraryVisibility;
}

/** Request model for copying a readable session template to the caller's own library. */
export interface CopySessionTemplateRequest {
}

/** Response model containing the pre-signed upload URL and the permanent blob URL. */
export interface UploadRecipeImageUrlResponse {
    /** Time-limited pre-signed URL the client should PUT the image file to. */
    uploadUrl?: string;
    /** Permanent blob URL at which the recipe image will be accessible after a successful upload.
Main slot: recipes/{recipeId}/main.{ext}.
Gallery slot: recipes/{recipeId}/gallery-{n}.{ext}. */
    blobUrl?: string;
}

/** Request model for generating a pre-signed upload URL for a recipe image. */
export interface UploadRecipeImageUrlRequest {
    /** MIME type of the image file (e.g. "image/jpeg", "image/png", "image/webp", "image/heic", "image/heif") */
    contentType: string;
    /** Declared file size in bytes. Must not exceed 5 MiB. */
    sizeBytes?: number;
}

/** Full recipe detail returned by get and create/update endpoints. */
export interface GetRecipeResponse {
    /** Public identifier of the recipe. */
    recipeId?: string;
    /** Name of the recipe. */
    name?: string;
    /** Optional description or preparation instructions. */
    description?: string | undefined;
    /** Estimated preparation/cooking time in minutes. */
    prepTimeMinutes?: number | undefined;
    /** Ordered preparation steps. */
    steps?: string[] | undefined;
    /** Optional tip or note. */
    note?: string | undefined;
    /** List of food items with denormalized nutrient snapshots. */
    foods?: MealFood[];
    /** Computed total macronutrients for the entire recipe. */
    totalNutrients?: NutrientTotals;
    /** Visibility of the recipe (Public = visible to all nutritionists, Private = visible only to its creator). */
    visibility?: RecipeVisibility;
    /** URL of the recipe's main image in blob storage, or null if no image has been uploaded. */
    imageUrl?: string | undefined;
    /** URLs of gallery images (up to 6 entries). Each entry points to a blob at
recipes/{recipeId}/gallery-{n}.{ext}. */
    galleryImageUrls?: string[];
    /** True when the authenticated caller is the nutritionist who created this recipe.
Clients of the API can use this flag to decide whether to show edit/delete affordances. */
    isOwnedByCurrentUser?: boolean;
    /** When the recipe was created. */
    dateCreated?: string;
    /** When the recipe was last updated. */
    dateUpdated?: string | undefined;
}

/** A food item within a meal — denormalized snapshot of food data at the time of addition. */
export interface MealFood {
    /** Reference to the original food document's ExternalId. */
    foodExternalId?: string;
    /** Snapshot of the food name at time of addition. */
    foodName?: string;
    /** Czech name. */
    foodNameCs?: string | undefined;
    /** English name. */
    foodNameEn?: string | undefined;
    /** German name. */
    foodNameDe?: string | undefined;
    /** Snapshot of food category at time of addition. */
    foodCategory?: string | undefined;
    /** Snapshot of nutritional values per 100 grams at time of addition. */
    nutrientValuePer100Grams?: NutrientValue;
    /** Amount of this food in grams. */
    amountGrams?: number;
    /** Optional note for this food in the plan. */
    note?: string | undefined;
}

/** Embedded document with nutritional values per 100 grams. */
export interface NutrientValue {
    /** Energy in kilocalories. */
    kcal?: number;
    /** Protein in grams. */
    protein?: number;
    /** Carbohydrates in grams. */
    carbs?: number;
    /** Total fat in grams. */
    fat?: number;
    /** Dietary fiber in grams. */
    fiber?: number | undefined;
    /** Sugar in grams. */
    sugar?: number | undefined;
    /** Saturated fat in grams. */
    saturatedFat?: number | undefined;
    /** Salt in grams. */
    salt?: number | undefined;
}

/** Controls who can see a recipe. */
export enum RecipeVisibility {
    Public = "Public",
    Private = "Private",
}

/** Request model for updating an existing recipe. */
export interface UpdateRecipeRequest {
    /** Updated name of the recipe. */
    name: string;
    /** Updated description or preparation instructions. */
    description?: string | undefined;
    /** Estimated preparation/cooking time in minutes. */
    prepTimeMinutes?: number | undefined;
    /** Ordered preparation steps. */
    steps?: string[] | undefined;
    /** Optional tip or note. */
    note?: string | undefined;
    /** Updated list of food items in the recipe. */
    foods: RecipeFoodDto[];
    /** Updated visibility. When omitted, the recipe's existing visibility is preserved.
Only the recipe's creator can change this value. */
    visibility?: RecipeVisibility | undefined;
}

/** Input DTO representing a food item to include in a recipe. */
export interface RecipeFoodDto {
    /** External identifier of the food to add. */
    foodExternalId?: string;
    /** Amount of this food in grams. */
    amountGrams?: number;
    /** Optional note for this ingredient (e.g. preparation tip, substitution hint). */
    note?: string | undefined;
}

/** Paginated response for recipe search results. */
export interface SearchRecipesResponse {
    /** List of matching recipes. */
    recipes?: RecipeSummaryDto[];
    /** Total number of matching recipes. */
    totalCount?: number;
    /** Current page number. */
    page?: number;
    /** Number of items per page. */
    pageSize?: number;
}

/** Lightweight recipe summary for list views. */
export interface RecipeSummaryDto {
    /** Public identifier of the recipe. */
    recipeId?: string;
    /** Name of the recipe. */
    name?: string;
    /** Number of food items in the recipe. */
    foodCount?: number;
    /** Computed total macronutrients. */
    totalNutrients?: NutrientTotals;
    /** Estimated preparation/cooking time in minutes. */
    prepTimeMinutes?: number | undefined;
    /** Visibility of the recipe (Public = visible to all nutritionists, Private = visible only to its creator). */
    visibility?: RecipeVisibility;
    /** URL of the recipe's main image, or null if no image has been uploaded. */
    imageUrl?: string | undefined;
    /** True when the authenticated caller is the nutritionist who created this recipe. */
    isOwnedByCurrentUser?: boolean;
    /** When the recipe was created. */
    dateCreated?: string;
    /** Distinct food categories from the recipe's ingredients. */
    foodCategories?: string[];
}

/** Request model for searching recipes. */
export interface SearchRecipesRequest {
}

/** Request model for retrieving a single recipe. */
export interface GetRecipeRequest {
}

/** Request model for deleting a recipe. */
export interface DeleteRecipeRequest {
}

/** Request model for creating a new recipe. */
export interface CreateRecipeRequest {
    /** Name of the recipe. */
    name: string;
    /** Optional description or preparation instructions. */
    description?: string | undefined;
    /** Estimated preparation/cooking time in minutes. */
    prepTimeMinutes?: number | undefined;
    /** Ordered preparation steps. */
    steps?: string[] | undefined;
    /** Optional tip or note. */
    note?: string | undefined;
    /** List of food items to include in the recipe. */
    foods: RecipeFoodDto[];
    /** Visibility of the recipe. Defaults to Public when omitted. */
    visibility?: RecipeVisibility;
}

/** Request model for confirming the uploaded recipe image blob URL. */
export interface ConfirmRecipeImageRequest {
    /** The permanent blob URL returned by POST /recipes/{id}/image/upload-url. */
    blobUrl: string;
}

export interface UpdateResponseRequest {
    answers: AnswerDto[];
}

export interface AnswerDto {
    questionPublicId?: string;
    valueText?: string | undefined;
    valueNumber?: number | undefined;
    valueJson?: string | undefined;
    fileUrl?: string | undefined;
}

export interface GetTrainerQuestionnaireResponse {
    publicId?: string;
    title?: string;
    description?: string | undefined;
    isActive?: boolean;
    isDefault?: boolean;
    questions?: QuestionDto[];
}

export interface QuestionDto {
    publicId?: string;
    orderIndex?: number;
    type?: string;
    label?: string;
    helperText?: string | undefined;
    isRequired?: boolean;
    isHidden?: boolean;
    config?: string | undefined;
    mappedField?: string | undefined;
}

export interface UpdateQuestionnaireRequest {
    title: string;
    description?: string | undefined;
    isActive?: boolean;
    isDefault?: boolean;
    questions?: UpdateQuestionDto[];
}

export interface UpdateQuestionDto {
    publicId?: string | undefined;
    orderIndex?: number;
    type?: string;
    label?: string;
    helperText?: string | undefined;
    isRequired?: boolean;
    isHidden?: boolean;
    config?: string | undefined;
    mappedField?: string | undefined;
}

export interface PreviewQuestionnaireRequest {
}

export interface GetTrainerQuestionnairesResponse {
    questionnaires?: QuestionnaireSummaryDto[];
}

export interface QuestionnaireSummaryDto {
    publicId?: string;
    title?: string;
    description?: string | undefined;
    isActive?: boolean;
    isDefault?: boolean;
    questionCount?: number;
    dateCreated?: string;
}

export interface GetQuestionnaireDetailRequest {
}

export interface GetClientSubmittedResponsesResponse {
    coaches?: CoachQuestionnairesItem[];
}

export interface CoachQuestionnairesItem {
    linkPublicId?: string;
    professionalName?: string;
    professionalRole?: string | undefined;
    responses?: SubmittedResponseItem[];
}

export interface SubmittedResponseItem {
    responsePublicId?: string;
    questionnaireTitle?: string;
    submittedAt?: string | undefined;
    answers?: SubmittedAnswerItem[];
}

export interface SubmittedAnswerItem {
    label?: string;
    type?: string;
    valueText?: string | undefined;
    valueNumber?: number | undefined;
    valueJson?: string | undefined;
    config?: string | undefined;
}

export interface GetClientResponsesResponse {
    responses?: ClientResponseItem[];
}

export interface ClientResponseItem {
    responsePublicId?: string;
    questionnaireTitle?: string;
    status?: string;
    submittedAt?: string | undefined;
    dateCreated?: string;
    answerCount?: number;
    answers?: ResponseAnswerDto[];
}

export interface ResponseAnswerDto {
    questionPublicId?: string;
    questionLabel?: string;
    questionType?: string;
    mappedField?: string | undefined;
    valueText?: string | undefined;
    valueNumber?: number | undefined;
    valueJson?: string | undefined;
    fileUrl?: string | undefined;
    /** Label of the questionnaire section (a Type="section" question) this
answer's question falls under, or null if it appears before the first
section header. Computed server-side via
QuestionSectionResolver — additive field, #713. */
    sectionLabel?: string | undefined;
    /** 0-based order of SectionLabel relative to the other
sections in the questionnaire, or null when SectionLabel
is null. Additive field, #713. */
    sectionOrder?: number | undefined;
}

export interface GetClientResponseResponse {
    responsePublicId?: string;
    questionnaireTitle?: string;
    submittedAt?: string | undefined;
    answerCount?: number;
    answers?: ResponseAnswerDto[];
}

export interface GetClientQuestionnaireResponse {
    questionnairePublicId?: string;
    title?: string;
    description?: string | undefined;
    linkPublicId?: string;
    professionalName?: string;
    professionalRole?: string | undefined;
    professionalCity?: string | undefined;
    questionCount?: number;
    questions?: ClientQuestionDto[];
    existingResponsePublicId?: string | undefined;
    existingResponseStatus?: string | undefined;
    existingAnswers?: ClientAnswerDto[] | undefined;
}

export interface ClientQuestionDto {
    publicId?: string;
    orderIndex?: number;
    type?: string;
    label?: string;
    helperText?: string | undefined;
    isRequired?: boolean;
    config?: string | undefined;
}

export interface ClientAnswerDto {
    questionPublicId?: string;
    valueText?: string | undefined;
    valueNumber?: number | undefined;
    valueJson?: string | undefined;
    fileUrl?: string | undefined;
}

/** Combined banner response. Diary requests come first (ordering convention documented on the endpoint summary); questionnaires second. */
export interface GetClientPendingQuestionnairesResponse {
    /** Pending photo-diary requests addressed to this client.
Always populated before Items so the mobile banner stack
renders diary banners above questionnaire banners. */
    pendingDiaryRequests?: PendingDiaryRequestItem[];
    /** Pending / in-progress questionnaires for this client (one per active professional link). */
    items?: PendingQuestionnaireItem[];
}

/** Banner DTO for a single pending photo-diary request. Shape mirrors PendingQuestionnaireItem so the mobile banner component can render both types with a single rendering pattern. */
export interface PendingDiaryRequestItem {
    /** Public identifier of the diary request (same as PhotoDiaryRequest.Id). */
    requestPublicId?: string;
    /** Display name of the professional who sent the request. */
    professionalName?: string;
    /** Role of the professional ("Trainer" or "Nutritionist"). */
    professionalRole?: string | undefined;
    /** How many days the client has to upload photos (default 7). */
    durationDays?: number;
    /** Always "Pending" for items returned by this endpoint. */
    status?: string;
    /** Optional MongoDB plan ID the diary request is scoped to.
Null when the request has no plan context. */
    planId?: string | undefined;
    /** When the request was created (UTC). */
    createdAt?: string;
}

export interface PendingQuestionnaireItem {
    linkPublicId?: string;
    professionalName?: string;
    professionalRole?: string | undefined;
    questionnairePublicId?: string | undefined;
    questionnaireTitle?: string | undefined;
    questionCount?: number;
    responsePublicId?: string | undefined;
    responseStatus?: string | undefined;
}

export interface DeleteQuestionnaireRequest {
}

export interface CreateResponseRequest {
    questionnairePublicId?: string;
}

export interface CreateQuestionnaireRequest {
    title: string;
    description?: string | undefined;
}

export interface SearchProfessionalsResponse {
    items?: ProfessionalSummaryDto[];
    totalCount?: number;
    page?: number;
    pageSize?: number;
}

export interface ProfessionalSummaryDto {
    publicId?: string;
    firstName?: string;
    lastName?: string;
    bio?: string | undefined;
    specializations?: string[];
    city?: string | undefined;
    estimatedPrice?: string | undefined;
    collaborationType?: string | undefined;
    languages?: string[];
    roles?: string[];
    avatarBlobUrl?: string | undefined;
}

export interface SearchProfessionalsRequest {
}

export interface GetPublicProfileResponse {
    publicId?: string;
    firstName?: string;
    lastName?: string;
    bio?: string | undefined;
    specializations?: string[];
    certificates?: string[];
    languages?: string[];
    city?: string | undefined;
    estimatedPrice?: string | undefined;
    collaborationType?: string | undefined;
    linkedIn?: string | undefined;
    instagram?: string | undefined;
    website?: string | undefined;
    roles?: string[];
    hasPendingRequest?: boolean;
    isLinked?: boolean;
    avatarBlobUrl?: string | undefined;
}

export interface GetPublicProfileRequest {
}

/** Request model for confirming the uploaded professional avatar blob URL. */
export interface ConfirmProfessionalAvatarRequest {
    /** The permanent blob URL returned by POST /professionals/me/avatar/upload-url. */
    blobUrl: string;
}

/** Response model containing the pre-signed upload URL and the permanent blob URL. */
export interface GenerateProfessionalAvatarUploadUrlResponse {
    /** Time-limited pre-signed URL the client should PUT the image file to. */
    uploadUrl?: string;
    /** Permanent blob URL at which the avatar will be accessible after a successful upload.
Always starts with avatars/. */
    blobUrl?: string;
}

/** Request model for generating a pre-signed professional avatar upload URL. */
export interface GenerateProfessionalAvatarUploadUrlRequest {
    /** MIME type of the image file (e.g. "image/jpeg", "image/png", "image/webp", "image/heic", "image/heif") */
    contentType: string;
    /** Declared file size in bytes. Must not exceed 5 MiB. */
    sizeBytes?: number;
}

/** Response returned after a client submits / finalizes a photo diary. */
export interface SubmitRequestResponse {
    id?: string;
    status?: PhotoDiaryStatus;
    completedAt?: string | undefined;
}

/** Lifecycle status of a PhotoDiaryRequest. */
export enum PhotoDiaryStatus {
    Pending = "Pending",
    Accepted = "Accepted",
    Dismissed = "Dismissed",
    InProgress = "InProgress",
    Completed = "Completed",
}

/** Route parameters for submitting / finalizing a photo diary. */
export interface SubmitRequestRequest {
}

/** Paginated list of photo diary requests for a trainer. */
export interface ListTrainerRequestsResponse {
    items?: PhotoDiaryRequestSummary[];
}

/** Summary DTO for a single photo diary request in a list. */
export interface PhotoDiaryRequestSummary {
    id?: string;
    professionalId?: string;
    linkId?: number | undefined;
    pendingInviteId?: number | undefined;
    planId?: string | undefined;
    durationDays?: number;
    mode?: PhotoDiaryMode | undefined;
    status?: PhotoDiaryStatus;
    dismissReason?: string | undefined;
    acceptedAt?: string | undefined;
    completedAt?: string | undefined;
    createdAt?: string;
    updatedAt?: string;
}

/** The mode a client chooses when accepting a photo diary request. */
export enum PhotoDiaryMode {
    Bulk = "Bulk",
    Workflow = "Workflow",
}

/** Query parameters for listing trainer photo diary requests. */
export interface ListTrainerRequestsRequest {
}

/** Paginated list of photo diary requests visible to the authenticated client. */
export interface ListClientRequestsResponse {
    items?: ClientPhotoDiaryRequestSummary[];
}

/** Summary DTO for a photo diary request in the client list view. */
export interface ClientPhotoDiaryRequestSummary {
    id?: string;
    professionalId?: string;
    linkId?: number | undefined;
    pendingInviteId?: number | undefined;
    planId?: string | undefined;
    durationDays?: number;
    mode?: PhotoDiaryMode | undefined;
    status?: PhotoDiaryStatus;
    dismissReason?: string | undefined;
    acceptedAt?: string | undefined;
    completedAt?: string | undefined;
    createdAt?: string;
    updatedAt?: string;
}

/** Query parameters for the client's photo diary request list. */
export interface ListClientRequestsRequest {
}

/** Response body returned after retroactively linking a photo diary request to a plan. */
export interface LinkPlanResponse {
    /** The photo diary request's ID. */
    id?: string;
    /** The professional (nutritionist/trainer) who owns this request. */
    professionalId?: string;
    /** The client-professional link this request is attached to (if link-based). */
    linkId?: number | undefined;
    /** The pending invite this request is bundled with (if invite-based). */
    pendingInviteId?: number | undefined;
    /** The plan now linked to this request. */
    planId?: string | undefined;
    /** How many days the client has to upload photos. */
    durationDays?: number;
    /** Current lifecycle status of the request. */
    status?: PhotoDiaryStatus;
    /** When the request was created. */
    createdAt?: string;
    /** When the request was last updated (bumped by this link operation). */
    updatedAt?: string;
}

/** Route + body for retroactively linking an existing photo diary request to a nutrition or training plan. Diary-level (whole-diary) granularity — mirrors #777's response-level linking rather than linking individual photos. */
export interface LinkPlanRequest {
    /** MongoDB external identifier of the nutrition or training plan to link.
Must belong to the same client the diary request is attached to. */
    planId: string;
}

/** Response returned after a client dismisses a photo diary request. */
export interface DismissRequestResponse {
    id?: string;
    status?: PhotoDiaryStatus;
    dismissReason?: string | undefined;
}

/** Route + body for dismissing a photo diary request. */
export interface DismissRequestRequest {
    /** Optional reason for dismissal (max 500 characters). */
    reason?: string | undefined;
}

/** Response body returned after creating a photo diary request. */
export interface CreateRequestResponse {
    /** The new request's ID. */
    id?: string;
    /** The professional (nutritionist) who created the request. */
    professionalId?: string;
    /** The client-professional link this request is attached to (if link-based). */
    linkId?: number | undefined;
    /** The pending invite this request is bundled with (if invite-based). */
    pendingInviteId?: number | undefined;
    /** Optional plan scope for this request. */
    planId?: string | undefined;
    /** How many days the client has to upload photos. */
    durationDays?: number;
    /** Current lifecycle status (always Pending on creation). */
    status?: PhotoDiaryStatus;
    /** When the request was created. */
    createdAt?: string;
}

/** Request body for creating a new photo diary request. Exactly one of LinkId or PendingInviteId must be set. */
export interface CreateRequestRequest {
    /** Internal ID of an existing client-professional link.
Mutually exclusive with PendingInviteId. */
    linkId?: number | undefined;
    /** Internal ID of a pending invite.
Mutually exclusive with LinkId. */
    pendingInviteId?: number | undefined;
    /** Optional MongoDB external identifier of the nutrition or training plan this request is scoped to.
When set, must belong to the same client as the link/invite. */
    planId?: string | undefined;
    /** How many days the client has to upload photos (Workflow mode).
Defaults to 7. Allowed range: 1–30. */
    durationDays?: number;
}

/** Response returned after a client accepts a photo diary request. */
export interface AcceptRequestResponse {
    id?: string;
    status?: PhotoDiaryStatus;
    mode?: PhotoDiaryMode | undefined;
    acceptedAt?: string | undefined;
}

/** Route + body for accepting a photo diary request. */
export interface AcceptRequestRequest {
    /** The upload mode chosen by the client.
Must be a valid PhotoDiaryMode value. */
    mode?: PhotoDiaryMode;
}

/** Full nutrition-plan-template detail including all weeks, days, meals, foods, recipes, and supplements. Used by the detail GET and by PUT's response. */
export interface NutritionPlanTemplateDetailDto {
    /** Template's public identifier. */
    templateId?: string;
    /** Display name. */
    name?: string;
    /** Optional free-text description. */
    description?: string | undefined;
    /** Primary fitness goal this template targets. */
    goal?: PrimaryGoal | undefined;
    /** Dietary style this template targets. */
    dietaryStyle?: DietaryStyle | undefined;
    /** Global daily nutrition targets. */
    globalSettings?: GlobalNutritionSettings | undefined;
    /** Supplement recommendations carried by this template. */
    supplements?: TemplateSupplementDto[];
    /** All weeks in the template with their days, meals, foods, and recipes. */
    weeks?: TemplateWeek[];
    /** Number of weeks, server-computed from Weeks. */
    weekCount?: number;
    /** Who can read this entry besides its owner. */
    visibility?: LibraryVisibility;
    /** True when the authenticated caller is the nutritionist who owns this template. */
    isOwnedByCurrentUser?: boolean;
    /** Optimistic concurrency version. */
    version?: number;
    /** When the template was created. */
    dateCreated?: string;
    /** When the template was last updated. */
    dateUpdated?: string | undefined;
}

/** Dietary style or eating pattern preference. */
export enum DietaryStyle {
    Standard = "Standard",
    Vegetarian = "Vegetarian",
    Vegan = "Vegan",
    GlutenFree = "GlutenFree",
}

/** Global daily nutrition targets for a plan. */
export interface GlobalNutritionSettings {
    /** Target daily kilocalories. */
    dailyKcal?: number | undefined;
    /** Target daily protein in grams. */
    proteinGrams?: number | undefined;
    /** Target daily carbohydrates in grams. */
    carbsGrams?: number | undefined;
    /** Target daily fat in grams. */
    fatGrams?: number | undefined;
    /** Target daily dietary fiber in grams. */
    fiberGrams?: number | undefined;
}

/** A single supplement entry in a nutrition-plan-template response DTO. */
export interface TemplateSupplementDto {
    /** Stable public identifier for the supplement. */
    externalId?: string;
    /** Name of the supplement. */
    name?: string;
    /** Optional dosage instruction. */
    dose?: string | undefined;
    /** Optional additional notes. */
    notes?: string | undefined;
}

/** A single week within a NutritionPlanTemplate. Slim compared to PlanWeek — carries no Status/DatePublished, which are meaningless outside a client plan. */
export interface TemplateWeek {
    /** Week number within the template (1-based). */
    weekNumber?: number;
    /** Days in this week. Reuses PlanDay unchanged — everything below the week
level (PlanDay, PlanMeal, MealFood,
MealRecipe) is a straight clone between a template and a client plan. */
    days?: PlanDay[];
}

/** A single day within a plan week (1 = Monday … 7 = Sunday). */
export interface PlanDay {
    /** Day of the week (1 = Monday, 7 = Sunday). */
    dayOfWeek?: number;
    /** Meals scheduled for this day. */
    meals?: PlanMeal[];
    /** Optional note for this day. */
    note?: string | undefined;
    /** Computed totals for this day. */
    dayTotals?: NutrientTotals | undefined;
}

/** A single meal within a plan day (e.g. Breakfast, Lunch). */
export interface PlanMeal {
    /** Unique identifier for this meal within the plan. */
    mealId?: string;
    /** Kind of meal (Breakfast, Lunch, Dinner, etc.). */
    kind?: MealKind;
    /** Display order within the day (1-based). */
    order?: number;
    /** Suggested time for the meal (e.g. "08:00"). */
    time?: string | undefined;
    /** Foods included in this meal. */
    foods?: MealFood[];
    /** Recipes included in this meal. */
    recipes?: MealRecipe[];
    /** Optional note for this meal. */
    note?: string | undefined;
    /** Computed totals for this meal. */
    mealTotals?: NutrientTotals | undefined;
}

/** Kind of meal within a nutrition plan day. */
export enum MealKind {
    Breakfast = "Breakfast",
    MorningSnack = "MorningSnack",
    Lunch = "Lunch",
    AfternoonSnack = "AfternoonSnack",
    Dinner = "Dinner",
    PreWorkout = "PreWorkout",
    PostWorkout = "PostWorkout",
}

/** A recipe item within a meal — denormalized snapshot of recipe data at the time of addition. */
export interface MealRecipe {
    /** Reference to the original recipe's ID. */
    recipeId?: string;
    /** Snapshot of the recipe name at time of addition. */
    recipeName?: string;
    /** Total nutritional values for one serving of this recipe. */
    nutrientValuePerServing?: NutrientValue;
    /** Number of servings. */
    servings?: number;
    /** Optional note for this recipe in the plan. */
    note?: string | undefined;
    /** Distinct food categories from the recipe's ingredients (snapshot). */
    foodCategories?: string[] | undefined;
}

/** Request for a full-state update of a nutrition plan template: replaces name, description, goal/dietary style, settings, week tree, and supplements. */
export interface UpdateTemplateRequest2 {
    /** Updated display name. */
    name: string;
    /** Updated free-text description. */
    description?: string | undefined;
    /** Updated primary fitness goal. */
    goal?: PrimaryGoal | undefined;
    /** Updated dietary style. */
    dietaryStyle?: DietaryStyle | undefined;
    /** Updated global daily nutrition targets. */
    globalSettings?: GlobalNutritionSettings | undefined;
    /** Full week structure to persist. Replaces all existing weeks, days, meals, foods, and
recipes. */
    weeks: TemplateWeekRequest2[];
    /** Full supplement list to persist. Replaces all existing supplements. */
    supplements?: TemplateSupplementRequest[];
    /** Expected version for optimistic concurrency control. */
    version?: number;
}

/** Represents a single week submitted when creating or full-state updating a nutrition plan template. */
export interface TemplateWeekRequest2 {
    /** Week number within the template (1-based). */
    weekNumber?: number;
    /** Days in this week. */
    days?: TemplateDayRequest2[];
}

/** Represents a single day submitted when creating or full-state updating a nutrition plan template. */
export interface TemplateDayRequest2 {
    /** Day of week (1 = Monday … 7 = Sunday). */
    dayOfWeek?: number;
    /** Optional note for this day. */
    note?: string | undefined;
    /** Meals planned for this day. */
    meals?: TemplateMealRequest[];
}

/** Represents a single meal submitted when creating or full-state updating a nutrition plan template. */
export interface TemplateMealRequest {
    /** Optional existing meal identifier. When provided the meal retains its identity;
when null a new Guid is generated. */
    mealId?: string | undefined;
    /** Kind of meal (Breakfast, Lunch, Dinner, etc.). */
    kind?: MealKind;
    /** Display order within the day (1-based). */
    order?: number;
    /** Suggested time for the meal (e.g. "08:00"). */
    time?: string | undefined;
    /** Optional note for this meal. */
    note?: string | undefined;
    /** Foods included in this meal. */
    foods?: TemplateMealFoodRequest[];
    /** Recipes included in this meal. */
    recipes?: TemplateMealRecipeRequest[];
}

/** Represents a single food entry submitted when creating or full-state updating a nutrition plan template. */
export interface TemplateMealFoodRequest {
    /** External (public) identifier of the food item. */
    foodExternalId?: string;
    /** Display name of the food (snapshot at time of planning). */
    foodName?: string;
    /** Czech name. */
    foodNameCs?: string | undefined;
    /** English name. */
    foodNameEn?: string | undefined;
    /** German name. */
    foodNameDe?: string | undefined;
    /** Food category snapshot. */
    foodCategory?: string | undefined;
    /** Nutrient values per 100 grams for this food. */
    nutrientValuePer100Grams?: NutrientValue;
    /** Amount in grams to include in this meal. */
    amountGrams?: number;
    /** Optional note for this food in the template. */
    note?: string | undefined;
}

/** Represents a single recipe entry submitted when creating or full-state updating a nutrition plan template. */
export interface TemplateMealRecipeRequest {
    /** Recipe identifier. */
    recipeId?: string;
    /** Display name of the recipe. */
    recipeName?: string;
    /** Nutrient values per one serving. */
    nutrientValuePerServing?: NutrientValue;
    /** Number of servings. */
    servings?: number;
    /** Optional note. */
    note?: string | undefined;
    /** Distinct food categories from the recipe's ingredients (snapshot). */
    foodCategories?: string[] | undefined;
}

/** Represents a single supplement entry submitted when creating or full-state updating a nutrition plan template. */
export interface TemplateSupplementRequest {
    /** Stable public identifier for this supplement. Clients generate this on creation and send
it back unchanged on subsequent updates. When empty, the endpoint generates a new
Guid. */
    externalId?: string | undefined;
    /** Name of the supplement (e.g. "Vitamin D3"). Required. */
    name?: string;
    /** Optional dosage instruction in free text (e.g. "1 capsule with breakfast"). */
    dose?: string | undefined;
    /** Optional additional notes. */
    notes?: string | undefined;
}

/** Paginated response containing a list of nutrition plan template summaries. */
export interface SearchTemplatesResponse2 {
    /** List of template summaries for the current page. */
    templates?: NutritionPlanTemplateSummaryDto[];
    /** Total number of templates matching the filter. */
    totalCount?: number;
    /** Current page number. */
    page?: number;
    /** Number of items per page. */
    pageSize?: number;
}

/** Lightweight nutrition-plan-template summary — used for search results and as the response of endpoints that create or clone a template without needing the full week tree back. */
export interface NutritionPlanTemplateSummaryDto {
    /** Template's public identifier. */
    templateId?: string;
    /** Display name. */
    name?: string;
    /** Optional free-text description. */
    description?: string | undefined;
    /** Primary fitness goal this template targets. */
    goal?: PrimaryGoal | undefined;
    /** Dietary style this template targets. */
    dietaryStyle?: DietaryStyle | undefined;
    /** Number of weeks, server-computed from the template's week tree. */
    weekCount?: number;
    /** Who can read this entry besides its owner. */
    visibility?: LibraryVisibility;
    /** True when the authenticated caller is the nutritionist who owns this template. */
    isOwnedByCurrentUser?: boolean;
    /** Optimistic concurrency version. */
    version?: number;
    /** When the template was created. */
    dateCreated?: string;
    /** When the template was last updated. */
    dateUpdated?: string | undefined;
}

/** Request to search nutrition plan templates with optional filters and pagination. */
export interface SearchTemplatesRequest2 {
}

/** Response describing the new client plan created by instantiating a template. */
export interface InstantiateTemplateResponse2 {
    /** The newly created plan's public identifier. */
    planId?: string;
    /** The client's ClientProfile.PublicId — echoes the caller-supplied value. */
    clientId?: string;
    /** Display name of the new plan. */
    name?: string;
    /** Status of the new plan — always Draft immediately after instantiation. */
    status?: string;
    /** When the new plan was created. */
    dateCreated?: string;
}

/** Request to instantiate a nutrition plan template into a new Draft client plan. */
export interface InstantiateTemplateRequest2 {
    /** The target client's ClientProfile.PublicId — the trainer-facing identifier, not
the internal ApplicationUser.Id storage key. */
    clientId: string;
    /** Display name for the new plan. */
    name: string;
    /** Optional start date. Must be a Monday and not in the past. */
    startDate?: string | undefined;
}

/** Request to fetch a single nutrition plan template's full detail. */
export interface GetTemplateRequest2 {
}

/** Request to save an existing nutrition plan as a new template. */
export interface CreateTemplateFromPlanRequest2 {
    /** The source plan's public identifier (NutritionPlan.ExternalId — NOT the client's
ClientProfile.PublicId). */
    planId: string;
    /** Display name for the new template. */
    name: string;
    /** Optional free-text description for the new template. */
    description?: string | undefined;
    /** Who can read the new template besides the caller. Defaults to Private. */
    visibility?: LibraryVisibility;
}

/** Request to create a new nutrition plan template — either empty (materialized from WeekCount) or with a full week tree supplied directly. */
export interface CreateTemplateRequest2 {
    /** Display name of the template. */
    name: string;
    /** Optional free-text description. */
    description?: string | undefined;
    /** Optional primary fitness goal this template targets. */
    goal?: PrimaryGoal | undefined;
    /** Optional dietary style this template targets. */
    dietaryStyle?: DietaryStyle | undefined;
    /** Optional global daily nutrition targets. */
    globalSettings?: GlobalNutritionSettings | undefined;
    /** Supplement recommendations to carry on the template. */
    supplements?: TemplateSupplementRequest[];
    /** A materialisation instruction for the empty-weeks path: creates this many weeks, each
with all 7 days and no meals. Mutually exclusive with Weeks. Never persisted
as supplied — WeekCount is always server-computed from
the resulting week tree. */
    weekCount?: number | undefined;
    /** A full week tree to persist directly. Mutually exclusive with WeekCount. */
    weeks?: TemplateWeekRequest2[] | undefined;
    /** Who can read this entry besides the caller. Defaults to Private. */
    visibility?: LibraryVisibility;
}

/** Detailed nutrition plan response including all weeks, days, meals, and foods. */
export interface GetPlanResponse {
    /** Plan's public identifier. */
    planId?: string;
    /** The client's ClientProfile.PublicId — the client-facing identifier consumed by
web/mobile to build routes like /trainer/clients/{clientId}/.... NOT the
internal Mongo storage key (ApplicationUser.Id since #840) — see
FromDocument. */
    clientId?: string;
    /** Nutritionist's public user identifier. */
    nutritionistId?: string;
    /** Display name of the plan. */
    name?: string;
    /** Current plan status as string (Draft, Active, Archived). */
    status?: string;
    /** Global daily nutrition targets. */
    globalSettings?: GlobalNutritionSettings | undefined;
    /** All weeks in the plan with their days, meals, and foods. */
    weeks?: PlanWeek[];
    /** Optimistic concurrency version. */
    version?: number;
    /** When the plan was created. */
    dateCreated?: string;
    /** When the plan was last updated. */
    dateUpdated?: string | undefined;
    /** The Monday when Week 1 begins, if set. */
    startDate?: string | undefined;
    /** When this plan was marked as completed, if applicable. */
    dateCompleted?: string | undefined;
    /** Linked questionnaire response (cross-DB reference to PostgreSQL QuestionnaireResponse.PublicId).
Null if no questionnaire is linked to this plan. */
    questionnaireResponseId?: string | undefined;
    /** Per-meal eaten state for all MealLog documents associated with this plan.
One entry per (MealId, LogDate) pair that has a log record; meals without a log entry
are absent (equivalent to not-touched / no badge).
Populated by the endpoint after loading the plan. Ownership is guaranteed by the
plan ownership gate above the MealLog query — filtering by PlanId is safe. */
    mealLogs?: MealLogDto[];
    /** Supplement recommendations attached to this plan. */
    supplements?: SupplementDto[];
}

/** A single week within a nutrition plan. */
export interface PlanWeek {
    /** Week number within the plan (1-based). */
    weekNumber?: number;
    /** Publish status of this week (Draft or Published). */
    status?: WeekStatus;
    /** When this week was published (status changed to Published). */
    datePublished?: string | undefined;
    /** Days in this week. */
    days?: PlanDay[];
}

/** Per-meal eaten state derived from a MealLog document. Lets the web layer render eaten/not-touched indicators and lock editing affordances on meals the client has already confirmed as eaten. */
export interface MealLogDto {
    /** The MealId this log belongs to. */
    mealId?: string;
    /** The calendar date (UTC) this log entry belongs to, as a date-only value.
Matches LogDate truncated to day precision. */
    logDate?: string;
    /** Whether the meal has been confirmed as eaten by the client.
true iff EatenAt was non-null in the document.
A log with EatenAt == null is a photo-only or note-only stub —
it is NOT considered eaten. */
    isEaten?: boolean;
    /** When the meal was eaten, if eaten. Null for photo-only / note-only stubs. */
    eatenAt?: string | undefined;
}

/** A supplement entry in a nutrition plan response DTO. */
export interface SupplementDto {
    /** Stable public identifier for the supplement. */
    externalId?: string;
    /** Name of the supplement. */
    name?: string;
    /** Optional dosage instruction. */
    dose?: string | undefined;
    /** Optional additional notes. */
    notes?: string | undefined;
}

/** Request for a full-state update of a nutrition plan: replaces name, settings, weeks/days/meals/foods, and supplements. */
export interface UpdatePlanRequest {
    /** Updated display name. */
    name: string;
    /** Updated global daily nutrition targets. */
    globalSettings?: GlobalNutritionSettings | undefined;
    /** Expected version for optimistic concurrency control. */
    version?: number;
    /** Full week structure to persist. Replaces all existing weeks, days, meals, and foods. */
    weeks: UpdateWeekRequest[];
    /** Updated start date. Must be a Monday and not in the past.
Null clears the start date (only if it hasn't arrived and no weeks are published). */
    startDate?: string | undefined;
    /** Full supplement list to persist. Replaces all existing supplements.
Omitting an entry removes that supplement (full-state replace pattern). */
    supplements?: UpdateSupplementRequest[];
    /** Optional primary fitness goal for this plan period.
When set, read sites prefer this value over the client's onboarding baseline. */
    goal?: PrimaryGoal | undefined;
    /** Optional target body weight in kilograms for this plan period.
Must be greater than zero when provided. */
    targetWeightKg?: number | undefined;
}

/** Represents a single week submitted in a full-state plan update. */
export interface UpdateWeekRequest {
    /** Week number within the plan (1-based). */
    weekNumber?: number;
    /** Days in this week. */
    days?: UpdateDayRequest[];
}

/** Represents a single day submitted in a full-state plan update. */
export interface UpdateDayRequest {
    /** Day of week (1 = Monday … 7 = Sunday). */
    dayOfWeek?: number;
    /** Optional note for this day. */
    note?: string | undefined;
    /** Meals planned for this day. */
    meals?: UpdateMealRequest[];
}

/** Represents a single meal submitted in a full-state plan update. */
export interface UpdateMealRequest {
    /** Optional existing meal identifier. When provided the meal retains its identity;
when null a new Guid is generated. */
    mealId?: string | undefined;
    /** Kind of meal (Breakfast, Lunch, Dinner, etc.). */
    kind?: MealKind;
    /** Display order within the day (1-based). */
    order?: number;
    /** Suggested time for the meal (e.g. "08:00"). */
    time?: string | undefined;
    /** Optional note for this meal. */
    note?: string | undefined;
    /** Foods included in this meal. */
    foods?: UpdateMealFoodRequest[];
    /** Recipes included in this meal. */
    recipes?: UpdateMealRecipeRequest[];
}

/** Represents a single food entry submitted in a full-state plan update. */
export interface UpdateMealFoodRequest {
    /** External (public) identifier of the food item. */
    foodExternalId?: string;
    /** Display name of the food (snapshot at time of planning). */
    foodName?: string;
    /** Czech name. */
    foodNameCs?: string | undefined;
    /** English name. */
    foodNameEn?: string | undefined;
    /** German name. */
    foodNameDe?: string | undefined;
    /** Food category snapshot. */
    foodCategory?: string | undefined;
    /** Nutrient values per 100 grams for this food. */
    nutrientValuePer100Grams?: NutrientValue;
    /** Amount in grams to include in this meal. */
    amountGrams?: number;
    /** Optional note for this food in the plan. */
    note?: string | undefined;
}

/** Represents a recipe entry submitted in a full-state plan update. */
export interface UpdateMealRecipeRequest {
    /** Recipe identifier. */
    recipeId?: string;
    /** Display name of the recipe. */
    recipeName?: string;
    /** Nutrient values per one serving. */
    nutrientValuePerServing?: NutrientValue;
    /** Number of servings. */
    servings?: number;
    /** Optional note. */
    note?: string | undefined;
    /** Distinct food categories from the recipe's ingredients (snapshot). */
    foodCategories?: string[] | undefined;
}

/** Represents a single supplement entry submitted in a full-state plan update. */
export interface UpdateSupplementRequest {
    /** Stable public identifier for this supplement. Clients generate this on creation
and send it back unchanged on subsequent updates so mobile reminder keys survive round-trips.
When empty, the endpoint generates a new Guid. */
    externalId?: string | undefined;
    /** Name of the supplement (e.g. "Vitamin D3"). Required. */
    name?: string;
    /** Optional dosage instruction in free text (e.g. "1 capsule with breakfast"). */
    dose?: string | undefined;
    /** Optional additional notes for the client. */
    notes?: string | undefined;
}

/** Request to publish a specific week of a nutrition plan. */
export interface PublishWeekRequest {
    /** Optimistic concurrency version. */
    version?: number;
}

/** Request to link or unlink a questionnaire response to/from a nutrition plan. */
export interface LinkQuestionnaireRequest2 {
    /** Questionnaire response PublicId to link. Set to null to unlink. */
    questionnaireResponseId?: string | undefined;
    /** Optimistic concurrency version. */
    version?: number;
}

/** Paginated response containing a list of nutrition plan summaries. */
export interface GetPlansResponse {
    /** List of plan summaries for the current page. */
    plans?: PlanSummaryDto[];
    /** Total number of plans matching the filter. */
    totalCount?: number;
    /** Current page number. */
    page?: number;
    /** Number of items per page. */
    pageSize?: number;
}

/** Lightweight plan summary for list views. */
export interface PlanSummaryDto {
    /** Plan's public identifier. */
    planId?: string;
    /** Display name. */
    name?: string;
    /** The client's ClientProfile.PublicId — the client-facing identifier consumed by
web/mobile to build routes like /trainer/clients/{clientId}/.... NOT the
internal Mongo storage key (ApplicationUser.Id since #840) — see
FromDocument. */
    clientId?: string;
    /** Current status as string (Draft, Active, Archived). */
    status?: string;
    /** Number of weeks. */
    weekCount?: number;
    /** Optimistic concurrency version. */
    version?: number;
    /** When the plan was created. */
    dateCreated?: string;
    /** When the plan was last updated. */
    dateUpdated?: string | undefined;
    /** The Monday when Week 1 begins, if set. */
    startDate?: string | undefined;
    /** When this plan was marked as completed, if applicable. */
    dateCompleted?: string | undefined;
    /** Linked questionnaire response ID, if any. */
    questionnaireResponseId?: string | undefined;
}

/** Request to list nutrition plans with optional filters and pagination. */
export interface GetPlansRequest {
}

/** Status of a nutrition plan. */
export enum NutritionPlanStatus {
    Draft = "Draft",
    Active = "Active",
    Completed = "Completed",
    Archived = "Archived",
}

/** Request to retrieve a single nutrition plan by its public identifier. */
export interface GetPlanRequest {
}

/** Request to soft-delete (archive) a nutrition plan. */
export interface DeletePlanRequest {
}

/** Request to create a new nutrition plan for a client. */
export interface CreatePlanRequest {
    /** The client's public user identifier. */
    clientId: string;
    /** Display name of the plan. */
    name: string;
    /** Optional global daily nutrition targets. */
    globalSettings?: GlobalNutritionSettings | undefined;
    /** Number of weeks to initialize (default 1). */
    weekCount?: number;
    /** Optional start date for the plan. Must be a Monday and not in the past.
Transmitted as ISO date string (e.g. "2026-03-30"), stored as midnight UTC. */
    startDate?: string | undefined;
    /** Optional questionnaire response to link to this plan (cross-DB reference).
Must be a submitted response owned by this professional for the same client. */
    questionnaireResponseId?: string | undefined;
    /** Optional primary fitness goal for this plan period.
When set, read sites prefer this value over the client's onboarding baseline. */
    goal?: PrimaryGoal | undefined;
    /** Optional target body weight in kilograms for this plan period.
Must be greater than zero when provided. */
    targetWeightKg?: number | undefined;
}

/** Request to mark a nutrition plan as completed. */
export interface CompletePlanRequest {
    /** Optimistic concurrency version. */
    version?: number;
}

/** Response model showing the calculated BMR, TDEE, and macro targets. */
export interface CalculateGoalsResponse {
    /** Calculated Basal Metabolic Rate (Mifflin-St Jeor) in kcal/day. */
    bmr?: number;
    /** Total Daily Energy Expenditure in kcal/day. */
    tdee?: number;
    /** Adjusted daily kcal after applying the goal (cut/maintain/bulk). */
    adjustedKcal?: number;
    /** Calculated macro split with daily targets. */
    macroTargets?: GlobalNutritionSettings;
}

/** Request model for calculating nutrition goals based on client anamnesis. */
export interface CalculateGoalsRequest {
    /** Body weight in kilograms. */
    weightKg?: number;
    /** Height in centimeters. */
    heightCm?: number;
    /** Age in years. */
    age?: number;
    /** Biological sex for BMR calculation. */
    sex: string;
    /** Physical activity level. */
    activityLevel: string;
    /** Nutrition goal (Cut, Maintain, Bulk). */
    goal: string;
    /** Custom protein percentage (default 30). */
    proteinPercent?: number;
    /** Custom carbs percentage (default 45). */
    carbsPercent?: number;
    /** Custom fat percentage (default 25). */
    fatPercent?: number;
}

export interface UnarchiveConversationRequest {
}

export interface ConversationDto {
    id?: string;
    participant?: ParticipantDto;
    lastMessage?: string;
    lastMessageAt?: string;
    lastMessageIsOwn?: boolean;
    unreadCount?: number;
    isFormer?: boolean;
}

export interface ParticipantDto {
    id?: string;
    name?: string;
    initials?: string;
    online?: boolean;
    /** Avatar URL for the participant. For professionals, prefers the professional-profile
avatar; falls back to the user-level avatar. For clients, uses the user-level avatar.
Null when neither has been uploaded. */
    avatarBlobUrl?: string | undefined;
}

export interface StartConversationRequest {
    /** The PublicId of the participant's profile (ClientProfile.PublicId or ProfessionalProfile.PublicId). */
    participantId: string;
}

export interface SendMessageResponse {
    id?: string;
    senderId?: string;
    text?: string;
    timestamp?: string;
    isRead?: boolean;
}

export interface SendMessageRequest {
    text: string;
}

export interface MarkReadRequest {
}

export interface GetMessagesResponse {
    items?: MessageDto[];
    cursor?: string | undefined;
}

export interface MessageDto {
    id?: string;
    senderId?: string;
    text?: string;
    timestamp?: string;
    isRead?: boolean;
}

export interface GetMessagesRequest {
}

export interface GetConversationsRequest {
}

export interface ConversationContextResponse {
    type?: string;
    inviteId?: string | undefined;
    icon?: string;
    title?: string;
    sub?: string;
    actionLabel?: string;
    actionRoute?: string;
}

export interface GetConversationContextRequest {
}

export interface ArchiveConversationRequest {
}

/** Full meal template detail returned by get, create, update, copy, and from-plan endpoints. */
export interface MealTemplateDetailResponse {
    /** Public identifier of the meal template. */
    templateId?: string;
    /** Display name of the saved meal. */
    name?: string;
    /** Optional description. */
    description?: string | undefined;
    /** Optional hint for which meal slot this template suits — not a constraint. */
    kind?: MealKind | undefined;
    /** Foods in this meal — the same snapshot shape used inside a nutrition plan, so this
response can be dropped directly into an UpdatePlan request's meal. */
    foods?: MealFood[];
    /** Recipes in this meal — the same snapshot shape used inside a nutrition plan. */
    recipes?: MealRecipe[];
    /** Server-computed total macronutrients across Foods and Recipes. */
    totalNutrients?: NutrientTotals;
    /** Who can read this template besides its owner. */
    visibility?: LibraryVisibility;
    /** True when the authenticated caller is the nutritionist who owns this template. */
    isOwnedByCurrentUser?: boolean;
    /** When the template was created. */
    dateCreated?: string;
    /** When the template was last updated. */
    dateUpdated?: string | undefined;
    /** Optimistic concurrency version, required on PUT requests. */
    version?: number;
}

/** Request model for updating an existing meal template. */
export interface UpdateMealTemplateRequest {
    /** Updated display name. */
    name: string;
    /** Updated description. */
    description?: string | undefined;
    /** Updated meal-slot hint — not a constraint. */
    kind?: MealKind | undefined;
    /** Updated foods — the existing MealFood snapshot shape, verbatim. */
    foods?: MealFood[];
    /** Updated recipes — the existing MealRecipe snapshot shape, verbatim. */
    recipes?: MealRecipe[];
    /** Updated visibility. */
    visibility?: LibraryVisibility;
    /** The version the caller last read. Used for optimistic-concurrency CAS; a stale value
returns 409. */
    version?: number;
}

/** Paginated response for meal template search results. */
export interface SearchMealTemplatesResponse {
    /** List of matching meal templates. */
    templates?: MealTemplateSummaryDto[];
    /** Total number of matching templates. */
    totalCount?: number;
    /** Current page number. */
    page?: number;
    /** Number of items per page. */
    pageSize?: number;
}

/** Lightweight meal template summary for search/list views. */
export interface MealTemplateSummaryDto {
    /** Public identifier of the meal template. */
    templateId?: string;
    /** Display name of the saved meal. */
    name?: string;
    /** Optional description. */
    description?: string | undefined;
    /** Optional hint for which meal slot this template suits — not a constraint. */
    kind?: MealKind | undefined;
    /** Number of foods in this template. */
    foodCount?: number;
    /** Number of recipes in this template. */
    recipeCount?: number;
    /** Server-computed total macronutrients. */
    totalNutrients?: NutrientTotals;
    /** Who can read this template besides its owner. */
    visibility?: LibraryVisibility;
    /** True when the authenticated caller is the nutritionist who owns this template. */
    isOwnedByCurrentUser?: boolean;
    /** When the template was created. */
    dateCreated?: string;
}

/** Request model for searching meal templates. */
export interface SearchMealTemplatesRequest {
}

/** Request model for saving a meal template from an existing nutrition plan meal. */
export interface SaveMealTemplateFromPlanRequest {
    /** The source nutrition plan's public identifier (NutritionPlan.ExternalId) — not
the client id. */
    planId: string;
    /** The week number (1-based) within the plan that contains the source meal. */
    weekNumber?: number;
    /** The day of week (1 = Monday … 7 = Sunday) within the week that contains the source meal. */
    dayOfWeek?: number;
    /** Identifier of the source PlanMeal within the addressed day. */
    mealId: string;
    /** Display name for the new template. */
    name: string;
    /** Optional description for the new template. */
    description?: string | undefined;
    /** Who can read the new template besides the caller. */
    visibility?: LibraryVisibility;
}

/** Request model for retrieving a single meal template. */
export interface GetMealTemplateRequest {
}

/** Request model for deleting a meal template. */
export interface DeleteMealTemplateRequest {
}

/** Request model for creating a new meal template. */
export interface CreateMealTemplateRequest {
    /** Display name of the saved meal. */
    name: string;
    /** Optional description. */
    description?: string | undefined;
    /** Optional hint for which meal slot this template suits — not a constraint. */
    kind?: MealKind | undefined;
    /** Foods to include — the existing MealFood snapshot shape, verbatim. */
    foods?: MealFood[];
    /** Recipes to include — the existing MealRecipe snapshot shape, verbatim. */
    recipes?: MealRecipe[];
    /** Who can read this template besides the caller. Defaults to
Private when omitted. */
    visibility?: LibraryVisibility;
}

/** Request model for copying a readable meal template to the caller's own library. */
export interface CopyMealTemplateRequest {
}

/** Response model containing the pre-signed upload URL and the permanent blob URL. */
export interface UploadFoodImageUrlResponse {
    /** Time-limited pre-signed URL the client should PUT the image file to. */
    uploadUrl?: string;
    /** Permanent blob URL at which the food image will be accessible after a successful upload.
Always starts with foods/. */
    blobUrl?: string;
}

/** Request model for generating a pre-signed upload URL for a food image. */
export interface UploadFoodImageUrlRequest {
    /** MIME type of the image file (e.g. "image/jpeg", "image/png", "image/webp", "image/heic", "image/heif") */
    contentType: string;
    /** Declared file size in bytes. Must not exceed 5 MiB. */
    sizeBytes?: number;
}

/** Common response DTO for food items used across multiple endpoints. */
export interface FoodSummary {
    /** Public-facing food identifier. */
    foodId?: string;
    /** Resolved food name for display (localized based on Accept-Language). */
    name?: string;
    /** Original (canonical) food name, unaffected by language resolution. */
    rawName?: string;
    /** English name, if available. */
    nameEn?: string | undefined;
    /** Czech name, if available. */
    nameCs?: string | undefined;
    /** German name, if available. */
    nameDe?: string | undefined;
    /** Nutritional values per 100 grams. */
    nutrientValue?: NutrientValueDto;
    /** Allergen identifiers. */
    allergens?: string[];
    /** Common serving sizes. */
    commonServings?: ServingSizeDto[];
    /** Food category. */
    category?: FoodCategory;
    /** Optional user note. */
    note?: string | undefined;
    /** Visibility of the food (Public = visible to all nutritionists, Private = visible only to its creator). */
    visibility?: FoodVisibility;
    /** URL of the food image in blob storage (e.g. foods/{foodId}.jpg).
Null when no image has been uploaded. */
    imageUrl?: string | undefined;
    /** URLs of gallery images (up to 6 entries). Each entry points to a blob at
foods/{foodId}/gallery-{n}.{ext}. */
    galleryImageUrls?: string[];
    /** True when the authenticated caller is the nutritionist who created this food.
Clients can use this flag to decide whether to show edit/delete affordances. */
    isOwnedByCurrentUser?: boolean;
}

/** Nutrient values DTO for API responses. */
export interface NutrientValueDto {
    /** Energy in kilocalories per 100 grams. */
    kcal?: number;
    /** Protein in grams per 100 grams. */
    protein?: number;
    /** Carbohydrates in grams per 100 grams. */
    carbs?: number;
    /** Fat in grams per 100 grams. */
    fat?: number;
    /** Fiber in grams per 100 grams. */
    fiber?: number | undefined;
    /** Sugar in grams per 100 grams. */
    sugar?: number | undefined;
    /** Saturated fat in grams per 100 grams. */
    saturatedFat?: number | undefined;
    /** Salt in grams per 100 grams. */
    salt?: number | undefined;
}

/** Serving size DTO for API responses. */
export interface ServingSizeDto {
    /** Human-readable label. */
    label?: string;
    /** Weight in grams. */
    weightGrams?: number;
}

/** Category of a food item. */
export enum FoodCategory {
    Other = "Other",
    Fruit = "Fruit",
    Vegetables = "Vegetables",
    Meat = "Meat",
    FishAndSeafood = "FishAndSeafood",
    Dairy = "Dairy",
    GrainsAndCereals = "GrainsAndCereals",
    Legumes = "Legumes",
    NutsAndSeeds = "NutsAndSeeds",
    OilsAndFats = "OilsAndFats",
    SweetsAndSnacks = "SweetsAndSnacks",
    Beverages = "Beverages",
    Supplements = "Supplements",
}

/** Controls who can see a custom food item. */
export enum FoodVisibility {
    Public = "Public",
    Private = "Private",
}

/** Request model for updating a custom food. */
export interface UpdateFoodRequest {
    /** Updated food name. */
    name: string;
    /** Updated English name. */
    nameEn?: string | undefined;
    /** Updated Czech name. */
    nameCs?: string | undefined;
    /** Updated German name. */
    nameDe?: string | undefined;
    /** Updated nutritional values per 100 grams. */
    nutrientValue?: NutrientValueDto;
    /** Updated food category. */
    category?: FoodCategory;
    /** Updated visibility. When omitted, the food's existing visibility is preserved.
Only the food's creator can change this value. */
    visibility?: FoodVisibility | undefined;
    /** Updated user note. */
    note?: string | undefined;
    /** Updated allergen identifiers. */
    allergens?: string[];
    /** Updated common serving sizes. */
    commonServings?: ServingSizeDto[];
}

/** Response model for food search results. */
export interface SearchFoodsResponse {
    /** List of matching food items. */
    foods?: FoodSummary[];
    /** Total number of matching foods. */
    totalCount?: number;
    /** Current page number. */
    page?: number;
    /** Number of items per page. */
    pageSize?: number;
}

/** Request model for searching foods. */
export interface SearchFoodsRequest {
}

/** Request model for retrieving a single food by its external ID. */
export interface GetFoodRequest {
}

/** Response model for a nutritionist's custom foods. */
export interface GetCustomFoodsResponse {
    /** List of custom food items. */
    foods?: FoodSummary[];
    /** Total number of custom foods. */
    totalCount?: number;
    /** Current page number. */
    page?: number;
    /** Number of items per page. */
    pageSize?: number;
}

/** Request model for retrieving a nutritionist's custom foods. */
export interface GetCustomFoodsRequest {
}

/** Request model for soft-deleting a custom food. */
export interface DeleteFoodRequest {
}

/** Request model for creating a custom food. */
export interface CreateFoodRequest {
    /** Name of the food item. */
    name: string;
    /** Optional English name. */
    nameEn?: string | undefined;
    /** Optional Czech name. */
    nameCs?: string | undefined;
    /** Optional German name. */
    nameDe?: string | undefined;
    /** Nutritional values per 100 grams. */
    nutrientValue?: NutrientValueDto;
    /** Food category. */
    category?: FoodCategory;
    /** Visibility of the food. Defaults to Public when omitted. */
    visibility?: FoodVisibility;
    /** Optional user note. */
    note?: string | undefined;
    /** Allergen identifiers. */
    allergens?: string[];
    /** Common serving sizes. */
    commonServings?: ServingSizeDto[];
}

/** Request model for confirming the uploaded food image blob URL. */
export interface ConfirmFoodImageRequest {
    /** The permanent blob URL returned by POST /foods/{id}/image/upload-url. */
    blobUrl: string;
}

/** Lightweight exercise DTO for list views. */
export interface ExerciseSummary {
    /** Public-facing exercise identifier. */
    exerciseId?: string;
    /** Resolved exercise name for display (localized based on Accept-Language). */
    name?: string;
    /** Original (canonical) exercise name, unaffected by language resolution. */
    rawName?: string;
    /** English name, if available. */
    nameEn?: string | undefined;
    /** Czech name, if available. */
    nameCs?: string | undefined;
    /** German name, if available. */
    nameDe?: string | undefined;
    /** Target muscle groups. First element is the primary muscle group. */
    muscleGroups?: MuscleGroup[];
    /** Equipment required for the exercise. */
    equipment?: ExerciseEquipment;
    /** Category of the exercise. */
    category?: ExerciseCategory;
    /** Difficulty level of the exercise. */
    difficulty?: ExerciseDifficulty;
    /** URL to the video thumbnail image. */
    thumbnailUrl?: string | undefined;
    /** Whether this is a custom exercise created by a trainer. */
    isCustom?: boolean;
    /** Optimistic concurrency version. Clients must echo this back on update/delete. */
    version?: number;
}

/** Target muscle groups for exercises. */
export enum MuscleGroup {
    Chest = "Chest",
    Back = "Back",
    Shoulders = "Shoulders",
    Biceps = "Biceps",
    Triceps = "Triceps",
    Forearms = "Forearms",
    Quadriceps = "Quadriceps",
    Hamstrings = "Hamstrings",
    Glutes = "Glutes",
    Calves = "Calves",
    Abs = "Abs",
    Obliques = "Obliques",
    LowerBack = "LowerBack",
    Traps = "Traps",
    FullBody = "FullBody",
}

/** Equipment required for an exercise. */
export enum ExerciseEquipment {
    None = "None",
    Dumbbells = "Dumbbells",
    Barbell = "Barbell",
    Machine = "Machine",
    TRX = "TRX",
    Kettlebell = "Kettlebell",
    Bodyweight = "Bodyweight",
}

/** Category of exercise. */
export enum ExerciseCategory {
    Strength = "Strength",
    Cardio = "Cardio",
    Mobility = "Mobility",
    Technique = "Technique",
    Warmup = "Warmup",
}

/** Request model for updating a custom exercise. */
export interface UpdateExerciseRequest {
    /** Optimistic concurrency version. Must match the current document version. */
    version?: number;
    /** Canonical name of the exercise. */
    name: string;
    /** Optional English name. */
    nameEn?: string | undefined;
    /** Optional Czech name. */
    nameCs?: string | undefined;
    /** Optional German name. */
    nameDe?: string | undefined;
    /** Optional description of the exercise. */
    description?: string | undefined;
    /** Target muscle groups. First element is the primary muscle group. */
    muscleGroups: MuscleGroup[];
    /** Equipment required for the exercise. */
    equipment?: ExerciseEquipment;
    /** Category of the exercise. */
    category?: ExerciseCategory;
    /** Difficulty level of the exercise. */
    difficulty?: ExerciseDifficulty;
    /** Technique notes in Markdown format. */
    techniqueNotes?: string | undefined;
}

/** Response model for exercise search results. */
export interface SearchExercisesResponse {
    /** List of matching exercises. */
    exercises?: ExerciseSummary[];
    /** Total number of matching exercises. */
    totalCount?: number;
    /** Current page number. */
    page?: number;
    /** Number of items per page. */
    pageSize?: number;
}

/** Request model for searching exercises. */
export interface SearchExercisesRequest {
}

/** Full exercise detail DTO including video and technique information. */
export interface ExerciseDetail {
    /** Public-facing exercise identifier. */
    exerciseId?: string;
    /** Resolved exercise name for display (localized based on Accept-Language). */
    name?: string;
    /** Original (canonical) exercise name, unaffected by language resolution. */
    rawName?: string;
    /** English name, if available. */
    nameEn?: string | undefined;
    /** Czech name, if available. */
    nameCs?: string | undefined;
    /** German name, if available. */
    nameDe?: string | undefined;
    /** Optional description of the exercise. */
    description?: string | undefined;
    /** Target muscle groups. First element is the primary muscle group. */
    muscleGroups?: MuscleGroup[];
    /** Equipment required for the exercise. */
    equipment?: ExerciseEquipment;
    /** Category of the exercise. */
    category?: ExerciseCategory;
    /** Difficulty level of the exercise. */
    difficulty?: ExerciseDifficulty;
    /** URL to the exercise demonstration video. */
    videoUrl?: string | undefined;
    /** URL to the video thumbnail image. */
    thumbnailUrl?: string | undefined;
    /** Technique notes in Markdown format. */
    techniqueNotes?: string | undefined;
    /** Whether this is a custom exercise created by a trainer. */
    isCustom?: boolean;
    /** Data source: "system" or "custom". */
    source?: string;
    /** Optimistic concurrency version. Clients must echo this back on update/delete. */
    version?: number;
}

/** Request model for retrieving a single exercise. */
export interface GetExerciseRequest {
}

/** Response model for custom exercise listing. */
export interface GetCustomExercisesResponse {
    /** List of custom exercises. */
    exercises?: ExerciseSummary[];
    /** Total number of custom exercises. */
    totalCount?: number;
    /** Current page number. */
    page?: number;
    /** Number of items per page. */
    pageSize?: number;
}

/** Request model for retrieving custom exercises. */
export interface GetCustomExercisesRequest {
}

/** Response model containing the pre-signed upload URL. */
export interface GenerateUploadUrlResponse {
    /** The pre-signed URL the client should PUT the video file to. */
    uploadUrl?: string;
    /** The permanent URL where the video will be accessible after upload. */
    videoUrl?: string;
}

/** Request model for generating a video upload URL. */
export interface GenerateUploadUrlRequest {
    /** Content type of the video file (e.g. "video/mp4"). */
    contentType?: string;
}

/** Request model for deleting a custom exercise. */
export interface DeleteExerciseRequest {
    /** Optimistic concurrency version. Must match the current document version. */
    version?: number;
}

/** Request model for creating a custom exercise. */
export interface CreateExerciseRequest {
    /** Canonical name of the exercise. */
    name: string;
    /** Optional English name. */
    nameEn?: string | undefined;
    /** Optional Czech name. */
    nameCs?: string | undefined;
    /** Optional German name. */
    nameDe?: string | undefined;
    /** Optional description of the exercise. */
    description?: string | undefined;
    /** Target muscle groups. First element is the primary muscle group. */
    muscleGroups: MuscleGroup[];
    /** Equipment required for the exercise. */
    equipment?: ExerciseEquipment;
    /** Category of the exercise. */
    category?: ExerciseCategory;
    /** Difficulty level of the exercise. */
    difficulty?: ExerciseDifficulty;
    /** Technique notes in Markdown format. */
    techniqueNotes?: string | undefined;
}

/** Request model for saving the complete photo and note state of a training session diary entry. The Photos list and Note are replaced with exactly what the client sends. */
export interface SaveSessionPhotosRequest {
    /** The complete list of photos to persist on the session log.
Replaces the existing Photos list entirely — pass an empty list to remove all photos.
Each item carries the blob URL and an optional per-photo caption.
Existing URLs that are re-submitted keep their original UploadedAt
timestamp; new URLs receive the current UTC time. */
    photos?: SessionPhotoInput[];
    /** Optional free-text note to persist on the session log entry (max 500 chars).
When non-null, the stored note is replaced with the trimmed value
(whitespace-only strings are treated as null). When null, the existing note is cleared. */
    note?: string | undefined;
}

/** A single photo input in a SaveSessionPhotosRequest. */
export interface SessionPhotoInput {
    /** The MinIO blob URL for this photo, as returned by the signed-URL upload helper. */
    blobUrl?: string;
    /** Optional per-photo caption (max 500 chars). */
    note?: string | undefined;
}

/** Response for un-marking a workout as complete. */
export interface MarkWorkoutIncompleteResponse {
    /** The session ID that was updated. */
    sessionId?: string;
    /** The workout ID that was un-marked. */
    workoutId?: string;
    /** The date for which the completion was removed. */
    date?: string;
    /** How many exercises in this session are now marked complete. */
    completedExerciseCount?: number;
    /** Total number of exercises in this session (from the plan). */
    totalExerciseCount?: number;
    /** Current version of the underlying completion document (for subsequent writes). */
    version?: number;
}

/** Request model for un-marking a workout as complete. */
export interface MarkWorkoutIncompleteRequest {
    /** The date for which the completion should be removed (UTC date only).
Defaults to today UTC when not provided. */
    completedOn?: string | undefined;
    /** Client-supplied version of the completion document for optimistic concurrency. */
    version?: number | undefined;
}

/** Response for marking a workout complete. Returns a lightweight progress summary so the mobile client can update workout progress indicators without an extra round-trip. */
export interface MarkWorkoutCompleteResponse {
    /** The session ID that was updated. */
    sessionId?: string;
    /** The workout ID that was marked complete. */
    workoutId?: string;
    /** The date for which the completion was recorded. */
    date?: string;
    /** How many exercises in this session are now marked complete. */
    completedExerciseCount?: number;
    /** Total number of exercises in this session (from the plan). */
    totalExerciseCount?: number;
    /** Current version of the underlying completion document (for subsequent writes). */
    version?: number;
}

/** Request model for marking a workout complete within a session. Used for workouts that have no exercises (e.g. a ForTime "Running" workout). */
export interface MarkWorkoutCompleteRequest {
    /** The date on which the workout was completed (UTC date only).
Defaults to today UTC when not provided. */
    completedOn?: string | undefined;
    /** Client-supplied version of the existing completion document, used for optimistic
concurrency. Required when a completion document already exists for this
(clientId, date, sessionId) tuple; ignored for new documents. */
    version?: number | undefined;
}

/** Response for marking all training sessions on a day complete. */
export interface MarkWholeDayCompleteResponse {
    /** The date that was marked complete. */
    date?: string;
    /** Summary of each session that was processed. */
    sessions?: SessionCompletionSummary[];
}

/** Per-session completion summary returned by MarkWholeDayCompleteResponse. */
export interface SessionCompletionSummary {
    /** The session ID. */
    sessionId?: string;
    /** Number of exercises marked complete in this session. */
    completedExerciseCount?: number;
    /** Total exercises in the session. */
    totalExerciseCount?: number;
    /** Current document version for subsequent writes. */
    version?: number;
}

/** Request model for marking all training sessions on a given day complete. */
export interface MarkWholeDayCompleteRequest {
    /** The date to mark complete (UTC date only).
Defaults to today UTC when not provided. */
    date?: string | undefined;
}

/** Response for un-marking a training session as complete. */
export interface MarkSessionIncompleteResponse {
    /** The session ID that was updated. */
    sessionId?: string;
    /** The date for which the completion was removed. */
    date?: string;
    /** How many exercises in this session are still marked complete. */
    completedExerciseCount?: number;
    /** Total exercises in the session. */
    totalExerciseCount?: number;
    /** Current document version. */
    version?: number;
}

/** Request model for un-marking an entire session as complete. */
export interface MarkSessionIncompleteRequest {
    /** The date for which the completion should be removed (UTC date only).
Defaults to today UTC when not provided. */
    completedOn?: string | undefined;
    /** Client-supplied version for optimistic concurrency. */
    version?: number | undefined;
}

/** Response for marking a whole session complete. */
export interface MarkSessionCompleteResponse {
    /** The session ID that was marked complete. */
    sessionId?: string;
    /** The date for which the session was marked complete. */
    date?: string;
    /** Number of exercises now marked complete. */
    completedExerciseCount?: number;
    /** Total exercises in the session. */
    totalExerciseCount?: number;
    /** Current document version. */
    version?: number;
}

/** Request model for marking an entire session complete (fans out to all exercises). */
export interface MarkSessionCompleteRequest {
    /** The date on which the session was completed (UTC date only).
Defaults to today UTC when not provided. */
    completedOn?: string | undefined;
    /** Client-supplied version for optimistic concurrency when updating an existing completion document. */
    version?: number | undefined;
}

/** Response for un-marking an exercise as complete. */
export interface MarkExerciseIncompleteResponse {
    /** The session ID that was updated. */
    sessionId?: string;
    /** The date for which the completion was removed. */
    date?: string;
    /** How many exercises in this session are still marked complete. */
    completedExerciseCount?: number;
    /** Total number of exercises in this session (from the plan). */
    totalExerciseCount?: number;
    /** Whether every exercise in this session is still complete. */
    sessionComplete?: boolean;
    /** Current version of the underlying completion document. */
    version?: number;
}

/** Request model for un-marking a single exercise as complete. */
export interface MarkExerciseIncompleteRequest {
    /** The date for which the completion should be removed (UTC date only).
Defaults to today UTC when not provided. */
    completedOn?: string | undefined;
    /** Client-supplied version of the completion document for optimistic concurrency. */
    version?: number | undefined;
}

/** Response for marking an exercise complete. Returns a lightweight progress summary so the mobile client can update session progress indicators without an extra round-trip. */
export interface MarkExerciseCompleteResponse {
    /** The session ID that was updated. */
    sessionId?: string;
    /** The date for which the completion was recorded. */
    date?: string;
    /** How many exercises in this session are now marked complete. */
    completedExerciseCount?: number;
    /** Total number of exercises in this session (from the plan). */
    totalExerciseCount?: number;
    /** Whether every exercise in this session is now complete. */
    sessionComplete?: boolean;
    /** Current version of the underlying completion document (for subsequent writes). */
    version?: number;
}

/** Request model for marking a single exercise complete within a session. */
export interface MarkExerciseCompleteRequest {
    /** The date on which the exercise was completed (UTC date only).
Defaults to today UTC when not provided. */
    completedOn?: string | undefined;
    /** Client-supplied version of the existing completion document, used for optimistic
concurrency. Required when a completion document already exists for this
(clientId, date, sessionId) tuple; ignored for new documents. */
    version?: number | undefined;
}

/** Response with today's planned training session(s). */
export interface GetTodaySessionResponse {
    /** Whether there is at least one session planned for today. */
    hasSession?: boolean;
    /** The training plan's public ID. */
    planId?: string | undefined;
    /** The plan name. */
    planName?: string | undefined;
    /** All training sessions scheduled for today, ordered by Order.
Empty when there are none. */
    sessions?: TrainingSession[];
    /** The first training session for today, if any.
Deprecated: use Sessions instead. This property mirrors
Sessions[0] (or null when empty) and is kept for backwards
compatibility with consumers that still reference the singular session. */
    session?: TrainingSession | undefined;
    /** Current week number in the plan cycle. */
    currentWeek?: number | undefined;
    /** Total number of weeks in the plan. */
    totalWeeks?: number | undefined;
    /** Plan status (Active, Completed, Archived). */
    status?: string | undefined;
    /** Linked questionnaire response public ID, if any. */
    questionnaireResponseId?: string | undefined;
    /** When this plan was marked as completed, if applicable. */
    dateCompleted?: string | undefined;
    /** Per-exercise muscle groups, keyed by SessionExercise.ExerciseExternalId.
Empty when an exercise no longer exists in the database. Populated by
looking up Exercise documents for every exercise referenced by today's
sessions. */
    exerciseMuscleGroups?: { [key: string]: MuscleGroup[]; };
    /** Per-session completed exercise IDs, keyed by SessionId. Sourced from
TrainingCompletion documents for today. Empty dictionary when no session
has any completed exercise for today (or when no active plan exists).
Deprecated. Use CompletedExerciseIdsByWorkoutAndSession for
workout-aware completion tracking. This field is retained for backward compatibility
with mobile and web clients that have not yet migrated to the new field. */
    completedExerciseIdsBySession?: { [key: string]: string[]; };
    /** Workout-aware completed exercise IDs for today.
Outer key = SessionId, inner key = WorkoutId, value = list of completed ExerciseExternalIds
within that workout.
Sourced from TrainingCompletion documents with read-time backfill for legacy data.
Empty dictionary when no exercises have been completed for today. */
    completedExerciseIdsByWorkoutAndSession?: { [key: string]: { [key: string]: string[]; }; };
    /** Per-session completed workout IDs, keyed by SessionId. Sourced from
TrainingCompletion documents for today. Empty dictionary when no
workout has been workout-completed for today (or no active plan exists).
Workouts appear here when the client tapped a workout-level checkbox
(e.g. on a ForTime "Running" workout that has no exercises) or when
MarkSessionComplete fanned out workout IDs. */
    completedWorkoutIdsBySession?: { [key: string]: string[]; };
    /** Per-session optimistic-concurrency version numbers for today, keyed by SessionId.
Matches the Version on the TrainingCompletion document. Used by the client to
send the If-Match-style version header on subsequent mark/unmark requests.
Missing entries imply a fresh document (server will accept Version=null/1). */
    versionBySession?: { [key: string]: number; };
    /** Per-session, per-exercise completed set numbers for today. Keyed by
SessionId → ExerciseExternalId → list of 1-based SetNumbers whose
CompletedAt is non-null in the latest
WorkoutLog for that session on today's date.
Empty when no live-training progress has been logged for today.
Keeps per-set state out of the planning-document tree (Sessions) which
represents prescription, not actuals. */
    completedSetsBySessionExercise?: { [key: string]: { [key: string]: number[]; }; };
    /** Per-session lock state, keyed by SessionId.
Possible values: "Stable" (no active lock), "Editing" (trainer holds an editing lock),
"Live" (client has an in-progress workout lock).
Missing entries are treated as "Stable".
Populated via a single batch GetStateAsync call on the session lock service. */
    lockStateBySession?: { [key: string]: string; };
    /** Per-session lock holder, keyed by SessionId.
Possible values: "Coach" (trainer/nutritionist holds the lock) or "Client".
Null / missing when the session is in the Stable state. */
    lockHolderBySession?: { [key: string]: string; };
    /** Per-session photo list for today, keyed by SessionId.
Each value is an ordered list of photos that were saved to the session log for today's date.
Sourced from the SessionLog MongoDB document for today.
Empty dictionary when no photos have been saved for any session today (or when no active plan exists). */
    photosBySession?: { [key: string]: SessionPhotoDto[]; };
    /** Per-session diary note for today, keyed by SessionId.
Only populated for sessions whose SessionLog has a non-null, non-empty Note.
Allows the mobile client to pre-load the existing note into its textarea so a subsequent
Save does not overwrite it with null (data-loss prevention).
Empty dictionary when no session has a note today (or when no active plan exists). */
    notesBySession?: { [key: string]: string; };
    /** Per-session, per-exercise logged set values for today.
Keyed by SessionId → ExerciseExternalId → list of LoggedSetDto (one per set).
Carries actual logged values, snapshot-planned values, and the backend-computed isModified flag.
Replaces the set-number-only CompletedSetsBySessionExercise for callers that
need actual vs planned comparison.
Empty when no live-training progress has been logged for today. */
    loggedSetsBySessionExercise?: { [key: string]: { [key: string]: LoggedSetDto[]; }; };
    /** Per-session hasModifications flag for today, keyed by SessionId.
True when any set under any exercise in the session has IsModified == true in the latest log.
Missing entries are treated as false (no modifications / no log). */
    hasModificationsBySession?: { [key: string]: boolean; };
    /** Per-session completed exercise INSTANCE ids for today, keyed by SessionId. Values are
raw ExerciseId values — unlike
CompletedExerciseIdsBySession and
CompletedExerciseIdsByWorkoutAndSession (both keyed on the catalog
ExerciseExternalId), this field lets a client address
completion for one specific placement of an exercise when the same catalog exercise
appears twice in one session (standalone AND nested, or nested twice) (#877).
Union of two sources — read this before consuming the field. The value set is
the union of:
Every id in the session's CompletedExerciseInstanceIds,
carried verbatim — these already identify a single placement.
Performance-derived completion, fanned out to every sibling instance sharing the
same catalog id. WorkoutExercise (the live-training-assistant side of
Performance) carries only ExerciseExternalId
— it has NO instance id, so a fully-logged exercise from a live workout cannot be
attributed to one specific placement. Rather than silently omitting it (which would make
this field strictly LESS complete than the catalog-keyed fields it supersedes, and a
session finished through the live-training assistant would render with no ticks at all),
every ExerciseId in the session whose
ExerciseExternalId matches a fully-logged Performance
exercise is added here too. Concretely: if a session holds catalog exercise X both
standalone and nested in a workout, and the client fully logs X via the live-training
assistant, BOTH instance ids appear in this field — the write path cannot distinguish
which placement was actually performed, so both are reported complete rather than
neither.
Empty dictionary when no active plan exists or no session has any completed exercise for
today. Additive alongside CompletedExerciseIdsBySession and
CompletedExerciseIdsByWorkoutAndSession, which keep their existing
catalog-keyed semantics unchanged. */
    completedExerciseInstanceIdsBySession?: { [key: string]: string[]; };
}

/** A photo attached to a session diary entry, as returned in PhotosBySession. */
export interface SessionPhotoDto {
    /** The MinIO blob URL for this photo. */
    blobUrl?: string;
    /** UTC timestamp when the photo was uploaded/persisted. */
    uploadedAt?: string;
    /** Optional per-photo caption (max 500 chars). Null when none was provided. */
    note?: string | undefined;
}

/** Full training plan response for the client mobile view. Contains all published weeks enriched with completion state and muscle group data. */
export interface GetFullTrainingPlanResponse {
    /** External identifier of the training plan. */
    planId?: string;
    /** Display name of the training plan. */
    planName?: string;
    /** Plan status (Draft, Active, Completed, Archived). */
    status?: string;
    /** The Monday when Week 1 begins, if set. */
    startDate?: string | undefined;
    /** Current week number (null if plan is upcoming). */
    currentWeek?: number | undefined;
    /** Total number of weeks in the plan (including draft). */
    totalWeeks?: number;
    /** Number of published weeks. */
    publishedWeekCount?: number;
    /** Linked questionnaire response public ID, if any. */
    questionnaireResponseId?: string | undefined;
    /** When this plan was marked as completed, if applicable. */
    dateCompleted?: string | undefined;
    /** Published weeks with sessions and completion state. */
    weeks?: WeekDto[];
}

/** A published week within the training plan. */
export interface WeekDto {
    /** 1-based week number. */
    weekNumber?: number;
    /** Week publish status (Draft, Published). */
    status?: string;
    /** When this week was published. */
    datePublished?: string | undefined;
    /** Start date (Monday) of this week, computed from plan.StartDate + (weekNumber-1)*7 days.
Null when no anchor date is available. */
    weekStartDate?: string | undefined;
    /** End date (Sunday) of this week. Null when no anchor date is available. */
    weekEndDate?: string | undefined;
    /** Optional day-level notes keyed by day of week (1=Monday … 7=Sunday). */
    dayNotes?: { [key: string]: string; };
    /** Sessions in this week. */
    sessions?: SessionDto[];
}

/** A single training session within a week. */
export interface SessionDto {
    /** Unique session identifier. */
    sessionId?: string;
    /** Day of the week (1 = Monday, 7 = Sunday). */
    dayOfWeek?: number;
    /** Display name of the session (e.g. "Push Day"). */
    name?: string;
    /** Display order within the day. */
    order?: number;
    /** Optional coach notes for this session. */
    notes?: string | undefined;
    /** Number of exercises where all planned sets are completed. */
    completedExerciseCount?: number;
    /** Total number of exercises in this session. */
    totalExerciseCount?: number;
    /** Estimated session duration in minutes.
Currently null — a reliable heuristic requires product-defined set-duration
assumptions that haven't been finalised. Will be added as an additive change. */
    estimatedDurationMinutes?: number | undefined;
    /** Workouts in this session, ordered by their Order field. Every document is built directly
from Workouts and StandaloneExercises — there is no legacy
flat-exercises shape and no read-time backfill (#857). */
    workouts?: WorkoutDto[];
    /** Read-only flat union of every exercise in this session — standalone exercises plus every
workout's nested exercises — ordered by the ONE shared Order sequence workouts and
standalone exercises occupy within a session (see UpdateTrainingPlanValidator's
cross-list duplicate-Order check). Computed on read only; there is no corresponding member
on the write side, so this field is ignored (not rejected) if present in a PUT body (#874). */
    allExercises?: ExerciseDto[];
    /** Standalone exercises programmed directly on this session — not grouped under any
WorkoutDto (#857 phase 3a). Also included (in shared-Order position) in the
flat AllExercises view above and counted in TotalExerciseCount /
CompletedExerciseCount. */
    standaloneExercises?: ExerciseDto[];
    /** Current lock state of this session.
Possible values: "Stable" (no active lock), "Editing" (trainer holds an editing lock),
"Live" (client has an in-progress workout lock).
Populated via a batch GetStateAsync call on the session lock service. */
    lockState?: string;
    /** Who currently holds the lock, if any.
Possible values: "Coach", "Client", or null when the session is Stable. */
    lockHolder?: string | undefined;
    /** True when at least one exercise in this session has HasModifications == true.
Always false when no workout log exists for this session. */
    hasModifications?: boolean;
}

/** An ordered section within a session (e.g. "Hlavní", "Warm-up", "Cool-down"). */
export interface WorkoutDto {
    /** Stable identifier for this workout. */
    workoutId?: string;
    /** Display order within the session (0-based). */
    order?: number;
    /** Display name (e.g. "Hlavní", "Warm-up"). */
    name?: string;
    /** Workout format for this workout (e.g. "Emom", "Amrap", "Tabata", "ForTime").
Null means the workout uses the default Standard format. */
    format?: string | undefined;
    /** Full format-configuration object for the workout (rounds, intervals, work/rest timings).
Null when Format is null or Standard.
Mirrors WodConfig on TrainingWorkout. */
    formatConfig?: WodConfig | undefined;
    /** Optional coach note for this workout.
Mirrors the Notes property on TrainingWorkout. */
    notes?: string | undefined;
    /** True when this workout is considered complete:
            for workouts with exercises → every exercise has IsCompleted=true;
            for workouts without exercises → the workout's id is in the
            TrainingCompletion.CompletedSectionIds set for the owning session. */
    isCompleted?: boolean;
    /** Exercises within this workout. */
    exercises?: ExerciseDto[];
}

/** An exercise within a session, enriched with muscle groups and completion state. */
export interface ExerciseDto {
    /** Instance identifier for this specific exercise entry within its session — the id the
client-facing mark-complete/incomplete routes require (#857 phase 3b). Distinguishes two
occurrences of the same catalog exercise (ExerciseExternalId) programmed
twice in one workout, or once standalone and once nested in a workout of the same
session. Mirrors ExerciseId. */
    exerciseId?: string;
    /** Reference to the exercise document's ExternalId — used for exercise metadata lookups. */
    exerciseExternalId?: string;
    /** Snapshot exercise name. */
    exerciseName?: string;
    /** Display order within the session. */
    order?: number;
    /** Optional coach notes for this exercise. */
    notes?: string | undefined;
    /** Rest time between sets in seconds. */
    restSeconds?: number | undefined;
    /** Movement type — drives which set field the prescription uses
(reps / duration / distance / reps-for-time). Required for the
client to render the correct summary string. Serialised as the
enum's string name (e.g. "Reps", "Time", "Distance",
"RepsForTime"); the client casts to its `MovementType` enum.
Defaults to "Reps" when the underlying exercise carries no
explicit value. */
    movementType?: string;
    /** Target muscle groups fetched from the Exercise document.
Empty list when the exercise no longer exists in the database. */
    muscleGroups?: MuscleGroup[];
    /** True when this specific exercise instance (ExerciseId) was marked complete
directly (checkbox/instance-keyed completion), OR every planned set has a completed
workout log entry. The instance-keyed check is what lets a set-less exercise ever report
complete, and what lets two placements of the same catalog exercise
(ExerciseExternalId) in one session be completed independently (#877). */
    isCompleted?: boolean;
    /** True when at least one set under this exercise has IsModified == true.
Always false when no workout log exists for this exercise. */
    hasModifications?: boolean;
    /** Planned sets with per-set completion timestamps and actual-vs-planned delta. */
    sets?: SetDto[];
}

/** A planned set with its completion state and actual-vs-planned delta derived from workout logs. */
export interface SetDto {
    /** 1-based set number within the exercise. */
    setNumber?: number;
    /** Set type (Normal, Warmup, Dropset, Superset). */
    type?: string;
    /** Target number of repetitions (from the plan prescription). */
    reps?: number | undefined;
    /** Target weight in kilograms (from the plan prescription). */
    weightKg?: number | undefined;
    /** Target duration in seconds (from the plan prescription). */
    durationSeconds?: number | undefined;
    /** Target distance in meters (from the plan prescription). */
    distanceMeters?: number | undefined;
    /** Rest time after this set in seconds. */
    restSeconds?: number | undefined;
    /** When this set was completed, or null if not yet done. */
    completedAt?: string | undefined;
    /** Actual repetitions logged. Null when not yet performed. */
    actualReps?: number | undefined;
    /** Actual weight (kg) logged. Null when not yet performed. */
    actualWeightKg?: number | undefined;
    /** Actual RPE logged. Null when not yet performed. */
    actualRpe?: number | undefined;
    /** Actual duration (seconds) logged. Null when not yet performed. */
    actualDurationSeconds?: number | undefined;
    /** Actual distance (meters) logged. Null when not yet performed. */
    actualDistanceMeters?: number | undefined;
    /** Snapshot-planned repetitions at log time. Null for legacy logs. */
    plannedReps?: number | undefined;
    /** Snapshot-planned weight (kg) at log time. Null for legacy logs. */
    plannedWeightKg?: number | undefined;
    /** Snapshot-planned RPE at log time. Null for legacy logs. */
    plannedRpe?: number | undefined;
    /** Snapshot-planned duration (seconds) at log time. Null for legacy logs. */
    plannedDurationSeconds?: number | undefined;
    /** Snapshot-planned distance (meters) at log time. Null for legacy logs. */
    plannedDistanceMeters?: number | undefined;
    /** Backend-computed flag: true when any actual field differs from its snapshot-planned
counterpart. Always false for legacy sets (no snapshot → treated as planned == actual). */
    isModified?: boolean;
}

/** Response for the session photo upload URL generation endpoint. */
export interface GenerateSessionPhotoUploadUrlResponse {
    /** Time-limited pre-signed URL the client uses to PUT the image directly to blob storage. */
    uploadUrl?: string;
    /** The permanent blob URL to pass to POST /client/training/log/sessions/{sessionId}/photos
after the upload completes. */
    blobUrl?: string;
}

/** Request model for generating a pre-signed training session photo upload URL. */
export interface GenerateSessionPhotoUploadUrlRequest {
    /** MIME type of the image file (e.g. "image/jpeg", "image/png", "image/webp", "image/heic", "image/heif"). */
    contentType: string;
    /** Declared file size in bytes. Must not exceed 10 MiB. */
    sizeBytes?: number;
}

/** Response model returned after sending a client request. */
export interface SendClientRequestResponse {
    publicId?: string;
    professionalPublicId?: string;
    professionalName?: string;
    message?: string | undefined;
    status?: ClientRequestStatus;
    sentAt?: string;
}

export enum ClientRequestStatus {
    Pending = "Pending",
    Accepted = "Accepted",
    Rejected = "Rejected",
    Cancelled = "Cancelled",
}

/** Request model for sending a client request to a professional. */
export interface SendClientRequestRequest {
    /** Public identifier of the professional to send the request to. */
    professionalPublicId?: string;
    /** Optional message to include with the request. */
    message?: string | undefined;
}

/** Response model containing the client's sent requests. */
export interface GetMyRequestsResponse {
    requests?: ClientRequestDto[];
}

export interface ClientRequestDto {
    publicId?: string;
    professionalPublicId?: string;
    professionalName?: string;
    message?: string | undefined;
    status?: ClientRequestStatus;
    sentAt?: string;
    respondedAt?: string | undefined;
}

/** Response shape for a single PlanPhoto record. Shared across the FinalizePlanPhoto and GetPlanPhotos slices within the ClientPlans feature area — both surfaces expose the same projection. */
export interface PlanPhotoResponse {
    /** Public identifier of the photo record. */
    id?: string;
    /** Permanent blob URL for the photo. */
    blobUrl?: string;
    /** Display / filtering category (Food / Body / FreeForm). */
    category?: PlanPhotoCategory;
    /** Optional caption. */
    description?: string | undefined;
    /** When the photo was taken (or uploaded), in UTC. */
    takenAt?: string;
    /** MongoDB MealLog ObjectId string for food photos. Null for non-food photos. */
    mealLogId?: string | undefined;
    /** External plan identifier this photo belongs to. */
    planId?: string | undefined;
    /** Whether this is a nutrition or training plan photo. */
    planType?: PlanPhotoType | undefined;
    /** When the record was created. */
    dateCreated?: string;
    /** The ApplicationUser.Id of the uploader. */
    uploadedByUserId?: string;
    /** The diary request this photo is associated with, or null if none. */
    diaryRequestId?: string | undefined;
}

/** Categorises a PlanPhoto for display grouping and filtering in the app. Maps to the three chips shown in the Fotky plánu gallery (Jídlo / Tělo / Volné). */
export enum PlanPhotoCategory {
    Food = "Food",
    Body = "Body",
    FreeForm = "FreeForm",
    Training = "Training",
}

/** Identifies whether a PlanPhoto is associated with a nutrition plan or a training plan. */
export enum PlanPhotoType {
    Nutrition = "Nutrition",
    Training = "Training",
}

/** Request model for listing plan photos with optional category filter and pagination. */
export interface GetPlanPhotosRequest {
}

/** Response for the plan photo upload URL generation endpoint. Consistent with the shape used by GenerateMealPhotoUploadUrlEndpoint and GenerateDayPhotoUploadUrlEndpoint. */
export interface GeneratePlanPhotoUploadUrlResponse {
    /** Time-limited pre-signed PUT URL for direct upload to blob storage. */
    uploadUrl?: string;
    /** The permanent blob URL where the image will be accessible after upload.
Pass this to POST /client/plans/{planId}/photos to finalize the record. */
    blobUrl?: string;
}

/** Request model for generating a pre-signed plan photo upload URL. */
export interface GeneratePlanPhotoUploadUrlRequest {
    /** MIME type of the image file (e.g. "image/jpeg", "image/png", "image/webp", "image/heic", "image/heif") */
    contentType: string;
    /** Declared file size in bytes. Must not exceed 5 MiB. */
    sizeBytes?: number;
}

/** Request model for finalizing a plan photo upload by inserting a PlanPhoto row. The caller must have already uploaded the photo to blob storage using the URL from POST /client/plans/{planId}/photos/upload-url. */
export interface FinalizePlanPhotoRequest {
    /** Permanent blob URL returned by the upload-url endpoint. */
    blobUrl: string;
    /** Display / filtering category (Food / Body / FreeForm). */
    category?: PlanPhotoCategory;
    /** Optional caption / description (max 500 chars). */
    description?: string | undefined;
    /** When the photo was taken. Defaults to UtcNow when not provided. */
    takenAt?: string | undefined;
    /** MongoDB MealLog ObjectId string. Required when Category is
Food, otherwise ignored. */
    mealLogId?: string | undefined;
    /** Optional diary request ID. When set, the photo is linked to this diary request.
The diary request must be owned by the calling client and must be in
Accepted or
InProgress status.
On the first upload for an Accepted request the request is transitioned to InProgress. */
    diaryRequestId?: string | undefined;
}

/** Request model for deleting a plan photo by its public identifier. */
export interface DeletePlanPhotoRequest {
}

/** Response for GET /trainer/clients/{id}/photos. Design decision — discriminated response in a single envelope: The endpoint serves two shapes depending on the groupByMonth query flag rather than splitting into two routes, because the filtering/auth logic is identical and a single URL is easier to cache and document. Exactly one of Photos or Groups will be non-null in any response: groupByMonth=false (default): Photos is populated,  Groups is null. Pagination applies to individual photos. groupByMonth=true: Groups is populated,  Photos is null. Pagination applies to month groups (one page = N groups). The X-Total-Count response header always reflects the total count of the active collection (photos or groups). */
export interface GetTrainerClientPhotosResponse {
    /** Flat list of photo records. Populated when groupByMonth=false. */
    photos?: ClientPhotoResponse[] | undefined;
    /** Month-grouped list of photo records. Populated when groupByMonth=true. */
    groups?: MonthGroupResponse[] | undefined;
}

/** DTO representing a single plan photo returned by the aggregation endpoints. Used by both the trainer view (GET /trainer/clients/{id}/photos) and the client self-view (GET /client/me/photos). */
export interface ClientPhotoResponse {
    /** Public identifier of the photo record. */
    id?: string;
    /** URL to the photo in blob storage (MinIO). */
    blobUrl?: string;
    /** Optional caption or description for the photo. */
    description?: string | undefined;
    /** Display / filtering category (Food / Body / FreeForm). */
    category?: PlanPhotoCategory;
    /** External identifier of the linked plan document in MongoDB.
Null for photos not attached to a plan. */
    planId?: string | undefined;
    /** Whether this photo belongs to a Nutrition or Training plan context.
Null when PlanId is null. */
    planType?: PlanPhotoType | undefined;
    /** MongoDB MealLog ObjectId string. Only set when Category is Food. */
    mealLogId?: string | undefined;
    /** When the photo was taken or uploaded, in UTC. */
    takenAt?: string;
    /** The ApplicationUser.Id of the person who uploaded the photo. */
    uploadedByUserId?: string;
    /** When the record was created (upload timestamp), in UTC. */
    uploadedAt?: string;
    /** The diary request this photo is associated with, or null if none. */
    diaryRequestId?: string | undefined;
}

/** Represents a calendar-month bucket of plan photos returned when groupByMonth=true is passed to the aggregation endpoints. */
export interface MonthGroupResponse {
    /** ISO-8601 year-month key derived from TakenAt,
e.g. "2026-04". */
    yearMonth?: string;
    /** All photos whose TakenAt falls within this year-month, ordered by
TakenAt descending. */
    photos?: ClientPhotoResponse[];
}

/** Query parameters for GET /trainer/clients/{ClientId}/photos. */
export interface GetTrainerClientPhotosRequest {
}

/** Response for GET /client/me/photos. Uses the same discriminated-envelope design as the trainer variant: exactly one of Photos or Groups is non-null. */
export interface GetMyPhotosResponse {
    /** Flat list of photo records. Populated when groupByMonth=false. */
    photos?: ClientPhotoResponse[] | undefined;
    /** Month-grouped list of photo records. Populated when groupByMonth=true. */
    groups?: MonthGroupResponse[] | undefined;
}

/** Query parameters for GET /client/me/photos. */
export interface GetMyPhotosRequest {
}

/** Request model for un-logging (removing) a previously logged meal for the current day. */
export interface UnlogMealEatenRequest {
}

/** Request model for saving the complete photo and note state of a meal diary entry. The Photos list and Note are replaced with exactly what the client sends. */
export interface SaveMealPhotosRequest {
    /** The complete list of photos to persist on the meal log.
Replaces the existing Photos list entirely — pass an empty list to remove all
photos. Each item carries the blob URL and an optional per-photo caption.
Existing URLs that are re-submitted keep their original UploadedAt
timestamp; new URLs receive the current UTC time. */
    photos?: MealPhotoInput[];
    /** Optional free-text note to persist on the meal log entry (max 500 chars).
This is the meal-level diary note. When non-null, the stored note is replaced
with the trimmed value (whitespace-only strings are treated as null). When null,
the existing note is cleared. */
    note?: string | undefined;
}

/** A single photo input in a SaveMealPhotosRequest. */
export interface MealPhotoInput {
    /** The MinIO blob URL for this photo, as returned by the signed-URL upload helper. */
    blobUrl?: string;
    /** Optional per-photo caption (max 500 chars). Distinct from the meal-level
Note — this note belongs to the individual
photo only (e.g. "Side of guac added"). */
    note?: string | undefined;
}

/** Request model for saving the complete photo and note state of a day diary entry. The Photos list and Note are replaced with exactly what the client sends (replace semantics). */
export interface SaveDayPhotosRequest {
    /** The complete list of plan-level photos to persist on the day log.
Replaces the existing Photos list entirely — pass an empty list to remove all photos.
Existing URLs that are re-submitted keep their original UploadedAt timestamp;
new URLs receive the current UTC time. */
    photos?: DayPhotoInput[];
    /** Optional free-text note at the day level (max 500 chars).
When non-null, replaces the stored note (whitespace-only treated as null).
When null, the existing note is cleared. */
    note?: string | undefined;
}

/** A single photo input in a SaveDayPhotosRequest. */
export interface DayPhotoInput {
    /** The MinIO blob URL for this photo, as returned by the signed-URL upload helper. */
    blobUrl?: string;
    /** Optional per-photo caption (max 500 chars). */
    note?: string | undefined;
    /** Display category for this photo (Food / Progress / Free). */
    category?: DayPhotoCategory;
}

/** Display category for a DayPhoto. Maps to the three chips shown in the Fotky plánu gallery (Jídlo / Postup / Volné). */
export enum DayPhotoCategory {
    Food = "Food",
    Progress = "Progress",
    Free = "Free",
}

/** Request model for logging a meal as eaten. */
export interface LogMealEatenRequest {
    /** Optional list of MinIO blob URLs for photos attached to this meal.
The client uploads photos via the signed-URL helper (Epic #65) and
submits the resulting URLs here. When null or empty, no photos are stored. */
    photoBlobUrls?: string[] | undefined;
    /** Optional free-text note attached to this meal log entry (max 500 chars). */
    note?: string | undefined;
}

/** Response model for the client's active nutrition plan for the current week. */
export interface GetWeekPlanResponse {
    /** External identifier of the nutrition plan. */
    planId?: string;
    /** Display name of the nutrition plan. */
    planName?: string;
    /** Current week number within the plan (1-based). */
    weekNumber?: number;
    /** All days in the current week. */
    days?: PlanDay[];
    /** Global daily nutrition targets for the plan. */
    globalSettings?: GlobalNutritionSettings | undefined;
}

/** Response model for the client's active nutrition plan for today. */
export interface GetTodayPlanResponse {
    /** External identifier of the nutrition plan. */
    planId?: string;
    /** Display name of the nutrition plan. */
    planName?: string;
    /** Current week number within the plan (1-based). */
    weekNumber?: number;
    /** Current day of the week (1 = Monday … 7 = Sunday). */
    dayOfWeek?: number;
    /** Meals scheduled for today. */
    meals?: PlanMeal[];
    /** Computed nutrient totals for today. */
    dayTotals?: NutrientTotals | undefined;
    /** Global daily nutrition targets for the plan. */
    globalSettings?: GlobalNutritionSettings | undefined;
    /** Optional nutritionist note for today's plan day. */
    dayNote?: string | undefined;
}

/** Response model for the client's meal log for today. */
export interface GetTodayLogResponse {
    /** Meals eaten today. */
    mealsEaten?: MealLogDto2[];
    /** Total nutrients consumed across all meals today. */
    totalConsumed?: NutrientTotals;
    /** Remaining nutrients to reach the daily target.
Null if the active plan has no global settings. */
    remaining?: NutrientTotals | undefined;
}

/** DTO representing a single logged meal with computed nutrient totals. */
export interface MealLogDto2 {
    /** Identifier of the meal that was eaten. */
    mealId?: string;
    /** Display name of the meal (resolved from the plan). */
    mealName?: string;
    /** When the meal was eaten. Null for photo/note-only log entries that have not
been confirmed as eaten via the quick-log button. */
    eatenAt?: string | undefined;
    /** Computed nutrient totals for this logged meal. */
    totals?: NutrientTotals;
    /** Photos attached to this meal log entry.
Empty list when the client logged without photos. */
    photos?: MealPhotoDto[];
    /** Optional free-text note the client attached when logging the meal.
Null when no note was provided. */
    note?: string | undefined;
}

/** DTO for a single photo reference on a meal log entry. */
export interface MealPhotoDto {
    /** The MinIO blob URL for this photo. */
    blobUrl?: string;
    /** UTC timestamp when the photo was uploaded. */
    uploadedAt?: string;
    /** Optional per-photo caption set by the client when saving photos.
Null when no caption was provided for this photo. */
    note?: string | undefined;
}

/** Response model for the client's day-level diary log for today. */
export interface GetTodayDayLogResponse {
    /** Plan-level photos attached to today's day log.
Empty when no photos have been uploaded for today. */
    photos?: DayPhotoDto[];
    /** Optional day-level note. Null when none was provided. */
    note?: string | undefined;
}

/** DTO for a single day-level photo reference in a day log response. */
export interface DayPhotoDto {
    /** The MinIO blob URL for this photo. */
    blobUrl?: string;
    /** UTC timestamp when the photo was uploaded. */
    uploadedAt?: string;
    /** Optional per-photo caption set by the client when saving photos.
Null when no caption was provided for this photo. */
    note?: string | undefined;
    /** Display category string (Food / Progress / Free). */
    category?: string;
}

/** Response model containing an aggregated shopping list from the nutrition plan. */
export interface GetShoppingListResponse {
    /** Aggregated food items with total amounts needed. */
    items?: ShoppingListItem[];
}

/** A single item on the shopping list with aggregated amount. */
export interface ShoppingListItem {
    /** External identifier of the food. */
    foodExternalId?: string;
    /** Display name of the food. */
    foodName?: string;
    /** Total amount needed in grams. */
    totalAmountGrams?: number;
}

/** Request model for generating a shopping list from the active nutrition plan. */
export interface GetShoppingListRequest {
}

/** Request model for retrieving a recipe detail as a client. */
export interface GetRecipeDetailRequest {
}

/** Response containing all published weeks of the client's active nutrition plan. */
export interface GetFullPlanResponse {
    /** External identifier of the nutrition plan. */
    planId?: string;
    /** Display name of the nutrition plan. */
    planName?: string;
    /** The Monday when Week 1 begins, if set. */
    startDate?: string | undefined;
    /** Global daily nutrition targets. */
    globalSettings?: GlobalNutritionSettings | undefined;
    /** Published weeks with pre-computed date ranges. */
    weeks?: FullPlanWeek[];
    /** Number of published weeks. */
    publishedWeekCount?: number;
    /** Total number of weeks in the plan (including draft). */
    totalWeeks?: number;
    /** Current week number (null if plan is upcoming). */
    currentWeek?: number | undefined;
    /** Current day of week 1-7 (null if plan is upcoming). */
    currentDayOfWeek?: number | undefined;
    /** Plan status (Active, Completed, Archived). */
    status?: string;
    /** Linked questionnaire response public ID, if any. */
    questionnaireResponseId?: string | undefined;
    /** When this plan was marked as completed, if applicable. */
    dateCompleted?: string | undefined;
    /** Set of meal IDs that the client has logged as eaten.
Meal IDs are unique across the plan, so a flat set covers all days. */
    eatenMealIds?: string[];
    /** Supplement recommendations for this plan. Clients use this list to configure
local reminder notifications; the list is read-only on the client side. */
    supplements?: SupplementDto[];
}

/** A published week with pre-computed start/end dates. */
export interface FullPlanWeek {
    /** 1-based week number. */
    weekNumber?: number;
    /** ISO date string for the Monday this week starts. */
    weekStartDate?: string;
    /** ISO date string for the Sunday this week ends. */
    weekEndDate?: string;
    /** Days in this week. */
    days?: PlanDay[];
}

/** Response model containing the pre-signed upload URL and the permanent blob URL for a meal diary photo. */
export interface GenerateMealPhotoUploadUrlResponse {
    /** Time-limited pre-signed URL the client should PUT the image file to. */
    uploadUrl?: string;
    /** Permanent blob URL at which the photo will be accessible after a successful upload.
Always starts with diary/{mealId}/. */
    blobUrl?: string;
}

/** Request model for generating a pre-signed meal diary photo upload URL. */
export interface GenerateMealPhotoUploadUrlRequest {
    /** MIME type of the image file (e.g. "image/jpeg", "image/png", "image/webp", "image/heic", "image/heif"). */
    contentType: string;
    /** Declared file size in bytes. Must not exceed 10 MiB. */
    sizeBytes?: number;
}

/** Response model containing the pre-signed upload URL and the permanent blob URL for a plan-level day diary photo. */
export interface GenerateDayPhotoUploadUrlResponse {
    /** Time-limited pre-signed URL the client should PUT the image file to. */
    uploadUrl?: string;
    /** Permanent blob URL at which the photo will be accessible after a successful upload.
Always follows the plan-photos/{planId}/{guid}.{ext} convention. */
    blobUrl?: string;
}

/** Request model for generating a pre-signed day-level plan photo upload URL. */
export interface GenerateDayPhotoUploadUrlRequest {
    /** MIME type of the image file (e.g. "image/jpeg", "image/png", "image/webp", "image/heic", "image/heif"). */
    contentType: string;
    /** Declared file size in bytes. Must not exceed 10 MiB. */
    sizeBytes?: number;
}

/** Aggregated statistics for a client's body weight measurements. */
export interface MeasurementStatsResponse {
    /** Minimum recorded weight in kilograms. */
    minWeight?: number | undefined;
    /** Maximum recorded weight in kilograms. */
    maxWeight?: number | undefined;
    /** Average recorded weight in kilograms. */
    avgWeight?: number | undefined;
    /** Most recently recorded weight in kilograms. */
    latestWeight?: number | undefined;
    /** Weight change over the last 30 days (latest minus ~30 days ago). Positive means gain. */
    weightChange30Days?: number | undefined;
    /** Total number of measurement records. */
    totalCount?: number;
    /** Client's target weight from onboarding data, if set. Null when no goal was specified. */
    targetWeightKg?: number | undefined;
}

/** Paginated response containing body measurement records. */
export interface GetMeasurementsResponse {
    /** List of measurement records for the current page. */
    items?: MeasurementDto[];
    /** Total number of matching records. */
    totalCount?: number;
    /** Current page number. */
    page?: number;
    /** Number of items per page. */
    pageSize?: number;
}

/** Data transfer object representing a single body measurement record. */
export interface MeasurementDto {
    /** Public identifier of the measurement. */
    measurementId?: string;
    /** Date and time when the measurement was taken. */
    measuredAt?: string;
    /** Weight in kilograms. */
    weightKg?: number | undefined;
    /** Body fat percentage. */
    bodyFatPercentage?: number | undefined;
    /** Chest circumference in centimeters. */
    chestCm?: number | undefined;
    /** Waist circumference in centimeters. */
    waistCm?: number | undefined;
    /** Hips circumference in centimeters. */
    hipsCm?: number | undefined;
    /** Biceps circumference in centimeters. */
    bicepsCm?: number | undefined;
    /** Thighs circumference in centimeters. */
    thighsCm?: number | undefined;
    /** Optional notes about the measurement session. */
    notes?: string | undefined;
}

/** Request model for listing body measurements with optional date filtering and pagination. */
export interface GetMeasurementsRequest {
}

/** Request model for a trainer to retrieve a client's body measurements. */
export interface GetClientMeasurementsRequest {
}

/** Request model for adding a new body measurement. */
export interface AddMeasurementRequest {
    /** Date and time when the measurement was taken. */
    measuredAt: string;
    /** Weight in kilograms. */
    weightKg?: number | undefined;
    /** Body fat percentage. */
    bodyFatPercentage?: number | undefined;
    /** Chest circumference in centimeters. */
    chestCm?: number | undefined;
    /** Waist circumference in centimeters. */
    waistCm?: number | undefined;
    /** Hips circumference in centimeters. */
    hipsCm?: number | undefined;
    /** Biceps circumference in centimeters. */
    bicepsCm?: number | undefined;
    /** Thighs circumference in centimeters. */
    thighsCm?: number | undefined;
    /** Optional notes about the measurement session. */
    notes?: string | undefined;
}

/** Request model for a trainer/nutritionist recording a body measurement on behalf of a linked client. */
export interface AddClientMeasurementRequest {
    /** Date and time when the measurement was taken. */
    measuredAt: string;
    /** Weight in kilograms. */
    weightKg?: number | undefined;
    /** Body fat percentage. */
    bodyFatPercentage?: number | undefined;
    /** Chest circumference in centimeters. */
    chestCm?: number | undefined;
    /** Waist circumference in centimeters. */
    waistCm?: number | undefined;
    /** Hips circumference in centimeters. */
    hipsCm?: number | undefined;
    /** Biceps circumference in centimeters. */
    bicepsCm?: number | undefined;
    /** Thighs circumference in centimeters. */
    thighsCm?: number | undefined;
    /** Optional notes about the measurement session. */
    notes?: string | undefined;
}

/** Response after successful onboarding submission. */
export interface SubmitOnboardingResponse {
    /** Confirmation message. */
    message?: string;
}

/** Request model for the client onboarding questionnaire submission. */
export interface SubmitOnboardingRequest {
    /** Client's age in years (converted to DateOfBirth server-side). */
    age?: number;
    /** Biological sex (string, parsed to BiologicalSex enum). */
    sex: string;
    /** Height in centimeters. */
    heightCm?: number;
    /** Current weight in kilograms. */
    weightKg?: number;
    /** Target weight in kilograms (optional). */
    targetWeightKg?: number | undefined;
    /** Body constitution type (string, parsed to BodyType enum). */
    bodyType: string;
    /** Primary fitness goal. */
    primaryGoal: string;
    /** Desired time horizon. */
    timeHorizon: string;
    /** Job activity type. */
    jobType: string;
    /** Average sleep hours per night (4-10). */
    sleepHours?: number;
    /** Self-reported stress level (1-5). */
    stressLevel?: number;
    /** Current training frequency. */
    currentTrainingFrequency: string;
    /** Desired training frequency. */
    desiredTrainingFrequency: string;
    /** Self-rated fitness level (1-10). */
    fitnessRating?: number;
    /** Gym access level (optional). */
    gymAccess?: string | undefined;
    /** Preferred activity types. */
    preferredActivities?: string[];
    /** Physical injuries or limitations. */
    injuries?: string[];
    /** Meals per day. */
    mealsPerDay: string;
    /** Dietary style. */
    dietaryStyle: string;
    /** Food allergies/intolerances. */
    allergies?: string[];
    /** Self-rated diet quality (1-5, optional). */
    dietRating?: number | undefined;
    /** Prior plan experience. */
    planExperience: string;
    /** Past blockers to fitness progress. */
    pastBlockers?: string[];
    /** Primary motivation source. */
    primaryMotivation: string;
}

export interface RegisterPushTokenRequest {
    token?: string;
    platform?: string;
}

/** Response model containing a client's weekly progress overview. */
export interface GetWeeklyOverviewResponse {
    /** Monday of the current week. */
    weekStart?: string;
    /** Sunday of the current week. */
    weekEnd?: string;
    /** Compliance percentage for the current week (0-100). */
    compliancePercent?: number;
    /** Total number of meals planned for the current week. */
    mealsPlanned?: number;
    /** Total number of meals logged for the current week. */
    mealsLogged?: number;
    /** Average daily macronutrient totals for the current week. */
    averageDailyMacros?: NutrientTotals;
    /** Current streak of consecutive compliant days. */
    currentStreak?: number;
}

/** Response model containing a client's compliance score and related metrics. */
export interface GetComplianceScoreResponse {
    /** Compliance percentage (0-100), representing how many planned meals were logged. */
    compliancePercent?: number;
    /** Total number of meals planned in the date range. */
    mealsPlanned?: number;
    /** Total number of meals logged in the date range. */
    mealsLogged?: number;
    /** Current streak of consecutive compliant days. */
    currentStreak?: number;
    /** Start date of the compliance calculation range. */
    from?: string;
    /** End date of the compliance calculation range. */
    to?: string;
}

/** Request model for retrieving a client's compliance score. */
export interface GetComplianceScoreRequest {
}

/** Response containing the client's plans. */
export interface GetClientPlansResponse {
    /** Plan summaries. */
    items?: ClientOwnPlanItem[];
}

/** Lightweight plan summary for the client's own plan list. Distinct from the trainer-facing ClientPlanItem in Features/Trainers/ListClientPlans — the two DTOs previously shared a name, which made NSwag's generated client's type-name assignment dependent on document processing order. */
export interface ClientOwnPlanItem {
    /** Plan public identifier. */
    planId?: string;
    /** Display name. */
    planName?: string;
    /** "nutrition" or "training". */
    type?: string;
    /** Plan status as string (Active, Completed, Archived). */
    status?: string;
    /** The Monday when Week 1 begins, if set. */
    startDate?: string | undefined;
    /** Total number of weeks in the plan. */
    totalWeeks?: number;
    /** Number of published weeks. */
    publishedWeekCount?: number;
    /** When this plan was marked as completed. */
    dateCompleted?: string | undefined;
    /** Linked questionnaire response ID, if any. */
    questionnaireResponseId?: string | undefined;
    /** The current week number within this plan, or null if the plan hasn't started yet.
Computed from StartDate (preferred) or the first published week's DatePublished. */
    currentWeek?: number | undefined;
    /** Target daily kilocalories from the plan's GlobalSettings. Populated for nutrition plans only. */
    dailyKcal?: number | undefined;
    /** Whether today's day-of-week has a session in the current week. Populated for training plans only.
Null when CurrentWeek is null or for nutrition plans. */
    hasTodaySession?: boolean | undefined;
}

/** Optional query parameters for filtering client plans. */
export interface GetClientPlansRequest {
}

/** Response for the GET /client/records endpoint. The total count of matching records is returned in the X-Total-Count response header. */
export interface GetClientRecordsResponse {
    /** Page of personal record summaries, sorted by AchievedAt descending. */
    items?: PersonalRecordSummary[];
}

/** Summary DTO for a single personal record. */
export interface PersonalRecordSummary {
    /** Public-facing identifier of this personal record. */
    externalId?: string;
    /** ExternalId of the exercise for which the PR was achieved. */
    exerciseExternalId?: string;
    /** Snapshot of the exercise name at the time the PR was achieved. */
    exerciseName?: string;
    /** Weight lifted in kilograms. */
    weightKg?: number;
    /** Repetitions completed in the PR set. */
    reps?: number;
    /** When the personal record was achieved (UTC). */
    achievedAt?: string;
    /** ExternalId of the workout log that contains this PR set. */
    workoutLogId?: string;
}

/** Query parameters for listing the authenticated client's personal records. */
export interface GetClientRecordsRequest {
}

export interface MarkReadRequest2 {
}

export interface GetNotificationsResponse {
    items?: NotificationDto[];
    cursor?: string | undefined;
}

export interface NotificationDto {
    id?: string;
    type?: string;
    title?: string;
    body?: string;
    timestamp?: string;
    read?: boolean;
    actionLabel?: string | undefined;
    actionPayload?: string | undefined;
}

export interface GetNotificationsRequest {
}

export interface PendingInviteResponse {
    id?: string;
    trainerId?: string;
    trainerName?: string;
    trainerRole?: string;
    trainerCity?: string;
    message?: string | undefined;
}

export interface DeclineClientInviteRequest {
}

export interface AcceptClientInviteRequest {
}

export interface GetCollaborationsResponse {
    collaborations?: CollaborationDto[];
}

export interface CollaborationDto {
    publicId?: string;
    professionalPublicId?: string;
    professionalName?: string;
    professionalCity?: string | undefined;
    role?: string;
    since?: string;
    /** Professional's avatar URL. Falls back to the underlying user's
personal avatar when the professional-profile-specific one isn't set.
Null when neither is uploaded. */
    avatarBlobUrl?: string | undefined;
}

/** Request model for email verification. */
export interface VerifyEmailRequest {
    /** The verification token from the email link. */
    token?: string;
}

/** Response body for POST /auth/social/nonce. */
export interface RequestNonceResponse {
    /** The raw nonce value to embed in the Apple/Google sign-in flow.
Apple expects SHA-256(nonce) in the id_token; Google expects the raw value. */
    nonce?: string;
}

/** Response returned after a successful Google social login. Shape is intentionally identical to LoginResponse so clients can reuse the same token-storage logic. */
export interface GoogleSocialLoginResponse {
    /** JWT access token (short-lived, 15 minutes). */
    accessToken?: string;
    /** Refresh token for obtaining new access tokens (7 days). */
    refreshToken?: string;
    /** Access token expiration time in UTC. */
    expiresAt?: string;
    /** Whether the user's email address has been verified.
Always true for Google-provisioned accounts because Google
verifies the email before issuing the ID token. */
    emailConfirmed?: boolean;
}

/** Request body for POST /auth/social/google. */
export interface GoogleSocialLoginRequest {
    /** The Google ID token returned by the Google Identity Services flow on the client. */
    idToken: string;
    /** The raw nonce value obtained from POST /auth/social/nonce.
The client must pass the same raw value that was embedded in the Google sign-in flow
(Google embeds the raw nonce directly in the id_token's nonce field — no hashing). */
    nonce: string;
}

/** Response returned on a successful Apple Sign-In. Identical shape to Google Social Login response. */
export interface AppleSocialLoginResponse {
    /** Short-lived JWT access token (15 minutes by default). */
    accessToken?: string;
    /** Long-lived opaque refresh token (7 days by default). */
    refreshToken?: string;
    /** UTC expiry time of the access token. */
    expiresAt?: string;
    /** Whether the account's email address has been confirmed.
Apple-provisioned and Apple-verified accounts always have this set to true,
which causes the client to redirect to /download-app rather than /verify-email. */
    emailConfirmed?: boolean;
}

/** Request body for the Apple Sign-In endpoint. The identity token is always required. The authorization code and name fields are only present on first authorization (Apple omits them on re-auth). */
export interface AppleSocialLoginRequest {
    /** The Apple identity token (JWT) returned by the Apple JS SDK or native Sign-In. */
    identityToken: string;
    /** The authorization code returned by Apple (forwarded for forward-compatibility).
The backend does not currently exchange this for tokens — identity verification
is performed via the identity token alone. */
    authorizationCode?: string | undefined;
    /** The user's first name, present only on first authorization.
Apple does not re-send this on subsequent authentications. */
    firstName?: string | undefined;
    /** The user's last name, present only on first authorization.
Apple does not re-send this on subsequent authentications. */
    lastName?: string | undefined;
    /** The raw nonce value obtained from POST /auth/social/nonce.
The client must pass the same raw value that was embedded in the Apple sign-in flow
(Apple hashes it before embedding: SHA-256(nonce) appears in the id_token's nonce claim). */
    nonce: string;
}

/** Request model for completing a password reset using a token. */
export interface ResetPasswordRequest {
    /** The password reset token received via email. */
    token: string;
    /** The user's email address. */
    email: string;
    /** The new password. */
    newPassword: string;
    /** Confirmation of the new password. */
    confirmPassword: string;
}

/** Request model for initiating a password reset. */
export interface RequestPasswordResetRequest {
    /** Email address of the account to reset. */
    email: string;
}

/** Response model returned after successful registration. */
export interface RegisterResponse {
    /** The newly created user's public ID. */
    userId?: string;
    /** The user's email address. */
    email?: string;
    /** Confirmation message. */
    message?: string;
}

/** Request model for user registration. */
export interface RegisterRequest {
    /** User's email address (used as login). */
    email: string;
    /** User's chosen password. */
    password: string;
    /** Password confirmation (must match Password). */
    confirmPassword: string;
    /** User's first name. */
    firstName: string;
    /** User's last name. */
    lastName: string;
    /** The roles the user is registering as. Must contain at least one of: Trainer, Nutritionist, Client. */
    roles: string[];
    /** Explicit GDPR consent for personal data processing (Art. 6 GDPR). Required for all roles. */
    gdprConsent?: boolean;
    /** Explicit consent for processing health data under GDPR Art. 9.
Must be true for the Client role; must be null for Trainer and Nutritionist roles. */
    healthDataConsent?: boolean | undefined;
}

/** Response model returned after successful token refresh. */
export interface RefreshTokenResponse {
    /** New JWT access token. */
    accessToken?: string;
    /** New refresh token (old one is invalidated via rotation). */
    refreshToken?: string;
    /** New access token expiration time in UTC. */
    expiresAt?: string;
}

/** Request model for refreshing an access token using a refresh token. */
export interface RefreshTokenRequest {
    /** The current refresh token to exchange for a new token pair. */
    refreshToken: string;
}

/** Request model for logging out (revoking a refresh token). */
export interface LogoutRequest {
    /** The refresh token to revoke. */
    refreshToken?: string;
}

/** Response model returned after successful login. */
export interface LoginResponse {
    /** JWT access token (short-lived, 15 minutes). */
    accessToken?: string;
    /** Refresh token for obtaining new access tokens (7 days). */
    refreshToken?: string;
    /** Access token expiration time in UTC. */
    expiresAt?: string;
    /** Whether the user's email address has been verified. */
    emailConfirmed?: boolean;
}

/** Request model for user login. */
export interface LoginRequest {
    /** User's email address. */
    email: string;
    /** User's password. */
    password: string;
}

/** Response model for an anonymous resend-verification request. Deliberately generic — the same message is returned regardless of whether the email is registered, already verified, or has hit its resend cap, so the response body never becomes an account-existence oracle. */
export interface AnonymousResendVerificationResponse {
    /** Generic, state-independent confirmation message. */
    message?: string;
}

/** Request model for requesting a verification-email resend by email address, without an authenticated session. */
export interface AnonymousResendVerificationRequest {
    /** Email address of the account to resend a verification email for. */
    email: string;
}

/** Response model returned after successfully accepting an invitation. */
export interface AcceptInvitationResponse {
    /** Confirmation message. */
    message?: string;
    /** Public identifier of the trainer whose invitation was accepted. */
    trainerPublicId?: string;
}

/** Request model for accepting a trainer's invitation. */
export interface AcceptInvitationRequest {
    /** The one-time invitation token received from the trainer. */
    token: string;
}

export class ApiException extends Error {
    override message: string;
    status: number;
    response: string;
    headers: { [key: string]: any; };
    result: any;

    constructor(message: string, status: number, response: string, headers: { [key: string]: any; }, result: any) {
        super();

        this.message = message;
        this.status = status;
        this.response = response;
        this.headers = headers;
        this.result = result;
    }

    protected isApiException = true;

    static isApiException(obj: any): obj is ApiException {
        return obj.isApiException === true;
    }
}

function throwException(message: string, status: number, response: string, headers: { [key: string]: any; }, result?: any): any {
    if (result !== null && result !== undefined)
        throw result;
    else
        throw new ApiException(message, status, response, headers, null);
}

function isAxiosError(obj: any): obj is AxiosError {
    return obj && obj.isAxiosError === true;
}